# CONTEXT — 專案脈絡與文件索引

> **這份是全專案的單一入口。** 對話被壓縮、或換一個新 session 接手時，先讀這份，
> 就能重建完整全局，再依索引跳到需要的文件。
>
> 最後更新：2026-07-26

---

## 1. 這個專案在做什麼

把 SSI《Demon's Winter》(1988/1989, DOS) 完整逆向、在 Go / Ebiten 上重寫引擎、
再做繁體中文化。定位是**文化資產保存**，替華人遊戲圈留下這款經典。

### 兩條硬性原則

1. **完整性 > 投報**。不得以「成本高、效益低」為由跳過任何素材版本、任何格式。
   卡關就換方法，記錄「卡在哪、試過什麼」，不寫「暫緩／低投報」當結論。
2. **SDD：spec 齊了才實作**。反組譯 → 收攏成規格（`docs/spec/`）→ 才動手寫程式。
   九份規格全部 READY 後才開工，目前引擎進度見 §2。

### 不做的事

不散布原版執行檔、資料檔、美術或音樂。公開產出只有引擎程式碼與翻譯文本，
玩家自備合法原版。原版資料一律 gitignore。

---

## 2. 現況一覽

### 已完成

| 領域 | 狀態 |
|---|---|
| 官方手冊繁中版 | 全 28 頁 + 附錄，`docs/manual/`（附錄 A 已於 2026-07-25 訂正三處抄錄錯誤）|
| **軟體世界中文說明書** | 1990 年台灣代理版全 51 頁，`docs/manual-cht/` |
| 社群攻略繁中版 | 全 1,914 行，`docs/walkthrough/` |
| 統一譯名表 | `translations/glossary.md`，第 22 節收 `DATA*.TXT` 中文化新增的譯名 |
| 資料格式 | 事件表／地圖／怪物／道具／存檔／CGA 與 EGA 素材，全解 |
| 反組譯 | 主迴圈、事件觸發、戰鬥、音效、移動、建角升級、**字型渲染** |
| Go 解碼器 | `internal/assets/{gamedata,world,scenario,gfx}` + `internal/rng` |
| Ghidra 環境 | docker 化，含跳表 override 修復 |
| DOSBox 環境 | docker 化，可自動送鍵與截圖 |

### 進行中／受阻

| 項目 | 狀態 |
|---|---|
| **EGA 調色盤先前解錯（已訂正）** | `.PIE` 的調色盤值是 **6 bit**，舊碼 `&0x0f` 後索引 16 色表，把次要色分量整組丟掉 —— 所有 EGA 美術的膚色土色都偏成洋紅。已改為 `gfx.EGAColor` 的 6-bit 換算，並用「標準預設調色盤換算後恰好等於標準 16 色表」當佐證。⚠ `docs/formats/graphics.md` 舊句「色調自然（紅棕色惡魔膚色）」是用錯誤解碼看到的顏色，已標作廢 |
| 標題畫面怎麼載入 | `OPEN.PIE` 的**影像已解**（608×336，見 `docs/formats/graphics.md` §5），但 `OPEN.*` 這兩個檔名在兩個執行檔裡都零命中；引擎檔名表列的是 `TITLE.PIC`，而它不在這份 dump 裡。原版在這份資料上根本開不了場 —— 這解釋了 DOSBox 截圖的「開場卡住」 |
| Go 引擎本體 | **M1–M5 完成**（見下），**開場標題畫面已接上**。原版版面刻意不復刻（中文需要 640×400）；**原版怪物 AI 三支全解**（`FUN_138d_03f5` 決策樹、`FUN_138d_065e` 選法術與效果分派、`FUN_138d_17b8` 噴吐）：目標記憶、選法術、投入法力、依 K 正負挑敵我、範圍誤傷否決、附身術、噴吐傷害都已實作並實機驗過。剩呈現層的噴吐動畫 |

### 引擎進度

| 里程碑 | 狀態 |
|---|---|
| M1 可走動的世界 | ✅ 載入地圖與地形圖塊、可通行性判定、子地圖進出、三層時間與步數推進。端到端驗證：從 (34,36) 連走 11 步 → X=45、Hour 5→6、Steps=1 |
| M2 事件與文字 | ✅ 觸發閘門（tile 0x11/0x53 查表、五個 tile 直接當索引、0x35 硬阻擋）、`EXITS.DAT` 掃描與事件索引累計、`DATA*.TXT` 文字顯示（中文化後改為 38 格逐字／逐詞斷行＋標點禁則，仍是每 5 行翻頁）。端到端：走進 (54,16) 跳出「骸骨廳。地上散落著數百具頭骨與骸骨……」並可翻頁。傳送已接（`TeleportTo`，不檢查可通行性也不推進時間，照原版直接寫座標）|
| M3 角色 | ✅ 規則層 + 建角畫面：存檔載入、剩餘智慧點數、建角擲點（種族修正＋下限鉗制＋人類 +2）、**三次重擲**（可一次勾多項）、升級。viewer 按 `P` 看名冊、`C` 建角，走完種族→擲點→職業→命名→入隊並可存檔。⚠ 重擲機制**反組譯仍未定位**，依英文原版手冊實作（oracle 優先序上手冊高於反編譯），兩本手冊的三處不一致已裁決並記在 spec |
| M4 戰鬥 | ✅ 規則＋流程：武器骰表、戰鬥風格、行動順序、普通攻擊（命中／爆擊／傷害／毒／功夫暈眩）、四個動作、法術系統（參數表 43 筆／通式／即死／束縛／枯萎）、回合狀態機、勝負判定、召喚三槽、怪物進場擲點。端到端：踏到事件格 → 讀敘述 → 6 隻怪物開打 → 跨回合結算。**玩家指令選單 + 施法選單已做**：行動點（初值＝速度）、走位與轉向、攻擊／施法／驅散不死／祈禱／汲取法力／閃避／音效開關／結束回合，熱鍵沿用原版。施法選單列 43 筆法術（中文名，最低法力對上 RE 表），通式數值增減與四類特殊判定都會生效。**5×5 AOE 已做**（選點游標＋範圍框，不分敵我）。**裝備已接進戰鬥**（武器骰索引＝ITEMS.DAT 索引+1、護甲防護＝索引−7、附魔與武器特效），使用道具選單依已驗證的過濾規則列出。**戰場地形與視線遮蔽已做**：戰場就是大地圖以遭遇點為中心的局部放大，看得到多大一塊由時辰決定（白天 9×9、深夜剩中央 3×3），擋視線的地形後方畫成空白（`FILES.DAT 0x0A8` 查表）。**投入點數輸入框已做**（原版的「How many S.P.」，數字鍵／↑↓／退格，範圍 [M, 目前法力]、預設全投；規則層在 `game.SPInput`，純邏輯測得到）。**單體法術的游標選目標也已做**（原版用 `FUN_138d_3fc9`）—— 順帶修掉「正前方沒人時法術靜默沒反應」的舊行為。**使用道具已接上**：道具槽 `+0x07` 是效果索引、`+0x08` 是強度，與法術**共用同一張效果表**（見 `docs/re/25`），用掉會扣次數。**城鎮設施依 `TOWN*.DAT` 旗標差異化**（`docs/re/02` §3.2）。**未做**：掉寶生成、噴吐動畫、旅店休息 |
| 存檔寫回 | ✅ `PARTY.DAT` 雙向。策略是**改寫不是重建** —— 從原始 bytes 出發只蓋已知欄位，未解區域一個 byte 都不動。驗收：讀出再寫回 byte-for-byte 相同（`PARTY.DAT`／`PARTY.BAK` 都過），另有逐欄位測試釘住「寫到哪個位移、動幾個 byte」。進度存檔預設寫到 `workplace/save/`，**不覆蓋玩家的原版 `PARTY.DAT`** |
| 音效 | ✅ PC speaker 全部九個效果（8 個 C 大調單音 C3–C4 + 1 段 11 音符陣亡旋律）。合成層 `internal/audio/pcspeaker` 是純函式（方波，不是正弦波 —— 喇叭只有開關），測試用數過零點反推頻率驗證音高。`cmd/dwsound` 可輸出 WAV 用耳朵驗收。**沒有音效裝置時整段跳過**：Ebiten 的 audio context 會掛進主迴圈，容器裡建起來會讓遊戲凍住（實測踩過）|
| 離開與存檔語意 | ✅ 照 `esc-cancel-f10-quit-autosave` 鐵則：ESC 只取消／退回上一層，**F10 才離開**並跳 Y／N 確認，選 Y 才自動存檔。存檔失敗就不離開 |
| 城鎮設施可操作 | ✅ 八種設施全部接上實際效果，含神殿的捐獻／祈禱／改宗三項 |
| 存檔真的寫得回去 | ✅ 道具槽、裝備槽、戰鬥狀態、時／日／月／步數、糧食、子地圖、光源、座標全部雙向。驗收是 byte 級的：載入 → 直接存檔與原版 `PARTY.DAT` **完全相同**；載入 → 換裝 → 存檔**只差那一個 byte**。改寫策略是「只蓋已解欄位，未解的 bytes 一個都不動」|
| M5 城鎮與經濟 | ✅ **八種設施全部可實際操作**：市集（議價／買／賣）、治療所（四項服務）、旅店（休息）、酒館（買糧）、神殿（捐獻／祈禱／改宗）、公會（免費升級）、碼頭（買船／修船）、學院（教技能）。價格體系完整；設施旗標 `+0x1ee`–`+0x1f6` 已解，畫面依實際設施過濾；**自動進城已完成**（tile `0x2e`/`0x64` → 25 筆座標表，25/25 對得上）|
| 建角擲點分佈（**已訂正**）| ✅ **公式是 `max(3, 2d6 + 種族修正 + 1)`，人類再 +2**（`DEMON.INT` 0x13bba）。原本推定的 `Roll(15)` 是錯的 —— 期望值剛好也是 8，所以「該種族平均」那一欄一直對，錯的是分布。**三次重擲機制也在同一段找到**（0x13b9d／0x13d60），初擲 + 兩次重擲、可一次勾多項、沒有「低於 6」門檻，三個原本靠手冊裁決的結論全部成立 |
| 中文化 | 🔶 顯示層 + 事件敘述完成：倚天 16×15 點陣字（Big5 分區索引已驗證）、中英混排排版格與標點禁則、畫布 640×400、viewer 全介面中文、**七個目錄共 356 條 100% 翻完**（事件敘述 105、法術 43、道具 30、怪物 99、城鎮 25、月份 22、技能 32）並有自動品質閘（`dwstrings check`）。**未做**：`DEMON.INT` 的 800 條 UI 文案只抽了清單（重製版不讀它，是參考不是資源）—— ~~城鎮／建角／戰鬥指令等畫面還沒實作所以沒有文案~~ **已過期**：那些畫面都做完了，介面文字也都是中文，只是直接寫在程式裡而不是走翻譯檔 |

程式碼結構：`internal/assets`（純解碼，回傳 `image.RGBA`，不認識 Ebiten）→
`internal/game`（規則層，不認識畫面）→ `internal/ui`（Ebiten 呈現層）→ `cmd/demonwinter`。
`internal/ui/textlayout`（斷行分頁）與 `internal/ui/layout`（版面座標）刻意不依賴 Ebiten ——
Ebiten 在 init 期就要求顯示器，混進去這兩件事在無頭環境下連測都測不了。

倚天字型與原版資料同樣**不隨本專案散布**，放在 gitignore 的 `workplace/eten/`，
執行時用 `-eten` 指向自備的 `STDFONT.15`／`SPCFONT.15`。

中文化的工作流：`cmd/dwstrings events` 抽字串成 `assets/lang/zh-Hant/*.txt`（原文與譯文並存）→
依 `translations/LOCALIZE_INSTRUCTIONS.md` 翻譯 → `cmd/dwstrings check` 過閘門。
閘門檢查五件事：原文是否被改動（那是查表 key）、控制序列數量、非 Big5 字元、
譯文長度、**譯名是否漂移**（原文出現的專有名詞，譯文必須用 `glossary.md` 指定的譯法）。

建置與驗證一律走 docker：`tools/go.sh <go 子指令>`、
`tools/screenshot.sh <out.png> [KEYS=Up,Right,…] [程式參數]`（headless Xvfb + xdotool 送鍵）。

**截圖驗收要帶 `-seed`。** 戰鬥的行動順序由怪物進場擲點決定，
時間種子讓「走五步再施法」這種固定按鍵序列每次跑到不同畫面，根本重跑不出結果。

### 這一輪（2026-07-26）：中文化顯示層 + 事件敘述全數翻完

三件事，每一件都在做的過程中翻出一個錯：

1. **倚天點陣字上畫面**。踩到的坑是把 ASCII 當半形（格寬 8）配上放大兩倍的 8×8 字模，
   畫面上每個英文字母疊掉前一個的一半。依 `rulebook/81` 改成兩者都 16 格。
2. **`cjk.Big5()` 原本只看編碼器有沒有報錯，不夠**。Go 的 Big5 編碼器會把一批字
   映到標準區以外：簡體字進造字區（`头`→`8960`）、而 `撐` 這種標準繁體字被編成
   倚天延伸區的 `FCB9`，儘管它在 `BCB5` 就有字模。兩種情況都是「編碼成功、畫面空白」。
   改成從**解碼方向反建** Unicode→標準 Big5 對照表，一次解掉整類問題。
   ⚠ 附帶推翻一個直覺：**「簡體字一定不在 Big5」是錯的** ——「只」「儿」都是標準 Big5 收錄字。
3. **105 筆事件敘述 100% 翻完**（4 個 subagent 分頭翻 + 統一譯名表 + 自動閘門）。
   閘門本身也抓到自己的兩個 bug（多譯法欄位配對錯、複合詞誤報），
   且用「故意種錯誤」證實過它會紅，不是空轉。

### 上一輪（2026-07-25）解掉的長期未解項

召喚／幻術、即死／束縛／枯萎、怪物進場擲點、兩個戰鬥邊界條件、爆擊門檻條件、
城鎮六大設施全部公式、市集議價與說服技能、治療所費率來源、
**種族欄位位置**、遊戲內部技能 id 表、`FILES.DAT` 表布局、道具槽陣列起點。

同日整理軟體世界 1990 年中文說明書（`docs/manual-cht/`）後另外確認：

- 拿它的附錄 A 對 `FILES.DAT 0x158`，**抓出 `docs/manual/part-4.md` 三處抄錄錯誤**（已訂正）
- 山丘「通過時間兩倍」＝ `04-movement` 的「某些 tile 步數 +2」，機制對上、待補 tile 清單
- 建角**三次重擲機制**——手冊有、程式碼未定位（⚠ 2026-07-26 已在 `DEMON.INT` 找到提示字串 `Enter #s to be rerolled!`，複數 `#s` 佐證「可一次重擲多項」）
- **MGA 單色版執行檔 `PLAY` 不在手上的 dump 裡**，該版本無從逆向
- ⚠ 「一天 26 小時」是英文手冊的中譯，**不構成第二個佐證**，26 vs wrap 38 的落差依舊未解

同日稍晚**推翻並訂正 sprite frame 尺寸**（見 §5 已被推翻的斷言）：
CGA `.SHP` 是 16×16（64 B）、EGA `.SHE` 是 16×28（224 B），
frame 數 COMBAT 44／CYPHER 27／DEMON 102／MONSTER 240／SHIP 32／WINTER 102。
連帶確認：EGA 素材一律「檔案存半寬、顯示時寬 ×2」（`.SHE` 載入時加倍、
`.PIE` blit 時加倍，`.PIE` 顯示尺寸是 288×252）；
`DEMON`/`WINTER` 的 102 格是地形圖塊集，`WINTER` 是雪地版。

追查 sprite 尺寸時，發現 **CGA 字型 `ASC.FNT` 一直解不出可讀字形**（atlas 全是雜訊，
但文件標著「已驗證」——那句其實驗的是 EGA）。一併解掉：
`.FNT` 是 **packed 2bpp**（byte0 = 左 4px、byte1 = 右 4px）不是 planar、
**無檔頭**（3,073 B = 3,072 資料 + 結尾 `0x1a` DOS EOF，原解碼器誤跳開頭一個 byte、
整份位移一格）、**96 字 × 2 bank**（bank0 一般、bank1 反白，bank 間距 1,536）。
`.FNE`（EGA 16×14）不受影響，原結論成立。

也認出**山丘 = tile `0x0e`／`0x2b`**（移動多算一步的那兩個值）：
圖塊畫出來是起伏等高線，與手冊「山丘通過時間為一般地形兩倍」對得上。

### 這一輪（2026-07-26）：自動進城 + `SUM.MAP` 解壓修正

從 `town%d.dat` 這個格式字串反推，在 `DEMON.INT` `DS:0x2f86` 找到 25 筆城鎮
`(X, Y)` 常數表，踩到 tile `0x2e`／`0x64` 就用它決定城鎮編號 —— 自動進城打通，
選單降為偵錯入口。

拿這張表去對地圖時 0/25，反過來抓出 `SUM.MAP` 的 RLE 解壓有兩個 bug
（次數 byte 0 代表 256、每個區塊首 byte 要跳過）。修好之後 23/23 個子地圖
精確填滿 4096 格，25/25 座城鎮座標全部落在城鎮格上，算繪出來也從雜訊
變成連貫的大陸海岸線（`workplace/dump/maps/`）。細節與教訓見 §5。

順帶推得**世界是子地圖組成的網格，ID = 欄×10 + 列**（當時只是假說，
**已於同日稍晚由換圖程式碼確認**，見下）。

同日再解掉三件：

- **`-map` 現在收子地圖編號**（`-map 34`）。在這之前世界地圖全打包在 `SUM.MAP` 裡、
  viewer 只認檔名，玩家根本走不到大陸上。實測從 (33,11) 往東一步進 New Gleon，
  物價 13、碼頭有船 —— 與攻略「到東北方的新格里昂買船」對得上。
- **tile `0x14` → `0x62` 的條件追出來了**：不是季節旗標，是乘 125 的擲硬幣
  （`15be:179f`，種子 10001）。兩者是海面的兩種浪花，可通行性都是 `0xff`，
  **純外觀** —— 原版用它讓大片海洋不要一模一樣。實作在 `world/ocean.go`，
  放算繪端不寫進 `Map`。
- 連帶**推翻 `town-and-map.md` §2.2 的 tile 猜測表**：tile 98（`0x62`）被猜成「牆」，
  實際是海面。那張表是「找不到 tileset 只能靠頻率猜」時代的產物，圖塊與可通行性
  都解出來之後已無理由留著。

再往下追七大地形時，翻出 **arena 索引 6（`FILES.DAT 0x0A8`，176 bytes）是
戰場視線遮蔽查表**（`docs/re/22` §3.5）：戰場 9×9、中央是視點，格子上有擋視線的
地形（`0x0d`/`0x12`/`0x13`/`0x2a`/`0x31`/`0x5e`–`0x61`）就把後面的格子塗黑。
49 組對應內部 7×7 的每一格，`0xff` 是「什麼都遮不到」的哨兵。

這件事**附帶把戰場尺寸釘死了**：`BattleGridWidth/Height` 原本註明「取 9×9 是為了
與呈現層一致，未經原版確認」，現在有原版證據 —— 索引以 9 展開、上限 81、
內部掃描 7×7，三者只有 9×9 自洽。

過程也示範了一次「別靠統計猜結構」：先前只看資料，切法只有 74% 的組像矩形，
起點也掃不出最佳解，於是先記成線索不動程式；改照 `rulebook/62` 去找讀取端，
迴圈邊界一算就得到「49 組」，回頭切資料精確吻合，順便發現起點是 `0x0a8` 不是
`0x0a5`（可通行性那段在 arena 裡佔 104 bytes，只有前 101 個是有效 tile）。

順著讀取端再往上，**戰場地形也解開了**（`docs/spec/02-combat.md`「戰場座標與地形」）：
戰場不是另一份資料，就是**大地圖以遭遇位置為中心的局部放大**，
視窗大小 (9−2k)×(9−2k) 貼在網格 (k,k)。k 是視野內縮量，戶外查一張
**時辰表**（`DS:0x209c`）—— 白天 k=0 全視野，深夜 k=3 只剩中央 3×3。
連帶認出**隊伍欄位 `+0x9f` 是「時辰」**（`0x164f7` 對它 `inc`，同時把
步數計數器 `+0xa0` 歸 1），這是存檔欄位表原本沒有的一格。

戰鬥畫面已接上：腳下畫得出地形，看不到的格子留白。實測 MAP1 (20,20)
開打，60 格地形有 37 格被牆擋住，走廊方向看得到 —— 就是地城該有的樣子。

再追一步，**世界網格從假說升級成已驗證**（`docs/formats/town-and-map.md` §2.5）。
證據是走到子地圖邊界時的換圖程式碼（0x16fec–0x17114），四個方向對稱：

| 方向 | 觸發 | 擋住條件 | 否則 |
|---|---|---|---|
| 西 | `X == 3` | 欄 == 1 | `map_id -= 10`、`X ← 59` |
| 東 | `X == 60` | 欄 == 7 | `map_id += 10`、`X ← 4` |
| 北 | `Y == 3` | 列 == 1 | `map_id -= 1`、`Y ← 59` |
| 南 | `Y == 60` | 列 == 7 | `map_id += 1`、`Y ← 4` |

「X 動十位數、Y 動個位數」把兩個軸釘死，不用再靠接邊統計與攻略方位。
擋住的條件同時給出範圍：**欄 1–7、列 1–7，世界編號 11–77**。

同時修正了假說裡的一個錯：舊表把 `MAP1/3/5` 畫成「欄 0」那一行，
其實**編號 10 以下不在世界網格上，是地城**（連 `SUM.MAP` 裡的 2、4 共五座）。
實際存在的世界子地圖 21 張，49 格裡其餘推測是純海洋、沒有存檔。

又多認出兩件事：**隊伍欄位 `+0xa3` 是目前的子地圖編號**（四段換圖都在改它，
別處拿 `>= 10` 當戶外／地城的判斷），以及**一張子地圖走得到的範圍是 4–59
而不是 0–63** —— 四周各三格是換圖邊界。

### 這一輪（2026-07-26）：怪物 AI 收尾 —— 挑目標與噴吐（`docs/re/23`）

原本掛在 `docs/spec/02-combat.md` 的缺口是「AI 挑到增益法術時要打誰」——
實測會看到怪物法師對玩家施放「聖域」。追下去發現效果分派在 `138d:0c65` 的
17 項跳表，索引是 `effect_type`：

- **只有效果 3–7 會看 K 的正負**：`K > 0` 打自己人（增益）、`K <= 0` 打玩家。
  其餘六支一律打玩家側。
- **範圍法術（效果 1）有誤傷否決**：掃完 5×5 方框，`己方命中 × 2 > 敵方命中`
  就中止並退還法力。施法者自己算在己方裡。
- **效果 0x10 = 附身術**，成功率 `投入 × 300 / (2×目標HP + 目標法力)`。

用真實資料交叉驗證：判成「打自己人」的七個法術全是護盾強化類
（FLAME SHIELD／STRENGTH／ARMOR／…／SANCTUARY），而且與 RUST ARMOR／
WEAKEN／CHILL／SLOW 構成四組「共用效果類型、K 正負相反」的對照。
英文名稱沒有參與推導。實機也走到了兩條分支（`烈焰之柱`打玩家、`聖域`給同伴）。

**順帶發現一個自己造成的陳舊標記**：`unit+0x20` 的值域表 `docs/re/20` §9.5
早就整理完，但 `ai.go` 與 `docs/spec/02` 掛著「語意未定案、已收回」。
收回本身沒錯（當時的依據確實不足），錯在收回之後沒回頭查既有文件，
等於把已解的東西重新標成未解。詳見 §5 的推翻清單。

同一輪把**噴吐**也讀完了（`docs/re/23` §7）。原函式分兩趟掃描：第一趟數命中、
第二趟扣血，中間夾一道**與範圍法術完全相同的否決**（己方×2 > 敵方就放棄）。

```
噴吐傷害 = rnd(施放者當前HP ÷ 3)
```

與投入、等級、目標護甲都無關 —— 受傷的龍噴得比較弱。兩趟用不同寫法描述
同一個錐形（一個從面前那一格算、一個從施放者算），互相對得上，等於自帶
一組交叉驗證。

順帶把 `gamedata.Monster.Special` 從「語意未確認」升級為已確認：它是
**種族／元素類型**。`MONSTER.DAT` 裡落在 AI 噴吐區間 8–12 的**只有四隻，
全是龍**（火龍 8／風龍 9／冰龍 10／巨龍 11），而免疫組 `{4,7,10}` 裡的
7 是冰系生物、10 是冰龍 ——**冰的吐息傷不了冰**。舊註解疑惑「元素系怪物的值
不連續、對不上五大符文系」，那是把兩件事混在一起了。

驗收工具也補了三樣：`-battle` 旗標（headless 截圖按不到 B 鍵）、
`-battle-monsters`（指定要放哪幾隻怪，例如三隻龍）與 `DW_LOG=1`
（戰鬥紀錄同步吐 stderr，畫面只留 8 行看不到完整時間軸）。
實機驗到 `Wind dragon 噴出吐息（波及 5 人）`。

### 這一輪（2026-07-26）：七大地形定案 + 隨機遭遇（`docs/re/24`）

專案裡掛最久的一項解了。答案是**引擎沒有第二張地形表 —— 地形就是可通行性
表的值**：同一張 `FILES.DAT 0x040`，移動時當「能不能走」，遭遇時當「這是哪一種
地形」。前面四次都在找一張不存在的對照表。

連帶把 `FILES.DAT` 剩下兩段「未知」也解了：`0x000`（64 bytes）是
**8 地形 × 8 個遭遇群組槽位**，`0x28E`（404 bytes）是 **18 個遭遇群組 × 22 bytes**。
`FILES.DAT` 到此每一個位元組都有主。

八個類別裡六個對上手冊、兩個是地城地板，**「Kudzu 樹叢」沒有自己的類別**
（否定結論，不是還沒查）。只有山丘是直接驗證（移動成本）；其餘靠「遭遇群組
內容 + 圖塊外觀 + 世界地圖佔比」三路佐證 —— 執行檔與資料檔裡都沒有地形名稱
字串。沼澤與沙漠兩條特別強：手冊點名的鬼火、鬼墳族、苦修僧、蜥蜴人四隻，
各自只出現在那一個地形掛的群組裡。

**隨機遭遇也一併接上了**（原版每走一步 1/64、`8 − rnd(7)` 隻、難度 ±1 的等級帶、
等級值 ≥100 的「清一色群」鎖定）。實機在森林走一走就遇到三隻蟲群加三隻巨蛛。
在這之前世界地圖上是完全安全的。

### 這一輪（2026-07-26）：道具效果接上（`docs/re/25`）

「選了道具不會有效果」解了。關鍵是回到第一性原理問「要生效只需要什麼」：
道具身上要有效果編號、編號要查得到效果、效果要能套到目標。前兩件機器碼直接
給死 —— Use 路徑餵給 `FUN_1000_114f` 的是道具槽 `+0x07`，而**那個載入函式就是
法術用的那一個**。一張表、一個載入函式、一個索引空間 ⇒ 道具的效果編號與法術
id 是同一個命名空間，不用再追「新道具怎麼生出 +0x07」。

道具槽補齊四個欄位：`+0x05` 已用次數、`+0x06` 總次數、`+0x07` 效果索引、
`+0x08` 強度（同時當可用旗標）。順帶解掉 `ITEMS.DAT` 那四個「語意未確認」
欄位：它們是**效果類別候選**，掉寶生成時擲兩層骰（先挑類別，再從
`DS:0x1941` 的候選表挑效果）。

**又清掉一條錯誤測試**：原本斷言「真實存檔至少列得出一件可用道具，否則過濾
規則太嚴」。實際 dump 起始存檔，五名角色 10 格的 `+0x05`–`+0x08` 全是 0 ——
**原版開局按 Use 本來就是空選單**。那條測試釘住的是還沒解出 `+0x08` 時的
不完整規則，已改成反向斷言。

實機驗證用新加的 `-give-item` 旗標塞一件 salve 進去：
`Wopple 使用 salve` → `Wopple 的力量術讓 Wopple 增加 7`。

### 這一輪（2026-07-26）：道具與怪物名稱中文化 + 修掉中文欄位對齊

畫面上冒出 `salve` 才發現**道具（30）與怪物（99）名稱整份沒進翻譯層**。
補了 `dwstrings items`／`dwstrings monsters` 兩個子命令，沿用既有的
（來源檔, 索引）機制；譯名優先取 `translations/glossary.md` 既有裁決。

接上譯名之後立刻看到「火蜥蜴」被顯示成「火蜥」——`name[:8]` 是**位元組**
截斷，中文會切在字元中間生出半個字。順著查下去發現 `%-Ns` 也一樣：
Go 依位元組補空白，中文那一列的數字欄整個往左跑。**法術選單的「最低法力」
欄其實早就歪了**，只是沒人拿截圖比對過。

加了 `textlayout.PadCells`（按排版格截斷與補白，本專案所有字元同寬 16px），
把戰鬥名冊、法術選單、道具選單、城鎮商店、名冊、治療所、神殿七處全部改掉。

**教訓**：中文化不是把字串換掉就結束 —— 換完之後每一個固定欄寬的排版都要
重新對過。這一輪如果只做翻譯不看截圖，就會留下一個「半個字」的 bug。

### 這一輪（2026-07-26）：城鎮設施差異化 + 又兩條陳舊標記

盤點畫面時發現城鎮選單掛著「城鎮設施清單尚未從原版解出，目前七種都列出」，
但 **`docs/re/02` §3.2/§3.4 早就把它解完了** —— 旗標在 `TOWN*.DAT` 的
`0x1ee`–`0x1f6`，而且該文還附了 25 座城鎮的完整 dump。接上之後每座城鎮
真的不一樣了：新格里昂只剩市集／公會／碼頭（對上攻略「到新格里昂買船」）、
鐵穹鎮七項全無只剩市集。

順帶發現 **`0x1f5` 一個位元組兼兩用**：非 0 就顯示碼頭選項，值本身又是
船價基礎。原本程式碼有兩個常數指向同一個位移，已收成一個並記錄。

同一輪清掉的另一條陳舊標記：城鎮選單抬頭的「座標對照尚未解出」——
那是上一輪就解的（`DS:0x2f86` 的 25 筆座標表），自動進城早就在跑。

### 這一輪（2026-07-26）：睡覺（`docs/re/26`）

追「旅店休息尚未接上」時翻出**整個紮營子系統** —— Camp 是一個 13 選項的
選單（Reorder／Sleep／Identify／Worship／Xorcise／View land／Trade／Drop／
Equip／Use／Hunt／Cast／Quit），睡覺只是其中一項。

睡覺的規則全部解出並實作（`internal/game/rest.go`）：

- 時辰 15–24 才睡得著，否則 `You are restless`
- 睡眠時數 =（27 − 時辰）+ rnd(6) − 1，但**時辰是直接設成 rnd(6)**，
  所以幾點躺下都在清晨 1–6 時醒來、日期 +1
- 旅店 HP+2／SP+10，野外紮營 HP+1／SP+5 並吃一份糧食（沒糧就每人 −2 HP）
- 中毒者依睡眠時數扣血，種族 4 免疫
- 紮營會重設光源、把道具的已用次數歸零（過夜充能）

**打獵也解了**（`1000:08af`）：`收穫 = max(0, rnd(16) − 6)`，0–10 份，
16 面裡 6 面空手。要角色狀態正常且會技能 8（狩獵）。兩個交叉印證：技能 8
在 `docs/re/21` 就是「狩獵」，而實測起始隊伍只有遊俠 Menhir 會；
「本日已打獵」的旗標 `char+0xef` 由睡覺清掉，合起來正好是手冊的「每日一次」。

兩個入口都接上了：**旅店**（城鎮設施 `I` → `R`）與**紮營畫面**
（世界地圖按 `R`，`S` 睡覺、`H` 打獵）。實機：20 時睡下 → 9 日 1 時醒、
睡 7 個時辰、吃掉一份糧食（8→7）。

**副產品：抓到一個標反的欄位。** 上一輪把道具槽 `+0x05`／`+0x06` 標成
「已用次數／總次數」，**標反了**。使用端只有一道 `CMP` 兩邊對調完全等價，
從那裡分不出來；睡覺常式把 `+0x06` 清成 0 才分得出 —— 清「已用次數」是
過夜充能，清「上限」會讓道具永久失效。**分不出兩個對稱欄位誰是誰時，
去找寫入端**：讀取端只說它們會被比較，寫入端才說它們是什麼。

順帶正名 `scenario.CombatStatusFlags` → `CombatStatus`：CONTEXT 的推翻清單
早就寫著「不是位元旗標，是單一列舉值」，欄位名卻沒改。

**這已經是這個 session 第三次撞到同一種問題**（`unit+0x20`、可通行性分組、
城鎮設施）。共通模式是：**結論寫進了 `docs/re/`，但把「未解」寫在程式碼註解
或畫面文字上的那一份沒有跟著改**。`rulebook/63` 講的就是這個 ——
以程式碼為準、別信過期的狀態標記；但反過來也成立：**解出來的時候，
要回頭把所有標「未解」的地方掃一遍**。

**教訓（值得記）**：第一次嘗試其實已經摸到答案，當時卻寫下「組別本身不等於
地形種類」把自己否決掉 —— 理由是「`0xfe` 一組有 19 格、太雜」，但 `0xfe` 屬於
不可通行那一側，本來就不參與地形分類。**否決一個假設前，先確認自己否決的是
它的核心而不是邊角。** 另外任務單裡「從 `DS:0x52f4` 追」那條線索是錯的：
那是個到處被重用的暫存字，照著追會一直繞。

### 這一輪（2026-07-26）：買賣裝備 + 存檔真的寫得回去 + 月份是名字（`docs/re/27`）

**經濟迴圈補完**（`internal/game/trade.go`）：議價早就實作了，但買不到東西、
裝備也換不了 —— 整套是裝飾。這一輪接上買（市集 `B`）、賣、換裝（紮營 `E`）。
店裡的貨一律是**沒有效果的平凡裝備**（效果是掉寶時生成的，起始存檔每一件的
`+0x05`–`+0x08` 都是 0）；裝備只改角色記錄的槽位索引 `+0x100`／`+0x101`，
道具本身不動；**身上正在用的不能賣**（賣掉會讓索引指向空格）。

**然後發現存的檔是假的。** 買賣換裝在畫面上都對，但 `Encode` 只把
`InventorySlotsRaw` 原樣抄回去 —— 規則層改的是解析後的 `Inventory`，兩者不同步。
`ApplyTo` 也沒寫裝備槽與戰鬥狀態，trailer 更是連時辰、日、月、糧食、
子地圖、光源都沒寫。**遊戲不會報錯，玩家只會發現東西買了又不見。**
全部補上，並補了三條測試（道具槽寫回、清空只寫型別、trailer 欄位逐一釘住）。

實機驗收比測試更有說服力：載入 → 換裝 → 存檔，與原版 `PARTY.DAT` 逐 byte 比對
**只差 `char0 +0x100` 那一個 byte**（正是換裝那一格）；載入 → 直接存檔則是
**byte-for-byte 完全相同**。

順手修掉兩個「存了卻讀不回來」：座標與時間。`-x`／`-y` 原本硬編 32,32，
存檔裡的 9,32 從來沒被用過（旗標改成負值代表「跟著存檔走」）；時鐘原本一律
`NewClock()` 從頭開始，現在走 `ClockAt(時, 日, 月, 步數)`。

**月份不是序數，是名字。** 追時鐘欄位時發現 `+0x9d`（月）在狀態列被拿去
`×4` 查 `ds:0x50f2` 的遠指標表，套進 `"Hour %d, Day %d in the Month of the %s"` ——
22 個名字（Ruby、Ebony、Gold…Hyacinth），已抽成翻譯目錄
（`assets/lang/zh-Hant/months.txt`），畫面上現在寫「13時 17日 紅玉月」。
所以月是 **0-based 的名稱索引**，原版存檔的 0 是合法值，不是未初始化
（新遊戲初始化那一段只寫日 8、時 5，月根本沒寫）。

**副產品：整個 FILES.DTT 的分段邊界從猜測升級成證據**（`docs/re/27`）。
`DEMON.INT 0x11d5d` 是切表常式，依序填 8 張遠指標表，每張的筆數就寫在迴圈的
`cmp` 常數裡，**八張相加正好 501 —— 與檔案裡的字串總數一個不差**。
兩段原本猜錯的因此修掉：`[123:153]` 是一整張 30 條的**道具型別名**表
（＝ ITEMS.DAT 的 30 筆，`Demon Crystal`／`Orb/Evertime` 是型別 28、29，
不是另一張「神器表」）、`[153:164]` 是 11 個**神祇名**。

神祇那張表還牽出**同一個 session 裡第四次的陳舊標記**：`docs/re/09` §2.2 與
`combat.go` 的註解都寫著「祈禱成功率初始值 20 未核對到，需要下一輪找出 `+0xeb`
的初始化點」，但 `docs/re/19` §3.4 早就解完了（標題還寫「這是任務單最想要的答案」），
連寫入點 `278d:107f` 都列了 —— 換算成 file offset 正是我這輪「找到」的 `0x1c54f`，
同一個地方。**我等於重解了一個已解的問題，還一度當成新發現寫進 commit message。**
兩份文件都已訂正。教訓要記反向的那一條：看到「未解」時，先查其他文件解過沒。

**教訓（與上一輪同源）**：`[467:489]` 被標成「隨機物品命名詞庫」躺了很久，
理由是「Comet Sword、Hyacinth Ring 這種組合很像老 RPG 的命名詞庫」——
那是**只看內容**得到的「像什麼」。找讀取端只花了幾分鐘就變成「是什麼」。
`rulebook/62` 又一次生效。

### 這一輪（2026-07-26）：城鎮設施從價目表變成真的能用

上一輪把買賣接上之後回頭看，發現**七個設施裡有五個只是價目表**：
治療所列得出「治療 24 金」卻治不了、酒館報得出糧價卻買不到、
神殿列得出捐獻與祈禱、公會列得出經驗值，全都按不下去。
定價公式 `docs/re/19` 早就全解完了，缺的是「做下去會怎樣」那一半。

`internal/game/services.go` 補上五項：

- **治療所**：服務項目由角色狀態決定（死亡 → 復活、束縛 → 解除、中毒 → 解毒、
  否則看外傷）。復活只回 **1 點**生命並清掉束縛等級，一般治療才補滿。
- **酒館**：買糧，份數 1–200，總價 = 單價 × 份數，隊伍糧食鉗在 255。
- **神殿**：捐獻 1 金換 1 點經驗（封頂 `0x00FFFFFF`）；祈禱費用 = 等級 × 50，
  把呼喚成功率補回 20。
- **公會**：免費升級，門檻用手冊的 20 級表（**出處是手冊不是反組譯**，
  原版那張表用軟浮點查表，還沒解碼）。
- **市集出售**：售價 = 底價的一半，不套城鎮係數、不受議價影響；
  身上正在用的不能賣。

順帶修掉一條**算錯的公式**：`HealerQuote` 的解束縛費用乘的是角色等級，
但 `docs/re/19` §5.2 寫的是 `char+0xec（束縛等級）× 費率`。畫面標籤
`Unbind %3d/lvl` 的那個 lvl 指的是束縛法術的等級，不是角色等級。
順著補上三個原本沒解碼的角色欄位：`+0xeb` 祈禱成功率、`+0xec` 束縛等級、
`+0xf0` 信奉的神祇 —— 前者原本被實作成**整隊共用一個值**，那是錯的。

實機逐項驗過（`workplace/dump/persist/`）：海濱鎮酒館買 5 份糧（8→13 份、
65→55 金）、神殿供奉 Camear 捐 10 金（經驗 1037→1047）、市集賣掉小斧（+3 金）、
特拉巴捐 26 金補滿門檻後公會升到 4 級（生命 +8、法力 +7、三項屬性各 +1）、
治療所治好 Menhir（付 24 金）。

兩個順手修掉的視覺缺陷：市集的「買價／賣價」欄名是左靠齊、數字是右靠齊，
差 3 格；升級的成長明細一行放不下、被畫布裁掉半個字。

**還沒做的**：碼頭買船／修船（船隻陣列的格式沒解，硬做只會做出一個假的）、
學院教技能（公式有了，但學院不在七種設施的熱鍵清單裡）、神殿改宗
（`(0x10 − god_id)` 的索引語意可疑，`docs/re/19` §10 明列為未解）。

### 這一輪（2026-07-26）：碼頭與學院 —— M5 只剩改宗（`docs/re/28`）

上一輪把 M5 的缺口列成三項：碼頭、學院、改宗。這一輪關掉前兩項。

**碼頭卡在「船隻記錄的格式沒解」。** `docs/re/19` §4 只寫到「10 格 × 6 bytes、
船體值在 `+0x25`」，其餘標未解 —— 沒有格式就放不了船。這一輪把讀取端與
**寫入端**都讀完，六個欄位全部定案：陣列在 trailer `+0x22`，
`X／Y／子地圖編號／船體值／固定 2／未寫`。

三個關鍵是寫入端給的（`2aed:13d6`–`144d`）：**空格的判定是船體值為 0**
（座標 (0,0) 是合法位置）、放船的目標格**必須是海面 tile `0x14` 或 `0x62`**、
**錢在成功放船之後才扣**。原版存檔依此解出兩艘船（圖 44 的 56/75、圖 1 的 67/75），
六個欄位全部落在合理值域。

「這艘船是不是我的」是個**問錯的問題** —— 原版根本沒有記載歸屬。
修船只看腳邊有沒有船，買船只是往海上多放一艘。

**學院**接成城鎮選單的第八項（熱鍵 `L`）。原版的學院是地圖上的獨立地點
（`278d:342d` 有 35 筆座標→技能對照表），但 `TOWN*.DAT` 也有三個學院槽，
本作把它當設施 —— 這是本作的安排，文件裡標明了。費用要同時付得起
金幣（`points × (5·points + 25)`）與智力點數。

**技能名也中文化了**（32 條，`assets/lang/zh-Hant/skills.txt`），
順手把 `dwstrings check` 的檢查範圍從「事件敘述 + 法術名」擴到全部七個目錄 ——
它原本一直回報「148/148 100%」，**而實際上有兩百多條名稱沒被算進去**。
現在是 356/356。進度數字漏算比沒有數字更糟，它會讓人以為翻完了。

**副產品：金幣是 4 bytes 不是 3。** 買船的扣款是 `SUB word [+0x0a]` /
`SBB word [+0x0c]` 一組 32-bit 減法。`docs/re/19` §2 早就這樣列了，
但 `save.go` 一直標「長度未知，兩份存檔的第 4 個 byte 都是 0，分不出」。
**分不出來就去找讀寫端** —— 這個 session 第三次用到同一條。

**踩到一個自己造的 crash**：`AllFacilities` 加了第八項，但 `drawTownMenu` 的
`labels` 還是七個 —— `labels[7]` 直接 panic，而且是在「走進城鎮」這條每個玩家
都會走的路徑上。headless 截圖當時只是「一直沒有畫面出來」，因為 xdotool 的
`--sync` 會等視窗出現、等不到就永遠掛著。已補上標籤與一條測試。

**順手修掉一個一直在騙人的驗收工具**：`tools/screenshot.sh` 的用法寫著
「`[-- 額外的程式參數…]`」，但 Go 的 flag 遇到 `--` 就停止解析 ——
後面的 `-map`／`-x`／`-gold` 會被整批忽略，程式照跑、照截圖，只是全用預設值。
之前幾輪的 `-x/-y` 其實從來沒生效過，是這一輪查「船怎麼找不到」才發現的。
腳本現在會剝掉誤加的 `--`，說明也改了。

實機驗過：龍牙鎮碼頭修好 56/75 的船（付 190 金）、買下新船（付 670 金）、
阿奇斯圖學院拒絕點數不足的 Wopple（要 9 點、剩 7）、讓 Podgom 學會鎚術
（付 30 金，該項改顯示「已學會」）。

### 這一輪（2026-07-26）：改宗解開 —— M5 八種設施全部可玩（`docs/re/29`）

上一輪把改宗列成「唯一沒做的」，理由是 `docs/re/19` §3.5 留的疑點：
`(0x10 − god_id)` 這個索引在 `god_id ≥ 2` 時會寫到「召喚」以下的技能格，
語意可疑，不釐清就實作等於亂寫技能旗標。

**那個算式讀錯了一個運算元。** 減掉的不是神祇編號，是**神祇編號 mod 2**。

改宗那支函式（`278d:0f38`）吃**兩個**參數，舊文件把兩個都當成 god_id：
`[bp+6]` 才是神祇編號（拿去比對 `char+0xf0`），`[bp+8]` 是餘數（拿去算教派）。
神殿入口的兩條路徑都印證這件事 —— 固定神殿走 `idiv 2` 取 `dx`，
座標表直接存餘數（表裡是 `(⌈god/2⌉, god mod 2)`，所以 `god = 2a − b`）。

於是 `0x10 − 餘數` 只會是 15（薩滿）或 16（司祭），永遠落不到別的格子。
十位神奇偶交錯 —— 奇數薩滿、偶數司祭，正好是舊文件講的「5 位司祭神 + 5 位薩滿神」；
第 11 位 Ancient One 不在座標表裡，入口另有一段專屬描述文字。

**順帶訂正祈禱的前置檢查。** `docs/re/19` §3.3 寫「旗標為零時整段比對跳過
（新關係不擋）」—— **跳轉方向讀反了**。`cmpb $0, char[+0xc8+教派] / je` 是
「沒有那個教派的技能就跳去拒絕」。所以祈禱**要求先改宗到這座神殿**，
本專案照舊文件寫成「沒有信仰的人不擋」，也一起改掉。兩條合起來才是完整流程。

**副產品：`party+0xa8` 是雙用途欄位。** `docs/re/19` §2 記成「當前位置的學院
教授技能 ID」，神殿把同一個 byte 當神祇編號讀 —— 兩邊都用 `0xFF` 代表
「這裡沒有，去查座標表」。它其實是「目前站著的特殊地點的參數」，
又一個「一個欄位兩種用途」的 1980 年代慣例。

實機：Norman 在海濱鎮改信 Camear，學會司祭（花 2 點智力），祈禱成功率立刻變 20%。

### 這一輪（2026-07-26）：掉寶生成解開（`docs/re/30`）

`docs/re/25` §3 把掉寶生成解到剩一角就停了：「第一個欄位為 0 時挑到 0 減 1
會索引到表外，沒把握就不寫進規格」。**那是把 `jne` 讀成了 `je`。**

分支方向反過來，四個欄位就有了兩種用法：`f[0] != 0` 時四個是**候選清單**、
`f[0] == 0` 時另外三個是**排除清單**。武器的 `0,10,8,9` 是「除了 10、8、9 都行」、
護甲的 `12,7,7,7` 是「12 或 7」、雕像的 `15,15,15,15` 是「一定是 15」。
兩條路都保證非零，減一之後永遠落在 0–16。30 件真貨全跑過。

整條生成鏈也一併解完（`internal/game/loot.go`）：

- **附魔**：`for i := 1..等級 { if rnd(125) < 12−i { 附魔 = i; break } }`，
  護甲與飾品 `(n+1)/2` 減半，被詛咒的取負
- **兩道效果門檻**：`rnd(100) > 等級×2+6` 或 `> 等級+16` 就沒有效果；
  被詛咒的一定沒有效果
- **強度與效果要配得起來**：`最低法力(效果) > 強度` 就整組重擲 ——
  沒有這一步會生出「有效果卻付不起」的道具
- **三種充能種類**：分支值就是 `ITEMS.DAT` 那個一直標「語意未確認」的
  `CategoryIndex`

**兩個獨立解出來的東西剛好互相解釋**：`docs/re/26` §3.3 記睡覺跳過
`+0x05 >= 100` 與 `+0x06 == 0xff` 兩種道具不充能 —— 正好就是充能種類 2
（上限一定 >= 100）與種類 1（已用次數寫 0xFF）。那個 255 是「不計次」的哨兵，
不是計數：剛生成的道具「已經用掉 255 次」講不通。

實機：`-loot 14,60,40` 發一批藥瓶，戰鬥的使用道具選單裡出現「藥瓶 +3（未鑑定）」。

**沒接上入口**：解的是「怎麼生」，不是「什麼時候生」—— 戰鬥勝利與寶箱的
掉落判定還沒追。目前只有偵錯旗標 `-loot` 這一條路。

### 這一輪（2026-07-26）：掉落接上戰鬥勝利（`docs/re/30` §5）

上一輪解完「怎麼生」，留下「什麼時候生」。生成器的進入點其實是 `1990:1107`
（上一輪引用的 `1250` 是函式中段），**全檔只有兩個呼叫端**，各自的字串一看就認得出來：

- `0xe3fc`：`"Exp per chr: %ld    Gold: %ld"`、`"Give to character ?"`、
  `"No more room!"` → **戰鬥勝利的結算**
- `0x1d6b0`：`"You see a group of merchants"`、`"They show you their wares..."`
  → **商隊遭遇**（只認出字串，流程沒讀）

戰鬥那條解完並接上了：**逐隻獨立判定** `rnd(100) <= 怪物等級×6 + 5`，
中的那隻掉一件。1 級怪 11%、10 級怪 65%、16 級以上必掉 ——
打贏一群低等怪很可能一件都沒有。道具型別是 `rnd(26) − 1` → 0–25，
**火把、提燈、惡魔水晶、恆世寶珠永遠不會掉**（前兩件雜貨、後兩件劇情物）。

**順帶解掉 `+0x0f`**：結算印道具名時拿它 ×4 去查 `ds:0x1852` 的遠指標表 ——
那個一直標「語意未解、原版實物都是 1」的 byte 是**名稱類別索引**。
分組規則還沒讀完，造新道具時仍照原版填 1。

**修掉一個結算只存在一幀的缺陷**：戰利品原本是在「按下空白鍵之後」才發，
而那一幀馬上 `a.battle = nil` 回世界地圖 —— 撿到什麼的訊息玩家永遠看不到。
改成戰鬥一結束就結算，空白鍵只負責關畫面。

兩個新的偵錯旗標：`-loot 型別,等級[,件數]` 直接發道具、`-battle-win` 直接進
勝利流程。用 xdotool 打完一整場戰鬥不切實際（要走位、要相鄰才打得到），
但「打贏之後會怎樣」得看得到。實機：打倒五隻高等怪 →「Wopple 撿到戒指」
「Wopple 撿到戰斧 +2」。

### 這一輪（2026-07-26）：搭船航行（`docs/re/31`）

碼頭買得到船之後，船只是海上一個圖案 —— 上不去，跟先前「議價能談但買不到」
是同一種空殼。這一輪把搭船接完。

整個搭船狀態就是**一個 byte**：隊伍欄位 `+0xb0` 存「目前搭的是第幾艘船」，
**格號 +1**，0 代表沒搭（0 要留給「沒搭船」，而 0 號格是合法船位）。
一個很乾脆的旁證：`0x3c0a` 在播腳步聲之前檢查這個欄位 ——
**船上不播腳步聲**。沒有人會為了別的用途在那裡檢查它。

- **上船**＝走到船那一格（比對 X、Y、子地圖三者）
- **航行**＝海面變成可通行，這是搭船唯一改變的移動規則
- **上岸**＝走上陸地，船的座標寫回記錄後清掉 `+0xb0`

**踩到一個自己造的坑，靠實機截圖抓到。** 第一版把上船判定放在
「不可通行」之後 —— 但船停在海上，海面本來就走不上去，結果是
「船看得到、上不去」（截圖：`前方無法通行`）。上船判定得排在
可通行性之前。這種錯單測抓不到：規則層的 `StepBoat` 全部綠燈，
壞的是它根本沒被呼叫到。

實機驗收用逐 byte 比對：登船 → 海上走三步 → 折回上岸 → 存檔，
與原版 `PARTY.DAT` 只差六個 byte —— 船的座標從 (39,21) 變成 (38,22)
（跟著隊伍走、停在下船處）、隊伍座標與子地圖、步數。**`+0xb0` 不在差異裡**，
上岸後回到 0。

**沒實作**：跨子地圖上船、撞岸音效、船體損耗（什麼時候掉血還沒追）。

### 這一輪（2026-07-26）：商隊遭遇的生成規則（`docs/re/32`）

掉寶生成器的第二個呼叫端讀完了：路上遇到一群商人，打招呼就能看他們的貨。

開場訊息是 `"You see a group of" + 形容詞 + " merchants"`，形容詞由**商隊規模**
決定 —— `ds:0x3612` 的對照表 `[0,1,2,2,3,4,5,5,6,6]` 讓**七個形容詞蓋十種規模**
（ragged looking / poor looking / travelling / adventuring / well dressed /
upper class / wealthy）。

貨是掉寶生成器生的：**7–10 件**（`rnd(4) + 6`），每件各自擲詛咒，
機率 `rnd(120) − 80` 鉗在 0 以上 —— **值域 0–40，而且超過一半的商隊完全乾淨**。
這是商隊與市集最大的差別：市集賣沒有效果的平凡裝備（安全但無趣），
商隊賣掉寶生成器生出來的東西（可能有效果、可能被詛咒，而且**未鑑定**）。

**踩到一個解遠指標的坑**：`ds:0x3632` 存的段值 `0x21f0` 直接套
`seg*16 − 0xC400` 會落到程式碼區 —— 這份 dump 的段值有 **+0x1000 偏移**
（與 `CALLF 0xN000:xxxx` 代表 `0xN0000 + xxxx` 是同一件事）。補上就對了。

**這一輪只做規則層，沒有遭遇畫面。** 價格公式沒解出來（生成器開頭那段
`310e` 軟浮點運算很可能是估價，但沒追它流去哪），沒有價格就沒辦法做購買介面。
硬湊一個「底價 × 某個係數」等於自己發明規則，與「反編當 oracle」相衝突。

### 這一輪（2026-07-26）：追軟浮點估價 —— 追錯方向，但撿到別的（`docs/re/30` §2.0）

上一輪說「下一步該追那段軟浮點估價，它卡著商隊購買介面」。**追過了，它不是估價。**

那個 32-bit 值是**掉落型別的最低價格門檻**：生成器先 `rnd(26) − 1` 擲型別，
再拿 `ITEMS.DAT` 的價格跟 `f(等級) + 25×等級` 比，**價格低於門檻就重擲型別**
（`cmp / jl → 回 0x1128`）。是「最低」不是「最高」—— 等級越高掉的東西越貴，
所以高等怪不會掉匕首。與商隊售價無關。

`f(等級)` 那一段軟浮點還是沒解碼（`bx` 指向 `ds:0x40e0`，但那裡是字串資料，
呼叫慣例沒讀懂）。**本專案沒有實作這道門檻** —— 用 `f = 0` 去湊等於自己發明
分布。已知的偏離是「各型別掉落機率一律均等」，寫進 `docs/re/30` §6 了。

順帶兩個確認：`[0x4e38]` 是道具型別名的遠指標表（正是 `docs/re/27` 切表常式
填的第 4 張，FILES.DTT `[123:153]`）；`[0x4cc8]` **不是神祇專用** ——
`docs/re/29` 是在神殿脈絡下看到它，掉寶也拿它存「目前要印的名字」，
它是個通用的暫存指標。

**這一輪的產出是「知道不用往那邊找了」**，程式碼沒有變。追錯方向也要記下來，
不然下一輪還會再追一次同一段。

### 這一輪（2026-07-26 續）：紮營選單其實有 14 項，Drop 與 Trade 接上了（`docs/re/33`）

`docs/re/26` 說紮營選單有 13 項，還警告過「顯示順序 ≠ 跳表索引順序」。
**兩件事都錯**：選單是 **14 項**（漏掉排在最前面的 `Party`），補上之後顯示順序
與跳表索引完全一致，連快捷鍵 `PRSIWXVTDEUHCQ` 都同一個順序。

判錯的原因值得記：當初只從字串區 `DS:0x0106` 往下讀名字，沒有讀**用它的那張
指標表**（`ds:0x94`，14 個遠指標，長度剛好抵著下一張表）。
表格類資料要從讀取端回頭定邊界，不能從資料區猜 —— `rulebook/62` 的老規矩。

實作接上 **Drop** 與 **Trade**（規則完整，沒有留白）：

- 型別 **0xfe 是地城道具**，在營地丟不掉。原版判定寫成 `> 0xfd`，所以空格
  （`0xff`）也吃到同一句「地城道具不能丟」—— 那是隨手複用，本專案分開講。
- 地城道具**丟不掉但給得出去** —— Trade 完全沒有型別檢查。
- Trade 撞到裝備中的東西會**退回選道具再問一次**，Drop 則是直接結束。
- Trade 用了漂亮的一手：把自己的戰鬥狀態暫時改成 6，借選人清單既有的過濾
  規則把自己排除，選完再改回來。順帶告訴我們**狀態 6 = 不可選取**。

實機驗收：交出小斧後存檔，與原版 `PARTY.DAT` 只差四個 byte，每一個都解釋得出來
（見 `docs/re/33` §5）。

## 3. 文件索引

### 規格層 `docs/spec/` — 實作的唯一依據

**只有標 READY 的才可以實作。**

| 檔案 | 狀態 |
|---|---|
| [`README.md`](docs/spec/README.md) | SDD 工作方式、規格分級定義 |
| [`01-rng.md`](docs/spec/01-rng.md) | **READY** — 亂數產生器 |
| [`02-combat.md`](docs/spec/02-combat.md) | **READY** — 戰鬥系統（全部子系統）|
| [`03-events.md`](docs/spec/03-events.md) | **READY** — 事件觸發 |
| [`04-movement.md`](docs/spec/04-movement.md) | **READY** — 移動與模式切換 |
| [`05-character.md`](docs/spec/05-character.md) | **READY** — 角色建立與升級 |
| [`06-time.md`](docs/spec/06-time.md) | **READY** — 時間系統 |
| [`07-graphics.md`](docs/spec/07-graphics.md) | **READY** — 素材格式與渲染 |
| [`08-town-economy.md`](docs/spec/08-town-economy.md) | **READY** — 城鎮與經濟 |
| [`09-fonts.md`](docs/spec/09-fonts.md) | **READY** — 字型與中文化 |

### 逆向筆記 `docs/re/` — 過程與證據

| 檔案 | 內容 |
|---|---|
| [`00-ghidra-setup.md`](docs/re/00-ghidra-setup.md) | **必讀** — Ghidra 環境、位址換算、六條踩雷 |
| [`01-dosbox-reference.md`](docs/re/01-dosbox-reference.md) | DOSBox 環境、timeline 語法、自動化踩雷 |
| [`02-data-loading-functions.md`](docs/re/02-data-loading-functions.md) | 事件表與城鎮的讀取函式 |
| [`03-audio-and-resources.md`](docs/re/03-audio-and-resources.md) | 音效（V1 結案）、SUM.MAP RLE |
| [`04-main-loop-state-machine.md`](docs/re/04-main-loop-state-machine.md) | 主迴圈、指令分派、時間、勝利條件 |
| [`05-event-triggering.md`](docs/re/05-event-triggering.md) | 座標 → EXITS → 事件索引 |
| [`06-combat-system.md`](docs/re/06-combat-system.md) | 戰鬥框架、命中傷害（⚠ 選單 case 表已作廢，見 09） |
| [`07-sprite-blit.md`](docs/re/07-sprite-blit.md) | EGA sprite blit、`.SHE` 格式 |
| [`08-movement-and-modes.md`](docs/re/08-movement-and-modes.md) | 移動、面向、可通行判定、模式切換 |
| [`09-spells-and-actions.md`](docs/re/09-spells-and-actions.md) | 8 個戰鬥動作的正確 case 編號與公式 |
| [`10-character-and-economy.md`](docs/re/10-character-and-economy.md) | 建角、升級公式 |
| [`11-dosbox-verification.md`](docs/re/11-dosbox-verification.md) | DOSBox 三項實測 |
| [`12-ghidra-jumptable-fix.md`](docs/re/12-ghidra-jumptable-fix.md) | 跳表 override 修復 |
| [`13-realmode-tooling-survey.md`](docs/re/13-realmode-tooling-survey.md) | real mode 工具鏈調查 |
| [`14-rng-float-equivalence.md`](docs/re/14-rng-float-equivalence.md) | RNG 浮點等價性證明 |
| [`15-spell-constants.md`](docs/re/15-spell-constants.md) | **法術 K/M 參數表**（在 `FILES.DAT`，不在 `DEMON.INT`） |
| [`16-combat-details.md`](docs/re/16-combat-details.md) | 命中修正、爆擊、戰敗判定、AOE |
| [`17-font-format.md`](docs/re/17-font-format.md) | **字型格式**（CGA 8×8 packed 2bpp 兩 bank、EGA 16×14 1bpp）與繪字函式鏈 |
| [`18-jumptable-sweep.md`](docs/re/18-jumptable-sweep.md) | 跳表全檔清掃（21 張）+ **即死／束縛／枯萎的精確判定公式** |
| [`19-town-economy.md`](docs/re/19-town-economy.md) | **城鎮經濟六大設施全部公式**；市集議價與說服技能；角色記錄 6 個新欄位 |
| [`20-summon-and-combat-units.md`](docs/re/20-summon-and-combat-units.md) | **召喚／幻術**、戰鬥單位 19 欄結構、怪物進場擲點、傷害與排序邊界 |
| [`21-skills-races-and-files-dat.md`](docs/re/21-skills-races-and-files-dat.md) | **遊戲內部技能 id 表**、種族系統與修正公式、`FILES.DAT` 完整布局 |
| [`22-resource-arena-and-passability.md`](docs/re/22-resource-arena-and-passability.md) | **18 段資源記憶體區**、**可通行性表**、子地圖退出、**事件消費者** |
| [`26-camp-and-rest.md`](docs/re/26-camp-and-rest.md) | **紮營選單（13 選項）**、睡覺的時間與回復公式、過夜充能；順帶抓到道具槽兩個欄位標反 |
| [`33-camp-menu-items.md`](docs/re/33-camp-menu-items.md) | **紮營選單 14 項**的完整對照（名稱／快捷鍵／處理常式）＋ Drop 與 Trade 的規則；訂正 `docs/re/26` 的 13 項與「順序對不上」|
| [`32-merchant-encounter.md`](docs/re/32-merchant-encounter.md) | **商隊遭遇**：規模→形容詞對照、7–10 件貨、詛咒機率 `rnd(120)−80`；價格公式未解故無購買介面 |
| [`31-sailing.md`](docs/re/31-sailing.md) | **搭船**：`+0xb0` 一個 byte 就是整個狀態、上船／航行／上岸的三段判定、「上船判定要排在可通行性之前」|
| [`30-loot-generation.md`](docs/re/30-loot-generation.md) | **掉寶生成**：四個類別欄位的候選／排除兩種用法、附魔與兩道效果門檻、強度與最低法力的配對重擲、三種充能種類 |
| [`29-temple-conversion.md`](docs/re/29-temple-conversion.md) | **改宗**：`0x10 − (神祇編號 mod 2)` 的推導、十位神的奇偶分派、Ancient One 的例外；順帶訂正祈禱前置檢查的跳轉方向 |
| [`28-ship-array.md`](docs/re/28-ship-array.md) | **船隻陣列**（trailer `+0x22`，10 格 × 6 bytes）：六個欄位、放船的水位與佔位判定、扣款時機；順帶釘死金幣是 4 bytes |
| [`27-string-table-loader.md`](docs/re/27-string-table-loader.md) | **FILES.DTT 的切表常式**：8 張表的筆數與邊界（相加 = 501），月份名、神祇名、道具型別名；修掉兩個猜錯的分段 |
| [`25-item-effects.md`](docs/re/25-item-effects.md) | **道具效果索引欄位定位**（槽 `+0x05`–`+0x08`）、道具與法術共用效果表、`ITEMS.DAT` f3–f6 ＝效果類別候選、`DS:0x1941` 生成表 |
| [`24-random-encounters.md`](docs/re/24-random-encounters.md) | **隨機遭遇**：地形＝可通行性值、18 個遭遇群組、1/64 觸發、隻數與等級公式；**七大地形對 tile 值定案** |
| [`23-ai-spell-dispatch.md`](docs/re/23-ai-spell-dispatch.md) | **怪物 AI 的施法效果分派**（17 項跳表）、**K 的正負決定打自己人還是打玩家**、範圍法術的誤傷否決、**附身術公式**、`+0x20` 陣營欄位定案 |

### 資料格式 `docs/formats/`

| 檔案 | 內容 |
|---|---|
| [`event-script.md`](docs/formats/event-script.md) | `DATA*.TXT` 事件表 |
| [`game-data-tables.md`](docs/formats/game-data-tables.md) | `MONSTER.DAT`、`ITEMS.DAT`、`PARTY.DAT` |
| [`town-and-map.md`](docs/formats/town-and-map.md) | 城鎮、地圖、`EXITS.DAT`、`SUM.MAP` |
| [`graphics.md`](docs/formats/graphics.md) | CGA / EGA 素材格式 |
| [`resource-index.md`](docs/formats/resource-index.md) | `FILES.DAT` / `FILES.DTT` |

### 研究與計畫

| 檔案 | 內容 |
|---|---|
| [`PLAN.md`](PLAN.md) | 專案計畫、待驗證項、階段分解、風險表 |
| [`README.md`](README.md) | 對外門面、文件索引、授權聲明 |
| [`docs/research/ssi-engine-architecture.md`](docs/research/ssi-engine-architecture.md) | **SSI 引擎架構研究** — 對 Gold Box 系列可能有用 |

### 翻譯

| 檔案 | 內容 |
|---|---|
| [`translations/glossary.md`](translations/glossary.md) | **統一譯名表** — 所有翻譯的唯一依據 |
| [`translations/LOCALIZE_INSTRUCTIONS.md`](translations/LOCALIZE_INSTRUCTIONS.md) | 遊戲內文字的翻譯指令（格式、硬規則、語氣），多人／多 agent 分頭翻前必讀 |
| [`docs/i18n/demon-int-strings.md`](docs/i18n/demon-int-strings.md) | `DEMON.INT` 的 840 條 UI 字串清單（**參考用，重製版不讀這個檔**）|
| `docs/manual/part-0..4.md` | 官方手冊（SSI 英文版）繁中翻譯 |
| [`docs/manual-cht/`](docs/manual-cht/) | **軟體世界 1990 年中文說明書**全 51 頁轉錄＋考據。`README` 出處與驗證價值、`01` 正文、`02` 附錄 A–E 全表、`03` 當年譯名與誤譯清單 |
| `docs/walkthrough/part-1..6.md` | 社群攻略繁中版 |

### 工具與 skill

| 路徑 | 內容 |
|---|---|
| `.claude/skills/ghidra-dos16-re/` | **16 位元 DOS Ghidra 反組譯 skill** |
| `tools/ghidra_headless.sh`、`tools/ghidra_scripts/` | Ghidra 封裝與 post-script |
| `tools/dosbox_run.sh` | DOSBox 封裝 |
| `tools/parse_*.py` | 各資料格式的 Python 解析器 |
| `tools/go.sh`、`tools/screenshot.sh` | docker 內編譯／測試；headless 跑起來送鍵截圖 |
| `internal/`、`cmd/` | Go 引擎（assets 解碼、game 規則層、ui 呈現層、demonwinter 執行檔） |

---

## 4. 術語表（Ubiquitous Language）

專案內外一致使用這些詞，寫程式碼、文件、commit 都照這個來。

### 遊戲資料

| 術語 | 意義 |
|---|---|
| **事件表** | `DATA1..5.TXT`，地城的「房間描述 + 遭遇怪物」宣告式表格。**不是 bytecode** |
| **dataset** | 一個地城／區域，切換時 `EXITS.DAT` 會被整份覆寫 |
| **sub-map** | `SUM.MAP` 內的 23 個 RLE 壓縮地圖之一，依 map ID 取用 |
| **slot（戰鬥）** | 戰鬥場上的 15 個單位槽位，0–6 怪物、7–14 玩家 |
| **slot（道具）** | 角色道具欄的 10 格，每格 17 bytes，從記錄內 **`0x0c`** 起 |
| **trailer** | `PARTY.DAT` 尾端 194 bytes 的隊伍共用資料區，從 `0x514` 起 |
| **frame（sprite）** | `.SHP`/`.SHE` 的單張圖。**CGA 16×16（64 B）、EGA 16×28（224 B）**，兩套 frame 數一一對應 |
| **glyph** | 字型單字。CGA `.FNT` 8×8 packed 2bpp（16 B，96 字 ×2 bank：一般／反白）、EGA `.FNE` 16×14 1bpp（28 B，96 字 0x20–0x7F）|
| **plane** | EGA 的位元平面。`.SHE` 是「列內 4 個 plane 各佔連續 2 bytes」（檔案）／「各佔連續 4 bytes」（載入加倍後的記憶體） |
| **載入時加倍** | EGA `.SHE` 讀進記憶體後被 `FUN_217b_0adf` 就地水平加倍（224 B → 448 B/frame）。`.PIE` 則在 blit 時加倍。EGA 素材一律「檔案存半寬、顯示時寬 ×2」 |

### 逆向工程

| 術語 | 意義 |
|---|---|
| **位址換算公式** | `file_offset = (segment − 0x1000) × 16 + offset + 0x3C00` |
| **間隙（gap）** | Ghidra 自動分析沒展開的程式碼區域，decompiler 會在上面編造控制流 |
| **跳表 override** | 用 `JumpTable.writeOverride()` 告訴 decompiler 正確的 switch 目標 |
| **oracle** | 判定正確性的依據來源，有優先序（見 §5） |
| **字串錨定** | 用語意明確的字串反查引用它的函式，找主邏輯的主要手段 |

### 規格分級

| 級別 | 意義 |
|---|---|
| **READY** | 行為、公式、邊界條件、驗收方式都明確，**可以實作** |
| **DRAFT** | 主要行為清楚，但有缺口，**需先補齊** |
| **BLOCKED** | 有已知未解問題擋著，**不可實作** |

---

## 5. 新 session 必須知道的關鍵事實

### 引擎本質

`DEMON.INT` 是**原生 8086 機器碼，不是 bytecode**。`.INT` 只是 SSI 的
「interpreter」命名慣例。這遊戲**沒有虛擬機** —— 控制流全寫死在機器碼裡，
資料檔只提供內容素材。所以重寫時要做的是「事件表直譯器」，不是 VM。

`DEMON.EXE`（6 KB）只是 loader，用 DOS EXEC（`INT 21h, AH=4Bh`）啟動 `DEMON.INT`。

### oracle 優先序（由高到低）

1. **人親自實測原版**
2. **DOSBox 原版實跑**（`tools/dosbox_run.sh`）
3. 官方手冊 / 社群攻略
4. 反組譯推論

**低層級不可推翻高層級。** 反組譯看起來再合理，被原版實跑打臉就是反組譯錯了。

> ⚠ **翻譯本不是獨立來源。** `docs/manual-cht/`（軟體世界中文說明書）是英文手冊的中譯，
> 它寫的數字與英文版相同時，只代表翻譯忠實，**不能當第二個佐證**——
> 「一天 26 小時」就是這樣一個誤判陷阱。
> 它的價值在轉錄層：同一份原始資料的兩份中文抄本同時對程式碼，抄錯的一方會現形。

### 三個反覆咬人的陷阱

**① decompiler 會靜默捏造控制流。**
Ghidra 對某些函式只展開前段，跳表後的 case body 落在間隙裡，
decompiler 不報錯反而編造。**已造成三次錯誤斷言，每次都被標成「已驗證」**。
判別法：反編譯行數遠大於宣告 byte 數、或大量 unreachable 警告 → 不可信。
含跳表的 switch 一律回原始指令讀。

**② 算術對不代表方向對，也不代表切分單位對。**
檔案大小的算術關係只約束「總位元數」。本專案踩過六次：
`OPEN.PIC` linear vs 交錯、`SUM.MAP` column-major vs row-major、
CGA sprite 32×16 vs 16×32、`.SHE` 16×56 vs 32×28、
**sprite frame 尺寸整批錯**（CGA 實際 16×16、EGA 實際 16×28），
以及 **`SUM.MAP` RLE 少解 256 格**（次數 0 = 256、區塊首 byte 要跳過）。
**錯誤版本幾乎都能通過全部測試**，有兩次還畫得出「看起來像那麼回事」的圖。
→ 視覺產物一律 dump 出來肉眼比對原版；一整批佈局假設全數失敗時，
回頭懷疑的應該是**切分單位**，不是佈局。

**②-b sprite 的專屬判準：每個 frame 必須自成一體。**
解出來若是「一格裡有 2 個或 4 個並排／堆疊的小圖」，
那是尺寸解錯把相鄰 frame 疊起來了，不是美術打包慣例。
這個徵狀出現過兩次，兩次都被合理化成「美術資源本來就這樣打包」，
於是錯誤結論頂著「已驗證」標籤活了下來。

**③ 「已驗證」標籤本身要存疑。**
特別是任何以反編譯 C 為唯一依據的結論。

**④ 最有效的檢查是「兩份互不相干的資料要對得上」。**
`SUM.MAP` 的 RLE 少解 256 格，格數、值域、size 加總、ASCII 圖全都照樣過關，
活了好幾輪。抓到它的是**城鎮座標表** —— 那張表在執行檔的程式碼段，
`SUM.MAP` 是資料檔，兩者沒有共同的錯誤來源，所以 25 個座標有沒有落在
城鎮格上是一個**沒得混過去**的問題（錯的時候 0/25，對的時候 25/25）。
→ 解開一份資料之後，優先找「另一份獨立資料能不能印證它」，
而不是再加一條自己驗自己的測試。

### 已被推翻的斷言（不要重蹈）

| 曾經寫過 | 真相 |
|---|---|
| `EXITS.DAT` 是 165 筆 2-byte 座標對 | 110 筆 3-byte `[X,Y,type]` |
| `FILES.DAT` 是資源索引 | 不是；`FILES.DTT` 才是字串池 |
| `TEMPLAT*.DAT` 是角色範本 | 是特定地點的特殊道具資料 |
| `PARTY.DAT` 的 `0x0c` 是種族 | **種族在 `0xf5`**。`0x0c` 是**道具槽陣列的起點**（一度誤判為「裝備武器槽索引」，那個在 `0x100`）|
| 戰鬥選單 case 0xa 是 Attack | 是 Examine；Attack 是 case 5 |
| EGA 素材都是 320×350 four-plane | 只有部分成立。EGA 素材的規則是「檔案存半寬、顯示時寬 ×2、高 ×1.75」|
| sprite frame 是 CGA 16×32、EGA 32×28（448 B）| **CGA 16×16（64 B）、EGA 16×28（224 B）**。錯在兩點：(1) 只讀 blit 端 `FUN_217b_07cf`，沒發現它讀的緩衝區在載入時已被 `FUN_217b_0adf` 水平加倍過，把記憶體 stride `0x1c0`(448) 當成磁碟格式；(2) 算術分不出來 —— 16×32 與 16×16 的位元組數都整除全部檔案，「3.5× 規律」兩種讀法都成立。真憑據是遊戲初始化時自己宣告的 `[0x5226]`（EGA `0xe0`=224、CGA `0x40`=64）並用它乘出各檔大小。詳見 `docs/formats/graphics.md` §4 |
| `CYPHER.SHE`/`CYPHER.SHP` 是 frame 尺寸的特例 | **不是特例，224/64 才是常態**。它只是 frame 數 27 為奇數，才無法被（錯誤的）448/128 整除，於是被當成特例硬湊過去 |
| `0a95`/`0b19` 位元展開家族與 `.SHE` 路徑無關 | 它們不在 **blit** 路徑上，但在 **載入** 路徑上（`FUN_1d9f_0a8b` → `0adf` → `0b19`），做的是水平像素加倍 |
| 一個 sprite frame 內裝 2（CGA）／4（EGA）個小圖是美術打包慣例 | 是解碼錯誤造成的假象 —— 相鄰 frame 被疊進同一格 |
| RNG 用 80 位元擴充精度 | IEEE double（53 bit） |
| 城鎮經濟的程式碼「落在 Ghidra 未發現區域」 | 只對一半。Ghidra 有線性掃描出指令，只是**沒建函式邊界、decompiler 沒被觸發**。改讀 `disassembly.asm` 的函式間隙（含 segment 開頭第一個命名函式**之前**的區域）就能讀（`docs/re/19` §1） |
| 角色記錄 `+0x102` 是戰鬥狀態位元旗標 | 是**單一列舉值**：`0`正常／`1`中毒／`2`,`3`,`4`束縛三級／`5`死亡 |
| 角色記錄裡「職業（Class）完全未找到」 | 在 `+0xf6`（0–9），兩個獨立呼叫點都拿它當技能學費表的欄索引 |
| EXP 是 3-byte 欄位 | **儲存是 4 bytes**（`0xc4`–`0xc7`），只是數值封頂在 `0x00FFFFFF`。兩種說法各對一半 |
| 傳送法術判的是 `spell_school_id(0x4e2c) == 0x11` | 判的是 **`effect_type`（`0x4e2e`）**。`0x4e2c` 全檔只跟 `1`/`4`/`6`/`8` 比較，值域到不了 `0x11` |
| 枯萎打擊可能是連續呼叫三次 `type 3/4/6` | **不是**。它是獨立的 `effect_type 14`，且套用方式不同 —— 三屬性各自「以現值為上限重擲」，不是扣減 |
| 束縛狀態是布林旗標 | 是**符文系 id**。這是「解除術系別要相符」規則能運作的前提 |
| 戰鬥單位 `+0x20` 「語意未定案，維持未解」| **收回得太保守，現已解（`docs/re/23` §5）。** 它就是陣營：`{1,2,3,4}` 怪物側、`{11,12,13,14}` 玩家側，`>= 10` 是玩家側；值域表其實 `docs/re/20` §9.5 早就整理完，`ai.go` 與 `docs/spec/02` 掛的「未定案」是**陳舊標記**。當初卡住的 `0x7b5a` 的 `== 2`：那條路徑確實只對槽位 7–14 成立，但 2 的意思是「這個玩家角色**被附身搶走了**」，不是「2 代表玩家陣營」——**前半對、後半錯**。教訓有兩條：(1) 兩個相符的呼叫點不等於解出來了；(2) 收回之後要回頭查既有文件有沒有更完整的答案，否則就是自己把已解的東西重新標成未解 |
| 建角基礎骰是 `Roll(15)`（均勻 1–15）| 是 **`2d6 + 1`**（三角分布 3–13）。舊推論說「期望值 8 只有 `Roll(15)` 成立」，漏掉「多顆骰子加常數」這一整類；觀察到的最大值 15 其實是人類 +2 之後的 13+2。**期望值對不代表分布對** —— 這個錯在畫面上完全看不出來，因為顯示的就是期望值 |
| `SUM.MAP` 的子地圖「不一定填滿 4096 格，讀完 bytes 就會提早停」 | **23/23 全部精確填滿**。那是 `decodeRLE` 兩個 bug 造成的假象：(1) 重複次數 byte = 0 代表 **256**（原函式是 do-while，先寫再減），(2) 每個區塊的**第一個 byte 不是 RLE 資料**要跳過。當時把觀察到的錯誤結果寫成規格，測試也照著寫成「不斷言等於 4096」，等於用測試把 bug 釘死 |
| 可通行性表「**組別本身不等於地形種類**，分組只能縮小範圍」 | **講反了，組別就是地形種類**（`docs/re/24`）。當時看到 `0xfe` 一組有 19 格、城鎮 tile 混在裡面就否決，但 `0xfe` 屬於不可通行那一側，本來就不參與地形分類；走得到的 0–7 那八組每組都很乾淨。**否決一個假設前，先確認自己否決的是它的核心而不是邊角** —— 這一否決讓七大地形又卡了四輪 |
| 「從 `DS:0x52f4` 的 tile 值追隨機遭遇路徑」（列了 16 個存取點）| `0x52f4` 是**到處被重用的暫存字**，在事件文字解析裡當迴圈計數器、在移動裡才暫存 tile。把暫存變數當成專用欄位去追，會一直繞 |
| `SUM.MAP` 未解，「熵值不像壓縮」「RLE 假設已被否證」 | 就是 RLE。否證那次是**值與次數的欄位順序猜反了** —— 否證「假設 + 一種實作」不等於否證那個假設 |
| 世界座標 → 城鎮編號「未解，擋住自動進城」 | 解了。tile `0x2e`/`0x64` 觸發，`DEMON.INT` `DS:0x2f86` 有 25 筆 `(X,Y)` 常數表，命中的序號就是城鎮編號 |
| 戰場 9×9「未經原版確認，只是為了配合呈現層」 | **已確認是 9×9**。`FILES.DAT 0x0A8` 的視線遮蔽表：索引以 9 展開、上限 81、內部掃描 7×7，三者只有 9×9 自洽 |
| `FILES.DAT 0x0a5` 那 179 bytes「疑似群體法術範圍模板，記錄結構沒釘死」 | 起點是 **`0x0a8`、176 bytes**，是**戰場視線遮蔽表**，不是法術範圍。當初卡住是因為只對資料做統計；改找讀取端就直接得到「49 組」的結構 |
| FILES.DTT `[467:489]` 是「隨機物品命名詞庫」（Comet Sword 之類）| 是**月份名**。狀態列 `0x70ac` 拿隊伍欄位 `+0x9d` ×4 查 `ds:0x50f2` 取名字，套進 `"...in the Month of the %s"`。只看內容只能得到「像什麼」，找讀取端才知道「是什麼」 |
| FILES.DTT `[136:151]` 是 15 個裝備類別、`[151:164]` 是 13 個神器 | 切點錯了。`[123:153]` 是一整張 **30 條的道具型別名**表（＝ ITEMS.DAT 的 30 筆），`[153:164]` 是 **11 個神祇名**。邊界由切表常式 `0x11d5d` 的筆數常數決定，八張表相加正好 501 |
| `docs/re/09`：祈禱訊息印的是「施法者姓名」（指標表 `0x4c98`）| `0x4c98` 是**神祇名**表（11 條），索引是角色記錄 `+0xf0 − 1`。訊息是「Omizeh hears you!」|
| 遊戲內的月是 1-based 的序數 | 是 **0-based 的名稱索引**。新遊戲初始化只寫日 8、時 5，月完全沒寫，所以起始值 0 是合法的第一個月（Ruby）|
| 存檔 `Encode` 已經涵蓋規則層會改的東西 | 差得遠。道具槽只抄 raw、`ApplyTo` 不寫裝備槽與戰鬥狀態、trailer 不寫時／日／月／糧食／子地圖／光源。**買賣換裝在畫面上全對，存檔時整批退回舊值** |
| 治療所的解束縛費用 = 角色等級 × 費率 | 乘的是**束縛等級**（角色記錄 `+0xec`），不是角色等級。畫面標籤 `Unbind %3d/lvl` 的 lvl 指的是束縛法術的等級 —— 標籤含糊，`docs/re/19` §5.2 的指令倒是寫得很清楚，是實作沒照著做 |
| 祈禱成功率是整隊共用一個值 | 是**每個角色各自一份**的持久欄位（`+0xeb`）。神殿改宗與祈禱都寫 20，戰鬥中成功一次 −5 |
| 存檔的金幣「長度未知，分不出 3 還是 4 bytes」 | 是 **4 bytes**。買船扣款是 `SUB word [+0x0a]` / `SBB word [+0x0c]` 一組 32-bit 減法。兩份存檔的第 4 個 byte 都是 0 所以資料面分不出來 —— 分不出來就去找讀寫端 |
| 船隻陣列「不知道哪些是我的船」 | **問錯了問題**：原版沒有記載歸屬。修船只看腳邊有沒有船，買船只是往海上多放一艘 |
| `dwstrings check` 回報「148/148，100% 翻完」 | 檢查範圍只有事件敘述與法術名，**道具／怪物／城鎮／月份／技能共 208 條沒被算進去**。已擴到 356/356 |
| 改宗的索引是 `0x10 − 神祇編號`（所以 `god_id ≥ 2` 語意可疑）| 減掉的是**神祇編號 mod 2**。那支函式吃兩個參數，舊筆記把兩個都當 god_id —— `[bp+6]` 是神祇編號、`[bp+8]` 是餘數。索引永遠是 15（薩滿）或 16（司祭）|
| 祈禱「旗標為零時整段比對跳過，新關係不擋」| **跳轉方向讀反了**：`cmpb $0, char[+0xc8+教派] / je` 是「沒有那個教派的技能就跳去拒絕」。祈禱要求**先改宗到這座神殿** |
| 掉寶的類別欄位「第一個為 0 時挑到 0 減 1 會索引到表外，沒把握不實作」| **`jne` 讀成 `je`**。`f[0] != 0` 才是「四個裡挑一個」；`f[0] == 0` 時另外三個是排除清單。兩條路都保證非零 |
| `ITEMS.DAT` 的 `CategoryIndex`「四種取值分組有規律但對不到已知的表，只能算假設」| 是**充能種類**：掉寶生成端拿它當 switch 分支值，決定使用次數怎麼算 |
| 道具槽 `+0x0f`「語意未解，兩份原版存檔的實物都是 1」| 是**名稱類別索引**：戰鬥結算印道具名時拿它 ×4 查 `ds:0x1852` 的遠指標表 |
| 掉寶生成器開頭那段軟浮點「很可能是估價」| **不是**。它是掉落型別的**最低價格門檻**（`f(等級) + 25×等級`），與售價無關 |
| `[0x4cc8]` 是神祇名稱的指標 | 是通用的「目前要印的名字」暫存指標 —— 神殿與掉寶都用它 |
| 紮營選單有 13 項，且「顯示順序 ≠ 跳表索引順序」| **14 項**（漏了 `Party`），補上之後兩者完全一致（`docs/re/33` §1）|
| 型別 0xfe 是空槽的另一種寫法 | 0xfe 是**地城道具**，只有 0xff 是空 —— Trade 找空格用的是 `== 0xff` |
| tile 98（`0x62`）是「牆」 | 是**海面**。與 `0x14` 是同一種地形的兩種浪花，載入時隨機摻著鋪。舊猜測表是「找不到 tileset、只能靠出現頻率猜」時代的產物 —— 頻率在地城裡看起來合理，是因為地城沒有海 |

---

## 6. 工作紀律

### 每一輪都要做

1. 更新受影響的 markdown
2. **重新檢視既有斷言，清掉被推翻的**（不是加註解了事，要讓讀者不被舊結論誤導）
3. commit + push
4. 更新這份 CONTEXT.md 的「現況一覽」

### 環境硬規則

- **編譯一律走 docker**（`golang:1.22-bookworm`），不污染系統
- Python 不得在系統 pip install，要用就 docker
- `workplace/orig/` 唯讀，不得修改
- 原版資料、截圖、Ghidra 專案全部 gitignore

### 多 agent 協調

- **agent 還在跑時量到的中間產物不能當結論**（踩過：把迭代暫態誤判為失敗）
- **`git add` 要指名檔案，不要加目錄**（踩過：掃進別的 agent 仍在編輯的檔案）
- 每個 agent 的結論**協調者要獨立複核一條證據鏈**才收

---

## 7. 下一步

**九份規格全部寫完、全部 READY。SDD 的規格階段到此結束，引擎本體可以開工。**

| 規格 | 涵蓋 |
|---|---|
| `01-rng` | 亂數（含浮點等價性證明）|
| `02-combat` | 完整戰鬥：行動順序、命中傷害、爆擊、毒、戰鬥風格、法術、召喚、怪物生成 |
| `03-events` | 座標→事件、傳送、一次性遭遇 |
| `04-movement` | 移動、可通行性、子地圖進出 |
| `05-character` | 建角、種族、技能、升級 |
| `06-time` | 時／日／月、日夜、隨機遭遇 |
| `07-graphics` | CGA/EGA 全部素材格式 |
| `08-town-economy` | 六大城鎮設施 |
| `09-fonts` | 字型 + 中文化設計 |

剩下的未解項**沒有一項擋住開工**：

1. `OPEN.PIE`（唯一未解的素材；開場可先用已驗證的 CGA 版頂著，
   但依完整性原則必須補上）
2. 資源 arena 索引 2/3/6/8/14/15 的內容、`ITEMS.DAT` 的 `f1`–`f6`
3. 各規格文末列的小項（都標明了不擋實作）
