# CONTEXT — 專案脈絡與文件索引

> **這份是全專案的單一入口。** 對話被壓縮、或換一個新 session 接手時，先讀這份，
> 就能重建完整全局，再依索引跳到需要的文件。
>
> 換 session 接手時再讀一份**最新的交接檔**（repo 根目錄的
> `demon_winter-handoff-<日期>.md`，目前是
> [`demon_winter-handoff-20260728.md`](demon_winter-handoff-20260728.md)）：那裡有
> 上一輪的進度、可重跑的指令、以及**已經踩過的坑**（腳本、規劃工具、背景作業）。
>
> 最後更新：2026-07-28

---

## 1. 這個專案在做什麼

把 SSI《Demon's Winter》(1988/1989, DOS) 完整逆向、在 Go / Ebiten 上重寫引擎、
再做繁體中文化。定位是**文化資產保存**，替華人遊戲圈留下這款經典。

### 兩條硬性原則

1. **完整性 > 投報**。不得以「成本高、效益低」為由跳過任何素材版本、任何格式。
   卡關就換方法，記錄「卡在哪、試過什麼」，不寫「暫緩／低投報」當結論。
2. **SDD：spec 齊了才實作**。反組譯 → 收攏成規格（`docs/spec/`）→ 才動手寫程式。
   九份規格全部 READY 後才開工，目前引擎進度見 §2。

### 不做的事

不散布原版執行檔、資料檔、美術或音樂。公開產出只有引擎程式碼與翻譯文本，
玩家自備合法原版。原版資料一律 gitignore。

---

## 2. 現況一覽

### 已完成

| 領域 | 狀態 |
|---|---|
| 官方手冊繁中版 | 全 28 頁 + 附錄，`docs/manual/`（附錄 A 已於 2026-07-25 訂正三處抄錄錯誤）|
| **軟體世界中文說明書** | 1990 年台灣代理版全 51 頁，`docs/manual-cht/` |
| 社群攻略繁中版 | 全 1,914 行，`docs/walkthrough/` |
| 統一譯名表 | `translations/glossary.md`，第 22 節收 `DATA*.TXT` 中文化新增的譯名 |
| 資料格式 | 事件表／地圖／怪物／道具／存檔／CGA 與 EGA 素材，全解 |
| 反組譯 | 主迴圈、事件觸發、戰鬥、音效、移動、建角升級、**字型渲染** |
| Go 解碼器 | `internal/assets/{gamedata,world,scenario,gfx}` + `internal/rng` |
| Ghidra 環境 | docker 化，含跳表 override 修復 |
| DOSBox 環境 | docker 化，可自動送鍵與截圖 |

### 進行中／受阻

| 項目 | 狀態 |
|---|---|
| **畫面改版（新，2026-07-28）** | 使用者指出「中英混排風格迥異」與「版面不像原版」。已做完**逐項量測評估**（`docs/ui/01-ui-assessment.md`）與**改版計畫**（`docs/ui/02-ui-plan.md`，併入四個重製版的調查）。定案約束：保留原版骨架重做觀感、**素材維持原版不動**、畫布先維持 640×400、中文一律點陣字。**字型那一項已完成**（`-ascii eten` 走倚天全形英數）；其餘七個階段待做，最急的是「金幣／糧食／隊伍血量根本不在畫面上」與「偵錯欄位混在正式畫面裡」|
| **EGA 調色盤：兩次都解錯，已定案** | 第一次錯在 `.PIE` 的調色盤值是 **6 bit**，舊碼 `&0x0f` 後索引 16 色表（已改為 `gfx.EGAColor`）。**第二次錯在 `.SHE` 素材**：檔裡沒調色盤就沿用標準 16 色表 —— 而原版開機時把那 16 格整組換掉了（`docs/re/97`：`ds:0x14b7` ＋ `2cdc:16b7`）。已加 `gfx.GamePalette`，14 種螢幕實測顏色 14/14 落在表內 |
| 標題畫面怎麼載入 | `OPEN.PIE` 的**影像已解**（608×336，見 `docs/formats/graphics.md` §5），但 `OPEN.*` 這兩個檔名在兩個執行檔裡都零命中；引擎檔名表列的是 `TITLE.PIC`，而它不在這份 dump 裡。原版在這份資料上根本開不了場 —— 這解釋了 DOSBox 截圖的「開場卡住」 |
| Go 引擎本體 | **M1–M5 完成**（見下），**M6 地城玩法本輪補齊**（陷阱、地城道具六個指令的五個、靈視兩支、治療水池、戰鬥檢視、A7 五格地點劇情），**開場標題畫面已接上**。原版版面刻意不復刻（中文需要 640×400）；**原版怪物 AI 三支全解**（`FUN_138d_03f5` 決策樹、`FUN_138d_065e` 選法術與效果分派、`FUN_138d_17b8` 噴吐）：目標記憶、選法術、投入法力、依 K 正負挑敵我、範圍誤傷否決、附身術、噴吐傷害都已實作並實機驗過，**噴吐動畫也接上了** |

### 引擎進度

| 里程碑 | 狀態 |
|---|---|
| M1 可走動的世界 | ✅ 載入地圖與地形圖塊、可通行性判定、子地圖進出、三層時間與步數推進。端到端驗證：從 (34,36) 連走 11 步 → X=45、Hour 5→6、Steps=1 |
| M2 事件與文字 | ✅ 觸發閘門（tile 0x11/0x53 查表、五個 tile 直接當索引、0x35 硬阻擋）、`EXITS.DAT` 掃描與事件索引累計、`DATA*.TXT` 文字顯示（中文化後改為 38 格逐字／逐詞斷行＋標點禁則，仍是每 5 行翻頁）。端到端：走進 (54,16) 跳出「骸骨廳。地上散落著數百具頭骨與骸骨……」並可翻頁。傳送已接（`TeleportTo`，不檢查可通行性也不推進時間，照原版直接寫座標）|
| M3 角色 | ✅ 規則層 + 建角畫面：存檔載入、剩餘智慧點數、建角擲點（種族修正＋下限鉗制＋人類 +2）、**三次重擲**（可一次勾多項）、升級。viewer 按 `P` 看名冊、`C` 建角，走完種族→擲點→職業→命名→入隊並可存檔。⚠ 重擲機制**反組譯仍未定位**，依英文原版手冊實作（oracle 優先序上手冊高於反編譯），兩本手冊的三處不一致已裁決並記在 spec |
| M4 戰鬥 | ✅ 規則＋流程：武器骰表、戰鬥風格、行動順序、普通攻擊（命中／爆擊／傷害／毒／功夫暈眩）、四個動作、法術系統（參數表 43 筆／通式／即死／束縛／枯萎）、回合狀態機、勝負判定、召喚三槽、怪物進場擲點。端到端：踏到事件格 → 讀敘述 → 6 隻怪物開打 → 跨回合結算。**玩家指令選單 + 施法選單已做**：行動點（初值＝速度）、走位與轉向、攻擊／施法／驅散不死／祈禱／汲取法力／閃避／音效開關／結束回合，熱鍵沿用原版。施法選單列 43 筆法術（中文名，最低法力對上 RE 表），通式數值增減與四類特殊判定都會生效。**5×5 AOE 已做**（選點游標＋範圍框，不分敵我）。**裝備已接進戰鬥**（武器骰索引＝ITEMS.DAT 索引+1、護甲防護查 `31f0:16c5` 那張表＋附魔、武器特效），使用道具選單依已驗證的過濾規則列出。**戰場地形與視線遮蔽已做**：戰場就是大地圖以遭遇點為中心的局部放大，看得到多大一塊由時辰決定（白天 9×9、深夜剩中央 3×3），擋視線的地形後方畫成空白（`FILES.DAT 0x0A8` 查表）。**投入點數輸入框已做**（原版的「How many S.P.」，數字鍵／↑↓／退格，範圍 [M, 目前法力]、預設全投；規則層在 `game.SPInput`，純邏輯測得到）。**單體法術的游標選目標也已做**（原版用 `FUN_138d_3fc9`）—— 順帶修掉「正前方沒人時法術靜默沒反應」的舊行為。**使用道具已接上**：道具槽 `+0x07` 是效果索引、`+0x08` 是強度，與法術**共用同一張效果表**（見 `docs/re/25`），用掉會扣次數。**城鎮設施依 `TOWN*.DAT` 旗標差異化**（`docs/re/02` §3.2）。**掉寶已接**（`RollBattleDrops` → `awardDrops`，見 `docs/re/30` §6）。**噴吐動畫已接**（`game.BreathCone` 逐格點亮，順序照原版 `138d:1a59` 的掃描，看起來是從嘴邊往外擴；圖塊來源未解先用單色方塊）|
| 存檔寫回 | ✅ `PARTY.DAT` 雙向。策略是**改寫不是重建** —— 從原始 bytes 出發只蓋已知欄位，未解區域一個 byte 都不動。驗收：讀出再寫回 byte-for-byte 相同（`PARTY.DAT`／`PARTY.BAK` 都過），另有逐欄位測試釘住「寫到哪個位移、動幾個 byte」。進度存檔預設寫到 `workplace/save/`，**不覆蓋玩家的原版 `PARTY.DAT`** |
| **地形渲染改用 EGA** | ✅ **2026-07-27**。引擎原本只載 CGA（`DEMON.SHP`，16×16 四色），EGA 解碼器早就寫好卻沒接。現在 `-video ega\|cga`（**預設 ega**）：EGA 走 `.SHE` ＋ `GamePalette`，一格顯示 32×28；CGA 保留（完整性原則，是原版的另一個發行版本不是畫質選項）。**版面沒有衝突** —— 兩種模式的格寬都是 32，地圖視窗仍 288 寬，右側 21 個中文格不受影響，只有高從 288 變 252。隊伍與船也改畫原版 glyph（`internal/game/partyglyph.go`），不再是黃框。驗收：拿 DOSBox 原版**原生 640×350** 截圖（`key:ctrl+F5`，`shot:` 抓的 1024×768 是縮放的、顏色不可信）在同一個存檔位置逐格對照，戶外與地城兩個場景的地形、河流走向、廢墟位置、隊伍 sprite 全部對上 |
| 音效 | ✅ PC speaker 全部九個效果（8 個 C 大調單音 C3–C4 + 1 段 11 音符陣亡旋律）。合成層 `internal/audio/pcspeaker` 是純函式（方波，不是正弦波 —— 喇叭只有開關），測試用數過零點反推頻率驗證音高。`cmd/dwsound` 可輸出 WAV 用耳朵驗收。**沒有音效裝置時整段跳過**：Ebiten 的 audio context 會掛進主迴圈，容器裡建起來會讓遊戲凍住（實測踩過）|
| 離開與存檔語意 | ✅ 照 `esc-cancel-f10-quit-autosave` 鐵則：ESC 只取消／退回上一層，**F10 才離開**並跳 Y／N 確認，選 Y 才自動存檔。存檔失敗就不離開 |
| 城鎮設施可操作 | ✅ 八種設施全部接上實際效果，含神殿的捐獻／祈禱／改宗三項 |
| 存檔真的寫得回去 | ✅ 道具槽、裝備槽、戰鬥狀態、時／日／月／步數、糧食、子地圖、光源、座標全部雙向。驗收是 byte 級的：載入 → 直接存檔與原版 `PARTY.DAT` **完全相同**；載入 → 換裝 → 存檔**只差那一個 byte**。改寫策略是「只蓋已解欄位，未解的 bytes 一個都不動」|
| M6 地城玩法 | ✅ **本週補齊**。陷阱（七種傷害＋`L` 偵測／解除）、地城道具六個指令的五個（`T` 拾取／`D` 丟棄／`E` 檢視／`M` 推開／`U` 使用，只剩 `I` 探查）、靈視兩支（`V` 觀室／`X` 鑑物）、治療水池、戰鬥 `?` 檢視、地點劇情 6 格（壓牆走廊、鐵匠鋪、兩道密語、艾瑞戈爾）。**兩條解謎鏈實跑驗過**：`Iron key`＋`Cage`、`Mallet`＋`Man in glass`。連帶鋪好 `MAPn.MAP` 的寫回路徑（第三份「就地改寫」的存檔資料），順手修好「密語門解完換張圖就白解」的舊 bug |
| M5 城鎮與經濟 | ✅ **八種設施全部可實際操作**：市集（議價／買／賣）、治療所（四項服務）、旅店（休息）、酒館（買糧）、神殿（捐獻／祈禱／改宗）、公會（免費升級）、碼頭（買船／修船）、學院（教技能）。價格體系完整；設施旗標 `+0x1ee`–`+0x1f6` 已解，畫面依實際設施過濾；**自動進城已完成**（tile `0x2e`/`0x64` → 25 筆座標表，25/25 對得上）|
| 建角擲點分佈（**已訂正**）| ✅ **公式是 `max(3, 2d6 + 種族修正 + 1)`，人類再 +2**（`DEMON.INT` 0x13bba）。原本推定的 `Roll(15)` 是錯的 —— 期望值剛好也是 8，所以「該種族平均」那一欄一直對，錯的是分布。**三次重擲機制也在同一段找到**（0x13b9d／0x13d60），初擲 + 兩次重擲、可一次勾多項、沒有「低於 6」門檻，三個原本靠手冊裁決的結論全部成立 |
| 中文化 | ✅ **資料字串 500/500（100%）**。顯示層完成、倚天 16×15 點陣字（Big5 分區索引已驗證）、中英混排排版格與標點禁則、畫布 640×400、全介面中文。翻譯目錄現況：事件敘述 117（`data1`–`data5`）、法術 43、道具 30、怪物 99、城鎮 25、月份 22、技能 32、劇情 20、**地城道具 112** —— 共 **500 條資料字串**，另加 **749 條介面文案**（2026-07-29 `dwstrings uicheck` 實數，語意化 key）。品質閘全綠。<br>地城道具那一批：`FILES.DTT` 164–463 共 300 條，**真正可翻的是 112 條** ——`+3` 座標、`+4` 查表鍵、`*` 佔位、`T`/`P`/`S` 參數都不能動（判準見 `cmd/dwstrings/dungeonitems.go`）。<br>**D1 已完成**：正式介面文案全走目錄；19 條開發者 debug／`-scene` 描述由檢查器明確列出，`dwstrings uicheck` 持續守門。<br>`DEMON.INT` 的 800 條 UI 文案只抽了清單（重製版不讀它，是參考不是資源）|

程式碼結構：`internal/assets`（純解碼，回傳 `image.RGBA`，不認識 Ebiten）→
`internal/game`（規則層，不認識畫面）→ `internal/ui`（Ebiten 呈現層）→ `cmd/demonwinter`。
`internal/ui/textlayout`（斷行分頁）與 `internal/ui/layout`（版面座標）刻意不依賴 Ebiten ——
Ebiten 在 init 期就要求顯示器，混進去這兩件事在無頭環境下連測都測不了。

倚天字型與原版資料同樣**不隨本專案散布**，放在 gitignore 的 `workplace/eten/`，
執行時用 `-eten` 指向自備的倚天 16×15 `STDFONT.15`／`SPCFONT.15`
（檔名大小寫皆可）。預設以 `-eten-bold=true` 橫向加粗一像素，做法與
《猴島小英雄 2》中文化相同；需要原始細字時可改用 `-eten-bold=false`。

中文化的工作流：`cmd/dwstrings events` 抽字串成 `assets/lang/zh-Hant/*.txt`（原文與譯文並存）→
依 `translations/LOCALIZE_INSTRUCTIONS.md` 翻譯 → `cmd/dwstrings check` 過閘門。
閘門檢查五件事：原文是否被改動（那是查表 key）、控制序列數量、非 Big5 字元、
譯文長度、**譯名是否漂移**（原文出現的專有名詞，譯文必須用 `glossary.md` 指定的譯法）。

建置與驗證一律走 docker：`tools/go.sh <go 子指令>`、
`tools/screenshot.sh <out.png> [KEYS=Up,Right,…] [程式參數]`（headless Xvfb + xdotool 送鍵）。

**截圖驗收要帶 `-seed`。** 戰鬥的行動順序由怪物進場擲點決定，
時間種子讓「走五步再施法」這種固定按鍵序列每次跑到不同畫面，根本重跑不出結果。

**畫面預設 EGA**（`-video ega|cga`）。素材比對用 `tools/tileatlas`／`tools/cgaatlas`
出帶編號的圖塊總覽、`tools/cropzoom` 裁放大並印顏色直方圖。

### 本週收尾（2026-07-27）：地城玩法補齊，缺口移到中文化那一側

這一段是週結，細節在下面各輪。**狀態的單一真相來源是 §7。**

**接上的**（全部有實跑截圖／trace 驗收）：

| | |
|---|---|
| 地城道具 | `T` 拾取／`D` 丟棄／`E` 檢視／`M` 推開／`U` 使用（**只剩 `I`**）|
| 靈視 | `V` 觀室、`X` 鑑物（A2 做完就解封） |
| 地點劇情 | case 1／2 壓牆走廊、case 4 鐵匠鋪（A7 從 3 格變 6 格）|
| 基礎設施 | `MAPn.MAP` 寫回路徑（第三份「就地改寫」的存檔資料）|

**順手修掉的**：密語門解完換張地圖就白解（三份文件都抄著同一句錯的
「原版只改記憶體」）；鍵位說明溢出畫面切掉 `F10`；`filepath.Dir(a.savePath)`
散在五處收成 `a.saveDir()`；鐵匠鋪的輸入排在文字框之前把翻頁鍵吃掉。

**這一週推翻的已發布斷言**（七條）：
`param == 2` 是死碼、`N` 的新道具怎麼出現、「牆只開在記憶體裡」、
`E` 看的是腳下那件、「`T` 是三組兩位數」、`docs/re/33` 的型別 `0xff`、
`docs/re/28` 的「固定 2、語意未解」。

**一個沒發生的事故**：差點把 `docs/re/58` 那張被十幾份文件引用的動作表
「訂正」成錯的 —— 關鍵的 `sub ax, 6` 被內嵌跳表的錯位反組譯藏住了。
教訓寫進記憶（`corrections-need-more-proof-than-claims`）。

**停手的一項**：`I` 探查周圍。連續三輪「找到看起來對的答案 → 套到別的指令
就壞」，是 `rulebook/41` 的訊號。整條鏈都讀出來了（`docs/re/04` §3.1.1／3.1.2），
卡在「選單指令碼 → 動作碼」中間還有一步沒讀到。**下一次的入口很明確**：
`FUN_222f_0b0e`（`0x169fe`）的開頭，把方向鍵與指令選單兩條輸入路徑分開。

**下一輪最划算的一項**：A6（全程試玩破關驗收）。邊界換圖已接上（`docs/playtest/10`），
路線問得出來了；剩下的是**買船 ＋ 練到 120 法力**這兩段真的跑一次。
與 A6（全程試玩 —— A1／A2 接上之後地城段第一次跑得完整）。

### 這一輪（2026-07-27 續三十九）：鐵匠鋪，順帶解出「劇情送道具」的共用常式

A7 再接一格：case 4（地圖 3 的 (23,26)）。老巨魔鐵匠白送一把
「為了要殺 Xeres 的人」打造的武器。

讀它的時候挖到一支**好幾個劇情 case 共用的常式** `15be:11ff`：
印一句話 → 問「哪個角色來拿」→ 找第一個空槽 → **逐欄位寫死一件道具** →
在 `party[0xb3 + param]` 記一格「拿過了」。

**`+0xb3` 起是「劇情道具拿過了沒」的旗標陣列** —— 而 case 4 的入口閘門
`+0xb7` 正好是 `0xb3 + 4`。兩邊互相印證，這一段之前在 save.go 完全沒有記載。

> ⚠ 跳表有 7 個 param，param 6 會寫到 `+0xb9` —— 而 `+0xb9` 是**劇情階段**
> （`docs/re/81` 驗過）。**已驗證的那個優先**，所以陣列只認 6 格。
> 這是這個 session 學到的那一條：**新結論與既有常數衝突時，先假設新結論錯。**

鐵匠那把的規格逐欄位照抄，包括**附魔 −1**（共用段先寫 0，param 4 再覆寫成
−1）。一把 −1 的闊劍配強度 4 的附帶法術、材質類別拉到頂 ——
那個 −1 大概是「殺 Xeres」的代價，**不要「修正」成 +1**。

其餘六個 param 沒讀：對應的劇情 case 還沒接，**沒有呼叫端就確認不了誰是誰**，
所以表只填一格，其餘明確回「不給」。

**實跑抓到一個排序 bug**：`updateBlacksmith` 我插在文字框處理**之前**，
於是它把翻頁鍵吃掉了 —— 台詞永遠翻不完，`trace` 停在「文字框」不動。
移到文字框之後就好了。**這種東西單元測試看不到**，只有實跑會卡住。

**實跑驗收**：(22,26) → (23,26) 觸發 → 台詞兩頁 → 選人 → Wopple 第 2 格
拿到闊劍（七個欄位逐一對上）；存檔重讀 `PlotGifts[4] = 1` 與道具都還在。

### 這一輪（2026-07-27 續三十八）：壓牆走廊 —— A7 第一組會殺人的 case

繼續挑有明確終點的：A7（地點劇情 16 格）在 worklist 上標著「每格是獨立
小工作」，而 `docs/re/83` 早就把 15 個 case 的觸發座標全解了。
挑 case 1／2 —— 它們是**一組**，而且是全遊戲唯一一個會直接殺死全隊的場景機關。

```
case 1  走廊兩端 (15,38)／(23,38) → 122f:28d0(1, 0) ＝ **重新載入 MAP1.MAP**
                                    ＋ 印「沉重機械運轉的聲音」→ 牆全部復位
case 2  走廊中段              → 由第 35 列往下找第一列還不是牆的，
                                把它與鏡射列（0x4c − 它）的 x=16..21 都填成牆；
                                填完隊伍腳下若是牆 → 全隊死亡
```

鏡射軸是第 38 列，兩側同時往中間夾。玩法就是**在被夾到之前跑到另一端**。

> **一個容易搞混的地方**：case 2 **不存檔**，它只動記憶體。
> 與 `M` 推家具、密語門開牆不一樣（那兩個會寫回 `MAPn.MAP`，續三十四）。
> 「不存」是這個機關的一部分 —— 復位靠的正是 case 1 從檔案重讀一次。
> 如果順手把它也存起來，走廊會變成永久封死，而且**存檔壞掉之後修不回來**。

順帶把 `filepath.Dir(a.savePath)` 收成 `a.saveDir()` —— 它已經散在五個
地方（存檔、特殊格、位置表、地圖寫回、地圖載入），而這五處**必須是同一個
目錄**，有一處漂掉的症狀是「存了但讀不回來」，沒有錯誤訊息。

**實跑驗收**：從 (16,38) 往東，牆一步一列夾進來、走廊縮成一格高（截圖對照）；
多走一步 → `隊伍被牆壓碎了` ＋ 全隊死亡畫面；退回 (15,38) → 機械聲 ＋ 走廊全開。

資料上的觀察：**(16,38) 不觸發**，最西邊會觸發的是 (17,38)。

### 這一輪（2026-07-27 續三十七）：`I` 停手，改接被 A2 解封的 A5b 鑑物

`I` 那條線停了（理由見續三十六：連續三輪「找到看起來對的答案 → 套到別的
指令就壞」，是 `rulebook/41` 的訊號）。改去看 worklist 上**因為 A2 做完而
解封**的項目 —— A5b 鑑物就是。

`docs/re/93` §2 早就把它讀完了，當時的結論是「**前置是 A2，不是它自己**，
現在接只會得到一個空選單」。現在地城道具在了，直接接。

它的語意值得記一筆：**鑑物不是鑑定價值，是解謎提示系統。**
原版讀的是地城道具的 `+4` 欄（`0x1949b`），也就是「這件東西要搭配哪一件」——
與 `U` 的比對用同一欄。手冊那句「可得知物品未來可能的用途」是字面意思。

三個容易漏的細節，全部照原版：

- **失敗照樣扣額度**（`inc` 在 `0x1943e`，排在擲點之前）。少了這一條，
  玩家可以一直按到成功為止。
- 失敗率是 **`Roll(3) == 2`** ＝ 1/3 **失敗**，不是 1/3 成功。
- 前置（技能／額度／擲點）在**選東西之前**跑完，所以失敗時連選單都不開。

順帶把 `partyHasViewRoom` 收成 `partyHasSkill(party, id)` —— 觀室與鑑物
本來要各抄一份「死人不算」的掃隊迴圈。

**畫面被擠爆了**：一鍵一行的鍵位說明加到第 17 行，`F10：離開遊戲` 被切掉
一半，而看起來只是「最後一行怪怪的」。壓成八行（21 格寬，一行放兩到三個）。

**實跑驗收**：站在 `Cage` 上按 `X` → `/Iron key 的影像浮現在腦海裡`；
接著兩次擲點失敗，各扣一次額度；第四次「靈視之力已經耗盡」。

### 這一輪（2026-07-27 續三十六）：找 `I` 的過程中，差點把一張對的表訂正成錯的

只剩 `I` 探查周圍。找它的入口花了幾步，結果有兩個。

**`I` 是原版主選單的 `Inspect`** —— 而這件事 `docs/re/04` §3.1 早就寫好了：
按鍵表 `ds:0x216d` ＝ `WPSCLTDMEUIVXRQ`，第 11 個就是 `I`。
**第九次「答案在自己的筆記裡」。** 我先去掃 `docs/re/58` 那張動作表找未解的格子，
再去翻手冊 —— 兩步都繞了路，而正確的第一步是查索引。

**第二個結果是一個沒發生的事故。** 我讀跳表（檔位移 `0x16453`，`cs:0x563`）時
看到邊界是 `cmp ax, 0x15`、第 0 筆是 `222f:1606`，而 `docs/re/58` 把它標成 `0x06`。
逐筆比對之後每一格都差 6，**看起來就是一個乾淨的 off-by-6**，
我已經把訂正文字寫好了。

動手改之前多看了一眼上游，找到這條：

```
1647d  2d 06 00   sub ax, 6      ; ← 就在 cmp 前面三個 byte
16480  3d 15 00   cmp ax, 0x15
```

**動作碼確實是 `0x06`–`0x1a`，`docs/re/58` 從頭到尾是對的。**
沒看到那條 `sub` 的原因：跳表是**內嵌在程式碼裡的 42 bytes 資料**，
反組譯從表中間開始就一路錯位，而 `2d 06 00` 剛好落在錯位的那一段裡
（我讀到的是 `add al,0xb3` 那種垃圾）。

> 這比之前那幾次「答案在筆記裡」嚴重一級：**那張表被十幾份文件引用著**，
> 訂正下去會把一整批正確的引用改成錯的，而且錯得很有說服力
> （commit message 會寫「逐筆比對，全部差 6」）。
>
> 兩條可帶走的：
> ① **內嵌跳表附近的反組譯，在找到表的結尾之前對齊都是錯的。**
> ② 驗一個 switch 的 base，`cmp`／`shl`／`jmp` 三條之外**要往前多看幾條** ——
> 我看的三條全都符合「0-based」的假設，那是因為關鍵那條在視窗外。

**`I` 還是沒接。** 卡在「主選單指令碼 → 動作碼」的轉換：
`ds:0x217c` 那 11 個 word（`0004 0007 0009 000f 0008 000a 000e 000c 000b 000d 0005`）
換過幾種 base 都只有兩三格對得上已驗證的動作表。找到正確的 base 再動手 ——
**猜一個接上去，症狀會是「按 I 跑到別的功能」，而那種錯很難看出來。**

### 這一輪（2026-07-27 續三十五）：`U` 接上了 —— 而「死碼」那句話是錯的

`U` 是地城解謎的入口，卡了好幾輪的 `N` 動作也在這裡。兩個都解了，
而**解法是去讀我一直沒讀的那一支**（`122f:1bf4`）。

`U` 的第二段是一個三選一的小選單：**`Use on: Character / Room / Quit`**
（`ds:0x2334`，熱鍵 `CRQ`）。手冊「從選單中依序選取物品」講的就是它。
`Room` 選腳下的、`Character` 選身上另一件。

兩個被推翻的斷言：

**「`param == 2` 是死碼」是錯的。** 我當時掃了 `222f:2088` 的外部呼叫端，
三個分別 push 0／1／3，就下了結論。**漏了第四個：函式自己** ——
`0x18114` 在 `Use on: Character` 那一支裡 `mov WORD PTR [bp+6], 2`。
又是「搜尋落空 ≠ 不存在」，這次的形狀是「只掃呼叫端，沒掃函式內對參數的寫入」。

**「新道具怎麼出現沒追到」的答案在 `0x183d7`**：`N` 的最後一步呼叫
`122f:2845` —— **與 `Drop` 同一支**，把新道具放在隊伍腳下。
所以 `Mallet` 敲 `Man in glass`：Mallet 用掉、玻璃人消失、
`Man in trance` 出現在你腳下。實跑驗過整條。

第三個小的：**「`T` 是三組兩位數」也是錯的**。資料裡有五個字元的
`T07203`（還有 `P04480`、`+3` 的 `09360`）。原版第二個字元讀到的是
字串結尾的 NUL，所以三段程式碼寫法不同、結果相同。
**判讀來自反組譯，驗證要來自資料** —— 我一度照著錯的判讀另寫一支解析器，
被測試打臉。

順帶把 `UnknownC1` 改名成 `EndingOffered`：`docs/re/95` §3.5 早就查出
它是 `S1` 結局抉擇的一次性閂鎖，欄位名還掛著「語意未解」。

**實跑驗收**（兩條獨立的解謎鏈）：
`Iron key` ＋ `Cage` → 文字框印出「老鼠從西牆中央的密門跑出去」；
`Mallet` ＋ `Man in glass` → `Man in trance` 出現在腳下，`T` 撿得到。

沒做完的：`S` 的 `15be:1694(n)`「播第 n 段劇情」還沒讀出來
（那支開頭在載資源檔，不是劇情播放器），所以 `S1`／`S2` 的劇情文字先缺；
`S1` 原版會問「誰要成為神」再選一名角色，現有的結局畫面只有接受／婉拒。

### 這一輪（2026-07-27 續三十四）：`M` 接上了，順帶修好一個「謎題白解」的 bug

`M` 的前置是地圖寫回，鋪完之後發現**它修的不只是 `M`**。

`122f:28d0`（存／載地圖）有七個呼叫端。查了每一個的模式參數，
其中 `0x1a383` 是 `(5, 1)` ——「子地圖 5、模式 1 ＝ 寫檔」，
而它的前一行正是 `docs/re/84` 那個密語門：`map[0x48b] = 0`。

所以 `docs/re/84` 那句「**牆只開在記憶體裡，離開地城再進來會回到原狀，
那是 1988 年的行為，照抄**」是錯的。同一句話在三個地方各抄了一份
（`docs/re/84`、`world/mapfile.go` 的 `SetTileAt` 註解、`riddleui.go`），
而引擎照著做 —— 症狀是**解完密語謎題換張地圖再回來，牆又長回去**，
畫面上完全看不出原因。三處都改了。

> 這是「一條規則只留一份實作」的變體：**一個錯誤的斷言抄成三份，
> 就要改三個地方才清得掉**。而且抄的那兩份看起來都很有說服力
> （都寫著「原版就是如此」）。

實作：`world.Map.Encode`／`world.SaveMap`，`LoadByID` 多一段
「先看存檔目錄」（與 `nSS.DAT`／`ITEMLOCB.DAT` 同一套）。
**只對 1／3／5 存** —— 原版的存檔端不分編號一律寫 `MAP%d.MAP`，
但載入端只有 1／3／5 讀這個檔名，在地圖 2／4 改的東西會留下一個
永遠不會被讀的檔然後消失。那是原版的洞，不複製。
（實務上碰不到：三件推得動的家具在 1／3，兩個 `P` 動作在 1／3，密語門在 5。）

`M` 本身有一條容易漏的：**那一格已經是目標 tile 就什麼也不做**
（原版 `0x1961b` 明文比對過）。少了它，推第二次還是會印
「發生了什麼事……」。另外 `+3` 的第三組**不是固定兩位**——
`Wooden altar` 的 `09360` 只有 5 個字元，原版對第三組是 `atoi` 讀到結尾。

順手把 `M` 鍵讓回原版（偵錯用的「就地遇到商隊」挪到 `F4`）。

**實跑驗收**：地圖 3 (46,12) 推 `Old bookcase` → (46,11) 的牆開了（截圖對照）；
再推一次說「什麼也沒發生」；`MAP3.MAP` 只有 offset 751 一個 byte
從 `0x31` 變 0；**關掉重開之後還是推不動**。

### 這一輪（2026-07-27 續三十三）：`E` 接上了，`M` 挖出第三份「就地改寫」的檔案

接 `E` 之前先把 `Examine`（`222f:2088(3)`）與 `Move:`（`222f:3621`）兩支讀完。
又是兩個推測被推翻：

**`E` 檢視的是身上那件，不是腳下那件。** 六個指令其實分成兩組
（`docs/re/95` §3.8）：`T`／`M` 選腳下這一格（`222f:2da5`），
`E`／`D`／`U` 選「哪個角色的第幾格」（`122f:1d23`）。
規格原本把 `E` 寫成「選一件腳下的」—— 那是從 `T` 類推的，不是讀出來的。

**`M` 會把整張地圖寫回 `MAPn.MAP`**（`0x19665` → `122f:28d0(子地圖, 1)`）。
所以那是**第三份就地改寫的資料**，前兩份是 `nSS.DAT` 與 `ITEMLOCB.DAT`。
原版直接蓋回自己的資料目錄 —— 與 `docs/re/87`「出貨的 `PARTY.DAT` 是玩過的」
同一個現象，只是這次輪到地圖檔。
**本專案不能照抄**（`workplace/orig` 唯讀），`M` 的前置因此是鋪
「母本 → 存檔目錄 → 寫回」那條路，不是它自己那幾行邏輯。

順帶解掉一個舊懸案：`Move:` 寫完檔案之後會把船重新蓋回地圖，
tile ＝ `船[+4] + 0x3f` —— **`docs/re/28` 那個「原版一律寫 2、語意未解」
的欄位是船的種類**。（只補第 9 格、只在地圖 1，是寫死配對出貨資料，
不必移植。）

`E` 實跑驗過：(21,27) 拿 `Scroll` → `E` → 文字框印出 Qoorik 的線索。
`Iron key` 是 50 件裡兩件沒有 `+2` 的其中之一，剛好把
`You see nothing special about the %s` 那條 fallback 也驗到了。

### 這一輪（2026-07-27 續三十二）：`T`／`D` 接上了 —— 而正確答案在我沒去讀的那一支裡

照規格的順序接 `T` 拾取與 `D` 丟棄。動手前先把兩支的消費端讀完，
結果**上一輪繞的那一大圈本來可以不用繞**：

```
1996f  mov BYTE PTR es:[bx+si+0xc], 0xfe      ; ← Take: 的道具欄寫入端
```

一行明碼。前兩輪我從 `N` 動作那一頭追（它在同一支函式裡，看起來更近），
而 `N` 在可達路徑上寫的是 `0xff`。**入口選錯，追得再仔細也只會確認錯的那一半。**
「答案在別的地方」這次不是在自己的筆記裡，是在**隔壁那支我沒去讀的函式**裡。

讀完之後有三件事與我原本寫在規格裡的**推測**不一樣：

| | 規格原本寫的（推的）| 實際（`docs/re/95` §3.6／§3.7）|
|---|---|---|
| 拿走時清位置表 | 子地圖那一個 byte 寫 0 | **三個 byte 全清**（`N` 才是只清子地圖）|
| 丟棄寫回哪一筆 | 「在目前座標**新增**一筆」 | **靠名字 strcmp 掃字串表回推索引**，改寫原本那一筆 |
| 欄位滿了的訊息 | 沿用 Trade 的 `%s is full` | `No more room.`（ds:0x27b6），而且**選完角色之後才檢查** |

第二條順帶暴露一個原版的洞：名字掃不到時 `i` 停在 51，
接著往 `itemloc[153..155]` 寫 —— 有效區之外。本專案加了檢查並拒絕。
名字也不是唯一的（`Bed` 兩件），原版取第一個命中的。

還有一個**只有實跑才會發現的**：清單裡把「拿不走的理由」接在名字後面，
`/Bed （It is too hea` 被 21 格的欄寬裁掉半句 —— 而畫面上看起來
就像資料本來就那樣寫。改成只列名字（原版也只列名字），理由等選了才說。

順手把 `T` 鍵讓回原版：它原本綁著偵錯用的「就地進城」，那個挪到 `F3`。

**實跑驗收**（地圖 1，三段獨立的路徑）：
(14,22) 拿 `Iron key` → 丟 → 撿回來；(13,22) 的 `Bed` 印 `It is too heavy`
且留在原地；(13,26) 同一格兩件血清各自拿得到。
存檔逐位元組比對：`ITEMLOCB.DAT` 只有那一筆的三個 byte 變 0，
`PARTY.DAT` 往返 0 差異，重讀後 `0xfe`／`Red serum` 都還在。

### 這一輪（2026-07-27 續三十一）：§3.1 定案 —— 而答案早就在 `docs/re/33` 裡

上一輪把 §3.1 降級成「未定案」，列了三個要逐指令追的問題。這一輪追完，
**但真正解掉它的不是反組譯**：

> `docs/re/33` §3：「**型別 0xfe 是地城道具。** 判定寫成 `> 0xfd` 而不是
> `== 0xfe`，所以空格（`0xff`）也會撞上同一句『地城道具不能在營地丟』。」
>
> §4：「找空格用的是 `== 0xff` 而不是 `> 0xfd`，所以 **0xfe 確實不是空格**，
> 是一個真的型別值。」

**兩條獨立證據，早就寫好了。** 這是這個 session 第八次「答案在自己的筆記裡」。

降級那一步是對的（避免蓋在錯前提上），但如果第一版寫 §3.1 時就
`grep 0xfe`，兩輪都省下來了。**寫下任何「原版的某個值是 X」之前先 grep X** ——
這條現在有八次案例，該進記憶了。

逐指令追完的部分（回答了降級時列的第 1、3 個問題）：
名字複製到 `角色記錄 + 角色×0x104 + 0x0d + 槽位×17`，NUL 結尾；
型別寫到同一個槽的 `+0x0c`（`si` 沒有 `+ k`，確認是同一格）。
所以槽內 `[0]` 是型別、`[1..16]` 是 16 bytes 的名字。

**剩一個洞**：`N` 動作在可達路徑（param 0／1／3）上寫的型別是 `0xff` ——
也就是把槽清空 ——「只改型別不清其餘 bytes」是原版既有行為
（`docs/re/33` 記過 Wopple 那一格的殘值）。所以可達路徑上 `N` 做的是
「消耗手上那件 ＋ 地圖那一格清掉」，**新道具怎麼出現沒追到**。
寫 `0xfe` 的那一支掛在 `param == 2`（**當時以為是死碼 —— 後來證實不是，
見續三十五**），所以 `0xfe` 是誰寫的仍開放。

規格因此解封大半：**`E`／`M`／`T`／`D` 可以動手**，只有 `U` 的 `N` 動作被擋。

### 這一輪（2026-07-27 續三十）：準備接 `T`／`D` 時發現自己上一輪讀錯了

要把地城道具放進道具欄，第一件事是看槽怎麼表示。一看就撞到衝突：
`docs/re/95` §3.1 我寫「型別 `0xff` ＝ 地城道具」，
而 `scenario` 的 **`slotEmpty` 就是 `0xff`**。
同一個值不可能同時代表「空槽」與「這裡有一件地城道具」。

回頭逐指令再看 `0x182ad`–`0x182ca`：那一段算的是 `槽位×17 + 角色×0x104`，
然後 `mov byte es:[bx+si+0xc], 0xff` —— `+0x0c` 是 `inventoryStart`，
所以那是**把槽清空**，不是「標記成地城道具」。而且它在 `param == 2`
的前置分支裡，那條**當時以為是死碼**（後來證實不是，見續三十五）。

`0xfe` 可能才是地城道具的型別（`0xff` 是清掉用掉的那件來源道具），
但 `0x18382` 與 `0x182ca` 寫的是不是同一個槽，我沒有追完。

**所以這一輪沒有接 UI，而是把那一節降級。** 規格從 READY 改成**部分 READY**：
`E` 檢視與 `M` 移動不碰道具欄可以先做，`T`／`D`／`U` 全部擋住。

> **教訓**：上一輪我把 §3.1 標成「已確認」，還寫進 commit message 與規格，
> 依據只是掃過一遍的定址算式。**「已確認」要留給逐指令追完的東西。**
> 這個 session 已經因為同一種急躁錯兩次（`docs/re/94` 的「沒有筆記」、本節）。
>
> 值得記的是**抓到它的方式**：不是再讀一次反組譯，而是
> **新結論與既有程式碼的常數對不上**。移植專案裡這種衝突是最便宜的檢查 ——
> 每次寫下一個「原版的某個值是 X」，順手 grep 一下自己有沒有把 X 用在別的地方。

### 這一輪（2026-07-27 續二十九）：A2 的存檔層 —— 而測試抓到一個會讓道具全部消失的 bug

照規格的第一步：拿與丟 ＋ 存回。

位置表既然是存檔（`docs/re/95` §3.2），就從 `gamedata` 搬到 `scenario`，
與 `nSS.DAT` 放同一個地方 —— **會被寫回的東西集中在一起**。
道具的**內容**（50 件 × 6 條字串）是靜態的，留在 `gamedata`。

補上 `Take`／`Drop`／`Encode` ＋ 三段式載入（存檔目錄 → `ITEMLOCX` 母本 →
資料目錄）與寫回，全部照 `LoadSpecialTileSet` 那條路。

#### 存檔往返的測試一跑就爆

`ParseItemLoc` 原本的判準是「掃到子地圖不在 1–5 就停」。
在**唯讀**的前提下它剛好切出 50 筆，六條測試全綠。

但拿走一件之後那一筆的子地圖會變成 0 —— 於是**重讀存檔時整張表在第一筆截斷**，
地城道具全部消失，而畫面上完全看不出原因。

改成**固定切 50 筆**（`docs/re/95` §1 讀出來的 `cmp [bp-4], 0x32`），
0 當成合法值「已拿走」。

> 與記憶 `fresh-state-paths-hide-bugs` 同一條：
> **判準不可以依賴「這份資料還沒被改寫過」。**
> 而抓到它的是「拿走 → 寫檔 → 重讀」這條往返 —— 唯讀的測試再多也碰不到。

七條新測試釘住：`Encode` 逐位元組還原（尾端殘留不許動）、拿走只改一個 byte、
拿走不刪記錄（索引就是道具身分）、丟下去原地撿得回來、子地圖 0 不是合法落點、
三段優先序、**不寫回 `workplace/orig`**（用 mtime 確認）。

### 這一輪（2026-07-27 續二十八）：A2 反組譯結案 ＋ 規格 READY

補完最後兩個動作碼，然後把規格寫出來。

**`P` ＝ 就地改寫地圖 tile**（`0x18518` 的 `les bx, ds:0x4c94` 就是當前地圖的
tile 陣列，`0x18530` 直接寫進去）。`P04480` ＝ 把 (04,48) 改成 tile 0 ——
**開一條路**。`+3`（推開的結果）那三筆數字是同一種形狀，
而 `Old bookcase` 的檢視敘述正是「It has been moved to reveal a passage」。兩邊互證。

**`S` ＝ 主線，而 `S1` 就是結局的抉擇。** `0x185f6` 印的是
**「Who wishes to become a god?」** —— `docs/re/82`／`ending.go` 已實作的
不朽／凡人抉擇，原來觸發點是**冰之祭壇 ＋ 祈禱卷軸**。
順帶給了 `+0xc1` 一個脈絡（§7 C9 列了很久的「門檻未解」）：
它是 `S1` 的**一次性閂鎖**。`S2`（光之環）則是播第 7 段劇情 ＋ 傳送到 (11,27)。

**`param == 2` 是死碼。**〔**後來證實這句是錯的，見續三十五**〕
`222f:2088` 全檔只有三個呼叫端，
傳的是 0（`0x16300`）／1（`0x1631a`）／3（`0x17b7f`），
對上提示詞表的 `Use `／`Drop `／`Examine `。中間那一格沒有人用 ——
與神殿樓梯（`docs/re/73`）、模式值 `1`（`docs/re/63`）同一類遺留碼。
**引擎只實作 `0xff`。**

**A2 的反組譯到此沒有洞了**，所以規格寫得出來：
[`docs/spec/10-dungeon-items.md`](docs/spec/10-dungeon-items.md)，狀態 READY。
照專案的 SDD 紀律，接下來可以動手。

### 這一輪（2026-07-27 續二十七）：動作碼確認，順帶解開兩個更關鍵的東西

上一輪把 `D`／`N`／`T`／`P`／`S` 標成「判讀，消費端未讀」。這一輪讀完了。

**分派表在 `0x186fd`**（Turbo C 把 switch 放函式尾端，`docs/re/50` §1.1）：
`cmp ax,0x4e/0x50/0x53/0x54` 四格，`'D'` 在前面用 `cmp al,0x44` 先擋掉。
`T` 的參數逐指令讀完：三組 2 位數 `atoi` → 隊伍的 `+0xa1`／`+0xa2`／`+0xa3`，
所以 `T564603` ＝ 傳送到 (56,46) 子地圖 3 —— **判讀完全正確**。

#### 但真正重要的是路上撿到的兩條

**一、拿到手的地城道具住在角色原有的道具槽裡。**
`N` 的分支把名字一個 byte 一個 byte 複製到
`角色記錄 + 0x0c + 槽位×17 + 1`，型別寫在 `+0`（`0xfe` 或 `0xff`）。
也就是 17-byte 的槽對地城道具是**另一種解讀**：型別 ＋ 16 字名字，
與一般道具（`docs/re/25` 的效果／強度欄）是同一塊記憶體的 union。
手冊說地城道具在清單裡前面加 `/` —— 那個記號就是這兩個型別值。
**不必另外設計背包結構。**

**二、`ITEMLOCB.DAT` 是存檔，`ITEMLOCX.DAT` 是母本。**
拿走一件時原版做 `itemloc[si+2] = 0`（子地圖清 0，從此對不上任何座標）。
這解掉了 `docs/formats/town-and-map.md` §4.1 留的「`B`／`X` 命名意圖未解」——
與 `nSS.DAT` vs `ALL_SS.DAT` 是同一個模式（`docs/re/78`）。
兩檔現在逐 byte 相同，只是因為那份資料還沒被玩過。
**存回直接沿用既有那條路，不必新設計。**

這兩條決定了整個實作的形狀，所以 A2 的規格現在寫得下去了。

#### 又一次「搜尋落空先懷疑查法」

我第一次掃「有沒有人比對這五個字母」得到 **`N` 零命中**，
差點據此推翻整個動作碼判讀。實際上漏了兩種編碼：
`3d`（`cmp ax,imm16`，分派表用的）與 `80 be`（`cmp byte [bp+disp16],imm`）。
與 `docs/re/89`（算出來的偏移搜不到字面值）、`docs/re/94` §1
（基底＋位移掃不到全域引用）同一類 —— 這 session 第七次。

### 這一輪（2026-07-27 續二十六）：地城道具的內容解開了 —— 整套解謎鏈都在裡面

`docs/re/95`。讀 `222f:2da5`（`Take:`／`Move:`／鑑物三個動作的共用常式），
三個常數一次落地：

| 原版 | 值 | 意義 |
|---|---|---|
| `add [bp-6], 6` | **6** | 每件道具佔 6 條字串 |
| `cmp [bp-4], 0x32` | **50** | 道具總數 |
| `cmp [bp-2], 9` | **9** | 同一格最多列 9 件 |

`50 × 6 = 300`，正好用完 `docs/re/27` 第 6 張表 303 條裡的前 300 條
（剩 3 條是 `ICICLE` 被切過的碎片）。同一支常式也把
**`docs/re/94` 的位置表格式從「檢視資料推出來的」升級成程式碼側確認**：
`si = i × 3`，比對隊伍的 `+0xa1`／`+0xa2`／`+0xa3`。

六個欄位：名稱／拿不走的理由／檢視敘述／推開的結果／**要搭配哪一件道具**／
用完發生什麼。第五欄對上了 `docs/re/93` 的鑑物 —— 它讀的
`[bx+0x55c8]` ＝ `0x55b8 + 4 筆` 就是這一欄，
而手冊寫「鑑物：可得知物品**未來可能的用途**」。

**最強的交叉驗證**：那 18 個「要搭配哪一件」存的是名字，
而 18 個字串**每一個**都能在這 50 件裡找到同名的一件。
切割只要錯一格就不可能全中。

#### 這一套就是地城玩法的全部內容

```
Cage + Mallet            → Iron key（老鼠跑出去，露出西牆密門）
Man in trance + Blue serum → 「Qoorik 的入口藏在 Idlewood 西北的森林」
Censer + Bag/red dust    → 惡魔：「只有持有魔晶的人能逃過他的注視」
Papyrus + Glowing pen    → 「Asaht 是上古諸神的聖城」
Ouiji board + Scryer     → 「沿冥河走，直到找到擺渡人」
Circle light + Facet mirror → S2   ← 主線的光之環
```

**主線的線索鏈全部從地城道具裡出來。** 所以「地城解謎沒有它就是空的」
比我原本以為的嚴重：沒有這一套，玩家根本拿不到破關需要的提示。

動作碼 `D`（敘述）／`N`（變成另一件）／`T`（傳送）／`P`（開路）／`S`（劇情）
**是判讀** —— 分布與內容都很一致、`N` 的參數也全部命中清單，
但逐支消費它們的程式碼還沒讀。實作動作層之前要先補上。

### 這一輪（2026-07-27 續二十五）：地城道具的位置表 —— 而我上一輪說錯了話

A2 的第一步是資料層。上一輪我寫「`ITEMLOCB.DAT`／`ITEMLOCX.DAT` 兩個檔名
擺明是道具位置，而 `docs/formats/` 至今沒有它們的解析筆記」。

**那句話是錯的。** `docs/formats/town-and-map.md` §4 早就解完了，
標題就寫著「已驗證：85 筆 (X, Y, map_id) 三元組」。
我只 grep 了 `docs/formats/` 的**檔名**，沒 grep 內容。

> 同一條紀律**第六次**踩到（記憶 `search-before-digging`）。
> 前五次：`route.py` 檔頭偏移、Python 道具槽步長、`0xe8` 字面值 grep、
> 三份 id→map、`nSS.DAT` 的 ad-hoc python。
> 這次多花的時間很少，但**錯誤的斷言已經寫進上一輪的 commit message
> 與 worklist** —— 那才是真正的成本。以後「某某沒有筆記」這種話，
> 要先 `grep -r 內容` 再說。

所以這一輪不重解格式，補的是已有筆記缺的那一半 —— **程式碼這一側**（`docs/re/94`）：

- **誰載入**：通用資源載入迴圈 `0x11fc2`，走六筆檔名遠指標表 `ds:0x14c9`
  索引 2。`docs/re/03` §2.1 找到過那張表但「沒能找到讀取它的程式碼」，
  現在找到了。
- **載到哪**：arena 索引 2（256 bytes，`ds:0x5494`），
  `docs/re/22` §1 原本標「未知（256-byte 檔的緩衝區）」—— 定案。
- **為什麼 `xref global` 一直查不到**：那些緩衝區從來不是用絕對位址讀的，
  永遠是 `[bx+0x548c]`。掃全域引用掃不到基底＋位移，
  與 `docs/re/89` 的「算出來的偏移搜不到字面值」同一類。

用專案自己的解碼器跑出來是 **50 筆**（1:17、3:27、4:5、5:1），
與當年 python 的分布逐項相同 —— 兩個獨立實作互相印證。
256÷3＝85 是塞得下的上限不是筆數；第 50 筆之後是 buffer 殘留
（**位元組反序、最高位被設起來**的 `Provisions: Go`），再往後全 `0xff`。

順帶一個交叉驗證：第 48 筆是**地圖 1 的 (60,1)**，那正是 `docs/re/83`
解出的艾瑞戈爾所在格。兩份獨立資料在同一格重合。

**還沒解的是「是什麼東西」** —— 位置表只有座標。另一半在 `ds:0x55c8`
（4-byte 遠指標、六個消費端，其中兩個就是 `docs/re/93` 的鑑物）。
六個指令全部卡在那張表後面。

### 這一輪（2026-07-27 續二十四）：觀室接上，鑑物卡在地城道具

`docs/re/93` 把兩個靈視技能讀完。**結果分兩半**：

**觀室（`0x0f`）可以現在接** —— 它只需要特殊格清單，而那套早就在。
往正前方最多三格、一天三次、命中就用 **peek 模式**交給事件消費者。
peek 模式就是手冊那句「用觀室技巧也能看到陷阱，但不會標記為『已注意』」：
原版把 `param = 1` 傳給 `25be:0263`，陷阱那一支看到非 0 就**只印
`A trap!` 然後返回** —— 不擲點、不扣血、不改寫 `nSS.DAT`。
比手冊更精確：不只不標記，連傷害都不會發生。

**鑑物（`0x10`）接不了。** 它印完 `View item:` 之後呼叫
`222f:2da5`（`Take:`／`Move:` 共用的「選一個目標」），
再查 `ds:0x55c8` 那張**地城道具**的敘述指標表。
也就是說 **A5b 的前置是 A2，不是它自己** —— 現在接只會得到一個空選單。
這一條記進 worklist，免得下一輪又把它排進「只差接線」那一類。

#### 手冊與執行檔對不上的一處

手冊「觀室」「鑑物」兩節都寫「每天只能使用一次」，
而機器碼兩處都是 `cmp …, 3`。三個靈視計數（觀地 `+0xac`、觀室 `+0xad`、
鑑物 `+0xae`）由睡覺同一段清 0，但**只有觀地是旗標**，另外兩個是計數。
手冊講的是 Apple II 版，引擎照 DOS 執行檔，差異列進 §7 C12 等 DOSBox 複核。

### 這一輪（2026-07-27 續二十三）：水池接上，三個「解完沒接」清光

`docs/re/90` 讀完之後放了兩輪的那一項。接法有一處與其他事件不同：
**攔截點在移動之前**（`World.TileAhead`）—— 原版的回傳碼 `0x11` 發生在
寫座標之前，所以隊伍轉了向但不會走上去。走 `checkEvent` 那條路是接不出來的，
因為它只在 `MoveOK` 之後才跑。

`TriggerHardBlock`／`tileHardBlock` 正式改名成 `TriggerPool`／`tilePool` ——
那個名字從 `docs/re/05` 的「**推測**：撞牆」一路留到現在，
`docs/re/90` 推翻它之後又留了兩輪（先標記、後改名，避免同一輪動太多）。

**全遊戲只有一格水池**：地圖 3 的 `(14,48)`。實跑 Menhir `15/27`
連喝三口 `+1`／`+1`／`+4` → `21/27`，額度 `7 → 4`，座標從頭到尾停在 `(15,48)`。
滿血的人喝下去顯示「回復了 0 點」而**額度照樣扣一次** —— 原版的 `dec`
排在擲點之前，改成「沒回血就不扣」比較合理但那不是原版。

順帶一條原版行為：**新遊戲喝不到水池**。初始化沒有寫 `+0xaa`
（全檔只有三處碰它），所以剛開局是 0，第一次睡覺之前池子是乾的。
`ApplyNewGame` 照做，不「順手補滿」。

至此 `docs/manual-coverage.md` §8 排的前三項（陷阱、戰鬥 `?`、水池）
**全部是「反組譯已全解、只差接線」，也全部清掉了**。
下一項 A5（觀室／鑑物）性質不同：入口已定位但**內部沒讀過**，
要先反組譯再接線。

### 這一輪（2026-07-27 續二十二）：把盤點排前兩名的兩項接上

`docs/manual-coverage.md` §8 排出來的前兩項都是「反組譯全解、只差接線」，
這一輪兩項都做掉，各自連帶讓兩個技能活過來。

**A1 陷阱**（`docs/playtest/09`）—— 七種傷害、`L` 查看陷阱、Noticed／Disarmed、
白框標示。實跑 35 步驗到毒針扣血、水池六輪 `2,2,2,1,0,1` 才游出來
（傷害 0 是 `Roll(3)−1` 的合法結果）、類別 6 先印「即使再小心／還是觸動了陷阱」。
順帶推翻 `specialtiles.go` 一條註解：出貨的 `1SS.DAT` **有**一格類別 6，
在 `(11,17)` —— `nSS.DAT` 是存檔、出貨存檔又是玩過的，所以那是
**1988 年那位玩家自己按 `L` 標下來的陷阱**。釘成 `TestShippedDataHasANoticedTrap`。

**A3 戰鬥 `?` 檢視**（`docs/re/92`）—— 從跳表 case 10 追到 `0xc8a6`，
逐行讀出版面。兩個技能閘門是靠**偏移差**認出來的：面板裡兩處條件式讀
`es:[bx+si-0x63b]` 與 `-0x64d`，差 `0x12` ＝ 18，而技能旗標裡怪物學識(25)
與戰術(7)的間距正好也是 18 —— 手冊那兩句話與機器碼獨立對上。
實機驗過：看怪物顯示「沒有人具備怪物學識」、看隊員顯示力量 12／技巧 13／
速度 15／護甲 0／武器匕首、C 走訪、白框游標。

> 走訪鍵是 **C／B／Q**（`Continue`／`Back`／`Quit`，與市集同一組字串），
> **不是手冊寫的 `←→`** —— 那是 Apple II 版的說法。方向鍵也收，
> 但那是刻意的加法，記在 `docs/re/92` §4 免得下一輪有人「修正」它。

兩輪共通的一件事：**這兩項都不是「還沒解」，是「解完沒接」。**
反組譯那一側已經沒剩多少了，剩下的落差幾乎都在這種地方。

### 這一輪（2026-07-27 續二十一）：照手冊盤點一次，缺口不在選單而在行走模式

使用者要求「參考說明書，看看哪些機制、哪些 menu 應該要串起來」。
以往的 worklist 都是從**反組譯的缺口**往回列的；這一輪反過來，
從**手冊列出的玩家可見功能**逐項查程式碼。結果寫成
[`docs/manual-coverage.md`](docs/manual-coverage.md)。

結論比預期樂觀也比預期難看：**選單層面幾乎補齊了** —— 紮營 14 項、
城鎮八設施、市集六指令、戰鬥九指令都在。**缺口集中在「行走模式」那一列指令**，
而那一列正好就是地城玩法的全部：

- **地城道具系統六個指令只剩 `I` 探查周圍**（掃描範圍還沒讀）。
  鑰匙、祭壇、壁爐這類「獨一無二、有特定用途」的道具整套不存在。
  `ITEMLOCB.DAT`／`ITEMLOCX.DAT` 兩個檔名擺明是道具位置，而 `docs/formats/` 沒有它們。
- **陷阱數值全解完了（`docs/re/68`）卻只印一行「有陷阱！」** —— 不扣血、
  沒有 `L` 查看陷阱，於是察覺陷阱／解除陷阱兩個技能等於沒有效果。
- **戰鬥 `?` 檢視沒綁鍵**，連帶戰術與怪物學識兩個技能也沒有效果。
- 31 個技能裡有 **8 個學得到但沒有任何效果**（另加種族的黑暗視覺、察覺靈光）。
  **學院會收玩家的錢教這些技能** —— 教完沒效果，比沒有那間學院更糟。

> 這一份是**視角不同的第二張表**，不重複記進度。狀態的單一真相來源仍是 §7。

#### 順手讀完水池：tile `0x35` 不是牆（`docs/re/90`）

盤點時發現 `docs/re/05` 的「寫死的阻擋（推測：撞牆）」一直沒人回去驗。
讀完 `222f:37c4`（312 bytes，不是 `docs/re/58` 估的 56）：

那是一口**治療用的水池**。走向它隊伍不會移動，但會問「哪個角色要喝」，
喝一次回 `Roll(4)` 點 HP、鉗到上限，然後**回到迴圈再問一次**，
直到玩家取消或額度用完（`The pool is empty`）。

額度是 trailer `+0xaa`，全檔只有三處碰它：兩處在水池（比較、遞減），
第三處在 `2aed:03e4` —— 印 `You sleep.` 的那支，把它**設回 7**。
所以那是**隊伍層級的每日次數，一天七次**，與觀地（`+0xac`）同一組；
換一口池子不會回滿。

順帶釐清一個會咬人的同名：手冊「地底 → 陷阱」列的**水池陷阱**
（掉進去、每回合 `Roll(3)−1`，酸池 +2，`docs/re/68`）與這口治療水池
是兩件事，數值不能互相搬。

清掉的過期斷言三條：`docs/spec/03-events.md` 的「`0x35` ＝ 寫死的阻擋」、
`townservices.go` 的「碼頭還是只有報價」（買船修船早就接上了）、
`battleui.go` 的「熱鍵沿用原版（含 `?` 檢視）」（那一格根本沒綁）。
`events.go` 的 `tileHardBlock` 這個名字先留著並標記 —— 接上水池那一輪才改。

### 這一輪（2026-07-27 續二十）：戰鬥一直沒有後果（`docs/playtest/08`）

這一輪要做 `docs/re/58` §4 的動作 `0x18`（全隊死亡）。接的時候發現
**那個動作永遠不會成立，因為戰鬥的傷害從來沒有回到隊伍身上。**

引擎把角色複製成 `Unit` 上場，傷害只寫在 `Unit` 上。搜
`writeBackFromUnit` 的呼叫端 —— **只有一個，在營地施法那裡**。所以：
打完一場慘勝回到地圖**全隊滿血**；打輸了每個人也還是滿血 →
`PartyWiped` 永遠 false → 死亡畫面永遠不出現。**戰鬥完全沒有後果。**

**為什麼四輪試玩沒看出來**（三個條件疊在一起）：
① 戰鬥畫面上的血量是對的（那是 `Unit` 的），單看戰鬥截圖沒問題；
② 軌跡只記位置與畫面，不記血量；
③ 要看出來得在戰鬥**前後各看一次隊伍名冊** —— 驗收從來沒這樣做過。

> 這是**移植架構自己引入的缺口**。原版沒有「複製一份上場」這回事 ——
> 角色記錄就是戰鬥用的那份資料（`docs/re/20`），所以它不會有這個問題，
> 也不需要對應的程式碼。**照原版逐指令對，是對不出這種 bug 的。**

修法：`game.WriteBackParty`，戰鬥結算時呼叫。配對用**槽位**不用名字
（三段槽位不相交；按名字找遇到同名角色會配錯，而遊戲允許同名）；
**判別式不看 `Side`** —— 被魅惑的隊員 `Side` 會變，槽位不會。

**血量加進軌跡**，因為這一整類 bug 之所以隱形就是軌跡裡沒有隊伍狀態。
實跑：`血24/…` → 結算後 `血21/…` → 回野外仍是 `21`（修之前是 24）。

**動作 `0x18` 接上並肉眼驗過**：先打硬仗把隊伍打成 `0/13/15/0/0`、存檔，
再從那份存檔開一場同樣的硬仗 —— 兩個殘血的人撐不過兩回合。
畫面「一陣冷風／讓空氣凝結……／……全隊／都死了。」順帶驗到存檔往返
（存出去 `[0,13,15,0,0]`、讀回來一樣）。

**順手修掉自己上一輪弄壞的東西。** 試玩一直失敗，log 是
`載入地圖：SUM.MAP 沒有子地圖 1` —— 我把 `-map` 預設改成「用存檔的 MapID」，
而出貨存檔的 `1` 被當成 `SUM.MAP` 的段編號查，但地圖 1 是獨立檔案。
`mapchange.go` 那份**有** 1／3／5 特例，`loadMapArg` 沒有，
而 `cmd/dwroute` 還自己抄了第三份。合成一份 `world.LoadByID` + 測試。

> **「同一件事只留一份實作」在這個 repo 的第四次教訓**
>（前三次：`route.py` 檔頭偏移、Python 道具槽步長、`0xe8` 字面值 grep）。
> 前三次的代價是誤判；**這一次的代價是上一輪 push 出去的東西啟動就壞**。

還有兩個工具缺陷順手補：外層 `timeout` 砍掉 `playthrough.sh` 時
`docker run --rm` 的容器會活下來（砍的是 shell 不是容器）——
三個孤兒容器搶 CPU，後續試玩慢到像卡住而 log 上什麼都看不到；
加 `--name` + `trap`。以及 `screenName()` 少了 `a.death`，
**軌跡與 `Update` 分派順序不一致就是在說謊**。

### 這一輪（2026-07-27 續十九）：`+0xe8` 不是未解欄位（`docs/re/89`），而且存檔裡的角色會換人

上一輪把兩處欄位複製標成「語意未解、不命名不猜」。**那句話錯了 ——
四個位移都早就命名過。**

`save.go` 的屬性位移全部**相對於 `expOffset`（`0xc4`）**，所以 `0xe8` 這個
字面值在整份檔案裡一次都沒出現（它是 `0xc4 + 0x24`）。
我拿字面值去 grep、落空、然後把「grep 不到」讀成「沒人命名過」。

| 記錄位移 | 算式 | 欄位 |
|---|---|---|
| `+0xe8` | `0xc4 + 0x24` | 力量（天生）|
| `+0xe9` | `0xc4 + 0x25` | 技巧（天生）|
| `+0xf8` | `0xc4 + 0x34` | 力量（含裝備加成）|
| `+0xfb` | `0xc4 + 0x37` | 技巧（含裝備加成）|

那兩行是「擲點結果從含加成那一組抄回天生」—— 新角色沒裝備，兩組本來就該相等。

> **同一種錯的第三次**（前兩次：`route.py` 的地圖檔頭偏移、Python 那邊的
> 道具槽步長 16 vs 17）。而這一次更糟 —— 不是算錯，是**拿字面值去搜一個
> 算出來的值**，落空之後下了「未解」的結論。
> `rulebook/62` 第一條就是「**搜尋落空 ≠ 不存在**」。踩到了。

**順著這條線抓到第二個「只有真開新遊戲才會暴露」的 bug**（第一個是十把匕首）：

規則層只用天生值，「含加成」那四個 byte 從來不讀 —— 而 `ApplyTo` 也
**從來不寫**，於是它們留著載入時的舊值。對載入的角色沒事；
**對新建的角色，那是出貨存檔那五個人的數字**：

```
角色0: 天生(力,技,速)=(10, 8,13)   含加成=(13,13,12)  ← 出貨角色 0 的數字
角色1: 天生(力,技,速)=( 3, 7, 8)   含加成=(12,13,15)  ← 出貨角色 1 的
```

名冊顯示的是自己擲的點，**存檔裡的另一半是別人的** —— 重讀存檔就換人，
而畫面上完全看不出來。共同點：**從存檔載入的路徑上每個 byte 都是有效資料，
所以缺口不會顯現。**

修法：`game.Character` 補一組 `TraitsWithBonus`，`FromSave` 讀、`ApplyTo` 寫 ——
讓 `ApplyTo` 註解自己要求的對稱成立（「FromSave 多讀一個欄位，這裡就要多寫
一個，否則那個欄位會在存檔時悄悄退回舊值」）。`Finish()` 同步成天生值。
**沒有動一般遊玩路徑**：引擎不模擬「裝備加屬性」，對已載入的角色照舊保留
原值，不拿天生值去蓋掉 —— 那會把存檔裡真有的裝備加成靜靜丟掉。

驗過：五個新角色的天生與含加成三欄全部一致。

### 這一輪（2026-07-27 續十八）：建角寫進角色記錄的東西驗完了（`docs/re/88`）

上一輪結尾記著「建好的角色狀態還沒與原版逐欄比對，現在的實作是
**推論不是驗證**」。這一輪驗完。

**角色記錄的基底是 `ds:0x4c7e`（步長 `0x104`），不是 `ds:0x4c76`**
（那是 trailer，全檔 425 處）。一開始找不到寫入端是因為我去掃
`ds:0x548c`（arena 索引 0，長度 ＝ `PARTY.DAT` 大小）—— **全檔零命中**，
那個槽只有一處寫入、沒有任何 `les` 讀取，不是實際用的指標。

建角對角色記錄做四件事：

| 位址 | 動作 |
|---|---|
| `0x14dc5` | `+0xf4 = 1` 等級 1 |
| `0x150d1` | 迴圈十次 `inventory[i].Type = 0xff` 清空背包 |
| `0x150ed`／`0x150ff` | `+0x100`／`+0x101 = 0xff` 裝備槽 ＝ 無 |
| `0x15109` | `inc trailer[+0x9a]` 每造一個人數 +1 |

**上一輪修「十把匕首」的方向是對的，但理由不完整** —— 我只推了
「零值 `Type == 0` 是匕首」，兩個裝備槽索引是靠「合理」補的。
現在兩者都有指令依據。順帶補上人數隨建角遞增（原本留著出貨的 5，
剛好對但那是巧合：「建到一半存檔」會寫出 5 人而其中幾個是別人的角色）。

**一個差點誤報的「編碼器 bug」。** 驗存檔時我在 Python 把道具槽步長
寫成 16，看到「只有第 0 格是 `0xff`」，一度以為編碼器沒辦法表達
「這一格變成空的」。**步長是 17**（`inventorySlotLen = 17`）——
換過來之後五個角色五十格全是 `0xff`，編碼器沒問題。

而 17 這個數字原版的迴圈自己就寫著：`shl ax,4`（×16）後面緊接
`add ax,dx`（+i）＝ ×17。只看 `shl 4` 會以為是 16。

> 教訓不是「小心算術」，是**別在第二個地方重寫一次布局常數**。
> `inventorySlotLen` 在 Go 那邊是對的；我為了看一眼存檔在 Python 抄了
> 一份布局，抄錯了。跟 `tools/route.py` 那次（地圖檔頭偏移）同一種錯，
> 同一種解法：**去問已經對的那一份**。

**存檔逐位元組驗過**：新遊戲寫出來的 `PARTY.DAT` 人數 5、地圖 34、
座標 (28,50)、糧食 20、金幣 75、陣型九格全 `0xff`、五個角色都是 1 級
且五十格背包全 `0xff`。順手把 `tools/playthrough.sh` 的存檔改寫到輸出目錄
（原本寫在容器內，跑完就丟）—— **試玩產生的存檔本身就是驗收對象**。

> **後續訂正：這兩處已解並實作。** `+0xe8/+0xe9` 是天生力量／技巧，
> `+0xf8/+0xfb` 是含裝備加成的有效值；驅邪、換裝與升級共用重建路徑
> （見 `internal/game/exorcise.go`、`docs/re/109`）。本句保留在歷史段落，
> 不再是 worklist。

### 這一輪（2026-07-27 續十七）：建角接進新遊戲，抓到「十把匕首」

`-newgame` 上一輪只設隊伍共用欄位，角色還是出貨那五個。這一輪接上
**強制建滿五個**的開場流程（`docs/playtest/07`）。

為什麼要強制：不強制的話玩家會拿著**別人玩過的角色**從正確的起點開始，
而**畫面上看不出那五個人不是自己建的**。兩道保險：建角畫面的 ESC 在
強制流程裡不關畫面；`Update` 裡再加一道「被關掉就開回來」。

**抓到一個 bug：新建的角色帶著十把匕首。**

`scenario.InventorySlot` 的**零值是 `Type == 0`**，而 `0` 是匕首那個道具型別；
空槽是 `0xff`。`Finish()` 沒動 `Inventory`，於是十格全是匕首 ——
名冊上五個人都寫「匕首」而不是「徒手」。

> **為什麼躲過了所有先前的驗收**：在這一輪之前，沒有任何路徑會產生
> 「零值的背包」。從存檔載入的角色十格都是檔案裡的真資料，零值永遠不會出現。
> `F1` 一直能用，但它是覆寫既有槽位，而測試與截圖看的都是
> 「擲點對不對、職業對不對」，沒有人看武器欄。
> 這是**真正開新遊戲**才會暴露的那一類 bug —— 不在任何既有測試的路徑上、
> 不在任何既有截圖的畫面裡，而它對玩家很顯眼（第一次開名冊就看到）。

**A4 的定義走到哪了**：

| 步 | 狀態 |
|---|---|
| 新開檔 | ✅ `-newgame` |
| 建角 | ✅ 強制建滿五個 |
| 走主線 | 部分（走得動、換得了地圖、進得了城鎮／神殿／學院、打得完戰鬥）|
| 破關 | ❌ |

**還沒比對**：建好的角色狀態還沒與原版建角程式逐欄比對。`docs/re/87`
只掃了 trailer 的隊伍共用欄位，角色記錄（0x104 × 5）那邊的初始化還沒讀 ——
可能有起始裝備或技能。現在的「什麼都沒有」是**推論不是驗證**。

### 這一輪（2026-07-27 續十六）：前四輪都從遊戲中段開始玩（`docs/re/87`）

去接「建角開新遊戲」，發現的不是「缺一個建角流程」，
而是**前四輪試玩的起點根本不對**。

**出貨的 `PARTY.DAT` 是玩過的存檔。** 建角程式（`0x148c8`–`0x1497f`）
寫死了起始值，與出貨存檔**九個欄位不同**：

| 欄位 | 出貨存檔 | 新遊戲 |
|---|---|---|
| **地圖** | **1（地城）** | **34（世界地圖）** |
| X, Y | (9,32) | (28,50) |
| 日／時 | 17／13 | 8／5 |
| 金幣／糧食 | 65／8 | 75／20 |

**那一個地圖 byte 就是「從中段開始」與「從頭開始」的差別。**

線索早就在第一張截圖裡：`docs/playtest/01` 的戰鬥畫面上 Menhir 的血是
**15/27** —— 剛建好的隊伍不會帶傷。我當時當成「原版就是這樣出貨」沒有追，
而 `docs/re/78` §2 明明記過同一組出貨資料的另一半也是髒的
（`1SS`／`2SS` 帶著 29 處遊玩痕跡）。

> **同一份出貨資料裡有一個檔案是髒的，其他就都要懷疑。**
> 四輪試玩、五篇文件全部建立在一個沒被驗證的前提上。
> 前面的觀察沒有錯（走了幾步、觸發了什麼都是真的），
> 但**它們描述的不是玩家會經歷的開場**。

**順帶炸出一個被巧合掩蓋的 bug**：引擎載哪張地圖看的是 `-map` 旗標，
**完全不看存檔的 MapID**。`-map` 預設 `MAP1.MAP`、出貨存檔剛好是圖 1 ——
兩者一致純屬巧合，所以這個洞一直沒被發現。接上 `-newgame` 才炸：
座標套用了、地圖還是圖 1，隊伍站在另一張圖的同一組座標上走兩步撞牆，
**畫面上完全看不出是哪一張圖**。修法：`-map` 預設空字串 ＝ 用存檔的編號，
而且地圖要在存檔決定之後才載。

**真正的開場**（`-newgame`，截圖見 [`docs/playtest/06`](docs/playtest/06-the-real-opening.md)）：
**5 時 8 日紅玉月，海邊草原，面向東，75 金、20 份糧食**，
往西北八步就是**海濱鎮**（物價指數 10，五項設施）。地城在 59 步之外。
這才是 1988 年 CRPG 該有的開場：先進城補給，再決定往哪走。

起始還有**一艘船體 67（不是滿值 75）的船**在 (13,51) 圖 1 ——
移植時不要順手補滿，那會改變玩家第一次要不要花錢修船的決定。

**仍未做**：`-newgame` 只設隊伍共用欄位，五個角色還是出貨那五個。
「開新遊戲 → 強制建五個角色」的流程還沒串（`createui.go` 已經是覆寫
槽位的形式，機制在、流程缺）。

### 這一輪（2026-07-27 續十五）：找路搬進 Go，然後真的走出了起始區

上一輪留的工具缺口：`tools/route.py` 不會在 `SUM.MAP` 的段上找路。
**補法不是在 Python 再寫一份 RLE 解壓，是把整支搬進 Go**（`cmd/dwroute`），
直接用引擎的 `world.LoadMap`／`LoadSumMap`／`gamedata.Tables` ——
**規劃用的判定與遊戲用的判定是同一份程式碼**，其他都是方便。
`tools/route.py` 已刪。

**搬完先證明沒搬壞**：拿已經在遊戲裡真的走過的那段路（95 步）兩邊各跑一次，
`diff` 完全相同。先證等價再往前走，不然「新工具找的路不一樣」與
「新工具是對的」分不開。

**順手發現舊工具漏掉判別式**：`route.py` 列「走得到的出口」時沒套
`docs/re/85` 的 `0xfd`，把別的地圖的出口也列進來。我在 `docs/playtest/03`
引用過那份清單（「五個出口」）—— **那是錯的，只有一個**。已在該文更正。

**起始區的連通性第一次問得出來**：

| 從 | 走 | 到 |
|---|---|---|
| 圖 1 (9,32) 起點 | 26 步 → (22,21) | 圖 34 (55,8) |
| 圖 34 (55,8) | 8 步 → (55,16) | 圖 1 (2,18) |
| 圖 1 (2,18) | 15 步 → (3,32) | 圖 34 (55,18) |
| 圖 34 (55,18) | 42 步 → (45,50) | **圖 2 (10,45)** |

每一段都沒有分岔，而且要在圖 1 與圖 34 之間**來回穿梭兩次**才出得去 ——
(55,8) 是死角，只通到 (55,16)，而那一格送你回圖 1 的另一頭。
**這不是資料的意外，是關卡設計：逼玩家把地城走兩遍。**
也順便解釋了為什麼壓牆走廊 (16,36) 與艾瑞戈爾 (60,1) 從起點 BFS 不可達 ——
它們在圖 1 的另一個連通區。

**實跑**（零捷徑，只有 `-autofight`）：**91 步、四次換地圖**，
從圖 1 起點一路走到圖 2 的 (10,45)。圖 2 沒有獨立檔案（在 `SUM.MAP` 裡），
所以這一趟把兩個地圖來源都走了一遍。詳見
[`docs/playtest/05`](docs/playtest/05-route-in-go-and-the-way-out.md)。

**又一個排序錯。** `settle()` 第一版列舉兩種模態，被商隊撞掉；
改白名單之後第一個事件格就卡住 —— 因為軌跡一行同時帶畫面名與旗標，
`*野外*` 排在 `*文字框*` 前面就先命中「野外」直接返回。
**模態旗標要排在畫面名之前。**

> 這是我在這套工具裡犯的**第三個同型錯誤**（前兩個：`box != nil` vs
> `Active()`、地圖檔頭偏移）。三個都是「判斷條件寫得像對的」——
> 程式跑得動、沒有錯誤、結果看起來合理。共同解法也一樣：
> **拿一個已知答案去對**（已走過的路、實測的撞牆點、已知會彈文字框的那一格）。
> 沒有已知答案時，這類錯不會自己浮出來。

### 這一輪（2026-07-27 續十四）：世界地圖上的 19 座神殿與 35 座學院（`docs/re/86`）

走出地城之後的第一格就是一座神殿 —— 而在這一輪之前，世界地圖上的
神殿與學院都只印一行「（世界地圖上的設施還沒接）」。

**補完神殿那張表**：`ds:0x310d`，19 筆 × 4 bytes（步長 4、上限 `0x4c`）。
原始記錄是 `(X, Y, a, b)`，程式算 `idx = a*2 − b`；資料裡 `a ∈ 1..5`、
`b ∈ {0,1}`，所以 **`idx ∈ 1..10` 且每組 (a,b) 唯一** —— 那就是神祇編號。
三條旁證：城鎮入口把城鎮記錄的值直接當 `idx`（同一索引空間）、
`FILES.DTT` 神祇名表有 11 筆（容得下 1–10）、實跑進去顯示 **Gamur** 而不是
「神祇 13」。

**學院那張表重抄一次驗證 `docs/re/74`** —— 從 `DEMON.INT` 直接讀 105 bytes，
35 筆座標全部相同。兩個要照抄的怪處都釘成測試：#19 技能欄的 `0` 是劍擊
不是哨兵（`docs/re/74` 自己踩過，一度寫成「20 座」）、
#20 與 #1 完全重複且永遠選不到（**不要去重**，去重會改變「第一筆」）。

**又一次「座標表不帶地圖編號」。** 跟 `EXITS.DAT`（`docs/re/85`）同一個設計：
判別式不在表裡，在地圖裡 —— 只有踩在 tile `0x25`／`0x26` 上才查表。
資料檢查：35 筆學院座標，**34 筆恰好落在一個地圖段的 `0x26` 上、
0 筆落在兩個以上**（26 張 64×64 的圖，零碰撞）。
三張互不相干的表（出口、神殿、學院）都這樣設計 ——
1988 年省的是磁片空間，但這也是為什麼單看資料完全解不出來。

**實作照原版的分工**：同一支函式、兩個入口（`docs/re/74` §5）。
查完表造一份只有那一項設施的臨時城鎮記錄，開一般的城鎮畫面 ——
四種服務全部是現成程式碼，行為與城鎮裡那份**保證一致，因為就是同一份**。

**實跑**（`docs/playtest/04`）：零捷徑走出地城 → 落在 (55,8) → 那一格是
Gamur 的神殿，五名隊員與三項服務都出得來；(46,7) 的觀地學院學費 750 金
＝ `CollegeGoldCost(10)`，公式對得上。

順手修掉一個介面錯：世界地圖設施只有一項，底下卻寫「Esc：回到設施選單」，
按下去會退到只有一項的清單、看起來像卡住。加 `worldSite` 旗標讓 Esc 直接離開。
**這是「重用現成畫面」的代價** —— 省下四種服務的實作，換來一處文案假設
不成立；而找出不成立的地方要靠把它跑起來看，不是讀程式碼。

> **工具缺口**（已補，見「續十五」）：`tools/route.py` 不會在 `SUM.MAP`
> 的段上找路。補法不是在 Python 再寫一份解壓，是把整支搬進 Go。

### 這一輪（2026-07-27 續十三）：走出地城 —— 出口的標記是可通行表的 `0xfd`（`docs/re/85`）

上一輪說「下一個障礙不是戰鬥了，是換地圖」。這一輪接上了，
而且過程中推翻一條文件、解掉一個未解項。

**缺的不是解碼，是呼叫端。** `EXITS.DAT` 兩個月前就解好（`docs/re/78` §6）、
啟動時也載進來了 —— **但沒有任何地方查過它**。這是最難發現的一類洞：
解碼有測試護著、測試全綠、文件完整，只有「誰在用它」是空的。
症狀是玩家走到樓梯上什麼都不會發生，**沒有錯誤也沒有訊息**。

**第一版的觸發條件是錯的。** 照 `docs/re/05` §3 寫成「tile 遮罩後 ==
`0x11` 或 `0x53`」，然後拿資料一對就穿了：從起點走得到的五個出口，
來源 tile 是 `0x24`／`0x13`／`0x00`，**一個 `0x11` 都沒有**。

真正的條件是**可通行表[tile & 0x7f] == `0xfd`**，
而答案本來就寫在 `docs/re/58` 的分派表裡（動作 `0x14`）——
跟 `docs/re/83` 那次一樣，**答案已經在 repo 裡，缺的是把兩頁翻到一起**。
順帶解掉 `docs/re/22` 留的「`253` 語意未確認」。

**這個設計還回答了一個沒人問出口的問題**：`EXITS.DAT` 沒有「來源地圖」
欄位，55 筆座標怎麼分給 26 張 64×64 的圖？**由 `0xfd` 分** ——
同一組 (X,Y) 在別的圖上那一格不是 `0xfd` 就不算出口。
可被資料打死的預測：每筆記錄應恰好被一張圖認領。實測 **53 筆恰好一張、
2 筆沒人認領、0 筆被兩張圖同時認領** —— 零碰撞才是證據。

> 值得記的是**怎麼發現的**：不是重讀反組譯，是**先算「哪些出口走得到」**，
> 再看那些格子的 tile 是什麼。實作逼出一個資料查詢，那個查詢立刻推翻文件。
> 照錯的條件寫完就收工的話，測試會全綠（沒有測試涵蓋觸發條件），
> 而遊戲裡的樓梯全部是死的。

**實跑**（零捷徑，只有 `-autofight`）：26 步走到 (22,21)，中途踩到陷阱
（「有陷阱！」—— 類別 3 那條路第一次在真實走路中觸發），
然後換到地圖 34 的 (55,8)，畫面從地城石牆換成世界地圖山地。
詳見 [`docs/playtest/03`](docs/playtest/03-first-map-change.md)。

另外釘了兩條資料測試：**13 個目的地編號全部載得到**（1／3／5 是獨立檔案，
其餘含地圖 2 與 4 都在 `SUM.MAP` 的段裡 —— 地圖 2、4 一度被當成缺檔），
**55 個落點沒有一個在牆上**。

### 這一輪（2026-07-27 續十二）：自動戰鬥驅動 —— 第一段主線真的走完了

上一輪的結論是「戰鬥需要會思考的玩家」。這一輪補上 `-autofight`
（詳見 [`docs/playtest/02`](docs/playtest/02-autofight-first-full-leg.md)）。

策略刻意笨：找最近的活敵人 → 面向它 → 貼著就打、沒貼著就走一步。
關鍵在它**走 `faceToward` 與 `runPlayerAction`，那正是按鍵處理器
自己在用的兩個函式** —— 一樣扣行動點、一樣會被拒絕、一樣會打不中。
它是在玩，不是在改結果。

> **界線要說清楚**：A4 禁的是「跳過內容」的捷徑（`-battle-win`、
> `-give-item` 那一類）。自動驅動器不跳過任何東西，所以不違規。
> 不講清楚的話，下一個人會拿它當「反正有工具」的藉口。

**接上之後第一件事是路線全歪。** 打仗時方向鍵一樣落空，腳本照樣送，
等回到野外已經偏離規劃路徑 —— 而這件事**完全沒有錯誤訊息**，
軌跡只顯示隊伍走到別的地方。跟上一輪的假訊號是同一類：
不是壞掉，是**看起來很正常地走錯**。修法是每走一步前後都等到可行動。

**然後第一段路真的走完了**（零捷徑，只有 `-autofight`）：

| 項目 | 數字 |
|---|---|
| 步數 | 95（BFS 規劃，逐步走完）|
| 事件文字框 | 6 次 |
| 自動打完的戰鬥 | 3 場 |
| 遊戲內時間推進 | 8 次（13 時 → 22 時）|
| 終點 | **(23,31)**，引擎吐出「地點劇情 3」|

**這是 `docs/re/83` 那份座標普查第一次被真實走路驗證。** 在此之前
「類別 5 的值 = 16 格表 case 編號」是靠反組譯 + 資料普查 + 旗標支撐的；
現在是玩家走 95 步過去、中間打三場仗、過九個小時，
引擎在**普查預測的那一格**吐出 case 3。

**抓到一個真的顯示缺陷**：狀態欄訊息被 `TruncateCells` 切掉
（「…見 docs/re」），而畫面上看不出那是裁切。固定格式那幾行截斷沒事，
**變動長度的句子截斷會把後半句吃掉**。改用 `WrapMixed` 斷行。
單點截圖沒抓到，因為那些畫面的訊息都夠短 —— 要走 95 步打三場仗
才會遇到比較長的那一則。**這就是 A4 存在的理由。**

> **下一個真正的障礙不是戰鬥了，是換地圖。** 主線要跨世界地圖 ↔ 地城，
> 而那個動作層還沒接（§7 A2）。地圖 1 的壓牆走廊與艾瑞戈爾那一格
> BFS 都不可達，中間要經過傳送格。

### 這一輪（2026-07-27 續十一）：A4 真的動了 —— 驗收工具 + 第一次從頭跑

**A4「全程試玩」在每一輪收尾都被記成「仍然沒動」。**
沒動的原因不是懶，是缺工具：手上只有 `screenshot.sh`，那是單點驗收 ——
開一個畫面、按幾下、拍一張。長路徑上它會騙人：xdotool 漏一次鍵，
隊伍少走一步，最後那張圖照樣是張合理的畫面，只是走到了別的地方，
**而肉眼沒有對照，看不出差別**。

三樣新工具（詳見 [`docs/playtest/01`](docs/playtest/01-harness-and-first-run.md)）：

| 工具 | 做什麼 |
|---|---|
| `-trace`（`cmd/demonwinter/trace.go`）| 每一次可觀察狀態改變寫一行。只放玩家看得到的東西 —— 內部旗標不放，那正是 A4 要繞開的訊號 |
| `cmd/dwroute` | 地圖 BFS 找路 + 列地點 + 列走得到的出口，輸出按鍵腳本。**不是捷徑**，隊伍照樣一步一步走。用引擎自己的載入與可通行判定（原 `tools/route.py` 已刪，見 `docs/playtest/05`）|
| `tools/playthrough.sh` | 跑腳本、記軌跡、沿路截圖。**不給任何 debug 旗標** |

**第一次跑的結果**（原版出貨存檔，地圖 1 的 (9,32)，零旗標）：
走 5 步在 (9,27) 觸發事件文字框 → 一路往東到 (14,26)（中途時間推進）
→ 第 15 步在 (15,26) 踩到帶遭遇的事件格 → **開打**。
移動判定、`nSS.DAT` 查表觸發、事件轉戰鬥三件事一次驗完，
而且是**逐行可查**不是「看起來對」。

**卡住的地方：戰鬥需要會思考的玩家。** 送 80 次 Enter／space 之後
還在第 1 回合，戰鬥紀錄整片「Norman 前方過不去」——
那不是當機，是遊戲在正確拒絕：Enter 是前進，正前方站著一隻巨鼠，
攻擊是 `A`。改送 Left+A 就打得動，但怪會移動，盲送固定鍵序只會打到空氣。

> **這才是 A4 真正的門檻，而且是這一輪才看見的。**
> 前面所有驗收都停在「畫面畫得出來」，從來沒有人問過
> 「一個不會看畫面的東西能不能把這場仗打完」。答案是不能。
> 下一步是自動戰鬥驅動（挑目標、轉向、逼近、攻擊）。

**兩個錯都在新工具裡，都是「看起來合理所以沒人懷疑」那一類：**

1. 軌跡寫 `a.box != nil`，遊戲判斷用 `a.box.Active()` ——
   文字框讀完物件還在。結果軌跡一路標「文字框」，腳本一直送 Return
   想關掉一個早就關了的東西，害我以為卡在事件文字上。
   **假訊號比沒訊號更貴。**
2. 找路工具把地圖錯開一格（`MAP*.MAP` 是 1 byte header + 4096 tile）。
   症狀不明顯 —— 路大致合理，只是偶爾穿牆。
   **抓到它靠的是拿引擎實測的撞牆點當對照**：先跑一次試玩，
   記下軌跡裡的「前方無法通行」座標，再要求工具同意。錯一格就露餡。

還有一個**差點誤報**的：兩張截圖裡巨鼠是 `4/4` 與 `3/3`，上限跟著掉。
那是兩次不同執行，`RollMonsterStats` 每場重擲怪物血量 —— 兩隻不同的老鼠。
跨執行比對隨機值本來就不成立。

順帶一條情報：地圖 1 的 (16,36)（壓牆走廊）與 (60,1)（艾瑞戈爾）
**從起點 BFS 不可達** —— 中間要經過傳送格或樓梯，不是走過去就能到。

### 這一輪（2026-07-27 續十）：兩道密語謎題接上（`docs/re/84`）

上一輪把 16 格表的分派接好，但 14 個 case 還是印「還沒接」。
這一輪補上 case 10／11 —— **全遊戲僅有的兩處自由文字輸入**
（其餘互動全是選單）。答案是明文存在資料段裡的：`VOID`、`JESRIC`。

**`0x48b` 這個常數值得記。** case 10 答對時把地圖某一格改成 0（開牆），
那一格是寫死的線性索引 `0x48b`。三份**互相獨立**的產物對得上：

| 檢查 | 結果 |
|---|---|
| `0x48b` 換算 `y*64 + x` | (11, 18) |
| `MAP5.MAP` 在 (11,18) 的 tile | `0x0d` |
| 可通行表在 `0x0d` 的值 | `0xff` ＝ 牆 |

而觸發格在 (11,20) —— 正南兩格、同一欄。case 11 也一樣：
答錯時 `party+0xa2 = 0x26`（Y=38），觸發格在 Y=39，就是把你趕出門一步。
**常數落在觸發格旁邊**，兩題都是自我驗證。

釘成測試（`riddledoor_test.go`）：座標公式寫反或地圖檔頭處理錯，
`0x48b` 就會落在別的地方 —— 而遊戲裡的症狀是「答對了但牆沒開」，
或「在地圖別處挖了個洞」，**兩種都沒有錯誤訊息**。

**比對是 `stricmp` 不是 `strcmp`。** 資料段的字串是全大寫、攻略寫的也是全大寫，
很容易實作成 `==`，然後測試照抄大寫 —— 於是「打小寫過不了關」永遠不會被發現，
直到玩家打了 `void`。

> **題目翻中文，輸入欄留 ASCII。** 答案是原版寫死的明文，中文化不能動它 ——
> 否則玩家照攻略打字會打不進去。這是「該翻的翻、不該翻的別碰」的分界線。

16 格表目前接上 10、11、14；其餘 12 個明確顯示「還沒接」。

### 這一輪（2026-07-27 續九）：類別 5 就是那張 16 格表，艾瑞戈爾接上（`docs/re/83`）

`docs/re/65` 留了一個未解：「`ds:0x52f6` 這個事件狀態變數誰在設」。
**它根本不是狀態變數** —— 是 `nSS.DAT` 查表函式 `222f:1321` 的輸出參數，
值就是特殊格 attr 的低 5 bit。所以：

> **`nSS.DAT` 類別 5 的記錄 ＝ 地點劇情事件，它的「值」就是 16 格表的 case 編號。**

`docs/re/77`（格式）與 `docs/re/65`（分派表）寫在同一個 repo 裡兩個月，
中間差的就是這一句。接上之後**主線劇情的觸發座標全部落地**。

**兩條互相獨立的證據鏈：**

1. **指令層級**：`0x19f15 cmp ds:0x5c62, 5 / jne` → 不等於 5 就走 `DATA*.TXT`，
   等於 5 才跳那張 16 格表。
2. **資料層級**：`ALL_SS.DAT` 的類別 5 記錄用掉值 1–15，
   **每個值只出現在一張地圖，跨五張地圖零碰撞**。若它只是區域旗標，
   五張獨立編號的地圖撞號幾乎必然。

而且**幾何對得上**：case 1（機械聲）在 (15,38)(23,38)，case 2（壓牆致死）
是夾在中間的 12 格走廊；case 15（光之環的門）是繞著 (11,50) 的四格十字。
case 語意來自字串、座標來自資料，兩邊獨立卻拼成同一張圖。

**艾瑞戈爾整場接上了**（`internal/game/eregore.go` + `cmd/demonwinter/eregoreui.go`，
`-eregore=0|1` 可驗）。三層選擇、七條出口、10 頁文字 ——
`EREGORE.TXT` 一頁都沒有多。**七條裡有六條會打一場三隻怪的架**，
只有一條談得下去。談崩會立 `+0x99`，下次見面直接跳結尾。

**順手改掉一個看效果不看成因的命名。** `+0xbe` 一直叫 `TownRuins`，
但它 11 處存取裡只有兩處寫入，**都在艾瑞戈爾那一場** ——
也就是馬利馮捏碎春之石的那一刻。城鎮變廢墟只是它的下游效果之一
（還有地圖 tile 替換、艾瑞戈爾那一格的閘門）。改名 `ShardShattered`。

> **`+0xb9 = 1` 的寫入還是沒找到。** 這一輪把整場艾瑞戈爾逐指令看完，
> 它只寫 `+0xbe`。`+0xb9` 全部 19 處存取裡，五處寫入寫的是 2 或 3，
> 沒有一處寫 1。線索用掉一條，剩下的路徑更窄了。

### 這一輪（2026-07-27 續八）：劇情文字全翻完 —— 388/388

剩下的 17 頁（結局 7 頁 + 艾瑞戈爾 10 頁）翻完，`dwstrings check` 回到
**388/388（100%）**，而且這次是把 20 頁劇情算進去之後的 100%。

**兩個 bug，兩個都是閘門或截圖抓到的：**

1. **Big5 閘門抓到 `⋯`**（U+22EF）不在 Big5 —— 我在三頁用了它。
   換成 `…`。這正是那個閘門存在的理由：畫面上會是空白，肉眼掃過去
   很容易當成「本來就有留白」。
2. **中文名單整行被裁掉。** `textlayout.WrapText` 按空白斷詞，
   而**中文整句沒有空白** → 被當成一個超長「單字」獨佔一行，然後撞牆。
   英文原文剛好有空白，所以接中譯之前完全看不出來。
   改用 `textlayout.WrapMixed`（CJK 感知 + 標點禁則）。

> 第 2 點值得記：**「英文版顯示正常」不能當成排版正確的證據。**
> 中英的斷行機制根本不同 —— 英文靠空白、中文靠字數與禁則。
> 中文化專案裡，**每一處排版都要拿中文再驗一次**。

順帶補一個接漏：名單那一頁的譯文放在翻譯目錄裡，但 `creditLines` 直接叫
`winText.Fate()` 讀英文原檔 —— 其他頁都中文了只有名單是英文，
而那正好是最後一幕。加 `fateFor()` 走目錄。

### 這一輪（2026-07-27 續七）：劇情文字可翻了，三場夢中文化

`dwstrings story` 把三個分頁劇情檔抽成翻譯目錄（20 頁）。

**一頁一條，不是一行一條。** 原版的行是照它 40 欄畫面斷的，中文的斷點本來
就不一樣（一行放得下的字數也不同），逐行對譯會逼譯者遷就英文的斷點。
抽成整頁之後譯文自帶換行，畫面直接照它切 —— 也就不必再走 `storyLines` 重斷。

三場夢已翻（`A4-dream-zh.png`：馬利馮的預言，詩的縮排層次照原文保留）。

#### 又抓到一次「會說謊的閘門」

加進來之後 `dwstrings check` 一開始還是回報 **368/368（100%）**——
因為 STORY 不在 `nameCatalogs` 裡，那 20 頁根本沒被算。
把它加進去之後是誠實的 **371/388（96%）**，STORY 3/20。

> `check.go` 裡本來就寫著一句「**進度數字漏算比沒有數字更糟，它會讓人以為
> 翻完了**」—— 那是上次漏算兩百多條名稱時寫的。同一個坑第二次踩。
> 寫下教訓不等於下次會想起來；**新增一類文字時要順手問「閘門看得到它嗎」**。

### 這一輪（2026-07-27 續六）：三場夢接上 —— 冬之魔在遊戲裡真的降臨了

`docs/re/80` 讀出來的睡覺劇情推進，整條接進引擎：

- `internal/game/plot.go`：`AdvancePlotOnSleep` 照原版**每晚只推進一段**
  （原版每一段都直接 `return`），`WipeFaith` 清薩滿／司祭／神祇
- 存檔加 `PlotStage`（`+0xb9`）與 `FirstDream`（`+0xbd`）
- `campSleep` 睡著之後才推進劇情，`dreamui.go` 整頁播 `T.TXT` 的夢
- 第三場夢連帶把 `drawTiles` 重建 —— **神殿當場變成瓦礫**

**實機驗完整條**（`A1-dream-arrival` / `A2-dream-godsdead` / `A3-after-godsdead`）：
`-plot 2 -hour 16` → 紮營 → 睡覺 → 諸神已死的夢 → 退出紮營，
地圖上的神殿已經是廢墟，時間也推到隔天早上。與原版 DOSBox 跑出來的一致。

**又是截圖抓到的排版問題**（第三次了）：原版斷行是照它的 40 欄畫面，
這裡只有 37 欄 —— 馬利馮預言有四行字尾被切掉（`claw upon y`、`be hear`、
`rise up in glor`）。加 `storyLines()` 重斷行，**續行沿用原縮排再多兩格** ——
頂格的話「the」「crystal」「glory」會被讀成新的詩句。

> 這次值得記的是：**「原版已經斷好行了」不等於「我們的畫面放得下」**。
> 移植時沿用原版的排版資料，前提是版面尺寸一樣；差三欄就會切字。

⚠ 起始階段 0 目前推不動 —— 把 `+0xb9` 從 0 設成 1 的那道寫入還沒找到
（`docs/re/81`）。測試 `TestAdvancePlotOnSleep_Stage0DoesNothing` **釘的是現況
不是正確行為**，找到寫入端之後要改。偵錯用 `-plot N` 可以直接設階段。

### 這一輪（2026-07-27 續五）：結局序列照原版接上

`docs/re/82` 解出 `WIN.TXT` 之後，把結局從「一句寫死的祝賀」換成原版的完整序列
（`docs/re/04` §5.1）：頁 0→1→2→3 →**「成為不朽 or 回歸凡人」的抉擇** →
接受走頁 4、婉拒走頁 5 再放**依職業不同的隊員下場名單**。

隊員下場就是 `docs/re/61` §2 那個一直沒讀出參數的 `"%s %s"` ——
第二個 `%s` 是 `WIN.TXT` 第 6 頁那十段句子（`docs/re/82` §6）。
⚠「索引 ＝ 職業編號」仍是判讀，註解裡標著。

**兩個 bug 都是截圖才看出來的，測試不可能抓到：**

1. 名單被右緣裁掉。第一版把欄寬按「半形 8px」算成 74 ——
   **這個介面把 ASCII 也畫成全形寬**，正確值是 37。
   而且裁掉的是句子中段，畫面上看起來像資料壞了，不像排版問題。
   改成從 layout 常數推（`(CanvasWidth − BoxPadX*3) / CellWidthCJK`）就不會再算錯。
2. 五個人的下場一頁放不下，第五個掉到畫面外。加分頁。

> 「視覺產物一律 dump 出來肉眼比對」這條硬規則今天賺回兩次。
> 兩個 bug 都不會讓任何測試變紅 —— 文字有產生、函式有回傳、
> 只是玩家看不到後半。

### 這一輪（2026-07-27 續四）：遊戲內手札 + README 更新（使用者要求）

**手札**（`F2`）：1988 年的遊戲把種族、職業、神祇、地形、陷阱這些查詢用的
資料印在紙本手冊裡（當年的慣例，兼作防拷）。三十幾年後那本手冊多半不在玩家
手上，所以搬進遊戲 —— **要查的東西按一個鍵就看得到，不必去翻 PDF。**

- 內容 `assets/manual/zh-Hant/manual.txt`，格式是 `== 章節` + 內文行，
  翻譯與增修都不必動程式。第一批七章（故事／七大地形／神祇／祈求神助／
  紮營／地底世界／關於這份手札），來源是 `docs/manual-cht/` 的轉錄稿
- `internal/manual` 解析 + 測試。測試會擋「編輯內容時把格式弄壞」——
  少一個 `==` 就整章併到上一章，畫面上只會看起來「有點長」；
  還有**行寬上限 34 字**，超過會被畫面裁掉
- 鍵位 `F2`，與 `F1`（建角）同一組「一看就不在原版裡」的位置 ——
  **這是本專案自己加的，不是還原原版**，所以不去佔用原版有意義的字母鍵
- 實機驗過：`97-manual-toc.png`（目錄）、`98-manual-gods.png`（神祇整章）
- ⚠ 目錄游標本來用 `▶`，**倚天字型沒有這個字**，畫面上是空白 →
  改用 `→`（同一行的 `↑↓` 本來就顯示正常，所以那組是安全的）

**README**：現況表原本寫「Go / Ebiten 引擎：未開始」「遊戲內文字中文化：未開始」
—— 陳舊到與事實相反。重寫成實際狀態，並放六張 headless 實跑截圖
（標題／探索／事件／戰鬥／城鎮／手札，`docs/images/`）。
**同時明寫還沒做到的**：完整試玩沒跑過，所以「能玩」指的是各段落分別驗證過，
不是「已確認可以通關」。

> README 是外面第一眼看到的東西，把「未開始」放在一個已經能玩的專案上，
> 比沒有 README 更糟。`rulebook/63`「以程式碼為準，別信過期的狀態標記」——
> 這條對外文件同樣適用，而且對外的過期成本更高。

### 這一輪（2026-07-27 續三）：三個「沒有載入路徑」的劇情文字檔（`docs/re/82`）

`docs/re/81` 播出的第二場夢，畫面上的字**逐字就是 `T.TXT` 的第二頁** ——
所以 `docs/re/70` §4 那句「這三個檔案在這份執行檔裡沒有載入路徑」是錯的。
順著查，把 `25be:19d1` 整支讀完了。

**`25be:19d1(page, mode)`：`mode` 選檔、`page` 選頁、`0xffff` 是載入。**
兩張 3 筆表：`ds:0x2f59`（檔名遠指標）、`ds:0x2f65`（檔案大小）。
**三個大小與實際檔案逐一相符**（3621／3987／1670）—— 不是判讀，是對答案。

| mode | 檔案 | 頁 | 內容 |
|---|---|---|---|
| 0 | `EREGORE.TXT` | 10 | 艾瑞戈爾與黑鏡 |
| 1 | `WIN.TXT` | 7 | 結局 |
| 2 | `T.TXT` | 3 | 三場夢 |

**`T.TXT` 第 2 頁把 `docs/re/79`／`80` 讀出來的程式碼逐句敘述了一遍**：
「Clerics all over Ymros wake up with the feeling that something has been torn
from their souls」＝ 清薩滿與司祭技能、「flaming thunderbolts turning temples
to ruins」＝ 神殿 tile 換成廢墟、「The gods are dead.」＝ 清神祇。
**敘事與實作一句一句對得上** —— 比任何靜態推論都強的佐證。

**`+0xbe` 的劇情定案**：`25be:1ae2` 呼叫 `25be:19d1(9, 0)` ＝ `EREGORE.TXT`
第 9 頁，鏡子裡的聲音對玩家說「Your meddling has become a threat」。
`docs/re/77` §6 記「那支函式沒有字串引用，所以查不出它演什麼」——
**文字在資料檔裡，不在程式碼裡**。

#### 為什麼上次找不到：又是掃錯定址形式

`docs/re/71` 收回了「用算出來的索引存取」這個推測，理由是
「索引式存取的基底也要出現在某處，而基底同樣掃不到」。
**基底出現了** —— 形式是 ModRM 的 disp16（`ff b7 59 2f` ＝ `push [bx+0x2f59]`），
不是當時搜的立即數。**索引式存取根本不需要把基底載進暫存器。**

> 這與 `docs/re/80` 是**同一個形狀的錯，方向相反**：
> 那次掃常數位移、漏掉暫存器偏移；這次掃立即數、漏掉常數位移。
> 兩次都下了否定式結論（「沒有寫入端」「沒有載入路徑」），兩次都錯。
>
> **否定式結論要列出「我掃了哪些定址形式」才算數。**
> 8086 同一個語意有好幾種編碼，掃一種就宣告不存在，命中率不會高。

**對中文化**：三個檔案共約 9 KB 敘事文字（`*` 分頁、純 ASCII、無壓縮），
是主線最重要的內容，而**引擎目前一頁都還沒接**。

### 這一輪（2026-07-27 續二）：紮營鍵改成 `C`，對齊原版

上一輪從手冊查到原版紮營是 `C`。把引擎的鍵位換過來：
**`C` 紮營**（原本 `R`）、建立角色移到 **`F1`** ——
原版的建角在遊戲外的 Character Utilities 選單，大地圖上根本沒這個鍵，
所以放在 `F1` 這種一看就「不在原版裡」的位置最誠實。
實機驗過（`96-camp-key-c.png`）。

`docs/i18n/ui-catalog.md` 補上**原版兩張選單的逐字原文**（C1 對照表）。
順帶記下一條慣例：原版**首字母撞了就改字**（`Xorcise`、`X)View item`）——
中文化沒有這個約束，熱鍵可以直接沿用原版字母標在中文前面。

> 舊註解寫「原版用哪個鍵沒查（手冊那句『按 P 鍵』指的是紮營畫面裡的另一個
> 功能，不是進入鍵）」——**手冊是查了，但查到錯的那一句。**
> §305 的「按 P 鍵」確實不是進入鍵，可是 §407「紮營」那一節開頭就寫著 `C`，
> 而且手冊還有一節叫「控制方法」，開宗明義說「選單指令一律鍵入該選項的第一個
> 英文字母」。**查到一句不合就停，比完全沒查更容易下錯結論** ——
> 因為它會留下「我查過了」的錯覺。

### 這一輪（2026-07-27 續）：DOSBox 十分鐘裁決掉上一輪的衝突（`docs/re/81`）

上一輪留了一個「反組譯普查 vs 攻略」的衝突，並寫明裁決方式是
第 2 級 oracle。這一輪就去做 —— **改三個 byte + 跑一次 DOSBox 就結案**。

把 `+0xb9` 改成 1、`+0xbd` 改成 1（跳過第一場夢）、時辰改成 16，
然後 `c` 紮營、`s` 睡覺。**第二場夢照播。**

所以那條路徑是活的。普查本身沒錯（19 處確實沒有寫 1），
**錯在把「掃不到」讀成「不存在」**。

> **卡在靜態層時，先問「有沒有辦法直接跑一次」。**
> 這次裁決的成本比繼續擴大靜態掃描便宜一個量級。

一次實跑拿到四樣東西：

1. **馬利馮預言全文**（`93-malifon-dream.png`）—— 主線最重要的一段文字，
   中文化要翻的內容。上一輪還記著「文字頁的實際文字沒 dump」
2. **寫入端的方向**：夢的第一行是 `The Orb of Evertime now is yours`，
   正好對上攻略說的觸發點 → **寫 `+0xb9 = 1` 的是給寶珠那個事件**。
   下一步從那邊往下追，別再從欄位往上掃
3. **原版主指令選單 15 項**（`94-orig-main-menu.png`）—— `docs/re/12` 只知道
   「19 項分派」沒有名字，現在有逐字原文
4. **原版紮營選單 14 項**（`95-orig-camp-menu.png`）—— 項數與 `docs/re/33`
   反組譯解出的一致，互相對上。原版把 Exorcise 拼成 **`Xorcise`**
   （`E` 被 `Equip` 佔了），而手冊寫的是 Exorcise ——**畫面為準**

後兩項是 C1（介面文案）的原文對照表。

**順帶更正**：`docs/re/26` §162 寫「原版紮營鍵沒查」—— 是 `C`，
手冊 §407 早就寫著，`docs/re/54-reference-comparison` §13 的
DOSBox 指令列也本來就是 `key:c`。
**問「原版按什麼鍵」時，手冊是第一個該翻的地方，不是反組譯。**

### 這一輪（2026-07-27）：冬之魔是在睡覺時降臨的（`docs/re/80`）

`docs/re/79` §5 留白的 `+0xb9` 語意，接在 `docs/re/26` §51 那句
「夢境內容與旗標語意未解」後面收掉。

**`1000:0206`（睡覺後的劇情推進）三段夢境**：

```
月份 > 3 且 +0xbd == 0        → +0xbd = 1，播文字頁 0（馬利馮的警告）
+0xb9 == 1                    → +0xb9 = 2，播頁 1（冬之魔降臨）
+0xb9 == 2 且 +0xba == 0      → 神殿全毀 + 全隊信仰歸零 + 播頁 2
```

**「信仰歸零」是三件事一起**：薩滿技能（`+0xd7`）、司祭技能（`+0xd8`）、
信奉的神祇（`+0xf0`）同時清 0。前兩者**全檔只有這一處寫入**，
所以驅散不死、祈禱、驅邪永久失效、無法還原。
順帶回答了攻略自己的疑問 —— `part-4` §105 寫「我確定不能再祈禱，
但不確定會不會影響驅散不死或驅邪」，答案是**都會**。
（這次是反組譯回答攻略，方向跟平常相反。）

**`+0xb9` 定案**：劇情階段，`2` ＝ 已降臨；`3`／`4` 是從 `2` 出發的暫態。
19 處存取逐一核對過基底都是 `ds:0x4c76`，排除了「同位移不同基底」的陷阱。

**`ds:0x15c8` 是死變數**：五處寫、零處讀，取位址也 0 次。價值是**防止下一輪去追它**。

#### 這一輪最重要的收穫是一條紀律

`+0xb9` 的 19 處存取**沒有一處寫 1**（換成不看 opcode 的掃法重驗過），
新遊戲初始化不寫它，兩份原版存檔都是 0 —— 看起來整條劇情鏈到不了。

**但攻略明確說那兩場夢與神殿全毀會發生。** 依 oracle 優先序
（攻略 > 反組譯推論），結論不是「劇情不存在」，是**掃描器有盲點**：
`tools/fieldscan.py` 只看常數位移，而隊伍結構有 **20 處偏移放在 `si`
的計算式寫入**（`mov es:[bx+si], al`），原理上掃不到。

> **好用的工具最容易被當成完備的。** 前幾輪靠它定案的
> 「`+0xbe` 十一處沒一處寫 0」「`+0xd7`／`+0xd8` 只有一處寫入」都還成立，
> 但那些是**肯定式**結論。**「某欄位沒有寫入端」這種否定式斷言，
> 只有常數位移掃描是不夠的。** 限制已寫進工具自己的 docstring。
>
> **但這一輪還有第二層**：我先拿「掃描器有盲點」當解釋，
> 然後真的去追那 20 處計算式寫入 —— 結果它們**全是 3×3 陣型格**
> （`si` 值域 0–8，碰不到 `0xb9`），loader 也查了。
> 所以那個解釋被自己推翻，`+0xb9 = 1` 的寫入端**兩種掃法都找不到**。
> **「找不到就一定是工具有盲點」本身也是偷懶的結論** ——
> 提出這種假設之後要真的去驗，不然它跟「反正一定有」沒兩樣。
>
> 現在的狀態是**一個記著的衝突**（反組譯普查 vs 攻略 `part-4` §105），
> 不是一個結論。裁決方式是第 2 級 oracle：DOSBox 實跑原版走到那個點、
> dump `+0xb9`。**不擋實作** —— 結果旗標（`+0xba`／`+0xbe`）的行為
> 已經照原版做完並驗過（`docs/re/79`），缺的只是「什麼時候立起來」。

### 這一輪（2026-07-26 續四）：tile `0x5b` 是廢墟 —— 標題的機制實作（`docs/re/79`）

B13 記著「tile `0x5b` 觸發回傳碼 `0x13` 但四個比較全部落空，第五個沒接」。
**看漏了**：`0x5b` 走的是同一支函式**前半段**的分支群，之前只讀了後半段。

> 教訓很具體：一支函式裡有**兩組**針對同一個變數的比較時，
> 只讀其中一組會得出「沒接」的錯誤結論。

順著它讀出這款遊戲標題的機制實作：

- **`+0xba` 立起來 → 繪製時把神殿 tile `0x25` 換成廢墟 tile `0x5b`**
  （`0x1739a`，在拷進繪製緩衝區的路上替換，規則陣列不動 ——
  與海面浪花同一種純外觀替換）
- **踩上去印 `You are walking through ruins`**
- **神殿被毀會永久剝奪全隊的薩滿與司祭技能。** 這條不是推論：
  `tools/fieldscan.py` 掃出那兩個技能旗標各 6 處存取，
  **寫入端只有一處而且寫 0**，其餘五處全是資格檢查的 `cmp 0`。
  「只有一處寫入」比任何邏輯推理都強 —— 光讀那段程式碼只知道
  「這裡清了兩個 byte」，掃完才知道「全遊戲只有這裡清它們」。
- `+0xbe`（`docs/re/78` 解出的單向閂鎖）**的劇情就是城鎮變廢墟**

**引擎那邊本來錯在哪**：五個地點 tile 被註成「tile 值就是 `DATA*.TXT` 索引」，
於是 `checkEvent` 拿 `0x25` 去取第 37 筆敘述 —— 而 `DATA1.TXT` 只有 35 筆。
**一個越界索引把 bug 藏成了「這格沒事」。** 改成 `game.SiteFor()` 分派
城鎮／神殿／學院／廢墟；世界地圖上的神殿與學院仍未接，但現在會明說。

三張實機圖：`90-temple-normal`（神殿建築）→ `91-temple-ruined`（**變成瓦礫**）
→ `92-ruins-message`（走上去顯示廢墟訊息，而狀態列**地形仍是 37** ——
證明替換只發生在繪製層）。

### 這一輪（2026-07-26 續三）：存回接上，拿原版當逐位元組的 oracle

上一輪留了一句「改寫目前只留在記憶體」。接完之後得到一個**比截圖更硬的驗證**。

`writeSave` 的註解本來就寫著「這裡漏一個欄位，玩家就會丟掉那部分進度
而且畫面上看不出來」—— `nSS.DAT` 的改寫正是那一類。

**做了什麼**：`scenario.LoadSpecialTileSet`（存檔目錄 → `ALL_SS.DAT` 母本 →
資料目錄，三段優先序）＋ `WriteSpecialTileSet`。全新開始**必須**從母本重建，
不然玩家會繼承原版出廠那份「玩到一半」的狀態。

**驗證**（`88-nss-save.png`／`89-nss-reload.png`）：

```
母本 ALL_SS 的 (9,27)   0x20
引擎走過去之後寫出       0x21   ← 整份 511 bytes 只差這一個
1988 年原版寫的          0x21   ← 逐位元組相同
第二次啟動讀存檔後       0x21   ← 沒被母本重置
```

**這是這個專案能拿到的最強驗證形式**：不是「畫面看起來對」，
是**引擎對同一個動作寫出的 byte 跟原版寫的一樣**。
`docs/re/78` §2 那 29 處差異在這裡從「證據」變成「對照答案」——
出廠檔案本身就是原版留下的一份執行紀錄。

**順帶一個結構調整**：`loadSpecialTiles`／`writeSpecialTiles` 原本寫在
`cmd/demonwinter`，加測試時才發現那個 package 一 import 就會因為
ebiten 沒有 DISPLAY 而 panic —— **檔案 I/O 與來源挑選本來就不是 UI 的事**，
移進 `internal/assets/scenario` 之後才測得到（6 條測試）。
測不動往往是分層錯了的徵狀，不是「這段不好測」。

### 這一輪（2026-07-26 續二）：把 `nSS.DAT` 接進引擎，順手抓到兩個活的 bug

反組譯的結論落地。**發現的不是「A2 還沒實作」，是 A2 實作在錯的資料上。**

引擎本來就有 `game.LookupEvent`，演算法跟原版一致（兩個計數器、
反向尾表公式都對），但餵的是 `EXITS.DAT` 且用 3-byte 切開 ——
那個檔其實是 6-byte，所以**每一筆的座標與類別全是錯位的**。
「測試綠」完全擋不住這種錯：測試驗的是演算法，不是資料來源對不對。

**做了什麼**：

- `internal/assets/scenario/specialtiles.go` —— `nSS.DAT` 解碼、查表、
  三種改寫、`Encode()` 存回、`SplitAllSS()` 新遊戲重建
- 事件查表改用 `a.special[a.mapID]`，`game.LookupEvent` 整段**刪掉**
  （留兩份實作只會讓下一個人挑錯的那份）
- `world.ExitTable` 改成正確的 **6-byte 出口表**
- **實機驗過**（`87-nss-event.png`）：從起始存檔往北五步到 (9,27)，
  類別 1 事件觸發、中文敘述顯示。那一格正是 `1SS.DAT` 裡的 `attr 0x21`

**順手抓到的第二個 bug（自己上一輪種的）**：`i18n.Verify` 沒跳過名稱型
條目，於是四條續接碼譯文每次啟動都被 `Index < 0` 判成「索引超出範圍」，
印出「重跑 `dwstrings events`」—— 而 `dwstrings check` 那邊是 368/368 通過的。
**會叫錯的閘門比沒有閘門更糟**：誤報一開始，真警告就被當雜訊。

**測試設計上的一條**：驗「6-byte 才對」時，第一版寫成門檻式斷言
（「四分之三以上要成對」），實測 40/55 差一點就不過。
**沒有放寬門檻** —— 改成斷言實測值 40 **加上對照組**：
同一份位元組用 3-byte 讀，成對數是 0。
門檻可以被放寬到讓錯的切法也過，對照組不行。
順帶發現沒有回程的 15 筆依 `+0xaf` 分群，其中六筆全部單向通往圖 5
（禁錮惡魔那張圖）—— 那是線索不是結論。

### 這一輪（2026-07-26 續）：`nSS.DAT` 其實是存檔（`docs/re/78`）

接著上一輪的兩個問號：那張特殊格清單被誰改寫、B8 剩的兩處 `+0x9c`。

**主結果**：`nSS.DAT` **會被寫回磁碟** —— 新遊戲把 `ALL_SS.DAT`（母本，2560）
切成 5×512 重寫五個檔（四個常數 2560／512／511／`'1'+i` 全部對上），
遊戲中則有三種就地改寫：**清零**（4 處）、**類別 +3**（1 處）、
**`(類別<<5)|1` 標記已造訪**（2 處）。

**最漂亮的一條驗證**：出廠的 `1SS`／`2SS` 與 `ALL_SS` 有 29 處差異，
型態只有三種，**三種都對得上上面那三個改寫指令，零例外**。
連 `0x1788e` 那個看起來很怪的守衛 `< 0xc1` 都被資料解釋了 ——
`0x66 + 0x60 = 0xc6 ≥ 0xc1`，**它就是「只推進一次」**。
所以出廠的 `1SS`／`2SS` 是**玩到一半的狀態**，不是 `resource-index.md`
猜的「兩份建置產物不同步」。

**對引擎**：`nSS.DAT` 跟 `PARTY.DAT` 一樣受「不得覆寫玩家原始存檔」管，
而且它正是 A2（世界事件）缺的那份資料來源。

**兩條紀律**：

- **別信陳舊的「未解」標記。** `0x14933` 早在 `docs/re/51` 就解成
  「新遊戲初始化」，`docs/re/75` 卻把它重新標成「未追」（函式邊界抓錯 →
  掃不到字串 → 判定無脈絡），於是它又進了一次 worklist、又被追了一次。
  `rulebook/63` 講「別信陳舊的完成標記」，這是它的**反向**。
  重掃很便宜，但重掃的結果要**跟既有筆記對照**，不是直接覆寫。
- **一種存取形狀不代表全部用途。** `docs/re/76` 從 30 處存取裡挑出 3 處
  `inc`／`dec` 就把 `ds:0x52d4` 結案成「巢狀深度」；本輪發現 `0x10713`
  拿它 **× 6** 去索引船隻陣列（stride 6 是 `docs/re/28` 驗證過的），
  講不通 → **重新標未定案**。定案前要問「有沒有哪一處我解釋不了」。

### 這一輪（2026-07-26）：`nSS.DAT` 定案，並拆掉一組互相支撐的錯誤（`docs/re/77`）

從 B8 剩的一欄（`party+0xa9` 的第二個設值點）進去，出來時推翻了三份文件的判讀。

**做法**：寫 `tools/fieldscan.py` 掃某個欄位的**全部**存取端（ModRM `mod=10` + disp16，
byte-level 比對）。`+0xa9` 16 處、`+0xbe` 11 處、`+0x99` 3 處。
**普查本身就在定語意**：`+0xbe` 十一處存取沒有一處寫 0 → 單向閂鎖，不用再讀任何邏輯。

**主結果**：`+0xa9` ＝ **`nSS.DAT` 快取有效性旗標**，而 `nSS.DAT` ＝ **子地圖的特殊格清單**
（`docs/formats/resource-index.md` 原判為開場序列的畫面繪製指令）。
表會被事件就地改寫（`ds:0x52f8`：清零／類別 +3），所以需要 per-dataset 快取，
而 `+0xa9` 就是防止回頭走一趟把已觸發的事件復活。

**被推翻的三處**（詳見 §5）：

1. `docs/re/05` §1.3「`0x5514` 就是 `EXITS.DAT`」（**標已驗證**）—— 是 `nSS.DAT`。
2. `docs/re/05` §1.4「類別 3–7 永遠走不到」—— 拿錯檔案驗的，類別 0–6 全部出現。
3. `docs/re/69` §3「511 bytes 是 64×64 探索地圖」—— 那是 `nSS.DAT` 的檔案大小。

**教訓有兩條，都不是「該先 grep」**（這次有 grep，也 grep 到了那幾篇）：

- **兩個錯誤會互相支撐。** §1.3 把緩衝區認錯，§1.4 就拿錯的檔案去驗 3-byte 假說 ——
  而 330 剛好能被 3 整除，於是兩邊看起來都成立。能戳破的硬證據（兩個載入呼叫的大小
  參數 `0x14a` vs `0x1ff`）**一直就寫在 §1.3 自己的引文裡**，只是從沒被對照。
- **太漂亮的算術要當紅旗。** `docs/re/69` 從 511 推出「64×64 bits 少一個 header」——
  算術漂亮到讓人不去追讀取端。它其實只是某個檔案的大小。
  （這是 §5「算術對不代表方向對」的第七次，這次的變體是「算術對不代表**單位**對」。）

### 這一輪（2026-07-26）：中文化顯示層 + 事件敘述全數翻完

三件事，每一件都在做的過程中翻出一個錯：

1. **倚天點陣字上畫面**。踩到的坑是把 ASCII 當半形（格寬 8）配上放大兩倍的 8×8 字模，
   畫面上每個英文字母疊掉前一個的一半。依 `rulebook/81` 改成兩者都 16 格。
2. **`cjk.Big5()` 原本只看編碼器有沒有報錯，不夠**。Go 的 Big5 編碼器會把一批字
   映到標準區以外：簡體字進造字區（`头`→`8960`）、而 `撐` 這種標準繁體字被編成
   倚天延伸區的 `FCB9`，儘管它在 `BCB5` 就有字模。兩種情況都是「編碼成功、畫面空白」。
   改成從**解碼方向反建** Unicode→標準 Big5 對照表，一次解掉整類問題。
   ⚠ 附帶推翻一個直覺：**「簡體字一定不在 Big5」是錯的** ——「只」「儿」都是標準 Big5 收錄字。
3. **105 筆事件敘述 100% 翻完**（4 個 subagent 分頭翻 + 統一譯名表 + 自動閘門）。
   閘門本身也抓到自己的兩個 bug（多譯法欄位配對錯、複合詞誤報），
   且用「故意種錯誤」證實過它會紅，不是空轉。

### 上一輪（2026-07-25）解掉的長期未解項

召喚／幻術、即死／束縛／枯萎、怪物進場擲點、兩個戰鬥邊界條件、爆擊門檻條件、
城鎮六大設施全部公式、市集議價與說服技能、治療所費率來源、
**種族欄位位置**、遊戲內部技能 id 表、`FILES.DAT` 表布局、道具槽陣列起點。

同日整理軟體世界 1990 年中文說明書（`docs/manual-cht/`）後另外確認：

- 拿它的附錄 A 對 `FILES.DAT 0x158`，**抓出 `docs/manual/part-4.md` 三處抄錄錯誤**（已訂正）
- 山丘「通過時間兩倍」＝ `04-movement` 的「某些 tile 步數 +2」，機制對上、待補 tile 清單
- 建角**三次重擲機制**——手冊有、程式碼未定位（⚠ 2026-07-26 已在 `DEMON.INT` 找到提示字串 `Enter #s to be rerolled!`，複數 `#s` 佐證「可一次重擲多項」）
- **MGA 單色版執行檔 `PLAY` 不在手上的 dump 裡**，該版本無從逆向
- ⚠ 「一天 26 小時」是英文手冊的中譯，**不構成第二個佐證**，26 vs wrap 38 的落差依舊未解

同日稍晚**推翻並訂正 sprite frame 尺寸**（見 §5 已被推翻的斷言）：
CGA `.SHP` 是 16×16（64 B）、EGA `.SHE` 是 16×28（224 B），
frame 數 COMBAT 44／CYPHER 27／DEMON 102／MONSTER 240／SHIP 32／WINTER 102。
連帶確認：EGA 素材一律「檔案存半寬、顯示時寬 ×2」（`.SHE` 載入時加倍、
`.PIE` blit 時加倍，`.PIE` 顯示尺寸是 288×252）；
`DEMON`/`WINTER` 的 102 格是地形圖塊集，`WINTER` 是雪地版。

追查 sprite 尺寸時，發現 **CGA 字型 `ASC.FNT` 一直解不出可讀字形**（atlas 全是雜訊，
但文件標著「已驗證」——那句其實驗的是 EGA）。一併解掉：
`.FNT` 是 **packed 2bpp**（byte0 = 左 4px、byte1 = 右 4px）不是 planar、
**無檔頭**（3,073 B = 3,072 資料 + 結尾 `0x1a` DOS EOF，原解碼器誤跳開頭一個 byte、
整份位移一格）、**96 字 × 2 bank**（bank0 一般、bank1 反白，bank 間距 1,536）。
`.FNE`（EGA 16×14）不受影響，原結論成立。

也認出**山丘 = tile `0x0e`／`0x2b`**（移動多算一步的那兩個值）：
圖塊畫出來是起伏等高線，與手冊「山丘通過時間為一般地形兩倍」對得上。

### 這一輪（2026-07-26）：自動進城 + `SUM.MAP` 解壓修正

從 `town%d.dat` 這個格式字串反推，在 `DEMON.INT` `DS:0x2f86` 找到 25 筆城鎮
`(X, Y)` 常數表，踩到 tile `0x2e`／`0x64` 就用它決定城鎮編號 —— 自動進城打通，
選單降為偵錯入口。

拿這張表去對地圖時 0/25，反過來抓出 `SUM.MAP` 的 RLE 解壓有兩個 bug
（次數 byte 0 代表 256、每個區塊首 byte 要跳過）。修好之後 23/23 個子地圖
精確填滿 4096 格，25/25 座城鎮座標全部落在城鎮格上，算繪出來也從雜訊
變成連貫的大陸海岸線（`workplace/dump/maps/`）。細節與教訓見 §5。

順帶推得**世界是子地圖組成的網格，ID = 欄×10 + 列**（當時只是假說，
**已於同日稍晚由換圖程式碼確認**，見下）。

同日再解掉三件：

- **`-map` 現在收子地圖編號**（`-map 34`）。在這之前世界地圖全打包在 `SUM.MAP` 裡、
  viewer 只認檔名，玩家根本走不到大陸上。實測從 (33,11) 往東一步進 New Gleon，
  物價 13、碼頭有船 —— 與攻略「到東北方的新格里昂買船」對得上。
- **tile `0x14` → `0x62` 的條件追出來了**：不是季節旗標，是乘 125 的擲硬幣
  （`15be:179f`，種子 10001）。兩者是海面的兩種浪花，可通行性都是 `0xff`，
  **純外觀** —— 原版用它讓大片海洋不要一模一樣。實作在 `world/ocean.go`，
  放算繪端不寫進 `Map`。
- 連帶**推翻 `town-and-map.md` §2.2 的 tile 猜測表**：tile 98（`0x62`）被猜成「牆」，
  實際是海面。那張表是「找不到 tileset 只能靠頻率猜」時代的產物，圖塊與可通行性
  都解出來之後已無理由留著。

再往下追七大地形時，翻出 **arena 索引 6（`FILES.DAT 0x0A8`，176 bytes）是
戰場視線遮蔽查表**（`docs/re/22` §3.5）：戰場 9×9、中央是視點，格子上有擋視線的
地形（`0x0d`/`0x12`/`0x13`/`0x2a`/`0x31`/`0x5e`–`0x61`）就把後面的格子塗黑。
49 組對應內部 7×7 的每一格，`0xff` 是「什麼都遮不到」的哨兵。

這件事**附帶把戰場尺寸釘死了**：`BattleGridWidth/Height` 原本註明「取 9×9 是為了
與呈現層一致，未經原版確認」，現在有原版證據 —— 索引以 9 展開、上限 81、
內部掃描 7×7，三者只有 9×9 自洽。

過程也示範了一次「別靠統計猜結構」：先前只看資料，切法只有 74% 的組像矩形，
起點也掃不出最佳解，於是先記成線索不動程式；改照 `rulebook/62` 去找讀取端，
迴圈邊界一算就得到「49 組」，回頭切資料精確吻合，順便發現起點是 `0x0a8` 不是
`0x0a5`（可通行性那段在 arena 裡佔 104 bytes，只有前 101 個是有效 tile）。

順著讀取端再往上，**戰場地形也解開了**（`docs/spec/02-combat.md`「戰場座標與地形」）：
戰場不是另一份資料，就是**大地圖以遭遇位置為中心的局部放大**，
視窗大小 (9−2k)×(9−2k) 貼在網格 (k,k)。k 是視野內縮量，戶外查一張
**時辰表**（`DS:0x209c`）—— 白天 k=0 全視野，深夜 k=3 只剩中央 3×3。
連帶認出**隊伍欄位 `+0x9f` 是「時辰」**（`0x164f7` 對它 `inc`，同時把
步數計數器 `+0xa0` 歸 1），這是存檔欄位表原本沒有的一格。

戰鬥畫面已接上：腳下畫得出地形，看不到的格子留白。實測 MAP1 (20,20)
開打，60 格地形有 37 格被牆擋住，走廊方向看得到 —— 就是地城該有的樣子。

再追一步，**世界網格從假說升級成已驗證**（`docs/formats/town-and-map.md` §2.5）。
證據是走到子地圖邊界時的換圖程式碼（0x16fec–0x17114），四個方向對稱：

| 方向 | 觸發 | 擋住條件 | 否則 |
|---|---|---|---|
| 西 | `X == 3` | 欄 == 1 | `map_id -= 10`、`X ← 59` |
| 東 | `X == 60` | 欄 == 7 | `map_id += 10`、`X ← 4` |
| 北 | `Y == 3` | 列 == 1 | `map_id -= 1`、`Y ← 59` |
| 南 | `Y == 60` | 列 == 7 | `map_id += 1`、`Y ← 4` |

「X 動十位數、Y 動個位數」把兩個軸釘死，不用再靠接邊統計與攻略方位。
擋住的條件同時給出範圍：**欄 1–7、列 1–7，世界編號 11–77**。

同時修正了假說裡的一個錯：舊表把 `MAP1/3/5` 畫成「欄 0」那一行，
其實**編號 10 以下不在世界網格上，是地城**（連 `SUM.MAP` 裡的 2、4 共五座）。
實際存在的世界子地圖 21 張，49 格裡其餘推測是純海洋、沒有存檔。

又多認出兩件事：**隊伍欄位 `+0xa3` 是目前的子地圖編號**（四段換圖都在改它，
別處拿 `>= 10` 當戶外／地城的判斷），以及**一張子地圖走得到的範圍是 4–59
而不是 0–63** —— 四周各三格是換圖邊界。

### 這一輪（2026-07-26）：怪物 AI 收尾 —— 挑目標與噴吐（`docs/re/23`）

原本掛在 `docs/spec/02-combat.md` 的缺口是「AI 挑到增益法術時要打誰」——
實測會看到怪物法師對玩家施放「聖域」。追下去發現效果分派在 `138d:0c65` 的
17 項跳表，索引是 `effect_type`：

- **只有效果 3–7 會看 K 的正負**：`K > 0` 打自己人（增益）、`K <= 0` 打玩家。
  其餘六支一律打玩家側。
- **範圍法術（效果 1）有誤傷否決**：掃完 5×5 方框，`己方命中 × 2 > 敵方命中`
  就中止並退還法力。施法者自己算在己方裡。
- **效果 0x10 = 附身術**，成功率 `投入 × 300 / (2×目標HP + 目標法力)`。

用真實資料交叉驗證：判成「打自己人」的七個法術全是護盾強化類
（FLAME SHIELD／STRENGTH／ARMOR／…／SANCTUARY），而且與 RUST ARMOR／
WEAKEN／CHILL／SLOW 構成四組「共用效果類型、K 正負相反」的對照。
英文名稱沒有參與推導。實機也走到了兩條分支（`烈焰之柱`打玩家、`聖域`給同伴）。

**順帶發現一個自己造成的陳舊標記**：`unit+0x20` 的值域表 `docs/re/20` §9.5
早就整理完，但 `ai.go` 與 `docs/spec/02` 掛著「語意未定案、已收回」。
收回本身沒錯（當時的依據確實不足），錯在收回之後沒回頭查既有文件，
等於把已解的東西重新標成未解。詳見 §5 的推翻清單。

同一輪把**噴吐**也讀完了（`docs/re/23` §7）。原函式分兩趟掃描：第一趟數命中、
第二趟扣血，中間夾一道**與範圍法術完全相同的否決**（己方×2 > 敵方就放棄）。

```
噴吐傷害 = rnd(施放者當前HP ÷ 3)
```

與投入、等級、目標護甲都無關 —— 受傷的龍噴得比較弱。兩趟用不同寫法描述
同一個錐形（一個從面前那一格算、一個從施放者算），互相對得上，等於自帶
一組交叉驗證。

順帶把 `gamedata.Monster.Special` 從「語意未確認」升級為已確認：它是
**種族／元素類型**。`MONSTER.DAT` 裡落在 AI 噴吐區間 8–12 的**只有四隻，
全是龍**（火龍 8／風龍 9／冰龍 10／巨龍 11），而免疫組 `{4,7,10}` 裡的
7 是冰系生物、10 是冰龍 ——**冰的吐息傷不了冰**。舊註解疑惑「元素系怪物的值
不連續、對不上五大符文系」，那是把兩件事混在一起了。

驗收工具也補了三樣：`-battle` 旗標（headless 截圖按不到 B 鍵）、
`-battle-monsters`（指定要放哪幾隻怪，例如三隻龍）與 `DW_LOG=1`
（戰鬥紀錄同步吐 stderr，畫面只留 8 行看不到完整時間軸）。
實機驗到 `Wind dragon 噴出吐息（波及 5 人）`。

### 這一輪（2026-07-26）：七大地形定案 + 隨機遭遇（`docs/re/24`）

專案裡掛最久的一項解了。答案是**引擎沒有第二張地形表 —— 地形就是可通行性
表的值**：同一張 `FILES.DAT 0x040`，移動時當「能不能走」，遭遇時當「這是哪一種
地形」。前面四次都在找一張不存在的對照表。

連帶把 `FILES.DAT` 剩下兩段「未知」也解了：`0x000`（64 bytes）是
**8 地形 × 8 個遭遇群組槽位**，`0x28E`（404 bytes）是 **18 個遭遇群組 × 22 bytes**。
`FILES.DAT` 到此每一個位元組都有主。

八個類別裡六個對上手冊、兩個是地城地板，**「Kudzu 樹叢」沒有自己的類別**
（否定結論，不是還沒查）。只有山丘是直接驗證（移動成本）；其餘靠「遭遇群組
內容 + 圖塊外觀 + 世界地圖佔比」三路佐證 —— 執行檔與資料檔裡都沒有地形名稱
字串。沼澤與沙漠兩條特別強：手冊點名的鬼火、鬼墳族、苦修僧、蜥蜴人四隻，
各自只出現在那一個地形掛的群組裡。

**隨機遭遇也一併接上了**（原版每走一步 1/64、`8 − rnd(7)` 隻、難度 ±1 的等級帶、
等級值 ≥100 的「清一色群」鎖定）。實機在森林走一走就遇到三隻蟲群加三隻巨蛛。
在這之前世界地圖上是完全安全的。

### 這一輪（2026-07-26）：道具效果接上（`docs/re/25`）

「選了道具不會有效果」解了。關鍵是回到第一性原理問「要生效只需要什麼」：
道具身上要有效果編號、編號要查得到效果、效果要能套到目標。前兩件機器碼直接
給死 —— Use 路徑餵給 `FUN_1000_114f` 的是道具槽 `+0x07`，而**那個載入函式就是
法術用的那一個**。一張表、一個載入函式、一個索引空間 ⇒ 道具的效果編號與法術
id 是同一個命名空間，不用再追「新道具怎麼生出 +0x07」。

道具槽補齊四個欄位：`+0x05` 已用次數、`+0x06` 總次數、`+0x07` 效果索引、
`+0x08` 強度（同時當可用旗標）。順帶解掉 `ITEMS.DAT` 那四個「語意未確認」
欄位：它們是**效果類別候選**，掉寶生成時擲兩層骰（先挑類別，再從
`DS:0x1941` 的候選表挑效果）。

**又清掉一條錯誤測試**：原本斷言「真實存檔至少列得出一件可用道具，否則過濾
規則太嚴」。實際 dump 起始存檔，五名角色 10 格的 `+0x05`–`+0x08` 全是 0 ——
**原版開局按 Use 本來就是空選單**。那條測試釘住的是還沒解出 `+0x08` 時的
不完整規則，已改成反向斷言。

實機驗證用新加的 `-give-item` 旗標塞一件 salve 進去：
`Wopple 使用 salve` → `Wopple 的力量術讓 Wopple 增加 7`。

### 這一輪（2026-07-26）：道具與怪物名稱中文化 + 修掉中文欄位對齊

畫面上冒出 `salve` 才發現**道具（30）與怪物（99）名稱整份沒進翻譯層**。
補了 `dwstrings items`／`dwstrings monsters` 兩個子命令，沿用既有的
（來源檔, 索引）機制；譯名優先取 `translations/glossary.md` 既有裁決。

接上譯名之後立刻看到「火蜥蜴」被顯示成「火蜥」——`name[:8]` 是**位元組**
截斷，中文會切在字元中間生出半個字。順著查下去發現 `%-Ns` 也一樣：
Go 依位元組補空白，中文那一列的數字欄整個往左跑。**法術選單的「最低法力」
欄其實早就歪了**，只是沒人拿截圖比對過。

加了 `textlayout.PadCells`（按排版格截斷與補白，本專案所有字元同寬 16px），
把戰鬥名冊、法術選單、道具選單、城鎮商店、名冊、治療所、神殿七處全部改掉。

**教訓**：中文化不是把字串換掉就結束 —— 換完之後每一個固定欄寬的排版都要
重新對過。這一輪如果只做翻譯不看截圖，就會留下一個「半個字」的 bug。

### 這一輪（2026-07-26）：城鎮設施差異化 + 又兩條陳舊標記

盤點畫面時發現城鎮選單掛著「城鎮設施清單尚未從原版解出，目前七種都列出」，
但 **`docs/re/02` §3.2/§3.4 早就把它解完了** —— 旗標在 `TOWN*.DAT` 的
`0x1ee`–`0x1f6`，而且該文還附了 25 座城鎮的完整 dump。接上之後每座城鎮
真的不一樣了：新格里昂只剩市集／公會／碼頭（對上攻略「到新格里昂買船」）、
鐵穹鎮七項全無只剩市集。

順帶發現 **`0x1f5` 一個位元組兼兩用**：非 0 就顯示碼頭選項，值本身又是
船價基礎。原本程式碼有兩個常數指向同一個位移，已收成一個並記錄。

同一輪清掉的另一條陳舊標記：城鎮選單抬頭的「座標對照尚未解出」——
那是上一輪就解的（`DS:0x2f86` 的 25 筆座標表），自動進城早就在跑。

### 這一輪（2026-07-26）：睡覺（`docs/re/26`）

追「旅店休息尚未接上」時翻出**整個紮營子系統** —— Camp 是一個 13 選項的
選單（Reorder／Sleep／Identify／Worship／Xorcise／View land／Trade／Drop／
Equip／Use／Hunt／Cast／Quit），睡覺只是其中一項。

睡覺的規則全部解出並實作（`internal/game/rest.go`）：

- 時辰 15–24 才睡得著，否則 `You are restless`
- 睡眠時數 =（27 − 時辰）+ rnd(6) − 1，但**時辰是直接設成 rnd(6)**，
  所以幾點躺下都在清晨 1–6 時醒來、日期 +1
- 旅店 HP+2／SP+10，野外紮營 HP+1／SP+5 並吃一份糧食（沒糧就每人 −2 HP）
- 中毒者依睡眠時數扣血，種族 4 免疫
- 紮營會重設光源、把道具的已用次數歸零（過夜充能）

**打獵也解了**（`1000:08af`）：`收穫 = max(0, rnd(16) − 6)`，0–10 份，
16 面裡 6 面空手。要角色狀態正常且會技能 8（狩獵）。兩個交叉印證：技能 8
在 `docs/re/21` 就是「狩獵」，而實測起始隊伍只有遊俠 Menhir 會；
「本日已打獵」的旗標 `char+0xef` 由睡覺清掉，合起來正好是手冊的「每日一次」。

兩個入口都接上了：**旅店**（城鎮設施 `I` → `R`）與**紮營畫面**
（世界地圖按 `R`，`S` 睡覺、`H` 打獵）。實機：20 時睡下 → 9 日 1 時醒、
睡 7 個時辰、吃掉一份糧食（8→7）。

**副產品：抓到一個標反的欄位。** 上一輪把道具槽 `+0x05`／`+0x06` 標成
「已用次數／總次數」，**標反了**。使用端只有一道 `CMP` 兩邊對調完全等價，
從那裡分不出來；睡覺常式把 `+0x06` 清成 0 才分得出 —— 清「已用次數」是
過夜充能，清「上限」會讓道具永久失效。**分不出兩個對稱欄位誰是誰時，
去找寫入端**：讀取端只說它們會被比較，寫入端才說它們是什麼。

順帶正名 `scenario.CombatStatusFlags` → `CombatStatus`：CONTEXT 的推翻清單
早就寫著「不是位元旗標，是單一列舉值」，欄位名卻沒改。

**這已經是這個 session 第三次撞到同一種問題**（`unit+0x20`、可通行性分組、
城鎮設施）。共通模式是：**結論寫進了 `docs/re/`，但把「未解」寫在程式碼註解
或畫面文字上的那一份沒有跟著改**。`rulebook/63` 講的就是這個 ——
以程式碼為準、別信過期的狀態標記；但反過來也成立：**解出來的時候，
要回頭把所有標「未解」的地方掃一遍**。

**教訓（值得記）**：第一次嘗試其實已經摸到答案，當時卻寫下「組別本身不等於
地形種類」把自己否決掉 —— 理由是「`0xfe` 一組有 19 格、太雜」，但 `0xfe` 屬於
不可通行那一側，本來就不參與地形分類。**否決一個假設前，先確認自己否決的是
它的核心而不是邊角。** 另外任務單裡「從 `DS:0x52f4` 追」那條線索是錯的：
那是個到處被重用的暫存字，照著追會一直繞。

### 這一輪（2026-07-26）：買賣裝備 + 存檔真的寫得回去 + 月份是名字（`docs/re/27`）

**經濟迴圈補完**（`internal/game/trade.go`）：議價早就實作了，但買不到東西、
裝備也換不了 —— 整套是裝飾。這一輪接上買（市集 `B`）、賣、換裝（紮營 `E`）。
店裡的貨一律是**沒有效果的平凡裝備**（效果是掉寶時生成的，起始存檔每一件的
`+0x05`–`+0x08` 都是 0）；裝備只改角色記錄的槽位索引 `+0x100`／`+0x101`，
道具本身不動；**身上正在用的不能賣**（賣掉會讓索引指向空格）。

**然後發現存的檔是假的。** 買賣換裝在畫面上都對，但 `Encode` 只把
`InventorySlotsRaw` 原樣抄回去 —— 規則層改的是解析後的 `Inventory`，兩者不同步。
`ApplyTo` 也沒寫裝備槽與戰鬥狀態，trailer 更是連時辰、日、月、糧食、
子地圖、光源都沒寫。**遊戲不會報錯，玩家只會發現東西買了又不見。**
全部補上，並補了三條測試（道具槽寫回、清空只寫型別、trailer 欄位逐一釘住）。

實機驗收比測試更有說服力：載入 → 換裝 → 存檔，與原版 `PARTY.DAT` 逐 byte 比對
**只差 `char0 +0x100` 那一個 byte**（正是換裝那一格）；載入 → 直接存檔則是
**byte-for-byte 完全相同**。

順手修掉兩個「存了卻讀不回來」：座標與時間。`-x`／`-y` 原本硬編 32,32，
存檔裡的 9,32 從來沒被用過（旗標改成負值代表「跟著存檔走」）；時鐘原本一律
`NewClock()` 從頭開始，現在走 `ClockAt(時, 日, 月, 步數)`。

**月份不是序數，是名字。** 追時鐘欄位時發現 `+0x9d`（月）在狀態列被拿去
`×4` 查 `ds:0x50f2` 的遠指標表，套進 `"Hour %d, Day %d in the Month of the %s"` ——
22 個名字（Ruby、Ebony、Gold…Hyacinth），已抽成翻譯目錄
（`assets/lang/zh-Hant/months.txt`），畫面上現在寫「13時 17日 紅玉月」。
所以月是 **0-based 的名稱索引**，原版存檔的 0 是合法值，不是未初始化
（新遊戲初始化那一段只寫日 8、時 5，月根本沒寫）。

**副產品：整個 FILES.DTT 的分段邊界從猜測升級成證據**（`docs/re/27`）。
`DEMON.INT 0x11d5d` 是切表常式，依序填 8 張遠指標表，每張的筆數就寫在迴圈的
`cmp` 常數裡，**八張相加正好 501 —— 與檔案裡的字串總數一個不差**。
兩段原本猜錯的因此修掉：`[123:153]` 是一整張 30 條的**道具型別名**表
（＝ ITEMS.DAT 的 30 筆，`Demon Crystal`／`Orb/Evertime` 是型別 28、29，
不是另一張「神器表」）、`[153:164]` 是 11 個**神祇名**。

神祇那張表還牽出**同一個 session 裡第四次的陳舊標記**：`docs/re/09` §2.2 與
`combat.go` 的註解都寫著「祈禱成功率初始值 20 未核對到，需要下一輪找出 `+0xeb`
的初始化點」，但 `docs/re/19` §3.4 早就解完了（標題還寫「這是任務單最想要的答案」），
連寫入點 `278d:107f` 都列了 —— 換算成 file offset 正是我這輪「找到」的 `0x1c54f`，
同一個地方。**我等於重解了一個已解的問題，還一度當成新發現寫進 commit message。**
兩份文件都已訂正。教訓要記反向的那一條：看到「未解」時，先查其他文件解過沒。

**教訓（與上一輪同源）**：`[467:489]` 被標成「隨機物品命名詞庫」躺了很久，
理由是「Comet Sword、Hyacinth Ring 這種組合很像老 RPG 的命名詞庫」——
那是**只看內容**得到的「像什麼」。找讀取端只花了幾分鐘就變成「是什麼」。
`rulebook/62` 又一次生效。

### 這一輪（2026-07-26）：城鎮設施從價目表變成真的能用

上一輪把買賣接上之後回頭看，發現**七個設施裡有五個只是價目表**：
治療所列得出「治療 24 金」卻治不了、酒館報得出糧價卻買不到、
神殿列得出捐獻與祈禱、公會列得出經驗值，全都按不下去。
定價公式 `docs/re/19` 早就全解完了，缺的是「做下去會怎樣」那一半。

`internal/game/services.go` 補上五項：

- **治療所**：服務項目由角色狀態決定（死亡 → 復活、束縛 → 解除、中毒 → 解毒、
  否則看外傷）。復活只回 **1 點**生命並清掉束縛等級，一般治療才補滿。
- **酒館**：買糧，份數 1–200，總價 = 單價 × 份數，隊伍糧食鉗在 255。
- **神殿**：捐獻 1 金換 1 點經驗（封頂 `0x00FFFFFF`）；祈禱費用 = 等級 × 50，
  把呼喚成功率補回 20。
- **公會**：免費升級，門檻用手冊的 20 級表（**出處是手冊不是反組譯**，
  原版那張表用軟浮點查表，還沒解碼）。
- **市集出售**：售價 = 底價的一半，不套城鎮係數、不受議價影響；
  身上正在用的不能賣。

順帶修掉一條**算錯的公式**：`HealerQuote` 的解束縛費用乘的是角色等級，
但 `docs/re/19` §5.2 寫的是 `char+0xec（束縛等級）× 費率`。畫面標籤
`Unbind %3d/lvl` 的那個 lvl 指的是束縛法術的等級，不是角色等級。
順著補上三個原本沒解碼的角色欄位：`+0xeb` 祈禱成功率、`+0xec` 束縛等級、
`+0xf0` 信奉的神祇 —— 前者原本被實作成**整隊共用一個值**，那是錯的。

實機逐項驗過（`workplace/dump/persist/`）：海濱鎮酒館買 5 份糧（8→13 份、
65→55 金）、神殿供奉 Camear 捐 10 金（經驗 1037→1047）、市集賣掉小斧（+3 金）、
特拉巴捐 26 金補滿門檻後公會升到 4 級（生命 +8、法力 +7、三項屬性各 +1）、
治療所治好 Menhir（付 24 金）。

兩個順手修掉的視覺缺陷：市集的「買價／賣價」欄名是左靠齊、數字是右靠齊，
差 3 格；升級的成長明細一行放不下、被畫布裁掉半個字。

**還沒做的**：碼頭買船／修船（船隻陣列的格式沒解，硬做只會做出一個假的）、
學院教技能（公式有了，但學院不在七種設施的熱鍵清單裡）、神殿改宗
（`(0x10 − god_id)` 的索引語意可疑，`docs/re/19` §10 明列為未解）。

### 這一輪（2026-07-26）：碼頭與學院 —— M5 只剩改宗（`docs/re/28`）

上一輪把 M5 的缺口列成三項：碼頭、學院、改宗。這一輪關掉前兩項。

**碼頭卡在「船隻記錄的格式沒解」。** `docs/re/19` §4 只寫到「10 格 × 6 bytes、
船體值在 `+0x25`」，其餘標未解 —— 沒有格式就放不了船。這一輪把讀取端與
**寫入端**都讀完，六個欄位全部定案：陣列在 trailer `+0x22`，
`X／Y／子地圖編號／船體值／固定 2／未寫`。

三個關鍵是寫入端給的（`2aed:13d6`–`144d`）：**空格的判定是船體值為 0**
（座標 (0,0) 是合法位置）、放船的目標格**必須是海面 tile `0x14` 或 `0x62`**、
**錢在成功放船之後才扣**。原版存檔依此解出兩艘船（圖 44 的 56/75、圖 1 的 67/75），
六個欄位全部落在合理值域。

「這艘船是不是我的」是個**問錯的問題** —— 原版根本沒有記載歸屬。
修船只看腳邊有沒有船，買船只是往海上多放一艘。

**學院**接成城鎮選單的第八項（熱鍵 `L`）。原版的學院是地圖上的獨立地點
（`278d:342d` 有 35 筆座標→技能對照表），但 `TOWN*.DAT` 也有三個學院槽，
本作把它當設施 —— 這是本作的安排，文件裡標明了。費用要同時付得起
金幣（`points × (5·points + 25)`）與智力點數。

**技能名也中文化了**（32 條，`assets/lang/zh-Hant/skills.txt`），
順手把 `dwstrings check` 的檢查範圍從「事件敘述 + 法術名」擴到全部七個目錄 ——
它原本一直回報「148/148 100%」，**而實際上有兩百多條名稱沒被算進去**。
現在是 356/356。進度數字漏算比沒有數字更糟，它會讓人以為翻完了。

**副產品：金幣是 4 bytes 不是 3。** 買船的扣款是 `SUB word [+0x0a]` /
`SBB word [+0x0c]` 一組 32-bit 減法。`docs/re/19` §2 早就這樣列了，
但 `save.go` 一直標「長度未知，兩份存檔的第 4 個 byte 都是 0，分不出」。
**分不出來就去找讀寫端** —— 這個 session 第三次用到同一條。

**踩到一個自己造的 crash**：`AllFacilities` 加了第八項，但 `drawTownMenu` 的
`labels` 還是七個 —— `labels[7]` 直接 panic，而且是在「走進城鎮」這條每個玩家
都會走的路徑上。headless 截圖當時只是「一直沒有畫面出來」，因為 xdotool 的
`--sync` 會等視窗出現、等不到就永遠掛著。已補上標籤與一條測試。

**順手修掉一個一直在騙人的驗收工具**：`tools/screenshot.sh` 的用法寫著
「`[-- 額外的程式參數…]`」，但 Go 的 flag 遇到 `--` 就停止解析 ——
後面的 `-map`／`-x`／`-gold` 會被整批忽略，程式照跑、照截圖，只是全用預設值。
之前幾輪的 `-x/-y` 其實從來沒生效過，是這一輪查「船怎麼找不到」才發現的。
腳本現在會剝掉誤加的 `--`，說明也改了。

實機驗過：龍牙鎮碼頭修好 56/75 的船（付 190 金）、買下新船（付 670 金）、
阿奇斯圖學院拒絕點數不足的 Wopple（要 9 點、剩 7）、讓 Podgom 學會鎚術
（付 30 金，該項改顯示「已學會」）。

### 這一輪（2026-07-26）：改宗解開 —— M5 八種設施全部可玩（`docs/re/29`）

上一輪把改宗列成「唯一沒做的」，理由是 `docs/re/19` §3.5 留的疑點：
`(0x10 − god_id)` 這個索引在 `god_id ≥ 2` 時會寫到「召喚」以下的技能格，
語意可疑，不釐清就實作等於亂寫技能旗標。

**那個算式讀錯了一個運算元。** 減掉的不是神祇編號，是**神祇編號 mod 2**。

改宗那支函式（`278d:0f38`）吃**兩個**參數，舊文件把兩個都當成 god_id：
`[bp+6]` 才是神祇編號（拿去比對 `char+0xf0`），`[bp+8]` 是餘數（拿去算教派）。
神殿入口的兩條路徑都印證這件事 —— 固定神殿走 `idiv 2` 取 `dx`，
座標表直接存餘數（表裡是 `(⌈god/2⌉, god mod 2)`，所以 `god = 2a − b`）。

於是 `0x10 − 餘數` 只會是 15（薩滿）或 16（司祭），永遠落不到別的格子。
十位神奇偶交錯 —— 奇數薩滿、偶數司祭，正好是舊文件講的「5 位司祭神 + 5 位薩滿神」；
第 11 位 Ancient One 不在座標表裡，入口另有一段專屬描述文字。

**順帶訂正祈禱的前置檢查。** `docs/re/19` §3.3 寫「旗標為零時整段比對跳過
（新關係不擋）」—— **跳轉方向讀反了**。`cmpb $0, char[+0xc8+教派] / je` 是
「沒有那個教派的技能就跳去拒絕」。所以祈禱**要求先改宗到這座神殿**，
本專案照舊文件寫成「沒有信仰的人不擋」，也一起改掉。兩條合起來才是完整流程。

**副產品：`party+0xa8` 是雙用途欄位。** `docs/re/19` §2 記成「當前位置的學院
教授技能 ID」，神殿把同一個 byte 當神祇編號讀 —— 兩邊都用 `0xFF` 代表
「這裡沒有，去查座標表」。它其實是「目前站著的特殊地點的參數」，
又一個「一個欄位兩種用途」的 1980 年代慣例。

實機：Norman 在海濱鎮改信 Camear，學會司祭（花 2 點智力），祈禱成功率立刻變 20%。

### 這一輪（2026-07-26）：掉寶生成解開（`docs/re/30`）

`docs/re/25` §3 把掉寶生成解到剩一角就停了：「第一個欄位為 0 時挑到 0 減 1
會索引到表外，沒把握就不寫進規格」。**那是把 `jne` 讀成了 `je`。**

分支方向反過來，四個欄位就有了兩種用法：`f[0] != 0` 時四個是**候選清單**、
`f[0] == 0` 時另外三個是**排除清單**。武器的 `0,10,8,9` 是「除了 10、8、9 都行」、
護甲的 `12,7,7,7` 是「12 或 7」、雕像的 `15,15,15,15` 是「一定是 15」。
兩條路都保證非零，減一之後永遠落在 0–16。30 件真貨全跑過。

整條生成鏈也一併解完（`internal/game/loot.go`）：

- **附魔**：`for i := 1..等級 { if rnd(125) < 12−i { 附魔 = i; break } }`，
  護甲與飾品 `(n+1)/2` 減半，被詛咒的取負
- **兩道效果門檻**：`rnd(100) > 等級×2+6` 或 `> 等級+16` 就沒有效果；
  被詛咒的一定沒有效果
- **強度與效果要配得起來**：`最低法力(效果) > 強度` 就整組重擲 ——
  沒有這一步會生出「有效果卻付不起」的道具
- **三種充能種類**：分支值就是 `ITEMS.DAT` 那個一直標「語意未確認」的
  `CategoryIndex`

**兩個獨立解出來的東西剛好互相解釋**：`docs/re/26` §3.3 記睡覺跳過
`+0x05 >= 100` 與 `+0x06 == 0xff` 兩種道具不充能 —— 正好就是充能種類 2
（上限一定 >= 100）與種類 1（已用次數寫 0xFF）。那個 255 是「不計次」的哨兵，
不是計數：剛生成的道具「已經用掉 255 次」講不通。

實機：`-loot 14,60,40` 發一批藥瓶，戰鬥的使用道具選單裡出現「藥瓶 +3（未鑑定）」。

**沒接上入口**：解的是「怎麼生」，不是「什麼時候生」—— 戰鬥勝利與寶箱的
掉落判定還沒追。目前只有偵錯旗標 `-loot` 這一條路。

### 這一輪（2026-07-26）：掉落接上戰鬥勝利（`docs/re/30` §5）

上一輪解完「怎麼生」，留下「什麼時候生」。生成器的進入點其實是 `1990:1107`
（上一輪引用的 `1250` 是函式中段），**全檔只有兩個呼叫端**，各自的字串一看就認得出來：

- `0xe3fc`：`"Exp per chr: %ld    Gold: %ld"`、`"Give to character ?"`、
  `"No more room!"` → **戰鬥勝利的結算**
- `0x1d6b0`：`"You see a group of merchants"`、`"They show you their wares..."`
  → **商隊遭遇**（只認出字串，流程沒讀）

戰鬥那條解完並接上了：**逐隻獨立判定** `rnd(100) <= 怪物等級×6 + 5`，
中的那隻掉一件。1 級怪 11%、10 級怪 65%、16 級以上必掉 ——
打贏一群低等怪很可能一件都沒有。道具型別是 `rnd(26) − 1` → 0–25，
**火把、提燈、惡魔水晶、恆世寶珠永遠不會掉**（前兩件雜貨、後兩件劇情物）。

**順帶解掉 `+0x0f`**：結算印道具名時拿它 ×4 去查 `ds:0x1852` 的遠指標表 ——
那個一直標「語意未解、原版實物都是 1」的 byte 是**名稱類別索引**。
分組規則還沒讀完，造新道具時仍照原版填 1。

**修掉一個結算只存在一幀的缺陷**：戰利品原本是在「按下空白鍵之後」才發，
而那一幀馬上 `a.battle = nil` 回世界地圖 —— 撿到什麼的訊息玩家永遠看不到。
改成戰鬥一結束就結算，空白鍵只負責關畫面。

兩個新的偵錯旗標：`-loot 型別,等級[,件數]` 直接發道具、`-battle-win` 直接進
勝利流程。用 xdotool 打完一整場戰鬥不切實際（要走位、要相鄰才打得到），
但「打贏之後會怎樣」得看得到。實機：打倒五隻高等怪 →「Wopple 撿到戒指」
「Wopple 撿到戰斧 +2」。

### 這一輪（2026-07-26）：搭船航行（`docs/re/31`）

碼頭買得到船之後，船只是海上一個圖案 —— 上不去，跟先前「議價能談但買不到」
是同一種空殼。這一輪把搭船接完。

整個搭船狀態就是**一個 byte**：隊伍欄位 `+0xb0` 存「目前搭的是第幾艘船」，
**格號 +1**，0 代表沒搭（0 要留給「沒搭船」，而 0 號格是合法船位）。
一個很乾脆的旁證：`0x3c0a` 在播腳步聲之前檢查這個欄位 ——
**船上不播腳步聲**。沒有人會為了別的用途在那裡檢查它。

- **上船**＝走到船那一格（比對 X、Y、子地圖三者）
- **航行**＝海面變成可通行，這是搭船唯一改變的移動規則
- **上岸**＝走上陸地，船的座標寫回記錄後清掉 `+0xb0`

**踩到一個自己造的坑，靠實機截圖抓到。** 第一版把上船判定放在
「不可通行」之後 —— 但船停在海上，海面本來就走不上去，結果是
「船看得到、上不去」（截圖：`前方無法通行`）。上船判定得排在
可通行性之前。這種錯單測抓不到：規則層的 `StepBoat` 全部綠燈，
壞的是它根本沒被呼叫到。

實機驗收用逐 byte 比對：登船 → 海上走三步 → 折回上岸 → 存檔，
與原版 `PARTY.DAT` 只差六個 byte —— 船的座標從 (39,21) 變成 (38,22)
（跟著隊伍走、停在下船處）、隊伍座標與子地圖、步數。**`+0xb0` 不在差異裡**，
上岸後回到 0。

**沒實作**：跨子地圖上船、撞岸音效、船體損耗（什麼時候掉血還沒追）。

### 這一輪（2026-07-26）：商隊遭遇的生成規則（`docs/re/32`）

掉寶生成器的第二個呼叫端讀完了：路上遇到一群商人，打招呼就能看他們的貨。

開場訊息是 `"You see a group of" + 形容詞 + " merchants"`，形容詞由**商隊規模**
決定 —— `ds:0x3612` 的對照表 `[0,1,2,2,3,4,5,5,6,6]` 讓**七個形容詞蓋十種規模**
（ragged looking / poor looking / travelling / adventuring / well dressed /
upper class / wealthy）。

貨是掉寶生成器生的：**7–10 件**（`rnd(4) + 6`），每件各自擲詛咒，
機率 `rnd(120) − 80` 鉗在 0 以上 —— **值域 0–40，而且超過一半的商隊完全乾淨**。
這是商隊與市集最大的差別：市集賣沒有效果的平凡裝備（安全但無趣），
商隊賣掉寶生成器生出來的東西（可能有效果、可能被詛咒，而且**未鑑定**）。

**踩到一個解遠指標的坑**：`ds:0x3632` 存的段值 `0x21f0` 直接套
`seg*16 − 0xC400` 會落到程式碼區 —— 這份 dump 的段值有 **+0x1000 偏移**
（與 `CALLF 0xN000:xxxx` 代表 `0xN0000 + xxxx` 是同一件事）。補上就對了。

**這一輪只做規則層，沒有遭遇畫面。** 價格公式沒解出來（生成器開頭那段
`310e` 軟浮點運算很可能是估價，但沒追它流去哪），沒有價格就沒辦法做購買介面。
硬湊一個「底價 × 某個係數」等於自己發明規則，與「反編當 oracle」相衝突。

### 這一輪（2026-07-26）：追軟浮點估價 —— 追錯方向，但撿到別的（`docs/re/30` §2.0）

上一輪說「下一步該追那段軟浮點估價，它卡著商隊購買介面」。**追過了，它不是估價。**

那個 32-bit 值是**掉落型別的最低價格門檻**：生成器先 `rnd(26) − 1` 擲型別，
再拿 `ITEMS.DAT` 的價格跟 `f(等級) + 25×等級` 比，**價格低於門檻就重擲型別**
（`cmp / jl → 回 0x1128`）。是「最低」不是「最高」—— 等級越高掉的東西越貴，
所以高等怪不會掉匕首。與商隊售價無關。

`f(等級)` 那一段軟浮點還是沒解碼（`bx` 指向 `ds:0x40e0`，但那裡是字串資料，
呼叫慣例沒讀懂）。**本專案沒有實作這道門檻** —— 用 `f = 0` 去湊等於自己發明
分布。已知的偏離是「各型別掉落機率一律均等」，寫進 `docs/re/30` §6 了。

順帶兩個確認：`[0x4e38]` 是道具型別名的遠指標表（正是 `docs/re/27` 切表常式
填的第 4 張，FILES.DTT `[123:153]`）；`[0x4cc8]` **不是神祇專用** ——
`docs/re/29` 是在神殿脈絡下看到它，掉寶也拿它存「目前要印的名字」，
它是個通用的暫存指標。

**這一輪的產出是「知道不用往那邊找了」**，程式碼沒有變。追錯方向也要記下來，
不然下一輪還會再追一次同一段。

### 這一輪（2026-07-26 續）：紮營選單其實有 14 項，Drop 與 Trade 接上了（`docs/re/33`）

`docs/re/26` 說紮營選單有 13 項，還警告過「顯示順序 ≠ 跳表索引順序」。
**兩件事都錯**：選單是 **14 項**（漏掉排在最前面的 `Party`），補上之後顯示順序
與跳表索引完全一致，連快捷鍵 `PRSIWXVTDEUHCQ` 都同一個順序。

判錯的原因值得記：當初只從字串區 `DS:0x0106` 往下讀名字，沒有讀**用它的那張
指標表**（`ds:0x94`，14 個遠指標，長度剛好抵著下一張表）。
表格類資料要從讀取端回頭定邊界，不能從資料區猜 —— `rulebook/62` 的老規矩。

實作接上 **Drop** 與 **Trade**（規則完整，沒有留白）：

- 型別 **0xfe 是地城道具**，在營地丟不掉。原版判定寫成 `> 0xfd`，所以空格
  （`0xff`）也吃到同一句「地城道具不能丟」—— 那是隨手複用，本專案分開講。
- 地城道具**丟不掉但給得出去** —— Trade 完全沒有型別檢查。
- Trade 撞到裝備中的東西會**退回選道具再問一次**，Drop 則是直接結束。
- Trade 用了漂亮的一手：把自己的戰鬥狀態暫時改成 6，借選人清單既有的過濾
  規則把自己排除，選完再改回來。順帶告訴我們**狀態 6 = 不可選取**。

實機驗收：交出小斧後存檔，與原版 `PARTY.DAT` 只差四個 byte，每一個都解釋得出來
（見 `docs/re/33` §5）。

### 這一輪（2026-07-26 續二）：trailer 開頭那九個 byte 是 3×3 陣型格（`docs/re/34`）

`docs/formats` 一直把它標成「**假設**是隊形/順序表，排列方式未解」，長度還猜成 10。
釘死了：**它是一張 3×3 的陣型圖**，格子排成 `A B C / D E F / G H I`，
各存一名成員的隊伍索引，`0xFF` 是空格。長度是 **9 不是 10** ——
第 10 個 byte（trailer `+0x09`）是另一回事。**後續已由全檔 consumer 稽核
結案為此 DOS binary 未使用的保留 byte**，現名 `Reserved09`，任意值照原樣
round-trip（`docs/re/112`、§7 C9）；不是目前 worklist。

四個呼叫端湊起來才是完整的證據鏈：Reorder（`1000:0379`）、離隊（`0x14af2`）、
入隊（`0x15089`）、佈陣（`0xc615`）。**關鍵那一環是離隊** —— 它把編號比
離開者大的整批減一，這只有在「格子裡存的是隊伍陣列索引」時才需要做。
單看 Reorder 只知道有人在編這張表，看不出編的是什麼。

Reorder 本身也和直覺不同：**不是交換兩個人，是整張表重填** ——
先清光九格，再照隊伍順序一個一個問「你站哪」。已實作（紮營選單按 `R`）。

**佈陣那一段讀出來了但沒接**：原版以 64 寬緩衝的 (13,13) 為中心攤開 3×3
（`0x34d` = 13×64+13 就是證據），而本專案的戰場網格是 9×9。兩者沒對上之前
硬塞座標等於自己發明規則。下一輪該追 `0x4fbe`／`0x4fc0`／`0x4fd4` 那組
38 bytes 一筆的結構與 `docs/re/20` 的 `0x4eb4` 平行陣列是什麼關係。

### 這一輪（2026-07-26 續三）：開戰是一團混戰，不是兩軍對峙（`docs/re/35`）

上一輪說「佈陣讀出來了但沒接，因為座標對不上」。這一輪把擺位規則讀完並接上了。

- **隊伍照 3×3 陣型站在中央**，陣型格怎麼排就怎麼站。沒被放進陣型的人不上場。
- **怪物散在中心 ±2**（`rnd(5) − 3`），開場就在近身距離。
  改動前本專案是「我方一整行貼右邊、怪物一整行貼左邊」—— 那是憑空發明的隊形。
- **佔位表就是地圖本身**：擺好一個單位就把圖塊蓋進地圖，那一格從此不再等於
  「空地值」，下一隻自然擲不中。空地值 `[0x51d8]` 取樣自**隊伍腳下那一格**
  （`地圖[0x34d]`，0x34d = 13×64+13）—— 拿隊伍站的地方當可站的定義。

順帶補上 `docs/re/20` 沒寫的：**單位結構間距 38 bytes**，陣列起點 `0x4eb4`，
**玩家單位從槽位 7 開始**（`0x4fbe = 0x4eb4 + 38×7`，X/Y/精靈圖三個欄位位移
同時對上）。整張槽位圖：怪物 0–6、玩家 7–11、召喚 12–14。

**訂正一句舊斷言**：`battlefield.go` 說 9×9 是「戰場邊界，已從原版確認」。
視線遮蔽表確實是 9 寬，這沒錯；但原版的 9×9 是一個**跟著行動單位捲動的視窗**，
單位座標是 64 stride 緩衝的索引。戰場真正的邊界還沒解（`docs/re/35` §4 列了
三條互相不相容的觀察）。**在解開之前不要動 9×9 模型** —— 它自洽、測試釘得住，
而且相對幾何與原版一致。

**刻意沒套的一條規則**：落點必須等於空地值。本專案的戰鬥沒有地形阻擋
（`CanStep` 只查邊界與佔位），只在生成時擋、移動時不擋是自相矛盾的一半規則。
實測過套上去的後果：戰場地形從大地圖切下來，中心與鄰格的 tile 幾乎都不同，
怪物一隻也放不下，進戰鬥直接「怪物全滅」。

### 這一輪（2026-07-26 續四）：戰場是 15×15，每格是世界 tile 的五分之一（`docs/re/36`）

上一輪說「戰場邊界還沒解，別動 9×9 模型」。解開了，而且**9×9 從頭到尾就是錯的**。

原版開戰時做四件事：清 64×64 緩衝 → 在第 5 與第 21 列／欄畫一圈 17×17 的牆 →
把大地圖上隊伍周圍的 **3×3 個 tile**、每一個攤成 **5×5** → 拼成 15×15 貼進 (6,6)。
中心 `(5+21)/2 = 13`，佈陣放在 (13,13) 的道理就在這裡。

九個區塊的貼圖起點 `ds:0x0a0e` = `0186 018b 0190 02c6 02cb 02d0 0406 040b 0410`，
換算就是 (6,6)(11,6)(16,6)(6,11)… 一格不差。**手冊那句「戰場是該區域的放大地圖」
是字面意思：真的放大五倍。**

**錯在哪**：`battleterrain.go` 原本照著 `0x172f4` 寫 —— 那一段確實在填地形緩衝、
確實有光照內縮，每一句都對，錯的是「這塊緩衝就是戰場」這個沒驗證的連接。
真正的戰場建圖在別處，整段沒有任何光照內縮。找到看起來對的程式碼之後，
還要問「誰讀它」。

**能不能站只有一條判定**：`地圖[目標] == [0x51d8]`（空地值 = 隊伍腳下那一格）。
所以戰場的可走範圍就是與隊伍腳下同一種地形的那一片 —— 地形決定戰場形狀，
不需要可通行性表。這也順手把上一輪「刻意沒套」的擺位判定補上了。

本專案現在**沿用原版的絕對座標**（緩衝 22×22、牆在 5 與 21、可站 6–20、
中心 13,13），畫面是一個對準行動單位的 9×9 攝影機。

踩到的兩個坑寫在 `docs/re/36` §4：**`0` 是合法的世界 tile**（拿它當哨兵會讓
整片戰場畫成黑的）、**視線遮蔽不能用在擺位判定上**。

補一筆**自我訂正**：當時把另一個位置的黑畫面記成「tile 17／0 取不到圖」的缺口。
查了 —— **不是缺圖**：`DEMON.SHP` 的 tile 0、17、86、92 本來就是整格純黑，
每個像素都不透明。隊伍站在那種地形上開打，整片黑是忠實呈現。
`gfx.BlackTiles` 加了常數與測試釘住，免得日後有人去「修」解碼器。
看到黑畫面就假設「圖沒載到」是個容易犯的錯。

### 這一輪（2026-07-26 續五）：鑑定接上了，兩條獨立的線在這裡交會（`docs/re/37`）

紮營選單的 **Identify** 讀完並實作。規則本身不複雜：成功率 = **智力 × 4.5**，
每人每天一次，**失敗也算用掉一天**（`+0xed` 在擲骰之前就先設起來，
睡覺時與打獵的 `+0xef` 在同一個迴圈裡清掉）。

真正值得記的是**分類**那一段。判定查角色記錄的 `+0xde`／`+0xdf`／`+0xe0`，
技能旗標從 `+0xc8` 起算，所以是技能 22／23／24 —— 翻開技能表正好是
**Weapon lore／Potion lore／Item lore**，而且三個型別範圍
（0–12、14/24/25、13/15–23）把 0–25 切得一格不漏也不重疊。

**這是兩條獨立的線在同一點交會**：欄位位移從這段程式碼推、技能名稱從
`FILES.DTT` 的字串表切（`docs/re/27`）。技能表的切點錯了、或 `+0xc8` 的起點
錯了，這裡就會湊出三個風馬牛不相及的技能。

順帶：技能 26 是 **View land（觀地）**，那也正好是紮營選單的一項 —— 還沒讀。

紮營選單目前做完七項（睡覺、打獵、換裝、丟棄、轉手、陣型、鑑定），
還缺 Party／Worship／Xorcise／View land／Use／Cast。

### 這一輪（2026-07-26 續六）：觀地 —— 原版唯一的「看地圖」手段（`docs/re/38`）

紮營選單的 **View land** 讀完並實作。平常畫面上只有腳邊那 9×9，
爬上高處才能把視窗推開去看遠處。

三道門檻各有值得記的地方：

- **`char+0xe2` 是技能 26**，技能表第 26 項就叫 `View land` ——
  與 `docs/re/37` 的學識技能同一種交叉印證（位移從程式碼推、名稱從字串表切）。
- **`party+0xac` 是隊伍層級的每日旗標**。鑑定（`+0xed`）與打獵（`+0xef`）
  是每人各自一份，觀地卻是**整隊共用一次** —— 三個都由睡覺清掉。
  同一個「每日一次」概念，作用範圍不同。
- **`party[+0xa3] > 10` 才看得到**（地城與室內不行）。這個分界與
  `world.SubMapMinID`（= 11）完全一致，而那個常數是從 SUM.MAP 的結構推的。

狀態門檻 `< 2` 比鑑定的 `< 3` 嚴一格：中毒還能研究道具，但爬不上高處。

檢視本身用的 `FUN_222f_1404(中心 − 4, 中心 − 4)` **與戰場攝影機是同一支常式** ——
看地圖與看戰場共用同一個「捲動視窗」機制。

新增偵錯旗標 `-give-skill`：起始隊伍沒有人會觀地或學識，那幾個紮營選項在
headless 驗收時一步都走不到。

紮營選單做完八項，還缺 Party／Worship／Xorcise／Use／Cast。

### 這一輪（2026-07-26 續七）：點火把的機制，與一條假的「沒實作」（`docs/re/39`）

紮營選單的 **Use** 讀完。兩個收穫，第二個比第一個重要。

**一、點火把。** 型別 26 是火把、27 是提燈（`ITEMS.DAT` 型別名表對得上）。
用掉即消失，並把 `party+0xa7` 設成 **2（火把）／3（提燈）**。
`docs/re/26` §5 記過「本專案還沒有點火把這些會改動光源的機制」—— 機制就在這裡。
紮營重設成 1、火把 2、提燈 3，湊成一組完整的光源循環。

**二、訂正一條假的「沒實作」。** `internal/game/item.go` 原本寫著
「原版還有兩條過濾條件沒有實作……反組譯的 `0x11`/`0x12` 超出槽長，
應該是載入後的記憶體結構」。**錯了** —— 那兩個位移是相對
「角色記錄 + 槽×17」算的，而槽本身從 `+0x0c` 起，減掉就是 `+0x05`／`+0x06`
（次數上限與已用）。所謂沒實作的兩條就是 `強度 != 0` 與 `上限 != 已用`，
`InventorySlot.Usable()` 早就實作了。

> 位移是相對誰算的，要跟著它的基底走。少了那一步，一個已經解出來的欄位
> 會被誤判成「還沒定位到的記憶體結構」，然後在文件裡留一條永遠不會有人
> 去查的假缺口。這是 `rulebook/63` 的另一面：**標記寫下的當下就可能是錯的。**

Use 只實作了光源那一條 —— 有效果的道具要走施法路徑，而本專案的施法目前
離不開 `Battle`。選單上照實標「戰鬥中才能用」。

紮營選單做完九項，還缺 Party／Worship／Xorcise／Cast。

### 這一輪（2026-07-26 續八）：Party 其實是「檢視角色」（`docs/re/40`）

選單上叫 `Party`，字串卻是 **`Inspect character:`** —— 這一項是攤開**一個人**的
角色卡，不是隊伍總覽。世界地圖上那個 `P`（隊伍名冊）是本作自己加的摘要，
原版沒有。兩者現在並存：一個看全隊、一個看單人。

`sel < 0` 那條分支比較有意思：先印一行 `"Hour %d, Day %d in the Month of the %s"`，
再印 `"%s followed in the footsteps of"`，然後把角色卡送去 `[0x514a]` 指定的地方
（預設 2）。**看起來是「把角色卡印到印表機」**，`0xffff` 那條是全隊都印。
**不實作** —— 就算解出來也沒有印表機可接。

角色卡的卡面是本專案自己排的（原版版面在 `FUN_278d_2f61` 裡，沒讀），
資料全部來自已解出的欄位：五項屬性、生命法力、武器護甲、經驗與門檻、
信仰與祈禱率、束縛等級、已學技能。驗收畫面上 Menhir 是人類遊俠 3 級、
技能「劍術／狩獵」—— 與 `docs/re/26` §4b 記的「只有遊俠 Menhir 有狩獵旗標」對得上。

紮營選單做完十項，還缺 Worship／Xorcise／Cast。

### 這一輪（2026-07-26 續九）：驅邪 —— 順帶解開「詛咒到底做了什麼」（`docs/re/41`）

紮營選單的 **Xorcise** 不是戰鬥裡的「驅散不死」，是**把身上被詛咒的裝備弄下來**。
只能對「正穿戴著」的東西驅 —— 這條限制本身就在說：詛咒是穿上去才發作的。

三個收穫，都比選項本身大：

**一、`+0x0d` 是驅邪成功率。** 道具槽那個 byte 一直躺在「語意未解」那一排，
這裡是它唯一的讀取端（`rnd(100) > 它` 就失敗，**越大越好驅**）。
起始存檔十件裝備全是 0 —— 沒有詛咒物，一件也驅不動。

**二、技能旗標不是布林。** 有第三種值：**2 = 學過但被詛咒封住**。
驅邪成功把所有 2 清成 **0 不是 1** —— 技能沒還回來，只是不再佔位置。
**這條差點被存檔悄悄吃掉**：本專案原本讀成 `on == 1` 的布林、寫回只寫 0／1，
值 2 存一次檔就變 0，詛咒自動解除。已改成 `Skills` + `CursedSkills` 兩個陣列。

**三、四對屬性欄位的第二次獨立確認。** 驅邪成功會做
`+0xf7←+0xf3`、`+0xf8←+0xe8`、`+0xfb←+0xe9`、`+0xfe←+0xea`，
換算成相對 `expOffset` 就是速度／力量／技巧／法力上限的
「含加成 ← 天生」四對，**一個不差**。那幾對原本是靠欄位間距與數值比對推的，
這裡是完全獨立的第二個來源。順帶也說清楚詛咒的第二種效果：壓低那四項。

沒實作的是四個含加成欄位的還原 —— 本專案的 `game.Character` 只帶天生值，
補這一條要先把「裝備加成」整套做出來，不是驅邪這一項的事。

紮營選單做完十一項，還缺 Worship／Cast。

### 這一輪（2026-07-26 續十）：把「施法離不開戰場」這個前提拆掉（`docs/re/42`）

紮營選單剩下的兩項卡在同一個地方：本專案的 `applySpell` 要 `battle.Kill`、
要 `battle.UnitAt`，沒有 `*Battle` 就跑不起來。**那是本專案自己造出來的限制** ——
原版的施法入口 `FUN_1000_11e5` 戰鬥內外是同一支，效果記錄、力度公式、判定全部共用。

拆的標準不是「哪些好做」，是**哪些在戰鬥外有意義**：生命／法力／解束縛／枯萎
作用在持久欄位，離得開；技巧／力量／速度／護甲增減改的是**戰鬥單位的暫時值**，
戰鬥一結束就沒了，在營地放等於什麼都沒做 —— 擋下來並說明理由，
不要讓玩家花法力買空氣。範圍傷害與即死要戰場網格，離不開。

作法是借一個戰鬥單位當計算載體，算完只把 HP／SP／狀態寫回角色 ——
`writeBackFromUnit` 刻意不碰那四項屬性，正是上面那條理由的程式碼版本。

**Cast 接上了**（紮營按 `C`，四步：施術者 → 法術 → 投入法力 → 目標）。
對自己人放攻擊法術是可以的，原版也沒攔。

**Worship 的規則也順帶讀完了**（`docs/re/42` §4），只差 `[0x4cc4]` 那張
「神祇 → 法術 id」表沒定位。三件事先記著：力度**固定 300**（一般施法是個位到
幾十，神祇出手是壓倒性的）、成功率與神殿祈禱共用 `+0xeb` 且同樣 −5、
表裡 −1 = 不賜法術、−2 = 隨機挑 27–37。

紮營選單做完十二項，只剩 Worship。

### 這一輪（2026-07-26 續十一）：紮營選單十四項全部接上（`docs/re/43`）

最後一項 **Worship** 卡在一張表：`[0x4cc4]` 那 11 個 word 在哪裡。
它是執行期指標，檔案裡找不到 —— 但它在資源競技場的**指標鏈**上，
而每一段的長度都是已知資料結構的大小：

	[0x4e28] → [0x5300]  = 0x1ae = 43 法術 × 10
	[0x5300] → [0x4cc4]  = 0x1a4 = 30 道具 × 14
	[0x4cc4] → [0x51d4]  = 0x16  = 11 word

三段全部命中。起點反推 `[0x5510] = 0x45e − 0x3c = 0x422`，正好是種族上限表
（`offRaceMax`，早就驗證過）。於是表的位置是 `FILES.DAT + 0x7ae`，
翻開來是 `0 37 23 19 34 12 1 13 37 34 −1` —— 十個合法法術 id 加一個 −1。
**`docs/re/29` 記過第 11 位神祇特殊，現在知道特殊在哪：祂不賜法術。**

> **用長度反推位置。** 指標查不到，但每一段的長度都是已知的，
> 鏈條一節一節扣下來，位置就是唯一解。不必動態除錯。

敬拜的規則：力度**固定 300**（實測打出 24 點傷害，一般施法是個位數量級）、
成功率與神殿祈禱共用 `+0xeb` 且同樣 −5、資料裡沒有任何神祇用 `−2`（隨機）那條路。

**紮營選單十四項到此全部接上**，對照表見 `docs/re/43` §4。
沒有一項是空殼 —— 各項目還沒解乾淨的地方都寫在各自的文件裡。

### 這一輪（2026-07-26 續十二）：軟浮點的真相，與估價公式的第一項（`docs/re/44`）

商隊售價是本專案最老的一筆未解帳。這一輪沒解完，但**解掉了卡住它的誤解**。

**軟浮點的常數在「程式段」，不是資料段。** 這一條我錯了兩層：先假設
`mov bx, imm` + `lcall 210e:0196` 的 `bx` 指向 `DS` 的浮點常數（結果
`ds:0x40e0` 是 `"tistics"`、`ds:0x35f2` 是 `"Dormant: "`），
然後下了第二個錯結論「常數是 `push` 推上堆疊的、`bx` 不是指標」。

真相：`210e:0196` **確實**是「把 `[bx]` 的常數載入暫存器 B」，
**只是 `bx` 相對的是 `CS`**。改用呼叫端自己的段基底去讀，三個位址立刻乾淨：
`278d:35ea` = **0.6**、`278d:35f2` = **0.8**、`1990:40e0` = **2.6**。
（`push` 推常數的形式也存在，但那是給 Turbo C 的 `long × double` 輔助函式用的。）

> **答案早就寫在我們自己的文件裡** —— `docs/re/14` §1 明明白白寫著
> 「常數放在程式段、由 `FUN_310e_0196` 這類函式讀取」，連範例位址都有。
> 撞牆時先翻自己的筆記，比再讀一次反組譯便宜得多。
> 這是 `rulebook/63` 的鄰居：不是標記過期，是**根本沒去查**。

**估價常式是 `278d:1c1b`（檔位移 0x1D0EB）**，公式第一項已解乾淨：

	價 = ITEMS.DAT 底價 × 材質倍率表[槽 +0x0f]

倍率表在 `ds:0x18b3`，九個 byte = `0 1 2 5 20 35 50 60 75`。
於是 **`+0x0f` 有了名字：材質／品質類別** —— 它同時決定名稱前綴
（`docs/re/30`）與價格倍率，同一把匕首類別 8 就是 **75 倍**價。
兩份原版存檔每一件實物都是類別 1（×1），與「起始裝備都是平凡貨」一致。

順帶訂正 `docs/re/32`：那裡說商隊的貨「未鑑定」。列貨時 `槽[+0x10]` 被寫成 1，
**擺出來給你看的時候是已鑑定的**；生成當下確實沒鑑定。

**第二項也解乾淨了**：`(槽+0x02 + 槽+0x04) × 225 × 1.2`，而
**225 × 1.2 = 270 剛好是整數** —— 浮點只是編譯器把 `* 1.2` 直譯的結果。
`ItemValue` 現在算前兩項並回報 `exact`：強度為 0 的道具算得出確切售價。

第三項（強度加價）還沒解，所以**商隊購買介面仍未做**，理由變得很具體：
商隊的貨是掉寶生成器生的、多數帶強度，也就是多數落在 `exact = false` 那一邊。
與其標一個算不準的價錢，不如等第三項讀完。

### 這一輪（2026-07-26 續十三）：商隊售價 = 估價 × [0.6, 1.4)（`docs/re/45`）

上一輪停在「後面那段浮點看不懂」。這一輪把 `310e` 那幾個 entry point **逐一點名**
就通了 —— 靠的是 `docs/re/14` 早就記下的兩個指標：
**`[0x482e]` 是暫存器 A、`[0x4830]` 是暫存器 B**。有了它們，函式開頭幾行自己招供：

| entry | 是什麼 |
|---|---|
| `021e` | 在堆疊上開 8 bytes，**把 A 寄放進去** |
| `0242` | 把那 8 bytes **載回 B** 並釋放 |
| `02e4` | **A ± B**（對階再加減）|
| `0732` | **AX（16-bit）→ A** |

`021e`／`0242` 是一對：算下一個運算元會蓋掉 A，所以先寄放、算完再撈回 B。

於是商隊的開價是：

	售價 = trunc( 估價 × (uniform() × 0.8 + 0.6) )

**`1d9f:0dd4` 是均勻亂數** —— 又是 `docs/re/14` 早就寫好的（LCG 推進後除以
2796203.0，留在暫存器 A）。**`02e4` 是加不是減**，不必再讀組合語言就能定：
減的話係數值域是 `[−0.6, 0.2)`，會出現負數售價。

所以**同一件東西在不同商隊手上開價差可以到兩倍多**（0.6–1.4 倍）。
商隊值得多看幾家，不是議價技巧，是純運氣。

實作：`rng.RNG.Uniform()` 與 `game.MerchantPrice`。**刻意用 float64 不用有理數** ——
`Roll` 能改寫成整數是因為 `docs/re/14` 證明了那個值域內 `floor()` 不會跨界；
這裡量級大得多，用同一組 IEEE double、同一個順序才逐位元對得上。

購買介面還差**估價的第三項**（強度加價）。缺口很窄了。

### 這一輪（2026-07-26 續十四）：估價的第三項讀完，商隊開價算得出來了（`docs/re/46`）

強度加價有三條分支，全部是整數：

	無限次數（+0x06 == 0xff）  ：trunc(5 × 強度² × 上限 × 0.9)
	上限 > 100                 ：(500 × 強度²) ÷ (上限 − 100)   有號 32-bit 除法
	其餘                       ：trunc(上限 × 強度 × 1.07 × 200) = × 214

**又是「浮點常數乘出整數」的同一個套路**：`225 × 1.2 = 270`（第二項）、
`1.07 × 200 = 214`（這裡）。原始碼裡寫了個小數，編譯器老實地叫浮點庫去乘。

**`> 100` 那條分界不是隨便切的**：那條要除以 `上限 − 100`，
**分界正好把除數 0 與負數擋在外面**。守衛與除數一組一組對得起來 ——
這是判讀正確的旁證：只要把比較讀成 `>=` 或把 `0xff9c` 讀錯，關係立刻崩掉。
`3016:000a` 是有號 32-bit 除法（查 Ghidra 反編就看得出來，不必猜 lmul／ldiv）。

還缺第四項：`槽[+0x0a]`（特效值 A 的原始 byte）不為 0 時，有一段以 **1.4**
為底的加價沒讀乾淨。所以 `ItemValue` 的 `exact` 條件是 `+0x0a == 0`。

**`+0x0a` 為 0 的道具現在算得出完整的商隊開價**：

	開價 = trunc( ItemValue(底價, 槽) × (uniform() × 0.8 + 0.6) )

購買介面在下一輪補上（`docs/re/47`）。

### 這一輪（2026-07-26 續十五）：商隊購買介面，與價格照出來的破洞（`docs/re/47`）

畫面本身沒什麼好記的（招呼／貨單／買）。值得寫下來的是**接上去才浮出來的事**：

**七件貨的開價一律是 0。** 估價的第一項是 `底價 × 材質倍率`，
而倍率表的類別 0 就是 0；`GenerateLoot` 從來沒填過 `+0x0f`（材質類別），
Go 的零值剛好落在那一格。不是估價公式錯，**是掉寶生成器少填一個欄位**。

這個破洞在「只生貨、不標價」的時候完全看不出來 —— `docs/re/32`
那一輪的測試釘的是附魔、效果、詛咒，**沒有一項的答案取決於 `+0x0f`**。
價格是第一個讀這個欄位的消費者，一接上就照出來了（`rulebook/60`：
能區分對錯的訊號才有用）。

暫時填 1（兩份原版存檔裡每一件實物都是 1，倍率 1 = 原價，保守的下界），
真規則 `1990:11b9`–`1228` 仍未讀，註解寫在 `loot.go` 原地而不是 TODO 清單裡。

**（當時）價格量級一個都沒跟原版對過。** 等級 12 的商隊開出匕首 2 到魔杖 26803，
跨四個數量級。倍率到 75 倍加上強度的平方項，這不必然錯，但在 DOSBox
比對之前只能算「公式跑得動」，不能算「公式對了」（`rulebook/65`）。
而且要對照得先解 `+0x0f`，否則對不上也分不清是誰錯。

介面的判斷：**算不準的貨不賣**（`exact = false` 顯示「說不出價」）——
湊個數字比較完整，但那是把「還沒讀懂」偽裝成「這東西就值這麼多」。
進入鍵 `M` 是偵錯用的，遭遇觸發（`docs/re/32` §4）仍未追。

### 這一輪（2026-07-26 續十六）：一個 `+1` 讓掉寶生成器五處判讀集體偏一格（`docs/re/48`）

去補 `docs/re/47` 留下的材質類別（`+0x0f`）規則，開頭三行就把 `docs/re/30` 掀了：

	e6b3  es:[bx] = al        ; 型別寫進槽 +0x00
	e6b6  inc word [bp-0x4]   ; ← 從這裡開始，每一次型別比較都是「型別 + 1」

`docs/re/30` 把那些常數（8、15、25、26）當 0-based 讀，**五處全部偏一格**：
布甲原本沒被減半、「戒指與火把固定 200 次」其實是**兩種藥瓶**、
王冠以上**根本沒有附魔**（畫面上「寶石 +4」就是這個 bug）、
詛咒只有武器護甲會中。

`+1` 不是猜的：`1990:1315` 讀道具資料寫的是 `es:[bx + si − 0xa]`，
`si = 14 × (型別+1)`，兩個一減正好是記錄裡的第 5 個 byte ——
**0-based 的話型別 0 會指到上一筆記錄外面去**。那個負偏移就是收據。

其餘六件：

- **材質類別**：六個型別（布甲、皮甲、藥瓶、寶石、藥膏、藥瓶）永遠是 1，
  其餘 `rnd(等級+10)` 分四段。名稱表 `ds:0x1852` 存的是
  Silver／Jade／Gold 這類**材質前綴** —— 與估價的倍率表是同一個索引的兩張平行表。
  等級 10 以上會讀出表外的 bytes，本專案鉗在 8。
- **型別重擲是價格上限不是下限**（`docs/re/30` §2.0 方向反了）。
  `f(等級) = 2.6^等級`（`1990:0a83` 是整數次方，底數在 CS:0x40e0）。
  反證是量級：最貴的寶石 500，`2.6^8 ≈ 2088` —— 當下限的話 8 級以上選不出東西。
  正確說法是**低等怪掉不出貴東西**（1 級只到 27，7 級全解鎖）。
- **商隊那條路一件詛咒品都生不出來**（`docs/re/32` 讀錯了）。
  `rnd(120) − 80` 不是詛咒機率，擲中的貨是**跳過第一道效果門檻**。
  真正會把東西變壞的只有掉落模式的 `rnd(10) == 10`。
- **兩組常駐效果 `+0x09`–`+0x0c`**（武器與護甲）：17–20 分別是
  MaxSP／速度／力量／技巧，21 是武器戰鬥特效且對護甲禁止。
  2026-07-29 由 `sub_11CBF` 更正舊說（`docs/re/109`）。
- **兩組附帶法術 `+0x01`–`+0x04`**（只有武器）：估價的已鑑定加價
  `(槽[+0x02] + 槽[+0x04]) × 270` 就是這兩個強度相加。
  那兩個「兩份存檔裡都是 0、看不出語意」的 byte 有名字了。
- **驅邪成功率 `+0x0d`** 只寫在模式 0 且真的變壞的詛咒品上，`51 − rnd(5×等級)`。

道具槽 17 個 byte 到這裡**全部有名字**。`loot.go` 整支重寫，
`GenerateLoot(…, cursed bool)` 換成 `GenerateDrop` 與 `GenerateWare` ——
詛咒本來就不是呼叫端能指定的，那個參數從一開始就是錯的抽象。

教訓：**「這個常數對應哪個東西」比「這個常數是幾」難得多。**
`cmp [bp-0x4], 0x19` 本身沒有歧義，錯的是那個變數三行前被 `inc` 過。
防呆靠的不是更小心，是找一個**對不上就會爆的旁證**（這裡是那個 `−0xa`）。
會抓到它，是因為 `docs/re/47` 把價格接上畫面之後，「寶石 +4」看得見了。

### 這一輪（2026-07-26 續十七）：估價六項全解（`docs/re/49`）

`docs/re/48` 讀出 `+0x0a`／`+0x0c` 是掉寶生成器寫的特效級數之後，
卡了兩輪的第四項一看就通，而且後面還藏著第五、第六項：

	第四／五項：槽[+0x0a]／[+0x0c] != 0 → trunc(1.4 × |n| × 250) × sign(n)，n = 該 byte − 10
	第六項    ：型別 <= 12 → trunc(1.7 × |附魔| × (型別 <= 7 ? 350 : 700))
	鉗制      ：槽[+0x0d] != 0（詛咒品）→ 0；算出負數 → 0

兩個卡住的 helper 一讀就完：`206a:000a` 是絕對值、`206a:0020` 是正負號。
先取絕對值算完再乘回符號，是為了讓截斷**朝零**。

**掉落生的詛咒品一文不值** —— `+0x0d` 只有那條路會寫，這一鉗剛好只打中它們。
第六項取絕對值之後**沒有補回符號**，所以負附魔在那一項照樣加分（照抄，
沒有旁證說它是 bug）。

⚠ **「小數乘出整數」這次不能用。** `1.4 × 250 = 350` 看起來跟
`225 × 1.2 = 270`、`1.07 × 200 = 214` 是同一個套路，但**乘法順序不同**：

	第二項：(a+b) × 225 先乘整數 → 再 × 1.2   → 誤差被吸收，捷徑成立
	第四項：1.4 × |n|   先乘小數 → 再 × 250   → 誤差放大 250 倍，n=3 時算出 1049 不是 1050

教訓：**「這個小數乘出整數」是關於算式的斷言，不是關於常數的。**
第二、三項留整數是跑了 `n = 1..600` 窮舉比對才敢。

六項全解，`ItemValue` 的 `exact` 與商隊介面的「說不出價」一起拿掉。
實測一支 12 級商隊開價落在 5–4353（`64-merchant-full-price.png`）。
**仍未與原版數字對照** —— 那要 DOSBox。

### 這一輪（2026-07-26 續十八）：商隊是事件動作，不是隨機遭遇（`docs/re/50`）

`docs/re/32` §4 最後一個缺口。做法是**從函式位址回推呼叫端**：
商隊常式在 `278d:1fd1`，遠呼叫的機器碼 `9a d1 1f 8d 17` 全檔只有兩個，
其中一個是五行的包裝函式；再回推誰指到那個包裝，也只有一處 ——
落在一整排 `call FUN_222f_xxxx / [bp-0x14] = ax / jmp 結束` 中間，
**那是事件動作的分派表**。所以 `M` 鍵替代的不是隨機遭遇，是事件。

**規模與等級是同一個區域變數**（`0x1d564` 算完規模、`0x1d6ad` 原封不動
當等級傳給掉寶生成器）。所以「衣衫襤褸的商隊」賣的一定也差 ——
同一個數字的兩種呈現。公式：

	規模 = clamp(基準 + rnd(3) − 2, ≤ 9)

上限 9 順帶解掉一個隱憂：材質類別公式在等級 10 以上會讀出表外的 bytes
（`docs/re/48` §1），**商隊永遠碰不到那條線**。

基準來自 `ds:0x5c60`（BSS），進地圖時填：地城用存檔 `+0xaf`、
戶外改用地圖記錄自帶的參數（那筆記錄沒認出來）。

⚠ **矛盾照實記著**：存檔 `+0xaf` 是 1，擲出來三分之一是等級 0，
而 `LootPriceCap(0) = 1` 連匕首（2）都選不出來 —— 原版的型別重擲迴圈
在那裡不會結束。三種可能（只發生在戶外／`+0xbe` 閘門擋掉／原版真的會卡）
沒有證據裁決，所以不猜；本專案的 `LootItemTypeFor` 用光次數退回匕首，
症狀（清一色匕首）留在畫面上看得見。偵錯旗標 `-merchant-base N` 是為此加的。

**順帶把事件動作分派表整張挖出來（`docs/re/50` §1.1）。** Turbo C 把 switch
的分派放在**函式最後面**，往前找沒有用；掃 `jmp word ptr cs:[bx+d16]` 的
指令碼 `2e ff a7` 直接命中。`sub ax,6 / cmp ax,0x15` →
**動作碼值域 6–26（21 個）**，跳表在 `222f:0563`。商隊是 **`0x17`**；
另外三個靠字串認出來：`0x0d`「It's too crowded here」、
`0x19`「Your crew refuses to sail any further…」（航到世界邊緣）、
`0x1a`「The sun is rising」（天亮）。
`docs/re/05` 卡住的「誰在什麼時候用什麼參數重新呼叫」現在有骨架可以對。

### 這一輪（2026-07-26 續十九）：那道 1/64 不是隨機戰鬥，是商隊（`docs/re/51`）

`docs/re/50` 挖出分派表之後，`case 0x17` 是商隊；而 `docs/re/04` §2.2
早就寫過 `0x17` 是「地城裡 1/64 的隨機遭遇戰鬥」。同一個碼兩個結論，
這一輪裁決掉 —— **而且發現本專案照著錯的那個實作了**。

三段接起來，中間沒有任何一步會把碼轉成別的東西：

	FUN_222f_0763  戶外(隊伍[+0xa3] >= 9) 且 ds:0x52d4 == 0 → 1/64 → 回傳 0x17
	FUN_222f_0b0e  結果 != 0 → 原封不動 retf（連碰都沒碰）
	外層主迴圈      switch(結果 − 6) → case 17 → 商隊

`docs/re/04` 當時卡在「分派邏輯落在 `FUN_222f_0b0e` 內被 decompiler
弄壞的那段 switch 裡」—— **分派根本不在 `0b0e` 裡面，在它的呼叫端**。

順帶訂正兩處：閘門是 `>= 9` 不是 `> 9`；而編號 10 以上才是戶外，
所以這一擲是**戶外**才有，「地城裡」講反了。這剛好回答 `docs/re/50` §4
掛著的矛盾 —— 商隊只在戶外，戶外的規模基準來自地圖記錄，
存檔 `+0xaf` = 1 那條退化路徑走不到。

**隨機戰鬥掛在 `case 0x16`**（`222f:2a5b`），觸發是倒數計時器
`隊伍[+0x9c]` 歸零，不是機率。六個寫入點全部讀完（都是 `rnd(n) + k`）：
**新遊戲 15–19、打完一場之後 28–77、觸發警報（"You set off an alarm!"）1–5**，
另外三個脈絡沒讀完就不實作。旁證：兩份原版存檔的 `+0x9c` 是 56 與 34，
**都落在 28–77 裡** —— 這個 byte 若是別的東西沒有理由剛好掉在這一段。

節奏差很多：1/64 的期望是 64 步一次而且沒有下限（可能連兩步撞到），
倒數是「保證至少 28 步、最多 77 步」。1/64 改接商隊。

教訓兩條：**「decompiler 弄壞了」不等於「讀不到」**（掃
`jmp word ptr cs:[bx+d16]` 的指令碼 `2e ff a7` 就直接找到跳表）；
**越像常識的推論越要標成推論** —— `docs/re/04` 誠實寫了「沒有追完整」，
但那句話講的是「怎麼接到戰鬥初始化」，已經預設了目的地。
**未解的是連接，不是目的地**，這種寫法會讓下一輪把目的地當成已知。

### 這一輪（2026-07-26 續二十）：噴吐動畫（`docs/re/23` §8）

M4 掛了很久的最後一項。`0x1a59` 那一行讀出來是
`draw(x + 5, y + 5, ds:0x4e2c)` —— 逐格畫，圖塊由**法術記錄的 school**
決定，`+5` 是 22 寬緩衝的牆偏移。關鍵是**畫在地形檢查之前**：
整個錐形都會亮，不是只亮打得到的格子。

掃描順序（先沿方向推進、每層再從側偏 −along 掃到 +along）決定了
動畫是**從嘴邊往外擴**。`game.BreathCone` 照這個順序輸出格子，
`cmd/demonwinter` 的 `breathAnim` 逐格點亮且**不擋輸入**。

圖塊來源沒解，先用單色方塊；每格延遲也是自己挑的（原版那個迴圈裡
沒有延遲，速度取決於當年的機器），這兩點都寫在註解裡。

驗收方式值得記一筆：**動畫只有 0.8 秒，headless 截圖抓不到**。
把每格延遲暫時調成 1 秒截一張（`66-breath-cone-slowed.png`）再調回來 ——
要眼睛驗的是「方塊有沒有畫在對的格子上」，時間常數是另一件事，
分開驗比想辦法卡在對的 frame 上可靠。

### 這一輪（2026-07-26 續二十一）：市集與商隊是同一支常式（`docs/re/52`）

`docs/re/50` 發現商隊常式開頭分岔，`mode == 0` 是別的東西 ——
讀出來是**市集（Marketplace）**，而商隊（Merchants）是 `mode == 1`。
兩個畫面共用一支常式、一張 14 個 case 的分派表。

**商隊有七個選項，本專案只做了兩個**：

	mode 0 市集  Purchase Sell Continue Back Haggle Identify 五個角色槽 Quit（12）
	mode 1 商隊  Purchase View-mind Continue Back Haggle Inspect Quit（7）

選項數有**三個獨立來源互相驗**：熱鍵字串長度（`PVCBHIQ`）、
遠指標表長度、`[bp-0x34]` 的 `12 − 5`。任一個讀錯三者就對不起來 ——
這種冗餘交叉驗證成本幾乎是零，比「反編譯看起來是這樣」硬得多。

**Haggle 兩個模式共用同一段程式**（case 4），所以市集議價與商隊議價
是同一條規則。三條分支解出來了：

	rnd(100) < 10 × 計數器  → "You have insulted the merchant"，那件貨報廢（計數器 = 1000）
	rnd(100) < 15 × 計數器  → "The merchant stands firm"，沒降價
	否則                    → 計數器++，成功

第一次議價一定成功（計數器 0）；成功一次之後翻臉 10%／僵持 15%，
再一次 20%／30%，第五次之後翻臉機率就到 50%。降價是**顯示時重算**：
`要價 = max(2, 開價 − trunc(開價 × n × 0.06))`，每成功一次打掉 6%。

⚠ **那條算式早就在 `internal/game/economy.go` 裡了**（市集那邊 `docs/re/19`
就解完了，連 `> 99 → −100` 與下限 2 都在）。本輪一度寫成「還沒讀」——
**沒查自己的實作**。同一個坑 `docs/re/45` 踩過一次（`1d9f:0dd4` 的身分
`docs/re/14` 早就寫好）。動手前先 grep 自己的程式碼與文件，
成本比重讀組合語言低一個數量級。

議價因此**接進商隊畫面了**（`H` 鍵），差別只有初值：商隊一律從 0 開始，
不吃市集那個說服技能的加成。

**Inspect 也接上了**（`I` 鍵）。名字容易讀錯 —— 它跟 Purchase／Haggle
並列，直覺是「看這件貨」，實際是 `"Inspect character:"`，選一名隊員之後
呼叫 `278d:2f61`，**那就是紮營選單 Party 的同一張角色卡**（`docs/re/40`）。
本專案把 `partyScreen` 從「掛在 camp 底下」解耦成呼叫端各自持有，兩邊共用。
（早一點讀那個字串就不會猜錯。）

`docs/re/47` 的「購買介面做好了」現在是真的做好了（見下一輪）。

### 這一輪（2026-07-26 續二十二）：View mind，與那個追了三篇的旗標（`docs/re/53`）

商隊七個選項的最後一個。讀完順便解掉一個身世很長的旗標：

	docs/re/32   rnd(120) − 80 是「詛咒機率」                    ← 錯
	docs/re/48   不是詛咒；生成器裡只做一件事：跳過第一道效果門檻   ← 對，但用途未解
	docs/re/53   商隊把它逐件記著，唯一的讀取端是 View mind
	             → 旗標的意思是「這一件商人在說謊」

`0x1dfa8` 讀的正是 `0x1d6c4` 寫的那個陣列 —— **寫入端與讀取端對上，
這條線才閉合**。兩次翻掉自己的結論（「詛咒機率」、「1/64 是隨機戰鬥」）
都是靠找讀取端：光看寫入端只知道「寫了什麼」，不知道「那是什麼意思」。

View mind 本身：**一支商隊只能用一次**，逐件檢查，說謊的貨**各有 2/3 機率**
被揭穿，議價計數器寫成 1001、貨單那一格的價格改印 ` LIE `。
（1000 是翻臉的 `Inslt`，兩個狀態共用同一個欄位。）

被揭穿的貨還買不買得到**沒讀**。本專案標出來、不賣 —— 原版那一欄
已經被字取代，畫面上根本沒有數字可以照著付。

**加了偵錯旗標 `-merchant-lies N`。** 三層機率疊起來（過半商隊擲到 0、
每件貨再各自中、揭穿又只有 2/3），試了十個 seed 都是「你讀不出什麼」——
**驗不到的東西等於沒驗**，與其賭運氣不如加一個標明是偵錯的入口。

**商隊畫面到此七個選項全部有著落**，從 `docs/re/32` 走了七篇。

### 這一輪（2026-07-26 續二十三）：第一次真的拿原版對答案（`docs/re/54`）

`docs/re/01` 建了 DOSBox 參考環境、寫明它是「裁判」，
**但在這一輪之前沒有一次真的拿它對過**。三件事對上了：

- **狀態列的數字**：金幣 65、糧食 8、五名隊員的 HP／SP 與存檔解碼逐項相同。
  存檔往返早就 byte-for-byte 驗過，但那證明的是「讀進去再寫出來沒變」；
  這一張證明的是「**讀出來的意思跟原版顯示的一樣**」——兩件不同的事。
- **紮營選單 14 項**、順序完全一致（`docs/re/33` 的表、本專案的熱鍵）。
  整個紮營子系統第一次有原版畫面背書。
- **`Inspect character:`**：紮營的 Party 印的提示，與商隊 Inspect
  （`ds:0x3901`）是同一句。`docs/re/52` §3.1 從組合語言推出「同一張卡」，
  現在原版自己在畫面上印了同一句話。

順帶發現原版 Party 頁有 `Print`（送印表機），本專案不做 ——
記下來是為了讓「14 項對上、這一項沒做」是有意識的決定而不是漏掉。

**而且對出了第一個 bug**：角色卡第二頁原版列三項技能
（Detect aura／Fire runes／Spirit runes），本專案只列兩項。
查存檔，Wopple 的技能旗標只有兩格是 1 —— `Detect aura` 不在那個陣列裡，
是**種族天生能力**（精靈），走 `gamedata.RaceBonusSkill`。
**那支函式早就解出來了，卻從來沒有被呼叫過。** 解碼做完、沒接上去，
函式在、測試也綠，**只有兩個畫面並排才看得出來**。

順帶挖到名字的來源：四個天生能力的字串不在 `FILES.DTT` 的技能表裡，
在 `ds:0x18fc` 的四筆遠指標表（且排序與本專案的偽 id 不同）。
已補 `RACESKILL` 譯名目錄，修完角色卡列出三項。

**（當時）價格量級還是沒對到**，原因很具體：起始存檔在地城，市集／商隊／掉寶
四項全部需要導航，而 DOSBox 這端只能送鍵、不能查詢現在在哪。
下一輪的做法寫在 `docs/re/54` §4：**不要用鍵盤走路，改存檔** ——
`workplace/dosbox/game/` 是可寫副本，直接把座標寫成城鎮門口、
遭遇倒數設成 1，**兩邊吃同一份存檔**再比。隨機的部分只能對分布，
不能拿一次抽樣當反證。

### 這一輪（2026-07-26 續二十四）：攻略才是更便宜的價格 oracle（`docs/re/54` §7）

用改存檔的方式把隊伍放到 Seaside（子地圖 34），DOSBox 那邊順利進城、
進市集 —— 路是通的。但使用者問了一句「我以為價目表找攻略補齊就好了」，
**對，而且更便宜**：`docs/walkthrough/part-5`／`part-6` 本來就有
完整的材質附魔費用表與**具體道具的實際售價**（地點／材質／附魔／效果／價格）。

用之前要先拆開：那些數字是「說服殺價後的最低價 + 附魔費」，
不是 `ItemValue` 的直接輸出；而且材質折扣是**線性每級 555**，
與估價的材質倍率 `{0,1,2,5,20,35,50,60,75}` 是兩條不同的曲線。
要拿單一數字去對，得先把「標價 → 物價係數 → 議價 → 顯示價」補完。

**順帶照出一個沒實作的服務：附魔。** 攻略用一整章講它，而本專案的
城鎮設施只有八項，`TownFacilities` 沒有欄位，市集 12 個選項裡也沒有 ——
**它在原版的哪裡還不知道**，新的待查項。

⚠ **這一輪第三次踩同一個坑**：`docs/re/45`（`1d9f:0dd4` 的身分 `docs/re/14`
早就寫好）、`docs/re/52`（議價幅度 `economy.go` 早就實作）、
這裡（價目表攻略裡有；四個種族天生能力的譯名 `translations/glossary.md`
早就定好，我還是手寫了一個「竊取法力」，與表上的「汲取法力」不符，已改正）。
三次都是**沒查手上已經有的東西就去挖新的**。這個專案累積的資料已經大到
「先查」永遠比「先挖」划算。

### 這一輪（2026-07-26 續二十五）：附魔費用從攻略反推出來（`docs/re/55`）

使用者說「反推就能得到價格」—— 對，而且不必等整條價格鏈補完。
攻略 `part-5` 的價目表是**八種材質 × 十級 = 80 個資料點**，夠直接解方程：

	累積附魔費用  F(n, c) = 35 × (20 − c) × n^1.7      c = 材質類別（1 = 一般）
	每一級的費用  cost(n, c) = F(n, c) − F(n−1, c)

**80 個點誤差沒有一個超過 6**（正是攻略自己註明的捨入雜訊 —— 它的
「總計」與「0→10」兩欄本來就差 2–3）。

方法上的收穫是**先分維再反推**：逐級費用兩維混在一起看不出規律；
固定等級只看材質 → 線性，一眼看出 555 與 19 分母；固定材質改看
**累積**而不是逐級 → 指數立刻掉出來（`F(10)/F(1) = 50.11 = 10^1.7`，
逐點回推收斂到 1.7000）。**逐級值是差分，差分會把冪次結構打散**，
先積回去問題就從「擬合十個沒規律的數」變成「量一個斜率」。

三個保留寫在 `docs/re/55` §2：(1) 這是社群攻略反推的**假說**，
oracle 優先序上反編譯在前，而我們連附魔在原版哪裡都還不知道；
(2) `n^1.7` 對 1988 年 16-bit 遊戲可疑（要 log/exp），
「存十格表再乘材質係數」同樣能吻合 80 個點，**光靠攻略分不出**；
(3) 1.7 這個常數在估價的附魔項也出現過（`trunc(1.7 × |附魔| × 350)`）——
線索不是結論。**因此不實作**：接一個沒有呼叫端的服務等於自己設計遊戲。

### 這一輪（2026-07-26 續二十六）：戰鬥經驗值 —— grep 常數而不是 grep 結果（`docs/re/56`）

使用者說「當年的數字可能都是用公式從某個基數算出來的，所以找不到跟
遊戲或攻略一樣的數字是有可能的」。那句話直接改變搜尋策略：
**不要 grep 結果，要 grep 常數。**

把懷疑的浮點常數當 IEEE double 掃全檔，**`1.7` 就在 `2.6` 旁邊 16 bytes**
（同一個常數池，`2.6` 是掉落價格上限的底數）。`1.4`／`1.2` 掃不到 ——
它們是用四個立即數 push 上堆疊的，**「常數在不在資料段」本身就是線索**。
順著 `mov bx,0x40f0`（全檔一處）就到了戰鬥勝利結算。

**經驗值（完整，已實作）**：

	總經驗 = Σ 單位[+0x24]                    每殺一隻累加
	每人   = 總經驗 ÷ 隊伍人數                3016:000a 有號 32-bit 除法
	發放   = 戰鬥狀態 < 2 的每一名，封頂 0x00FFFFFF

「Exp per chr」的 per chr 是真的除過的；**分母是全隊人數不是拿得到的人數**
（有人被束縛或死亡，那一份直接消失）；分界 `>= 2` 剛好切在
「還能自己行動」那條線 —— **中毒照樣拿經驗**。

**本專案原本完全沒有發放戰鬥經驗值** —— `Character.Experience` 只有讀
沒有加，打怪不給經驗，核心成長線是斷的。這一輪接上了。

金幣那一半式子讀出來了（`Σ 1.7^X + rnd(2.1^X) + 3`），但 `X = 單位[+0x1a]`
依 `docs/re/20` 是**附魔加成**，拿它當金幣指數講不通，當時沒有證據裁決所以沒實作。
→ **下一輪解掉了**（`docs/re/57`）：對怪物那一欄是 `MONSTER.DAT` 的 level。

⚠ 踩到的坑：第一次把那個 Σ 當成經驗值 —— **C 的 printf 參數是右到左推的**，
最後 push 的才是第一個 `%ld`。差一步就把整條公式接錯。

順帶削弱 `docs/re/55` 的線索：資料段那個 1.7 是 `1.7^n`（底數），
與附魔費用需要的 `n^1.7`（指數）方向相反，三個 1.7 不是同一件事。

### 這一輪（2026-07-26 續二十七）：怪物表與召喚表是同一套 schema（`docs/re/57`）

上一輪卡在「`unit+0x1a` 對怪物是什麼」。三個可能裡有一個**早就寫在
`docs/re/02`**（`party+0xa6` 就是怪物數）—— 又一次該省下的功夫。

掃寫入點掃出一個空洞：`ds:0x4ece` 全檔只有兩處寫（召喚、玩家），
**怪物建立段根本沒寫 `+0x1a`**；`ds:0x4ed8`（擊殺經驗）更是全檔只出現一次，
還是個讀。怪物屬性不是在主段填的。

轉而比對兩張屬性表 —— **召喚生物表 12 筆 × 11 word，`MONSTER.DAT` 99 筆 × 11 欄，
而 12 隻召喚生物在 `MONSTER.DAT` 裡都有同名怪物。**

	速度／力量／技巧／HP／攻擊型別／w5／w6／w8  八欄逐筆完全相同
	96 個數值零誤差

**`unit+0x1a` 對怪物 = `MONSTER.DAT` 的 level（1–10）**，金幣公式成立：
1 級怪 5–6 枚、5 級怪 18–57 枚、10 級怪 205–1871 枚。金幣進隊伍錢包
（`+0x0a`，本專案早就解出的 `goldOffset`），封頂同樣 `0x00FFFFFF`。

實機（`73-battle-gold.png`）：獸人(lv2)＋蜘蛛＋巨鼠＋1級法師 →
經驗 `(26+16+14+22)÷5 = 15` ✓、金幣 26 落在區間 21–27 ✓。

**同構順帶推翻四處判讀**（詳見 §5「已被推翻的斷言」）。

~~怪物屬性的搬移指令仍未定位~~ —— 已由 DS:`0A44` 間接映射表結案
（`docs/re/111`）。金幣也已由 DOSBox 固定遭遇動態 oracle 結案：
原版輸出 31，落在公式預測 26–33，且同畫面 EXP 24 交叉吻合
（`docs/re/113`）。

## 3. 文件索引

### 規格層 `docs/spec/` — 實作的唯一依據

**只有標 READY 的才可以實作。**

| 檔案 | 狀態 |
|---|---|
| [`README.md`](docs/spec/README.md) | SDD 工作方式、規格分級定義 |
| [`01-rng.md`](docs/spec/01-rng.md) | **READY** — 亂數產生器 |
| [`02-combat.md`](docs/spec/02-combat.md) | **READY** — 戰鬥系統（全部子系統）|
| [`03-events.md`](docs/spec/03-events.md) | **READY** — 事件觸發 |
| [`04-movement.md`](docs/spec/04-movement.md) | **READY** — 移動與模式切換 |
| [`05-character.md`](docs/spec/05-character.md) | **READY** — 角色建立與升級 |
| [`06-time.md`](docs/spec/06-time.md) | **READY** — 時間系統 |
| [`07-graphics.md`](docs/spec/07-graphics.md) | **READY** — 素材格式與渲染 |
| [`08-town-economy.md`](docs/spec/08-town-economy.md) | **READY** — 城鎮與經濟 |
| [`09-fonts.md`](docs/spec/09-fonts.md) | **READY** — 字型與中文化 |

### 逆向筆記 `docs/re/` — 過程與證據

| 檔案 | 內容 |
|---|---|
| [`00-ghidra-setup.md`](docs/re/00-ghidra-setup.md) | **必讀** — Ghidra 環境、位址換算、六條踩雷 |
| [`01-dosbox-reference.md`](docs/re/01-dosbox-reference.md) | DOSBox 環境、timeline 語法、自動化踩雷 |
| [`02-data-loading-functions.md`](docs/re/02-data-loading-functions.md) | 事件表與城鎮的讀取函式 |
| [`03-audio-and-resources.md`](docs/re/03-audio-and-resources.md) | 音效（V1 結案）、SUM.MAP RLE |
| [`04-main-loop-state-machine.md`](docs/re/04-main-loop-state-machine.md) | 主迴圈、指令分派、時間、勝利條件 |
| [`05-event-triggering.md`](docs/re/05-event-triggering.md) | 座標 → EXITS → 事件索引 |
| [`06-combat-system.md`](docs/re/06-combat-system.md) | 戰鬥框架、命中傷害（⚠ 選單 case 表已作廢，見 09） |
| [`07-sprite-blit.md`](docs/re/07-sprite-blit.md) | EGA sprite blit、`.SHE` 格式 |
| [`08-movement-and-modes.md`](docs/re/08-movement-and-modes.md) | 移動、面向、可通行判定、模式切換 |
| [`09-spells-and-actions.md`](docs/re/09-spells-and-actions.md) | 8 個戰鬥動作的正確 case 編號與公式 |
| [`10-character-and-economy.md`](docs/re/10-character-and-economy.md) | 建角、升級公式 |
| [`11-dosbox-verification.md`](docs/re/11-dosbox-verification.md) | DOSBox 三項實測 |
| [`12-ghidra-jumptable-fix.md`](docs/re/12-ghidra-jumptable-fix.md) | 跳表 override 修復 |
| [`13-realmode-tooling-survey.md`](docs/re/13-realmode-tooling-survey.md) | real mode 工具鏈調查 |
| [`14-rng-float-equivalence.md`](docs/re/14-rng-float-equivalence.md) | RNG 浮點等價性證明 |
| [`15-spell-constants.md`](docs/re/15-spell-constants.md) | **法術 K/M 參數表**（在 `FILES.DAT`，不在 `DEMON.INT`） |
| [`16-combat-details.md`](docs/re/16-combat-details.md) | 命中修正、爆擊、戰敗判定、AOE |
| [`17-font-format.md`](docs/re/17-font-format.md) | **字型格式**（CGA 8×8 packed 2bpp 兩 bank、EGA 16×14 1bpp）與繪字函式鏈 |
| [`18-jumptable-sweep.md`](docs/re/18-jumptable-sweep.md) | 跳表全檔清掃（21 張）+ **即死／束縛／枯萎的精確判定公式** |
| [`19-town-economy.md`](docs/re/19-town-economy.md) | **城鎮經濟六大設施全部公式**；市集議價與說服技能；角色記錄 6 個新欄位 |
| [`20-summon-and-combat-units.md`](docs/re/20-summon-and-combat-units.md) | **召喚／幻術**、戰鬥單位 19 欄結構、怪物進場擲點、傷害與排序邊界 |
| [`21-skills-races-and-files-dat.md`](docs/re/21-skills-races-and-files-dat.md) | **遊戲內部技能 id 表**、種族系統與修正公式、`FILES.DAT` 完整布局 |
| [`22-resource-arena-and-passability.md`](docs/re/22-resource-arena-and-passability.md) | **18 段資源記憶體區**、**可通行性表**、子地圖退出、**事件消費者** |
| [`26-camp-and-rest.md`](docs/re/26-camp-and-rest.md) | **紮營選單（13 選項）**、睡覺的時間與回復公式、過夜充能；順帶抓到道具槽兩個欄位標反 |
| [`56-battle-rewards.md`](docs/re/56-battle-rewards.md) | **戰鬥經驗值**：Σ 怪物經驗 ÷ 隊伍人數，束縛／死亡拿不到且那份消失；靠「grep 常數不 grep 結果」找到；金幣式子讀出但輸入欄位講不通故不實作 |
| [`55-enchant-cost-derived.md`](docs/re/55-enchant-cost-derived.md) | **附魔費用反推**：`F(n,c) = 35 × (20−c) × n^1.7`，攻略全表 80 點誤差 ≤6；方法是「先分維、看累積不看逐級」；標為假說（附魔在原版哪裡還沒找到）|
| [`54-reference-comparison.md`](docs/re/54-reference-comparison.md) | **第一次拿 DOSBox 原版對答案**：狀態列數字、紮營 14 項、`Inspect character:` 三項對上；價格量級仍未對到（要改存檔跳過導航，做法見 §4）|
| [`53-view-mind.md`](docs/re/53-view-mind.md) | **View mind**：一次性、逐件 2/3 機率揭穿說謊的貨（計數器 1001 → ` LIE `）；順帶解掉那個被誤讀成「詛咒機率」、又標了兩輪「用途未解」的旗標 |
| [`52-marketplace-and-merchants.md`](docs/re/52-marketplace-and-merchants.md) | **市集與商隊共用一支常式**；商隊有七個選項（本專案只做兩個）；議價的三條分支與「翻臉就報廢那件貨」；選項數的三重交叉驗證 |
| [`51-encounter-dispatch-corrected.md`](docs/re/51-encounter-dispatch-corrected.md) | **那道 1/64 是商隊不是戰鬥**（訂正 `docs/re/04` §2.2 與本專案的實作）；隨機戰鬥掛在動作碼 `0x16`，由 `隊伍[+0x9c]` 倒數歸零觸發 |
| [`50-merchant-encounter-trigger.md`](docs/re/50-merchant-encounter-trigger.md) | **商隊是事件動作不是隨機遭遇**（從函式位址回推呼叫端找到的）；規模＝等級＝`clamp(基準 + rnd(3) − 2, ≤ 9)`；等級 0 會讓原版的型別重擲轉不完 |
| [`49-item-valuation-complete.md`](docs/re/49-item-valuation-complete.md) | **估價六項全解**：兩組特效值 `trunc(1.4×|n|×250)×sign(n)`、附魔項（武器 350／護甲 700，取絕對值不補符號）、詛咒品估價 0；「小數乘出整數」的捷徑**因乘法順序而不成立** |
| [`48-loot-generator-reread.md`](docs/re/48-loot-generator-reread.md) | **掉寶生成器重讀**：型別比較全是 1-based（五處判讀偏一格）；材質類別規則；價格門檻是上限不是下限（`2.6^等級`）；商隊不生詛咒品；兩組特效與兩組附帶法術（道具槽 17 byte 全解）|
| [`47-merchant-interface.md`](docs/re/47-merchant-interface.md) | **商隊購買介面**；接上價格才發現掉寶生成器沒填材質類別（估價第一個乘數一直是 0）；價格量級尚未與原版對照 |
| [`46-charge-bonus.md`](docs/re/46-charge-bonus.md) | **估價第三項**：強度加價的三條分支（`0.9`／`÷(上限−100)`／`×214`）；`> 100` 的分界正好擋掉除數 0 |
| [`45-merchant-price.md`](docs/re/45-merchant-price.md) | **商隊售價** `估價 × (uniform × 0.8 + 0.6)`，±40% 浮動；順帶替 `310e` 的 `021e`／`0242`／`02e4`／`0732` 命名 |
| [`44-item-valuation.md`](docs/re/44-item-valuation.md) | **道具估價**：軟浮點常數是 push 進去的不是資料段指標；`底價 × 材質倍率`（`+0x0f` = 材質類別，表 `0 1 2 5 20 35 50 60 75`）|
| [`43-worship.md`](docs/re/43-worship.md) | **敬拜**＋神祇法術表（`FILES.DAT + 0x7ae`，用指標鏈的長度反推位置）；紮營選單十四項的完成對照表 |
| [`42-camp-cast.md`](docs/re/42-camp-cast.md) | **戰鬥外施法**：哪些效果離得開戰場、借戰鬥單位當計算載體；附 Worship 的完整規則（缺 `[0x4cc4]` 表）|
| [`41-exorcise.md`](docs/re/41-exorcise.md) | **驅邪**：拆掉身上的詛咒裝備；解開槽 `+0x0d`（驅邪成功率）、技能旗標值 2（被詛咒封住）、四對「天生 vs 含加成」屬性欄位的第二次確認 |
| [`40-inspect-character.md`](docs/re/40-inspect-character.md) | **檢視角色**：選單叫 Party、字串是 `Inspect character:`；另一條分支看起來是印到印表機（不實作）|
| [`39-use-item-in-camp.md`](docs/re/39-use-item-in-camp.md) | **營地用道具**：火把 26／提燈 27 點起來設光源 2／3；訂正 `item.go` 那條「兩個欄位沒定位到」的假缺口 |
| [`38-view-land.md`](docs/re/38-view-land.md) | **觀地**：技能 26、隊伍層級的每日旗標 `+0xac`、只在大地圖上能用；與戰場攝影機共用同一支捲動視窗常式 |
| [`37-identify.md`](docs/re/37-identify.md) | **鑑定**：成功率 = 智力 × 4.5、每天一次（失敗也算）、三種學識技能各管一段型別範圍 |
| [`36-battlefield-shape.md`](docs/re/36-battlefield-shape.md) | **戰場的形狀**：15×15 於 (6,6)–(20,20)、17×17 牆框、3×3 世界 tile 各放大 5×5、可走 = 與腳下同地形；訂正「戰場 9×9」與「光照內縮」|
| [`35-battle-deployment.md`](docs/re/35-battle-deployment.md) | **開戰擺位**：隊伍照陣型站中央、怪物散在 ±2、地圖兼作佔位表；單位結構間距 38、玩家從槽位 7 起；9×9 是捲動視窗不是邊界 |
| [`34-formation-grid.md`](docs/re/34-formation-grid.md) | **3×3 陣型格**（trailer `+0x00`–`+0x08`）：四個呼叫端、Reorder 是整張重填、佈陣的座標換算（讀出來但未接）|
| [`112-party-trailer-reserved-09.md`](docs/re/112-party-trailer-reserved-09.md) | **C9 結案**：IDA 全檔稽核證明 trailer `+09h` 沒有 DOS gameplay consumer；四條陣型路徑上界都是 9，下一欄直接是 `+0Ah/+0Ch` 金幣；remake 保留非零值 round-trip |
| [`113-dosbox-price-and-battle-gold-oracle.md`](docs/re/113-dosbox-price-and-battle-gold-oracle.md) | **C1 結案**：原版 Seaside 市集 dagger = 2 Gold；固定神殿遭遇原版結算 Gold = 31，吻合 remake 對該 level 組合的 26–33 預測，EXP 24 同畫面交叉驗證 |
| [`115-illusion-turn-vanish.md`](docs/re/115-illusion-turn-vanish.md) | **幻象消失結案**：敵我 side 3／13 輪到行動前執行 `Roll(10)<3`；Roll 值域 1–10，故為 20%。另記死亡清理、一次性 UI 事件與 2／3 邊界測試 |
| [`33-camp-menu-items.md`](docs/re/33-camp-menu-items.md) | **紮營選單 14 項**的完整對照（名稱／快捷鍵／處理常式）＋ Drop 與 Trade 的規則；訂正 `docs/re/26` 的 13 項與「順序對不上」|
| [`89-natural-vs-bonus-attributes.md`](docs/re/89-natural-vs-bonus-attributes.md) | **更正 `docs/re/88` §3**：`+0xe8`／`+0xe9`／`+0xf8`／`+0xfb` 不是未解欄位，是天生 vs 含裝備加成的力量／技巧 —— `save.go` 的屬性位移全部相對 `expOffset`（`0xc4`），所以 `0xe8` 的字面值從沒出現過，**拿字面值去搜一個算出來的值**必然落空。建角的兩處複製是「擲點結果從含加成抄回天生」。**順帶抓到 bug**：`ApplyTo` 不寫那四個 byte，新角色的存檔留著出貨那五個人的數字 —— 名冊是自己擲的點、存檔是別人的，重讀就換人 |
| [`88-character-creation-record-init.md`](docs/re/88-character-creation-record-init.md) | **建角寫進角色記錄的四件事**：等級 1（`0x14dc5`）、十格背包 `Type = 0xff`（`0x150d1` 迴圈）、裝備武器／護甲槽 `0xff`（`0x150ed`／`0x150ff`）、trailer 人數 +1（`0x15109`）。**角色記錄的基底是 `ds:0x4c7e`（步長 `0x104`）**，不是 trailer 的 `ds:0x4c76`；`ds:0x548c` 全檔零 `les`，不是實際指標。收掉 `docs/re/87` §6 的「推論不是驗證」。道具槽長度 **17** —— 原版迴圈用 `shl 4` + `add i` 湊出來，只看 `shl 4` 會誤讀成 16。`+0xe8`／`+0xe9` 兩處複製語意未解 |
| [`87-shipped-party-dat-is-not-a-new-game.md`](docs/re/87-shipped-party-dat-is-not-a-new-game.md) | **出貨的 `PARTY.DAT` 是玩過的存檔，不是新遊戲。** 建角程式 `0x148c8`–`0x1497f` 寫死起始值，與出貨存檔九個欄位不同 —— 最關鍵是**地圖 1（地城）vs 34（世界地圖）**。完整起始值表（位置 (28,50)、日 8 時 5、金 75、糧 20、船體 67 的船、陣型全 `0xff`）。線索早在第一張截圖裡（Menhir 15/27 帶傷），而 `docs/re/78` §2 已記過同組資料另一半是髒的。順帶炸出被巧合掩蓋的 bug：引擎載地圖看 `-map` 不看存檔 MapID，而預設 `MAP1.MAP` 剛好等於出貨存檔的圖 1 |
| [`86-world-map-temples-and-colleges.md`](docs/re/86-world-map-temples-and-colleges.md) | **世界地圖上的 19 座神殿（`ds:0x310d`，19×4）與 35 座學院（`ds:0x342d`，35×3）**。神殿的 `(X,Y,a,b)` 用 `idx = a*2 − b` 壓成 1–10 的神祇編號（`a∈1..5`、`b∈{0,1}` 每組唯一）；三條旁證：城鎮入口共用同一索引空間、`FILES.DTT` 神祇名表 11 筆、實跑顯示 Gamur。學院表重抄驗證 `docs/re/74`（35 筆全同）；#19 的 `0` 是劍擊不是哨兵、#20 與 #1 重複且永遠選不到（**不要去重**）。**又一次「座標表不帶地圖編號」**（同 `docs/re/85`）：判別式在地圖裡不在表裡，34/35 筆恰好落在一個段的 `0x26` 上、**0 筆落在兩個以上** |
| [`85-exit-marker-is-passability-fd.md`](docs/re/85-exit-marker-is-passability-fd.md) | **出口的標記是可通行表的 `0xfd`**，不是某個 tile 值。推翻 `docs/re/05` §3 的「tile == `0x11`／`0x53` 才查 `EXITS.DAT`」——實際走得到的五個出口來源 tile 是 `0x24`／`0x13`／`0x00`，一個 `0x11` 都沒有。答案本來就在 `docs/re/58` 的動作 `0x14`（`可通行表 == 0xfd`）；順帶解掉 `docs/re/22` 的「253 語意未確認」。帶這個值的九個 tile 是樓梯／洞口的美術變體 —— 閘門放在表裡而不是 tile 清單，是 1988 年的資料驅動。**並回答「沒有來源地圖欄位的表怎麼分給 26 張圖」**：由 `0xfd` 分，實測 53/55 筆恰好被一張圖認領、**0 筆被兩張圖認領**。落點 55/55 不在牆上 |
| [`84-password-riddles.md`](docs/re/84-password-riddles.md) | **兩道密語謎題**（16 格表 case 10／11，全遊戲僅有的自由文字輸入）：答案明文存在資料段（`VOID` @ `ds:0x2c7b`、`JESRIC` @ `ds:0x2d08`），比對走 `stricmp`。case 10 答對把 `map[0x48b]` 設 0 開牆 —— **`0x48b` ＝ (11,18)、`MAP5.MAP` 那格是 `0x0d`、可通行表 `0x0d` ＝ `0xff` 牆**，三份獨立產物閉合；觸發格在正南兩格。case 11 答錯把 `party+0xa2` 設 38（趕出門一步），**沒有持久旗標可重試**。答對 case 10 沒有台詞（`docs/re/65` §3 把「司祭化成霧氣」歸錯支）。牆只開在記憶體，離開地城會復原 —— 原版行為，不修 |
| [`83-class5-location-plot.md`](docs/re/83-class5-location-plot.md) | **`nSS.DAT` 類別 5 ＝ 地點劇情事件，值 ＝ `docs/re/65` 16 格表的 case 編號**。`ds:0x52f6` 不是狀態變數，是查表函式 `222f:1321` 的輸出參數（in：座標；out：序號＋值＋類別）。連結點是 `0x19f15 cmp ds:0x5c62,5`。**資料反證**：五張地圖用掉值 1–15，每值只出現一次，**零碰撞**；而且幾何對得上字串（機械聲夾住 12 格壓牆走廊、光之環四格十字）。**艾瑞戈爾整場對話樹解完**（三層選擇、七條出口、10 頁；六條開打）。訂正 `+0xbe` 的身分 ＝「春之石已碎」（寫入只在艾瑞戈爾那一場），城鎮廢墟只是下游效果 → 改名 `ShardShattered`。清掉 `docs/re/22` 全文把 `nSS.DAT` 寫成 `EXITS.DAT` 的檔名錯貼 |
| [`82-story-text-pages.md`](docs/re/82-story-text-pages.md) | **劇情文字頁的載入路徑**：`25be:19d1(page, mode)` —— `mode` 索引兩張 3 筆表（`ds:0x2f59` 檔名、`ds:0x2f65` 大小，**三個大小與實際檔案逐一相符**），`page == 0xffff` 是載入。推翻 `docs/re/70` §4「這三個檔案沒有載入路徑」。`T.TXT` ＝ 三場夢，**第 2 頁把程式碼逐句敘述了一遍**（信仰歸零、神殿成廢墟、諸神已死）。`+0xbe` 的劇情定案 ＝ **艾瑞戈爾與黑鏡**（`EREGORE.TXT` 第 9 頁）。三檔約 9 KB 敘事文字，引擎一頁都還沒接 |
| [`81-dosbox-settles-b9.md`](docs/re/81-dosbox-settles-b9.md) | **DOSBox 十分鐘裁決 `docs/re/80` 的衝突**：把 `+0xb9` 改成 1 再睡一覺 → **第二場夢確實播出來**，路徑是活的。拿到**馬利馮預言全文**（中文化內容）＋第一行 `The Orb of Evertime now is yours` 直接指出寫入端在寶珠事件裡。順帶 dump 到**原版主指令選單 15 項與紮營選單 14 項的逐字原文**（C1 對照表；原版把 Exorcise 拼成 `Xorcise`）。更正 `docs/re/26`「原版紮營鍵沒查」→ 是 `C`，手冊 §407 早就寫了 |
| [`80-sleep-dreams-plot-stages.md`](docs/re/80-sleep-dreams-plot-stages.md) | **冬之魔是在睡覺時降臨的**：`1000:0206` 的三段夢境（月>3 且 `+0xbd==0` → 頁 0；`+0xb9==1` → 頁 1 並進階段 2；階段 2 → **神殿全毀＋全隊信仰整套歸零**＋頁 2）。`+0xb9` 語意定案（`2` ＝ 已降臨，19 處同基底核對過）。`ds:0x15c8` 是死變數（五處寫零處讀）。⚠ **`+0xb9 = 1` 的寫入端找不到**，而攻略證明它存在 → `tools/fieldscan.py` 只掃常數位移，**否定式斷言不能只靠它** |
| [`79-ruins-tile-5b.md`](docs/re/79-ruins-tile-5b.md) | **tile `0x5b` ＝ 廢墟**，冬之魔降臨的機制實作：`+0xba` 立起來後繪製時把神殿換成廢墟、踩上去印 `walking through ruins`；**神殿被毀永久剝奪全隊薩滿與司祭技能**（那兩個技能旗標全檔只有一處寫入）；`+0xbe` 的劇情 ＝ 城鎮變廢墟。並更正引擎把五個地點 tile 當文字索引（越界所以靜默無反應）|
| [`78-nss-dat-is-a-savefile.md`](docs/re/78-nss-dat-is-a-savefile.md) | **`nSS.DAT` 是存檔的一部分**：新遊戲把 `ALL_SS.DAT`（母本）切成 5×512 重寫五個檔；遊戲中三種改寫（清零／類別 +3／標記已造訪）**全部對上出廠 `1SS`／`2SS` 的 29 處差異，三種型態零例外** → 推翻「兩份建置產物不同步」。收掉 B8 的 `+0x9c` 最後兩處。⚠ **重新打開 `docs/re/76`**：`ds:0x52d4 × 6` 索引船隻陣列，「＝ 巢狀深度」結案下得太早 |
| [`77-nss-dat-special-tiles.md`](docs/re/77-nss-dat-special-tiles.md) | **`nSS.DAT` 是子地圖的特殊格清單**（不是開場序列資料）＋ **`+0xa9` 是它的快取有效性旗標**。格式：前段 3-byte `(X,Y,屬性)`、**檔尾往前長的 2-byte 傳送目的表**（類別 4 筆數 = 尾表對數，5/5 零誤差）。表會被事件就地改寫，所以才需要 per-dataset 快取。推翻 `docs/re/05` §1.3／§1.4 與 `docs/re/69` §3，訂正 `FUN_307c_0005` 的 src／dst 方向；順帶結案 `+0xbe`（單向閂鎖，11 處沒一處寫 0）與新解 `+0x99` |
| [`76-mode-is-nesting-depth.md`](docs/re/76-mode-is-nesting-depth.md) | **`ds:0x52d4` 是巢狀深度**：`inc`／`dec` 成對裁決了「二元開關 vs 計數器」；教訓＝新開 worklist 項目之前也要 grep，假的未解比漏掉真的更糟 |
| [`75-encounter-countdown-writers.md`](docs/re/75-encounter-countdown-writers.md) | **遭遇倒數 `+0x9c` 的八處寫入**：三條「這裡不開打」的路（跨子地圖邊界、無遭遇群組、地城延後）、為什麼設 1 不設 0；worklist 原本記「兩處」是低估 |
| [`74-colleges-on-world-map.md`](docs/re/74-colleges-on-world-map.md) | **世界地圖上 35 座學院**：tile `0x26`、`ds:0x342d` 的 (X,Y,技能) 三元組表、靈視系四技能各有一座；tile `0x5b` 沒有分派 |
| [`73-temple-menu-unreachable.md`](docs/re/73-temple-menu-unreachable.md) | **神殿樓梯在這條路上到不了**：回傳值＝索引×2+2、選單只三筆、兩個呼叫端都不傳表 → 遺留碼；順帶 tile `0x25` ＝ 神殿 |
| [`72-cypher-rune-font.md`](docs/re/72-cypher-rune-font.md) | **`CYPHER.SHP` 是符文字型**（27 個 16×16 字形）：`%` 開頭觸發載入、大小自證、dump 肉眼確認，四條獨立證據；`%RING.BELL...AT.....MIDNIGHT` 首次接上 `docs/re/65` case 12 的鐘 |
| [`71-eregore-t-cypher.md`](docs/re/71-eregore-t-cypher.md) | **`EREGORE.TXT`／`T.TXT` 沒有載入路徑**（掃過六種定址模式零命中，與 `OPEN.PIE`／`TITLE.PIC` 同一類「資料在、程式沒接」）；`CYPHER.SHP` 載入點 `0x1b0da` |
| [`70-t2c-t2d-trigger.md`](docs/re/70-t2c-t2d-trigger.md) | **`T2C`／`T2D` 的觸發**：子地圖 <3 或 ≥3，讀不同偏移；兩者是同一個字串被就地改寫第 3 個 byte（`T2D.TXT` 在檔案裡不存在）；更正 `docs/re/60` §3 記錯的表位址 |
| [`69-remaining-step-actions.md`](docs/re/69-remaining-step-actions.md) | **移動分派表最後四格**：`0x08` 地城探索地圖、`0x0c` Use／`0x0a` Examine（參數選提示詞）、`0x0f` 觀室、`0x10` 鑑物（技能 27／28）；更正「`0x0c`／`0x12` 成對」|
| [`68-dungeon-traps.md`](docs/re/68-dungeon-traps.md) | **地城陷阱的數值**：毒針 `Roll(4)` 完整追過並接上動作 `0x18` 全滅；其餘四種列擲點位置；更正「兩格＝偵測／踩到」為「普通版／有害版」 |
| [`67-temple-stairway.md`](docs/re/67-temple-stairway.md) | **神殿後面的樓梯**：直接寫座標傳送到子地圖 1 的 `(22,21)`；與起始存檔同一張地圖，疑似捷徑而非隱藏關卡 |
| [`66-remaining-jumptables.md`](docs/re/66-remaining-jumptables.md) | **剩下五張跳表一次清完**（B7 清空）：地城陷阱 9 格、神殿 9 格（含沒被記載過的「後面的樓梯」）、角色檢視 12 格；定位方法可批次腳本化 |
| [`65-location-event-dispatch.md`](docs/re/65-location-event-dispatch.md) | **地點劇情事件 16 格**（動作 `0x12` 的內層分派）：壓牆陷阱、殺 Xeres 的武器、**兩道密碼 `VOID`／`JESRIC`**、鐘、墓碑、光之環的門；附「用已知的表反推 `cs base`」的方法 |
| [`64-mainline-playable.md`](docs/re/64-mainline-playable.md) | **主線全線實機驗收**（七段截圖），與一個「把成功條件當顯示條件會抹掉失敗分支」的實作缺陷 |
| [`63-step-hp-tick.md`](docs/re/63-step-hp-tick.md) | **每步 HP 變動**：`FUN_222f_0619` 一個迴圈兩條路 —— 模式 0 是巨魔再生（解開天生能力 31 的實作位置）、模式 0x80 是符印流血且巨魔不免疫；實機對照組 |
| [`62-mainline-implemented.md`](docs/re/62-mainline-implemented.md) | **主線實裝紀錄**：四方驗證（地圖只有三格 `0x63`，正好在 55/56/66）、四處刻意的差異（切換條件是設計決定、門擋在移動前、兩門檻不統一、先扣費照抄）、實機驗到哪 |
| [`61-endgame-confirmed.md`](docs/re/61-endgame-confirmed.md) | **結局函式確認**：`0000:xxxx` 的 offset 是真的，用它反查就找得到（推翻「重定位＝靜態看不到」）；`0x07175` 是破關畫面；UNCURSE/IMPRISON 是通用施法選單的另一組選項 |
| [`60-imprison-and-endgame.md`](docs/re/60-imprison-and-endgame.md) | **禁錮與終局**：UNCURSE／IMPRISON 是主線專屬選單（不在 43 筆法術表）、禁錮條件（子地圖 5 或 Y≤6，地點不對照扣 100 SP）、結局文字檔的遠指標表、**完整破關路徑**與六項保留 |
| [`59-glyphs-and-circle-of-light.md`](docs/re/59-glyphs-and-circle-of-light.md) | **主線骨幹**：三個緋紅符印（tile `0x63`）→ 解咒扣 50 法力 → 旗標 `0x80` → 光之環放行；子地圖 55/56/66 → 索引 0/1/2 的映射；反組譯與攻略三方閉合 |
| [`58-step-dispatch-and-plot-flags.md`](docs/re/58-step-dispatch-and-plot-flags.md) | **每走一步的回傳碼分派**（21 格，定性修正）：回傳碼產生條件全表、事件表經 `FUN_222f_0a90` 走同一張表、**主線劇情旗標 `+0x96`–`+0x98`**、字串錨定要驗起點 |
| [`57-monster-summon-same-schema.md`](docs/re/57-monster-summon-same-schema.md) | **怪物表與召喚表同構**（96 個數值零誤差）：`unit+0x1a` 對怪物＝`MONSTER.DAT` level、戰鬥金幣定案；推翻 `num_attacks`／護甲索引／w5 未解／w7 死欄位四處判讀 |
| [`111-monster-to-combat-unit-map.md`](docs/re/111-monster-to-combat-unit-map.md) | **C5 直接寫入端**：DS:`0A44` 的 11-word 間接 schema 將 MONSTER field 7／8 搬到 `unit+24h/+1Ah`，解釋為何搜尋固定 MOV 一直漏掉 |
| [`32-merchant-encounter.md`](docs/re/32-merchant-encounter.md) | **商隊遭遇**：規模→形容詞對照、7–10 件貨、詛咒機率 `rnd(120)−80`；購買介面見 `47`，只剩遭遇觸發未解 |
| [`31-sailing.md`](docs/re/31-sailing.md) | **搭船**：`+0xb0` 一個 byte 就是整個狀態、上船／航行／上岸的三段判定、「上船判定要排在可通行性之前」|
| [`30-loot-generation.md`](docs/re/30-loot-generation.md) | **掉寶生成**：四個類別欄位的候選／排除兩種用法、附魔與兩道效果門檻、強度與最低法力的配對重擲、三種充能種類 |
| [`29-temple-conversion.md`](docs/re/29-temple-conversion.md) | **改宗**：`0x10 − (神祇編號 mod 2)` 的推導、十位神的奇偶分派、Ancient One 的例外；順帶訂正祈禱前置檢查的跳轉方向 |
| [`28-ship-array.md`](docs/re/28-ship-array.md) | **船隻陣列**（trailer `+0x22`，10 格 × 6 bytes）：六個欄位、放船的水位與佔位判定、扣款時機；順帶釘死金幣是 4 bytes |
| [`27-string-table-loader.md`](docs/re/27-string-table-loader.md) | **FILES.DTT 的切表常式**：8 張表的筆數與邊界（相加 = 501），月份名、神祇名、道具型別名；修掉兩個猜錯的分段 |
| [`25-item-effects.md`](docs/re/25-item-effects.md) | **道具效果索引欄位定位**（槽 `+0x05`–`+0x08`）、道具與法術共用效果表、`ITEMS.DAT` f3–f6 ＝效果類別候選、`DS:0x1941` 生成表 |
| [`24-random-encounters.md`](docs/re/24-random-encounters.md) | **隨機遭遇**：地形＝可通行性值、18 個遭遇群組、1/64 觸發、隻數與等級公式；**七大地形對 tile 值定案** |
| [`23-ai-spell-dispatch.md`](docs/re/23-ai-spell-dispatch.md) | **怪物 AI 的施法效果分派**（17 項跳表）、**K 的正負決定打自己人還是打玩家**、範圍法術的誤傷否決、**附身術公式**、`+0x20` 陣營欄位定案 |
| [`104-event-gate-class-one-versus-two.md`](docs/re/104-event-gate-class-one-versus-two.md) | **A8 結案，而且題目本身是錯的**。worklist 把它寫成「逐筆判定哪些事件是一次性的」，實際上 105 筆類別 1／2 記錄**一筆都不用判** —— 一次性與否就是**類別編號**：類別 1 播完標記後永遠沒反應，類別 2 站上去會武裝，按 **`R) Read descr.`** 可以重讀。閘門在 `FUN_222f_0a90`（`docs/re/05` §1.5 早就完整反編譯，只是沒讀出這一層）＋`222f:0c0c` 的倒數遞減＋`222f:1282` 的 case 6。<br>三邊互證：反組譯、出貨存檔（`1SS`／`2SS` 有 12 筆**已造訪的類別 2**，所以那條分支不是死碼）、以及原版截圖裡 `Read descr.` 被畫成**網點**（＝當下不可用）。<br>連帶：`ALL_SS.DAT` 是母本（值全 0）、`nSS.DAT` 才是那份玩過的存檔；`a.lastTile` 撞牆時存的是擋路的那一格，狀態列的「地形」一直印錯 |
| [`103-armor-tables-and-the-last-three-skills.md`](docs/re/103-armor-tables-and-the-last-three-skills.md) | **A9 結案 ＋ 兩張護甲表補完**。三項技能一次補齊：13 附身的入口是「施法選單只列學過的系」那道**共用閘門** —— 補上之後 12/13/14 與五系符文 17–21 共**八項**技能第一次有效果（換算 `16 + school_id`，與怪物 AI 那側 `aiSchoolSkill` 同一條；吟唱系三筆共用 school 6，只能靠法術索引 24/25/26 分）；30 硬化皮膚 `+2`；精靈的察覺靈光 `Roll(9) <= 精靈人數 + 4`（看種族不看技能，死人也算，只查四個欄位）。<br>⚠ 追那個 `+2` 的來源時把 `docs/re/06` §545 掛了很久的**護甲防護表**核完：`31f0:16c5` = `00 01 02 04 05 06 03`，**不是等差** —— 舊實作「防護 = 型別 − 7」從鎖子甲起每件少 1 點，而且漏了附魔。<br>旁邊還有一張沒人看過的**職業護甲上限表**（`31f0:03de`）：巫師／術士只能穿布甲、靈視者到鎖子甲、遊俠不限 —— 手冊三處對得上，等於同時驗了表位址與職業排序。引擎原本完全沒有這道關。<br>連帶推翻 `+0xe6` 的兩種讀法（盾牌旗標／裝備旗標）：那是技能旗標第 30 格 |
| [`102-enchant-cost-and-a-hand-rolled-pow.md`](docs/re/102-enchant-cost-and-a-hand-rolled-pow.md) | **附魔費用定案，C2 結案**：`費用 = (ItemValue(新附魔) − ItemValue(舊附魔)) × (20 − 材質類別) ÷ 10`。`docs/re/55` 反推的 `35 × (20−c) × n^1.7` 就是這條式子的展開（估價的附魔項係數 350 ÷ 10 = 35）。<br>⚠ **那個 `1.7` 是指數不是係數** —— `2000:78dc`（`0x1b4dc`）是**手寫的 pow**：`log` → 乘 → `exp`，開頭一道 `n == 0 → 0.0` 的守衛。`docs/re/55` §2(2) 擔心「這個引擎算不出非整數次方」的疑慮到此解除。**連帶訂正估價的第四／五項**（特效值也是 `|n|^1.4 × 250`）—— `docs/re/49` §4 那一整節的推理沒錯、前提錯了。<br>裁決證據：攻略 80 個點，指數版 69 個完全相同、11 個差 1（全同向）；係數版第一級就差七成（1130 vs 665）|
| [`101-enchanting-proving-rooms-and-the-orb.md`](docs/re/101-enchanting-proving-rooms-and-the-orb.md) | **A7 最後四格讀完，兩個別處掛著的問題跟著關掉**。case 6 ＝**矮人大師的附魔工坊**（地圖 2 (28,5)）→ **C2 的入口找到了，它不在城鎮設施裡**；case 9 ＝**十間試煉室**（一個職業一間，十座標與 `ds:0x0cae`／`0x0cc2` 兩張表 **10/10 零誤差**，過關寫 `+0x8a + 索引`）；case 8 ＝**恆世寶珠**（十間全過才給）—— **它就是 `docs/re/80` §3 那個「找不到的 `+0xb9 = 1` 寫入端」**，形狀是「基底 ＝ `party+0xb3` 存進區域遠指標」的 `[bx+si]` 寫入，常數位移掃不到；case 7 ＝**惡魔水晶**（地圖 4 (7,4)），送道具跳表七格到此全部有主 |
| [`100-tombstones-bell-and-the-travellers-bed.md`](docs/re/100-tombstones-bell-and-the-travellers-bed.md) | **A7 剩下三格字串已知的 case 全部讀完**。case 5 ＝**墓園每次都重排**（從 `MAP3.MAP` 重讀 → 隨機把 30 格墓碑變擋路石 → 三格強制留通，保證踩進入口後至少有一步可走）；⚠ 方向與直覺相反 —— **`0x56` 墓碑是可以走的、`0x54` 才擋路**。case 12 ＝**鐘只在 `hour >= 24` 有效**，開的是大半張地圖外的一道門 (21,59)。case 13 ＝**旅人的床唯一的功能是把時辰設成 25**（不回血、不換日）。**後兩者是同一條設計的兩端**：先睡覺撥到入夜，鐘才敲得響。§4.1 順帶給 C8 一條證據：`hour >= 24` 這條分支存在，時辰上限就不可能是 23 |
| [`99-armory-four-pedestals.md`](docs/re/99-armory-four-pedestals.md) | **地點劇情 case 3 ＝兵器庫**（地圖 1 一個房間的四個角各一座台座）。呼叫端一出現，「劇情送道具」跳表的 **param 0–4 規格全部落地** —— 型別／材質前綴／附帶法術三組欄位分別對回 `ds:0x27cc` 的四個名字，**六處零誤差**，再加攻略 `part-4` §3.2 那四樣道具（含匕首 +1、4 點寒顫）是第七個印證。編號來自一條三段 if 鏈（預設值本身就是有效編號），四座旗標各一格互不影響。§4.1 順手抓到**鐵匠鋪一直都有的輸入 bug**：選人頁的 ↑↓ 同時被移動迴圈吃掉，按一次隊伍就走一步 |
| [`98-circle-of-light-door-and-a-shared-temp.md`](docs/re/98-circle-of-light-door-and-a-shared-temp.md) | **光之環的門（A7 case 15）不是「不准走」**：走上四格十字之一 → 印訊息 → 用兩條算式把座標寫回去，代進四格全部落回「剛才那一格」。**推翻 `docs/re/62` §3.2** —— 那條「刻意的差異」只涵蓋時序，實作少了座標比對，等於整張地圖 5 都不能走；`-glyphs` 讓實機驗收繞過了那個判斷。順帶：`ds:0x4e32` 在這一支裡是**面向**（上游 `222f:0b34`），並補一條「出現在 `IDIV` 運算元位置的就是編譯器暫存」的快速判別法 |
| [`97-ega-palette-and-party-glyph.md`](docs/re/97-ega-palette-and-party-glyph.md) | **原版自己換掉了 EGA 調色盤**（表在 `ds:0x14b7`，開機時 `INT 10h AH=10h AL=00h` 逐格設）—— 正確解出來的地形圖塊配標準 16 色表看起來像雜訊，連帶造成 `graphics.md` §4.4 的錯誤斷言；**隊伍圖示是地形圖塊集裡的一格**（`ds:0x210f`，八格＝四向 × 走路交替，船另一組）；§5 **視野內縮的完整規則**（戶外看時辰、地城看光源、矮人黑暗視覺 +1）|
| [`96-inspect-and-menu-dispatch.md`](docs/re/96-inspect-and-menu-dispatch.md) | **選單分派整條鏈定案**：`FUN_2cdc_033d` 等輸入時回傳 **index + 4**，轉換表在 `ds:0x217c` 是 **15 個 word**（推翻 11 個 @ `0x2174`）；**`I) Inspect` 不回傳動作碼**，走 switch case 5 就地設 `ds:0x5c66 = 0x80` 讓繪圖把有道具的格子改畫 tile `0x38`；順帶落地動作 `0x06`／`0x08`／`0x0d`／`0x0e` |
| [`95-dungeon-items-decoded.md`](docs/re/95-dungeon-items-decoded.md) | **地城道具 50 件 × 每件 6 條字串**（件數與欄位數都是原版明寫的常數，不是猜的）：名稱／別名／描述／動作碼／參數／結果文，整套解謎鏈都在裡面 |
| [`94-itemloc-table.md`](docs/re/94-itemloc-table.md) | `ITEMLOC*.DAT` 的**載入端**（格式 `town-and-map.md` §4 早就解完了）；附一次「只 grep 檔名沒 grep 內容」的誤判紀錄 —— 錯誤斷言已經寫進 commit message 才是真成本 |
| [`93-visionary-view-room-and-items.md`](docs/re/93-visionary-view-room-and-items.md) | **兩個靈視技能**：觀室（技能 27）可接、鑑物（技能 28）當時缺前置；一天上限 **3 次**（`trailer[+0xad]`，`cmp …,3`）—— §4.1／4.2 記 C12 用 DOSBox 相鄰兩值定案的做法 |
| [`92-battle-examine-panel.md`](docs/re/92-battle-examine-panel.md) | **戰鬥 `?` 檢視**（動作跳表 case 10 ＝ `138d:24e0`）與**戰術（技能 7）／怪物學識（技能 25）**兩道閘門 —— 那格不接這兩個技能就永遠沒效果 |
| [`91-trap-detect-disarm-chain.md`](docs/re/91-trap-detect-disarm-chain.md) | **陷阱偵測／解除整條鏈**（`L` ＝ 動作 `0x07`）：技能 10／11 的閘門、25%／100% 兩條分支、「已注意」的存放位置；**手冊每個數字都對得上**；掃描距離 ＝ 光源強度 |
| [`90-pool-tile-35.md`](docs/re/90-pool-tile-35.md) | **tile `0x35` 不是牆，是一天七次的治療水池**（動作 `0x11` ＝ `222f:37c4`）—— 推翻 `docs/re/05` §3 的「寫死的阻擋」與引擎裡的 `TriggerHardBlock` |

### 資料格式 `docs/formats/`

| 檔案 | 內容 |
|---|---|
| [`event-script.md`](docs/formats/event-script.md) | `DATA*.TXT` 事件表 |
| [`game-data-tables.md`](docs/formats/game-data-tables.md) | `MONSTER.DAT`、`ITEMS.DAT`、`PARTY.DAT` |
| [`town-and-map.md`](docs/formats/town-and-map.md) | 城鎮、地圖、`EXITS.DAT`、`SUM.MAP` |
| [`graphics.md`](docs/formats/graphics.md) | CGA / EGA 素材格式 |
| [`resource-index.md`](docs/formats/resource-index.md) | `FILES.DAT` / `FILES.DTT` |

### 試玩紀錄 `docs/playtest/` — 實機跑出來的東西

**最高階的 oracle。** 版面溢出、輸入順序、「解完但沒接」這三類問題
單元測試看不到，只有實跑抓得到（`rulebook/60`）。

| 檔案 | 內容 |
|---|---|
| [`01-harness-and-first-run.md`](docs/playtest/01-harness-and-first-run.md) | headless 驗收流程、`playthrough.sh`、軌跡檔為什麼非有不可 |
| [`02-autofight-first-full-leg.md`](docs/playtest/02-autofight-first-full-leg.md) | `-autofight`：第一段主線真的走完 |
| [`03-first-map-change.md`](docs/playtest/03-first-map-change.md) | 第一次換地圖 |
| [`04-world-map-sites.md`](docs/playtest/04-world-map-sites.md) | 世界地圖的地點格 |
| [`05-route-in-go-and-the-way-out.md`](docs/playtest/05-route-in-go-and-the-way-out.md) | 找路搬進 Go（`cmd/dwroute`）—— 規劃與遊戲用同一份判定 |
| [`06-the-real-opening.md`](docs/playtest/06-the-real-opening.md) | **前四輪都從遊戲中段開始玩**：出貨 `PARTY.DAT` 是玩過的存檔 |
| [`07-character-creation-and-ten-daggers.md`](docs/playtest/07-character-creation-and-ten-daggers.md) | 建角流程、強制建滿五人 |
| [`08-battle-had-no-consequences.md`](docs/playtest/08-battle-had-no-consequences.md) | 戰鬥傷害沒寫回、全隊死亡 |
| [`09-traps-wired.md`](docs/playtest/09-traps-wired.md) | 陷阱七種傷害實機驗收 |
| [`12-empty-battles-and-the-ship-you-already-own.md`](docs/playtest/12-empty-battles-and-the-ship-you-already-own.md) | **每一場遭遇都是空的**：建角沒把人放進陣型（`Formation.AddMember` 是本輪第二個零呼叫端），所以新遊戲的隊伍永遠打不了仗 —— 四場仗、四個數字一個都沒動。順帶查出**新遊戲就有一艘船**（地圖 1 的 (13,51)，36 步走得到），買船是備案不是必經 |
| [`11-a6-leg1-and-the-silent-zero-sp.md`](docs/playtest/11-a6-leg1-and-the-silent-zero-sp.md) | **A6 第一段**：建角 ＋ 陸路 101 步到新格里昂（買船的城鎮）。挖出「施法職業初始法力永遠是 0」—— 保守的預設值把 120 點法力那條路擋住了，原版的規則是單一個 `職業 > 2` 的比較（連盜賊都有法力）|
| [`10-world-edge-was-never-wired.md`](docs/playtest/10-world-edge-was-never-wired.md) | **世界只有一格**：`CrossEdge` 解完沒有呼叫端。加上跨圖規劃器之後量出「沒有船主線走不完」（陸路 4 張 vs 含航海 23 張）、破關在 220 步外、41／51／52 進不去（未解）|

### 研究與計畫

| 檔案 | 內容 |
|---|---|
| [`PLAN.md`](PLAN.md) | 專案計畫、待驗證項、階段分解、風險表 |
| [`README.md`](README.md) | 對外門面、文件索引、授權聲明 |
| [`docs/research/ssi-engine-architecture.md`](docs/research/ssi-engine-architecture.md) | **SSI 引擎架構研究** — 對 Gold Box 系列可能有用 |

### 翻譯

| 檔案 | 內容 |
|---|---|
| [`translations/glossary.md`](translations/glossary.md) | **統一譯名表** — 所有翻譯的唯一依據 |
| [`translations/LOCALIZE_INSTRUCTIONS.md`](translations/LOCALIZE_INSTRUCTIONS.md) | 遊戲內文字的翻譯指令（格式、硬規則、語氣），多人／多 agent 分頭翻前必讀 |
| [`docs/i18n/demon-int-strings.md`](docs/i18n/demon-int-strings.md) | `DEMON.INT` 的 840 條 UI 字串清單（**參考用，重製版不讀這個檔**）|
| `docs/manual/part-0..4.md` | 官方手冊（SSI 英文版）繁中翻譯 |
| [`docs/manual-cht/`](docs/manual-cht/) | **軟體世界 1990 年中文說明書**全 51 頁轉錄＋考據。`README` 出處與驗證價值、`01` 正文、`02` 附錄 A–E 全表、`03` 當年譯名與誤譯清單 |
| `docs/walkthrough/part-1..6.md` | 社群攻略繁中版 |

### 工具與 skill

| 路徑 | 內容 |
|---|---|
| `.claude/skills/ghidra-dos16-re/` | **16 位元 DOS Ghidra 反組譯 skill** |
| `tools/ghidra_headless.sh`、`tools/ghidra_scripts/` | Ghidra 封裝與 post-script |
| `tools/dosbox_run.sh` | DOSBox 封裝 |
| `tools/parse_*.py` | 各資料格式的 Python 解析器 |
| `tools/xref.py` | `seg:off` ↔ 檔案位移換算、誰呼叫／誰指到、switch 跳表 |
| `tools/fieldscan.py` | **掃某個結構欄位的所有存取端**（`fieldscan.py 0xa9 0xbe`）。用 ModRM `mod=10` + disp16 做 byte-level 比對，不靠線性反組譯對齊 —— 線性掃描在資料段會錯位漏掉大半。**「有幾處寫 0」這種普查是拿來定語意的**：`+0xbe` 十一處存取沒有一處寫 0，一句話就定案它是單向閂鎖。⚠ **只掃常數位移** —— 偏移放在暫存器的存取（`mov es:[bx+si], al`，隊伍結構就有 20 處）掃不到，所以「**某欄位沒有寫入端**」這種否定式斷言不能只靠它（`docs/re/80` §3 踩過）|
| `tools/go.sh`、`tools/screenshot.sh` | docker 內編譯／測試；headless 跑起來送鍵截圖 |
| `tools/tileatlas/`、`tools/cgaatlas/` | 把 `DEMON.SHE`／`DEMON.SHP` 整套圖塊排成**帶編號的一張圖**。`tileatlas -gamepal` 用原版調色盤（`gfx.GamePalette`）、`-revplanes` 反轉 plane 順序 —— 兩個旗標就是 `docs/re/97` 那場「顏色錯還是佈局錯」的判準工具。**要對某個 tile 編號講話時先出一張 atlas**，比逐格猜快得多 |
| `tools/cropzoom/` | 裁一塊、放大、印顏色直方圖。比對實機截圖與自家渲染時用；直方圖是關鍵 —— 肉眼分不出「深棕」與「暗紅」，數字分得出 |
| `internal/`、`cmd/` | Go 引擎（assets 解碼、game 規則層、ui 呈現層、demonwinter 執行檔） |

---

## 4. 術語表（Ubiquitous Language）

專案內外一致使用這些詞，寫程式碼、文件、commit 都照這個來。

### 遊戲資料

| 術語 | 意義 |
|---|---|
| **事件表** | `DATA1..5.TXT`，地城的「房間描述 + 遭遇怪物」宣告式表格。**不是 bytecode** |
| **dataset** | 一個地城／區域，切換時 `EXITS.DAT` 會被整份覆寫 |
| **sub-map** | `SUM.MAP` 內的 23 個 RLE 壓縮地圖之一，依 map ID 取用 |
| **slot（戰鬥）** | 戰鬥場上的 15 個單位槽位，0–6 怪物、7–14 玩家 |
| **slot（道具）** | 角色道具欄的 10 格，每格 17 bytes，從記錄內 **`0x0c`** 起 |
| **trailer** | `PARTY.DAT` 尾端 194 bytes 的隊伍共用資料區，從 `0x514` 起 |
| **frame（sprite）** | `.SHP`/`.SHE` 的單張圖。**CGA 16×16（64 B）、EGA 16×28（224 B）**，兩套 frame 數一一對應 |
| **glyph** | 字型單字。CGA `.FNT` 8×8 packed 2bpp（16 B，96 字 ×2 bank：一般／反白）、EGA `.FNE` 16×14 1bpp（28 B，96 字 0x20–0x7F）|
| **plane** | EGA 的位元平面。`.SHE` 是「列內 4 個 plane 各佔連續 2 bytes」（檔案）／「各佔連續 4 bytes」（載入加倍後的記憶體） |
| **載入時加倍** | EGA `.SHE` 讀進記憶體後被 `FUN_217b_0adf` 就地水平加倍（224 B → 448 B/frame）。`.PIE` 則在 blit 時加倍。EGA 素材一律「檔案存半寬、顯示時寬 ×2」 |

### 逆向工程

| 術語 | 意義 |
|---|---|
| **位址換算公式** | `file_offset = (segment − 0x1000) × 16 + offset + 0x3C00` |
| **間隙（gap）** | Ghidra 自動分析沒展開的程式碼區域，decompiler 會在上面編造控制流 |
| **跳表 override** | 用 `JumpTable.writeOverride()` 告訴 decompiler 正確的 switch 目標 |
| **oracle** | 判定正確性的依據來源，有優先序（見 §5） |
| **字串錨定** | 用語意明確的字串反查引用它的函式，找主邏輯的主要手段 |

### 規格分級

| 級別 | 意義 |
|---|---|
| **READY** | 行為、公式、邊界條件、驗收方式都明確，**可以實作** |
| **DRAFT** | 主要行為清楚，但有缺口，**需先補齊** |
| **BLOCKED** | 有已知未解問題擋著，**不可實作** |

---

## 5. 新 session 必須知道的關鍵事實

### 引擎本質

`DEMON.INT` 是**原生 8086 機器碼，不是 bytecode**。`.INT` 只是 SSI 的
「interpreter」命名慣例。這遊戲**沒有虛擬機** —— 控制流全寫死在機器碼裡，
資料檔只提供內容素材。所以重寫時要做的是「事件表直譯器」，不是 VM。

`DEMON.EXE`（6 KB）只是 loader，用 DOS EXEC（`INT 21h, AH=4Bh`）啟動 `DEMON.INT`。

### oracle 優先序（由高到低）

1. **人親自實測原版**
2. **DOSBox 原版實跑**（`tools/dosbox_run.sh`）
3. 官方手冊 / 社群攻略
4. 反組譯推論

**低層級不可推翻高層級。** 反組譯看起來再合理，被原版實跑打臉就是反組譯錯了。

> ⚠ **翻譯本不是獨立來源。** `docs/manual-cht/`（軟體世界中文說明書）是英文手冊的中譯，
> 它寫的數字與英文版相同時，只代表翻譯忠實，**不能當第二個佐證**——
> 「一天 26 小時」就是這樣一個誤判陷阱。
> 它的價值在轉錄層：同一份原始資料的兩份中文抄本同時對程式碼，抄錯的一方會現形。

### 三個反覆咬人的陷阱

**① decompiler 會靜默捏造控制流。**
Ghidra 對某些函式只展開前段，跳表後的 case body 落在間隙裡，
decompiler 不報錯反而編造。**已造成三次錯誤斷言，每次都被標成「已驗證」**。
判別法：反編譯行數遠大於宣告 byte 數、或大量 unreachable 警告 → 不可信。
含跳表的 switch 一律回原始指令讀。

**② 算術對不代表方向對，也不代表切分單位對。**
檔案大小的算術關係只約束「總位元數」。本專案踩過六次：
`OPEN.PIC` linear vs 交錯、`SUM.MAP` column-major vs row-major、
CGA sprite 32×16 vs 16×32、`.SHE` 16×56 vs 32×28、
**sprite frame 尺寸整批錯**（CGA 實際 16×16、EGA 實際 16×28），
以及 **`SUM.MAP` RLE 少解 256 格**（次數 0 = 256、區塊首 byte 要跳過）。
**錯誤版本幾乎都能通過全部測試**，有兩次還畫得出「看起來像那麼回事」的圖。
→ 視覺產物一律 dump 出來肉眼比對原版；一整批佈局假設全數失敗時，
回頭懷疑的應該是**切分單位**，不是佈局。

**②-b sprite 的專屬判準：每個 frame 必須自成一體。**
解出來若是「一格裡有 2 個或 4 個並排／堆疊的小圖」，
那是尺寸解錯把相鄰 frame 疊起來了，不是美術打包慣例。
這個徵狀出現過兩次，兩次都被合理化成「美術資源本來就這樣打包」，
於是錯誤結論頂著「已驗證」標籤活了下來。

**③ 「已驗證」標籤本身要存疑。**
特別是任何以反編譯 C 為唯一依據的結論。

**④ 最有效的檢查是「兩份互不相干的資料要對得上」。**
`SUM.MAP` 的 RLE 少解 256 格，格數、值域、size 加總、ASCII 圖全都照樣過關，
活了好幾輪。抓到它的是**城鎮座標表** —— 那張表在執行檔的程式碼段，
`SUM.MAP` 是資料檔，兩者沒有共同的錯誤來源，所以 25 個座標有沒有落在
城鎮格上是一個**沒得混過去**的問題（錯的時候 0/25，對的時候 25/25）。
→ 解開一份資料之後，優先找「另一份獨立資料能不能印證它」，
而不是再加一條自己驗自己的測試。

### 已被推翻的斷言（不要重蹈）

| 斷言 | 實際 |
|---|---|
| `startBattle` 的註解：「入隊會找第一個空格、**建角與新遊戲都會把人放進陣型**，所以『不在陣型裡』只會是刻意的」| **建角完全沒碰陣型。** `ApplyNewGame` 把九格寫成 `0xff`（照原版），而建角那一側沒有對應的 `AddMember` —— 結果是新遊戲的隊伍**每一場遭遇都只有怪物**，一回合結束、沒傷害沒金幣，畫面與軌跡都正常（`docs/playtest/12`）|
| `docs/playtest/10` §5.1：「買船不是選配，是主線必經」| 量測沒錯（陸路只到 4 張子地圖），但**新遊戲就給了一艘船**（`0x14956`–`0x1497a`：地圖 1 的 (13,51)，船體 67）。從起點 36 步走得到。買船（570–680 金）是備案 |
| `docs/spec/05-character.md`：「初始 SP 依職業而異，只實測了巫師（＝智力）與遊俠（＝0），其餘 8 個職業未測」＋ 實作把未測的一律給 0 | **原版是單一個 `cmpw $0x2 / jle`**（`0x14f40`）：職業索引 0–2 沒有法力，3–9 全部等於智力，**連盜賊都有**。舊註解推測「依是否天生具備符文／吟唱魔法決定」會把盜賊排除。<br>更重要的是**那個保守值把主線擋住了**：破關要 120 點法力，一支隊伍只有巫師有法力怎麼練都湊不到。「未測就給 0」不會報錯，只會讓某條路走不完（`docs/playtest/11`）|

| 曾經寫過 | 真相 |
|---|---|
| A8 是「把五張 `nSS.DAT` 的類別 1／2 逐筆對回 `DATA*.TXT`，判斷哪些該 `Consume`」的**分類工作** | **題目本身錯了。** 分類寫在**類別編號**裡：類別 1 一次性、類別 2 可用 `R` 重讀，105 筆一筆都不用判。而且類別 1／2 **都不會 `Consume`**，兩者的寫回動作都是 `MarkVisited`；`Consume` 的四處全在別條路上（戰鬥結算兩處、陷阱解除一處、`25be:06fc` 一處）。缺的是一道**觸發閘門**，不是一張表（`docs/re/104`）。<br>**教訓：待辦寫成「逐筆判斷」時，先確認那個判斷真的沒有被編碼在資料裡。** 這一條的答案整整躺在 `docs/re/05` §1.5 那段早就完整反編譯的程式碼裡兩年沒人讀出來 —— 反編譯貼上去不等於讀懂了 |
| 護甲防護值「是手冊記載的 1–5，對應 `ITEMS.DAT` 索引 8–12，所以**防護值 = 索引 − 7**」（`character.go` 舊註解）| **是查表不是等差**（`docs/re/103` §4）。表在 `31f0:16c5`，七個 byte `00 01 02 04 05 06 03`，索引 = 型別 − 7 → 布 1／皮 2／**鎖 4**／**鱗 5**／**板 6**，外加槽位的附魔直接相加。舊算式從鎖子甲起每件少 1 點。<br>這張表在 `docs/re/06` §545 掛了很久（「未逐一核對」），從沒人回去讀那七個 byte。**教訓：頭兩項對得上等差，不代表第三項也是** —— 老軟體的數字多半是表查出來的。<br>⚠ 手冊 `part-3.md` 的舉例說板甲吸收 5 點，與表差 1；以出貨的 DOS 資料為準，差異記在 `docs/re/103` §4 |
| `+0xe6` 是**盾牌旗標**（`docs/re/06` 行 433）／**逐件裝備累加的裝備旗標**（`docs/re/57` §4.2）| **兩份筆記讀錯同一個位移。** 那是技能旗標陣列的第 30 格（`0xc8 + 30`），也就是**硬化皮膚**（`docs/re/103` §2）。判準有三：位移剛好落在技能陣列最後一格、那個檢查**只跑一次不在迴圈裡**（角色只有一件護甲）、以及**這遊戲的 30 件道具裡根本沒有盾牌**。<br>值也對得上手冊：`+2` 正好等於防護表裡的皮甲，而手冊說硬化皮膚「額外提供皮甲等級的防護」 |
| 施法選單「把 43 筆法術列給每個人」是可接受的簡化 | **那讓八項技能完全沒有效果**（12 幻術／13 附身／14 召喚／17–21 五系符文），學院收錢教的東西學了等於沒學（`docs/re/103` §1）。閘門的換算怪物 AI 那側早就有了（`aiSchoolSkill`），從頭到尾只差沒接到玩家這一邊。<br>**教訓：「某項技能沒有呼叫端」有時不是缺一條規則，是缺一道共用的閘門** —— A9 表上那幾列各自看像獨立工作，實際上八項是同一個修法 |
| 「`+0xb9` 寫 1 的地方找不到，而攻略證明它存在」（`docs/re/80` §3 掛了兩輪的衝突）| **找到了，在地點劇情 case 8（恆世寶珠）**（`docs/re/101` §3.1）。寫入端是送道具常式的 `mov byte es:[bx+si],1`，而 **`bx` 的基底是「`party + 0xb3` 算好之後存進區域遠指標」、`si` 是 param** —— param 6 就落在 `+0xb9`。`fieldscan.py` 掃不到是因為那一處的 disp16 是 **0**，不是 `0xb9`；`docs/re/80` §3 追過的 20 處 `mov es:[bx+si], al` 也不含它（那些的基底是結構本身或區域陣列）。**第三種形狀：結構加常數之後存成遠指標。** 而 `docs/re/81` 從預言第一行 `The Orb of Evertime now is yours` 推測「寫入端在寶珠事件裡」——**那個推測完全正確**。<br>連帶定案：`+0xb9` 同時是「寶珠拿過了」與「劇情階段」，**是刻意共用不是溢位** |
| 估價第四／五／六項的浮點常數是**係數**（`1.4 × |n| × 250`、`1.7 × |n| × 350`，`docs/re/49` §2／§4）| **是指數**（`docs/re/102`）：`|n|^1.4 × 250`、`|n|^1.7 × 350`。原版呼叫的 `2000:78dc`（`0x1b4dc`）是**手寫的 pow** —— `log`（`3000:094c`）→ 乘上那個 double → `exp`（`3000:0cba`），開頭還有一道 `n == 0 → 0.0` 的守衛，那是 `exp(y·ln x)` 才需要的。<br>`docs/re/49` §4 花一整節論證「不能用 1.4 × 250 = 350 的整數捷徑」—— **推理沒錯，前提錯了**，它假設那是乘法。<br>**能裁決是因為附魔費用把這一項乘上 19/10 放大了**：攻略 80 個點，指數版 69 個完全相同、11 個差 1；係數版第一級就是 1130 vs 665，**差七成**。單看估價本身，小 n 時兩者差得不多，而本專案至今沒有任何估價的外部對照數字。<br>**教訓：「這個浮點常數是係數」是假設不是事實** —— 要看那支函式做什麼，不是看常數長什麼樣子 |
| 佈陣時「陣型裡沒有他就散到中心附近」是一層無害的保險（`startBattle` 的註解：**本專案不讓人憑空消失**）| **那層保險把一整套機制廢掉了。** 十間試煉室的「單挑」正是靠「把 3×3 陣型清空、只填回符合職業的人」做出來的（`docs/re/101` §2）—— 保險一開，五個人全上場。實跑抓到（遊俠的試煉室出現五個「我」）之後改回原版：**不在陣型裡就不上場**。<br>那個風險本來也不存在：入隊會找第一個空格、建角與新遊戲都會把人放進陣型。<br>**教訓：寫「本專案不讓 X 發生」這種保險時，要寫下它擋掉了哪些可能性** —— 這一條擋掉的剛好是原版用來做單挑的手法 |
| `Consume`（一次性事件用掉自己）之後那一格「不會再命中任何分支」| **會掉到 `DATA*.TXT` 路徑，顯示第 0 筆房間敘述。** 屬性清 0 → 類別 0，而 `TriggerLookup` 沒有擋類別 0，`hit.EventIndex` 就變成 0。實跑抓到：走回過關的盜賊試煉室，蹦出「你走進一間詭異的停屍間…」—— 一段與那一格完全無關的文字。<br>**陷阱解除那條路（`traps.go` 的 `Consume`）一直有同一個病**，只是從來沒人踩回去看。<br>判準來自出貨資料：`1SS.DAT` 的類別 0 共四筆，**四筆屬性都剛好是 `0x00`** —— 「屬性 0」就是作者標「這一格沒事」的寫法。已加 `SpecialTile.Dead()` |
| 鐵匠鋪的選人畫面「實跑驗過」（`docs/re/65` §3.2 記了七個欄位逐一對上）| **流程對，輸入順序是壞的。** 選人游標用 ↑↓，而世界地圖的移動迴圈也吃方向鍵，而那個 block 排在移動**之後** —— 按一次 `Down` 游標動了、**隊伍也走了一步**（`docs/re/99` §4.1，trace 抓到 (33,31) → (33,32)）。當初的實跑只按 `Enter`（選第一個人），**從來沒送過方向鍵**。<br>**判準：一個畫面有幾個按鍵就要送幾種按鍵。** 只驗 happy path 的那一鍵等於沒驗輸入層；而畫面看不出來（選人頁蓋掉了地圖），單元測試也看不到（它測規則層不測輸入順序）。同記憶 `screen-bugs-tests-cannot-see` |
| `docs/re/62` §3.2：光之環的門「本專案在移動前擋，玩家看到的結果一樣，是**刻意的差異**」| **只在時序那一維成立。** 實作是 `mapID == 5 && 符印沒解完 → 擋`，**沒有比對座標** —— 效果是符印沒解完就在整張地圖 5 一步都走不了。原版只在繞著 (11,50) 的四格十字判定（`5SS.DAT` 類別 5 值 15，剛好四筆）。**那是錯，不是差異**（`docs/re/98` §1）。<br>兩條教訓：① **宣告「刻意的差異」時要把維度寫清楚** —— 只驗了時序就說等價，範圍的錯就被那句話蓋住一整輪；② **偵錯旗標會把驗收路徑繞過去** —— 主線截圖全帶 `-glyphs`，那個閘門從沒被執行過，與 `rulebook/63` 的陳舊標記同形狀 |
| EGA 素材用**標準 EGA 16 色表**解（`.SHE` 檔裡沒有調色盤，所以沿用出廠值）| **原版開機時把 16 個調色盤暫存器整組換掉了**（`docs/re/97`）：表在 `ds:0x14b7`（檔位移 `0x26fb7`），`2cdc:16b7` 用 `INT 10h AH=10h AL=00h` 逐格設，只在 EGA 模式（`ds:0x19fb`）。七格不在標準表裡。用標準表解出來的地形**看起來像雜訊**（沙地變黃、水變紅、樹幹變青），本專案因此把一整批正確解出的圖塊誤判成「解碼失敗的格子」。**找到它靠的是掃資料不是掃程式碼** —— `0x3c0` 與 `INT 10h AH=10h` 在反組譯裡零命中，而 `grep "0x14b7"` 也零命中（位址是以立即數載入 `b8 b7 14`，要掃機器碼位元組）|
| `DEMON`／`WINTER` 的 102 格「不是角色 sprite，是地形圖塊集」（`docs/formats/graphics.md` §4.4，標**已驗證**）| **混合的**：24–25／27–28／30–31／33–34 是**隊伍 glyph**（四面向各兩格走路動畫）、63–66 是**船**，其餘才是地形。原版把 glyph 索引直接寫進地圖繪製緩衝區（`222f:0bbc`），與地形共用同一條繪製路徑，所以索引空間共用是設計不是意外 |
| 手冊：靈視技能「每天只能使用一次」（兩節都這樣寫）| **DOS 版是三次**（`docs/re/93` §4.1，DOSBox 實跑複核）。手冊描述的是 **Apple II 版**。⚠ oracle 優先序裡手冊高於反組譯，但**前提是同一個平台的建置** —— 這款遊戲的手冊與 DOS 執行檔在這一條上就是不同規則。套手冊之前先問「這一頁講的是哪個版本」|
| `docs/playtest/09` §5：「這支隊伍剛好也沒有察覺陷阱技能，所以 `L` 的 25% 分支實際上有跑到」| **兩句都錯，結論還是反的。** 出貨存檔的 Podgom 職業 5 **就是盜賊**、技能旗標 `[1, 10]` 而 `SkillDetectTraps = 10` —— 他**有**察覺陷阱。所以那次走的是 **100% 分支**，25% 那條反而沒被驗到。同一段的「隊伍裡沒有盜賊」也錯（錯在把職業當成技能；正確說法是沒有人會**解除陷阱**技能 11）|
| 出貨存檔「有三個矮人、光照 0」（本輪 `docs/re/97` 一度寫進驗收表）| **光源 1、一個精靈＋四個人類、沒有矮人**。錯誤來源是為了「看一眼」手寫的 `PARTY.DAT` 解析（步長／偏移漂掉）—— 與 `one-implementation-per-rule` 記的第 2 個案例同一個坑。**判準：問已經逐位元組驗過的載入器（`scenario.LoadSaveGame`），不要重抄一份。** 另一個誤導來源是狀態列的「光照」印的是 `clock.Light()`（只是戶外時辰值），在地城裡與生效的內縮量無關 —— 已改成兩邊共用 `app.viewInset()` |
| `ds:0x4e32` ＝ 隊伍面向（`docs/re/08` 已驗證）—— 於是在 `FUN_222f_0003` 裡讀成「面向北時視野變差」| **同一個位址在不同函式裡語意不同**。`0x4e32` 全檔有 **40 個寫入點**、跨 `138d`／`1990`／`17c5`／`222f` 四段，是共用暫存字。在 `FUN_222f_0003` 裡它是「**隊伍裡有活著的矮人**」（`222f:002c`／`0053` 掃五個隊員，種族 `+0xf5 == 2` 且戰鬥狀態 `+0x102 <= 1`）—— 也就是**黑暗視覺**。判別法：**數寫入點**，40 個跨五段就不可能是專用欄位。同 `0x52f4` 那個坑（`docs/re/97` §5.2）|
| 「原版的隊伍 glyph 動畫表（`0x210f`）尚未解讀」| 解了（`docs/re/97` §3）。`ds:0x210f` ＝ `1e 1b 18 21`（北30／東27／南24／西33），`glyph = 表[面向] + (行進軸座標 & 1)` —— **東西向看 X、南北向看 Y**，走一步換一格，不需要計時器。搭船是 `0x3f + 面向` |
| `EREGORE.TXT`／`WIN.TXT`／`T.TXT`「在這份執行檔裡沒有載入路徑」（`docs/re/70` §4）| **有**（`docs/re/82`）：`25be:19d1` 用 `mode` 索引 `ds:0x2f59` 的 3 筆表。而且原本「用算出來的索引存取」那個推測**是對的**，`docs/re/71` 收回錯了 —— 收回的理由是「基底掃不到」，但基底**出現了**，形式是 **ModRM 的 disp16**（`ff b7 59 2f`）不是立即數。**索引式存取根本不需要把基底載進暫存器** |
| 「函式裡沒有字串引用 ⇒ 查不出它演哪一段劇情」（`docs/re/77` §6）| 文字可以在**資料檔**裡。`25be:1ae2` 沒字串，但它呼叫 `25be:19d1(9, 0)` ＝ `EREGORE.TXT` 第 9 頁 |
| 「`+0xb9` 從 0 出發永遠動不了，整條劇情鏈到不了」| **DOSBox 十分鐘就推翻**（`docs/re/81`）：把 `+0xb9` 改成 1 再睡一覺，第二場夢照播。普查沒錯（19 處確實沒有寫 1），錯在**把「掃不到」讀成「不存在」**。裁決成本是改三個 byte + 一次 DOSBox，**比繼續擴大靜態掃描便宜一個量級** —— 卡在靜態層時先問「有沒有辦法直接跑一次」 |
| `docs/re/26`：原版的紮營鍵沒查過 | 是 `C`。**手冊 §407 就寫著**「隨時都可按 `C` 紮營休息」，而且手冊有一節叫「控制方法」，開宗明義說「選單指令一律鍵入該選項的第一個英文字母」。`docs/re/54-reference-comparison` §13 的 DOSBox 指令列也本來就是 `key:c`。**問「原版按什麼鍵」時，手冊是第一個該翻的地方，不是反組譯** |
| 「`+0xb9` 找不到寫 1 的地方 → 一定是 `fieldscan.py` 只掃常數位移的盲點，計算式寫入（20 處 `mov es:[bx+si], al`）裡有」| **追完那 20 處，這個解釋不成立**（`docs/re/80` §3）：imm 型的 6 處寫 0／0xff，register 型的 7 處有兩處基底其實是區域指標、剩五處**全在操作 3×3 陣型格**（`si` 值域 0–8，碰不到 `0xb9`）；loader 也查了。**「找不到就一定是工具有盲點」本身是偷懶的結論** —— 提出這種假設之後要真的去追。目前這是一個**記著的衝突**（反組譯普查 vs 攻略），裁決方式是 DOSBox 實跑 dump `+0xb9` |
| tile `0x5b` 是「資料在、路徑沒接」的第五個（`docs/re/74` §1）| **接得好好的**（`docs/re/79`）：它走 `222f:3233` **前半段**的分支群，不是後半段那四個設施比較。教訓：一支函式裡有兩組針對同一個變數的比較時，只讀其中一組會得出「沒接」的錯誤結論 |
| 五個地點 tile（`0x25`/`0x26`/`0x2e`/`0x5b`/`0x64`）「tile 值本身就是 `DATA*.TXT` 記錄索引」| 是**設施選擇器**（城鎮／神殿／學院／廢墟），不是文字索引。錯得剛好沒人發現：`0x25` ＝ 37 而 `DATA1.TXT` 只有 35 筆，取不到就什麼都不顯示 —— **一個越界索引把 bug 藏成了「這格沒事」** |
| `EXITS.DAT` 是 165 筆 2-byte 座標對 | ~~110 筆 3-byte `[X,Y,type]`~~ → **兩種說法都錯**：是 **55 筆 6-byte** `(來源X, 來源Y, 目的子地圖, 目的X, 目的Y, +0xaf)`，也就是**跨地圖出口表**（`docs/re/78` §6）。3-byte 那個判讀屬於 **`nSS.DAT`**，被誤套過來。**資料自己證明切法**：出口成對（A→B、B→A，目的地差一格），6-byte 讀出 40/55 對，3-byte 讀出 **0** 對，其他欄位順序也全是 0 |
| `0x5514:0x5516` 指向的資料就是 `EXITS.DAT`（`docs/re/05` §1.3，**標已驗證**）| 是 **`nSS.DAT`**（511 bytes，`sprintf("%dSS.DAT", 子地圖)` 載入）。`EXITS.DAT`（330 bytes）進的是 `[0x4c94]`。**能戳破它的硬證據就在同一節的引文裡** —— 兩個載入呼叫的大小參數一個 `0x14a`、一個 `0x1ff`，從沒被對照過。「已驗證」標的其實是「字串 → 載入呼叫」那一段，最後一步「另一個指標指向同一份資料」是假設 |
| `FUN_222f_1321` 的類別 3–7（含類別 4 傳送）「程式碼存在但現有資料永遠走不到」| 拿錯檔案驗的（330 bytes 剛好能被 3 整除，所以錯的假說看起來自洽）。`1SS`–`5SS.DAT` 裡**類別 0–6 全部出現**，類別 4 共 14 筆，而且「類別 4 筆數 ＝ 檔尾反向座標對數」5/5 零誤差 |
| `nSS.DAT`／`ALL_SS.DAT` 是「同一份資料的兩份建置產物，1、2 號沒同步」| 是**母本 vs 工作副本**（`docs/re/78`）。新遊戲把 `ALL_SS.DAT` 切成 5×512 重寫五個 `nSS.DAT`；遊戲中會把改動寫回 `nSS.DAT`。出廠的 `1SS`／`2SS` 是**玩到一半的狀態**，29 處差異只有三種型態且**三種都對得上程式碼的改寫指令**。「差異全部落在第 3 欄」這個觀察本來就是最強的線索 —— 只有屬性欄是執行期會寫的 |
| `ds:0x52d4` ＝ 子地圖巢狀深度，「深度計數器結案」（`docs/re/76`）| **結案下得太早，改標未定案**（`docs/re/78` §5）。`0x10713` 拿 `ds:0x52d4 × 6` 索引船隻陣列（stride 6 是 `docs/re/28` 獨立驗證過的），用深度講不通；而上船 `inc`／下船 `dec` 同樣會成對。教訓：**一種存取形狀不代表全部用途** —— 30 處存取裡挑 3 處 `inc`／`dec` 就結案，漏掉那一處 `× 6`。定案前先問「有沒有哪一處我解釋不了」|
| `nSS.DAT` 是開場序列資料 / 畫面繪製指令清單 | 是**子地圖 n 的特殊格清單**（事件 + 傳送）。`docs/formats/resource-index.md` 的**結構**分析（3-byte `(X,Y,屬性)`、高 3 bit 分類）全對，錯的只有用途 —— 那一項當初的建議是「跑 DOSBox 看開場動畫」，方向從一開始就偏，因為用途猜測先於機制定位 |
| `FUN_307c_0005(a, b, n)` 的 `a` 是目的 | **`a` 是來源、`b` 是目的**（反編譯裡 `*param_2 = *param_1`）。`docs/re/05` §3 註反了，於是把「存出去」讀成「載入進來」|
| `party+0xa9` 是「`docs/re/02` 已知的自動接續重繪旗標」| **假引用** —— `docs/re/02` 全文沒有 `+0xa9`。它是 `nSS.DAT` 的**快取有效性旗標**（`docs/re/77`）|
| `FILES.DAT` 是資源索引 | 不是；`FILES.DTT` 才是字串池 |
| `TEMPLAT*.DAT` 是角色範本 | 是特定地點的特殊道具資料 |
| `PARTY.DAT` 的 `0x0c` 是種族 | **種族在 `0xf5`**。`0x0c` 是**道具槽陣列的起點**（一度誤判為「裝備武器槽索引」，那個在 `0x100`）|
| 戰鬥選單 case 0xa 是 Attack | 是 Examine；Attack 是 case 5 |
| EGA 素材都是 320×350 four-plane | 只有部分成立。EGA 素材的規則是「檔案存半寬、顯示時寬 ×2、高 ×1.75」|
| sprite frame 是 CGA 16×32、EGA 32×28（448 B）| **CGA 16×16（64 B）、EGA 16×28（224 B）**。錯在兩點：(1) 只讀 blit 端 `FUN_217b_07cf`，沒發現它讀的緩衝區在載入時已被 `FUN_217b_0adf` 水平加倍過，把記憶體 stride `0x1c0`(448) 當成磁碟格式；(2) 算術分不出來 —— 16×32 與 16×16 的位元組數都整除全部檔案，「3.5× 規律」兩種讀法都成立。真憑據是遊戲初始化時自己宣告的 `[0x5226]`（EGA `0xe0`=224、CGA `0x40`=64）並用它乘出各檔大小。詳見 `docs/formats/graphics.md` §4 |
| `CYPHER.SHE`/`CYPHER.SHP` 是 frame 尺寸的特例 | **不是特例，224/64 才是常態**。它只是 frame 數 27 為奇數，才無法被（錯誤的）448/128 整除，於是被當成特例硬湊過去 |
| `0a95`/`0b19` 位元展開家族與 `.SHE` 路徑無關 | 它們不在 **blit** 路徑上，但在 **載入** 路徑上（`FUN_1d9f_0a8b` → `0adf` → `0b19`），做的是水平像素加倍 |
| 一個 sprite frame 內裝 2（CGA）／4（EGA）個小圖是美術打包慣例 | 是解碼錯誤造成的假象 —— 相鄰 frame 被疊進同一格 |
| RNG 用 80 位元擴充精度 | IEEE double（53 bit） |
| 城鎮經濟的程式碼「落在 Ghidra 未發現區域」 | 只對一半。Ghidra 有線性掃描出指令，只是**沒建函式邊界、decompiler 沒被觸發**。改讀 `disassembly.asm` 的函式間隙（含 segment 開頭第一個命名函式**之前**的區域）就能讀（`docs/re/19` §1） |
| 角色記錄 `+0x102` 是戰鬥狀態位元旗標 | 是**單一列舉值**：`0`正常／`1`中毒／`2`,`3`,`4`束縛三級／`5`死亡 |
| 角色記錄裡「職業（Class）完全未找到」 | 在 `+0xf6`（0–9），兩個獨立呼叫點都拿它當技能學費表的欄索引 |
| EXP 是 3-byte 欄位 | **儲存是 4 bytes**（`0xc4`–`0xc7`），只是數值封頂在 `0x00FFFFFF`。兩種說法各對一半 |
| 傳送法術判的是 `spell_school_id(0x4e2c) == 0x11` | 判的是 **`effect_type`（`0x4e2e`）**。`0x4e2c` 全檔只跟 `1`/`4`/`6`/`8` 比較，值域到不了 `0x11` |
| 枯萎打擊可能是連續呼叫三次 `type 3/4/6` | **不是**。它是獨立的 `effect_type 14`，且套用方式不同 —— 三屬性各自「以現值為上限重擲」，不是扣減 |
| 束縛狀態是布林旗標 | 是**符文系 id**。這是「解除術系別要相符」規則能運作的前提 |
| 戰鬥單位 `+0x20` 「語意未定案，維持未解」| **收回得太保守，現已解（`docs/re/23` §5）。** 它就是陣營：`{1,2,3,4}` 怪物側、`{11,12,13,14}` 玩家側，`>= 10` 是玩家側；值域表其實 `docs/re/20` §9.5 早就整理完，`ai.go` 與 `docs/spec/02` 掛的「未定案」是**陳舊標記**。當初卡住的 `0x7b5a` 的 `== 2`：那條路徑確實只對槽位 7–14 成立，但 2 的意思是「這個玩家角色**被附身搶走了**」，不是「2 代表玩家陣營」——**前半對、後半錯**。教訓有兩條：(1) 兩個相符的呼叫點不等於解出來了；(2) 收回之後要回頭查既有文件有沒有更完整的答案，否則就是自己把已解的東西重新標成未解 |
| 建角基礎骰是 `Roll(15)`（均勻 1–15）| 是 **`2d6 + 1`**（三角分布 3–13）。舊推論說「期望值 8 只有 `Roll(15)` 成立」，漏掉「多顆骰子加常數」這一整類；觀察到的最大值 15 其實是人類 +2 之後的 13+2。**期望值對不代表分布對** —— 這個錯在畫面上完全看不出來，因為顯示的就是期望值 |
| `SUM.MAP` 的子地圖「不一定填滿 4096 格，讀完 bytes 就會提早停」 | **23/23 全部精確填滿**。那是 `decodeRLE` 兩個 bug 造成的假象：(1) 重複次數 byte = 0 代表 **256**（原函式是 do-while，先寫再減），(2) 每個區塊的**第一個 byte 不是 RLE 資料**要跳過。當時把觀察到的錯誤結果寫成規格，測試也照著寫成「不斷言等於 4096」，等於用測試把 bug 釘死 |
| 可通行性表「**組別本身不等於地形種類**，分組只能縮小範圍」 | **講反了，組別就是地形種類**（`docs/re/24`）。當時看到 `0xfe` 一組有 19 格、城鎮 tile 混在裡面就否決，但 `0xfe` 屬於不可通行那一側，本來就不參與地形分類；走得到的 0–7 那八組每組都很乾淨。**否決一個假設前，先確認自己否決的是它的核心而不是邊角** —— 這一否決讓七大地形又卡了四輪 |
| 「從 `DS:0x52f4` 的 tile 值追隨機遭遇路徑」（列了 16 個存取點）| `0x52f4` 是**到處被重用的暫存字**，在事件文字解析裡當迴圈計數器、在移動裡才暫存 tile。把暫存變數當成專用欄位去追，會一直繞。⚠ **精化**（`docs/re/77` §4）：在 `FUN_222f_1321` 裡 `0x52f4`／`0x52f6` 是一組**進出參數對** —— 進去是要查的 `(X, Y)`，出來被覆寫成記錄序號與屬性低 5 bit。所謂「到處被重用」有一部分其實是這個呼叫慣例 |
| `SUM.MAP` 未解，「熵值不像壓縮」「RLE 假設已被否證」 | 就是 RLE。否證那次是**值與次數的欄位順序猜反了** —— 否證「假設 + 一種實作」不等於否證那個假設 |
| 世界座標 → 城鎮編號「未解，擋住自動進城」 | 解了。tile `0x2e`/`0x64` 觸發，`DEMON.INT` `DS:0x2f86` 有 25 筆 `(X,Y)` 常數表，命中的序號就是城鎮編號 |
| 戰場 9×9「未經原版確認，只是為了配合呈現層」 | **已確認是 9×9**。`FILES.DAT 0x0A8` 的視線遮蔽表：索引以 9 展開、上限 81、內部掃描 7×7，三者只有 9×9 自洽 |
| `FILES.DAT 0x0a5` 那 179 bytes「疑似群體法術範圍模板，記錄結構沒釘死」 | 起點是 **`0x0a8`、176 bytes**，是**戰場視線遮蔽表**，不是法術範圍。當初卡住是因為只對資料做統計；改找讀取端就直接得到「49 組」的結構 |
| FILES.DTT `[467:489]` 是「隨機物品命名詞庫」（Comet Sword 之類）| 是**月份名**。狀態列 `0x70ac` 拿隊伍欄位 `+0x9d` ×4 查 `ds:0x50f2` 取名字，套進 `"...in the Month of the %s"`。只看內容只能得到「像什麼」，找讀取端才知道「是什麼」 |
| FILES.DTT `[136:151]` 是 15 個裝備類別、`[151:164]` 是 13 個神器 | 切點錯了。`[123:153]` 是一整張 **30 條的道具型別名**表（＝ ITEMS.DAT 的 30 筆），`[153:164]` 是 **11 個神祇名**。邊界由切表常式 `0x11d5d` 的筆數常數決定，八張表相加正好 501 |
| `docs/re/09`：祈禱訊息印的是「施法者姓名」（指標表 `0x4c98`）| `0x4c98` 是**神祇名**表（11 條），索引是角色記錄 `+0xf0 − 1`。訊息是「Omizeh hears you!」|
| 遊戲內的月是 1-based 的序數 | 是 **0-based 的名稱索引**。新遊戲初始化只寫日 8、時 5，月完全沒寫，所以起始值 0 是合法的第一個月（Ruby）|
| 存檔 `Encode` 已經涵蓋規則層會改的東西 | 差得遠。道具槽只抄 raw、`ApplyTo` 不寫裝備槽與戰鬥狀態、trailer 不寫時／日／月／糧食／子地圖／光源。**買賣換裝在畫面上全對，存檔時整批退回舊值** |
| 治療所的解束縛費用 = 角色等級 × 費率 | 乘的是**束縛等級**（角色記錄 `+0xec`），不是角色等級。畫面標籤 `Unbind %3d/lvl` 的 lvl 指的是束縛法術的等級 —— 標籤含糊，`docs/re/19` §5.2 的指令倒是寫得很清楚，是實作沒照著做 |
| 祈禱成功率是整隊共用一個值 | 是**每個角色各自一份**的持久欄位（`+0xeb`）。神殿改宗與祈禱都寫 20，戰鬥中成功一次 −5 |
| 存檔的金幣「長度未知，分不出 3 還是 4 bytes」 | 是 **4 bytes**。買船扣款是 `SUB word [+0x0a]` / `SBB word [+0x0c]` 一組 32-bit 減法。兩份存檔的第 4 個 byte 都是 0 所以資料面分不出來 —— 分不出來就去找讀寫端 |
| 船隻陣列「不知道哪些是我的船」 | **問錯了問題**：原版沒有記載歸屬。修船只看腳邊有沒有船，買船只是往海上多放一艘 |
| `dwstrings check` 回報「148/148，100% 翻完」 | 檢查範圍只有事件敘述與法術名，**道具／怪物／城鎮／月份／技能共 208 條沒被算進去**。已擴到 356/356 |
| 改宗的索引是 `0x10 − 神祇編號`（所以 `god_id ≥ 2` 語意可疑）| 減掉的是**神祇編號 mod 2**。那支函式吃兩個參數，舊筆記把兩個都當 god_id —— `[bp+6]` 是神祇編號、`[bp+8]` 是餘數。索引永遠是 15（薩滿）或 16（司祭）|
| 祈禱「旗標為零時整段比對跳過，新關係不擋」| **跳轉方向讀反了**：`cmpb $0, char[+0xc8+教派] / je` 是「沒有那個教派的技能就跳去拒絕」。祈禱要求**先改宗到這座神殿** |
| 掉寶的類別欄位「第一個為 0 時挑到 0 減 1 會索引到表外，沒把握不實作」| **`jne` 讀成 `je`**。`f[0] != 0` 才是「四個裡挑一個」；`f[0] == 0` 時另外三個是排除清單。兩條路都保證非零 |
| 掉寶生成器的型別比較（`docs/re/30` 的 8／15／25／26）| **全部是 1-based**。型別寫進槽 `+0x00` 之後區域變數被 `inc` 過（`e6b6`），後面每一次比較都是「型別 + 1」。五處判讀集體偏一格：布甲沒被減半、「戒指與火把 200 次」其實是兩種藥瓶、王冠以上根本沒有附魔、詛咒只有武器護甲會中。旁證是 `es:[bx + si − 0xa]` 那個負偏移 —— 0-based 的話型別 0 會指到記錄外面（`docs/re/48` §2）|
| 每走一步 1/64 的那個訊號是隨機遭遇戰鬥（`docs/re/04` §2.2）| 是**商隊**。回傳碼 `0x17` 對到動作分派表的 `case 0x17`，中間沒有任何轉換。連帶三處小錯：閘門 `>= 9` 不是 `> 9`、那是**戶外**不是地城、分派表不在 `FUN_222f_0b0e` 裡面而在它的呼叫端。三個事實（訊號、機率、閘門）都對，錯的只有最後一步推論 —— 越像常識的推論越要標成推論（`docs/re/51`）|
| 掉落型別的價格門檻是「最低」，所以高等怪不會掉匕首 | 是**上限**。`f(等級) = 2.6^等級`，最貴的寶石才 500 而 `2.6^8 ≈ 2088` —— 當下限的話 8 級以上一件都選不出來，迴圈轉不完。正確說法是**低等怪掉不出貴東西**（`docs/re/48` §3）|
| 商隊的貨可能被詛咒（附魔為負、沒有效果）| **商隊那條路一件詛咒品都生不出來**。`rnd(120) − 80` 是**「這一件商人在說謊」的機率**（`docs/re/53`：唯一的讀取端是 View mind），生成器那邊的作用是跳過第一道效果門檻。會把東西變壞的只有掉落模式的 `rnd(10) == 10`。錯在從「掉寶生成器會生詛咒品」推過去，沒驗證那個參數在模式 1 底下做什麼（`docs/re/48` §5）|
| `MONSTER.DAT` 第 7 欄是「每回合攻擊次數」 | 是**護甲點數**。它搬進 `unit+0x0a`，而那一欄被 `Armor: %3d pts.`（`ds:0x0b15`）印出來、命中判定時直接減掉（`0x9f4d`）。原本的依據「戰士序列 1,2,3,4,4,4,5,6,6 遞增」對護甲同樣成立 —— **分不出兩者的證據不是證據**（`docs/re/57` §4.1）|
| `unit+0x0a` 是「護甲索引」，來源是角色 `+0x101` 裝備槽 | 是**護甲點數**，玩家側是**逐件裝備累加**算出來的（`0xc43e`：檢查 `+0xe6` 旗標，成立就 +2）。`%3d pts.` 也說明它不是索引（`docs/re/57` §4.2）|
| 召喚表 w5「未解，寫進 `ds:0x5210` 的遭遇型別清單」 | 是 **sprite index**。同構表直接對上 `MONSTER.DAT` 已驗證的那一欄（棕熊 7 ＝熊系、幼龍 27 ＝龍系）。`ds:0x5210` 則是**槽位→sprite 對照陣列**（`docs/re/57` §4.3）|
| 召喚表 w7 是「為召喚設計卻沒用到的成本欄位」 | **方向對，理由錯**。它是**經驗值欄位**（怪物側就是 `unit+0x24`），只是召喚生物不給玩家經驗，所以在召喚流程裡沒有讀取端（`docs/re/57` §4.4）|
| `MONSTER.DAT` 的 `level` 欄「用途未知，可能是法術抗性或遭遇表分級」 | 用途是**戰鬥勝利金幣的指數**（`1.7^level + Roll(2.1^level) + 3`），也是召喚成本基數的同位欄位（`docs/re/56`、`57`）|
| 道具槽 `+0x01`／`+0x03`／`+0x09`–`+0x0c` 語意未解，寫回時留原值 | 全部解開了：`+0x01`–`+0x04` 是兩組**附帶法術**（索引 + 強度，只有武器有），`+0x09`–`+0x0c` 是兩組**特效**（條件碼 + 值）。找的是**唯一的寫入端**（掉寶生成器），比從讀取端猜語意可靠得多（`docs/re/48` §6）|
| `ITEMS.DAT` 的 `CategoryIndex`「四種取值分組有規律但對不到已知的表，只能算假設」| 是**充能種類**：掉寶生成端拿它當 switch 分支值，決定使用次數怎麼算 |
| 道具槽 `+0x0f`「語意未解，兩份原版存檔的實物都是 1」| 是**名稱類別索引**：戰鬥結算印道具名時拿它 ×4 查 `ds:0x1852` 的遠指標表 |
| 掉寶生成器開頭那段軟浮點「很可能是估價」| **不是**。它是掉落型別的**最低價格門檻**（`f(等級) + 25×等級`），與售價無關 |
| `[0x4cc8]` 是神祇名稱的指標 | 是通用的「目前要印的名字」暫存指標 —— 神殿與掉寶都用它 |
| 紮營選單有 13 項，且「顯示順序 ≠ 跳表索引順序」| **14 項**（漏了 `Party`），補上之後兩者完全一致（`docs/re/33` §1）|
| 型別 0xfe 是空槽的另一種寫法 | 0xfe 是**地城道具**，只有 0xff 是空 —— Trade 找空格用的是 `== 0xff` |
| trailer `+0x00`–`+0x09` 是 10 bytes 的「隊形/順序表」，排列意義未解 | 是 **9 bytes 的 3×3 陣型格**（`A B C / D E F / G H I`，存隊伍索引，`0xFF` 空）；`+0x09` 是另一個未解的 byte（`docs/re/34`）|
| 戰場邊界是 9×9，已從原版確認 | **戰場是 15×15**（座標 6–20，中心 13,13），9×9 只是捲動視窗（`docs/re/36`）|
| 戰場地形是「大地圖的 9×9 局部、外圈依光照變黑」（`0x172f4`）| 那塊緩衝不是戰場。戰場是 **3×3 個世界 tile 各放大 5×5**，建圖過程**沒有光照內縮**（`docs/re/36` §2）|
| 道具 Use 的過濾有兩條「原版有、我們沒實作」，欄位在存檔槽裡定位不到 | 那兩個位移是相對「角色記錄 + 槽×17」算的，減掉槽起點 `0x0c` 就是 `+0x05`／`+0x06`（次數上限／已用）—— **早就實作了**（`docs/re/39` §3）|
| 技能旗標是布林（0／1）| 有**第三種值 2 = 學過但被詛咒封住**。原本讀成布林、寫回只寫 0／1，值 2 存一次檔就被悄悄解掉（`docs/re/41` §3）|
| 軟浮點的常數在 `ds:0x40e0`／`ds:0x35f2` | 那兩個位址要用 **CS** 而不是 DS 去讀 —— 常數放在**呼叫端的程式段**（`docs/re/14` §1 早就寫過）。值分別是 2.6 與 0.8（`docs/re/44` §1）|
| 商隊的貨是未鑑定的 | 生成時未鑑定，但**列出來給玩家看的時候會標成已鑑定**（`docs/re/44` §2）|
| tile 98（`0x62`）是「牆」 | 是**海面**。與 `0x14` 是同一種地形的兩種浪花，載入時隨機摻著鋪。舊猜測表是「找不到 tileset、只能靠出現頻率猜」時代的產物 —— 頻率在地城裡看起來合理，是因為地城沒有海 |

---

## 6. 工作紀律

### ⚠ 動手挖之前，先查手上已經有的

**這個專案累積的資料已經大到「先查」永遠比「先挖」划算。**
一個 session 內踩了三次，每次都是花好幾輪讀組合語言，
去解一個自己早就寫下來／實作好的東西：

| 踩到的 | 答案其實早就在 |
|---|---|
| 「`1d9f:0dd4` 是什麼？」重讀軟浮點庫 | `docs/re/14` §2 寫好了 |
| 「議價的降價幅度沒解」重讀 `0x1dac5` | `internal/game/economy.go` 的 `HagglePrice` 早就實作了（`docs/re/19` 解市集時做的）|
| 「價目表要開 DOSBox 對」 | `docs/walkthrough/part-5`／`part-6` 本來就有；四個種族天生能力的譯名 `translations/glossary.md` 也早就定好，卻手寫了一個不一致的 |

**下手前的三十秒檢查（照這個順序）：**

1. `grep` **`docs/`** —— 這個結論以前寫過嗎？（尤其 `docs/re/` 的交叉引用）
2. `grep` **程式碼** —— 這個函式已經有了嗎？（`internal/game`、`internal/assets`）
3. 看 **`docs/walkthrough/`** 與 **`docs/manual*/`** —— 玩家文件有沒有直接寫答案
4. 看 **`translations/glossary.md`** —— 要造新譯名之前，表上有沒有

**為什麼會一再踩**：反組譯的慣性是「不知道 → 去讀組合語言」，
那條反射太強，跳過了「不知道 → 先查」。而且**查不到的成本是三十秒，
挖錯方向的成本是好幾輪**——不對稱到根本不必猶豫。

順帶一個判準：**如果一個問題「感覺以前碰過」，那就是碰過。** 相信那個感覺，
先去查。

#### ⚠ 更難防的一種：查了，但查到過期的那份

`docs/re/72` 踩的是這種。`docs/formats/event-script.md` §129 標著「**推測**」，
而同一件事 `docs/re/02` §2.4 早就標「**已驗證**」——
`docs/re/02` §2.5 有一張「對 `event-script.md` 的修正總表」，
**但那份文件的原文沒有跟著改**。

於是「先查手上有的」這條紀律**照做了也會踩**：查了一份，查到過期的那一份。

兩條可行的做法：

1. **寫「對 X 的修正總表」時，同時改 X 的原文**（行內加 `⚠ 已於 … 更正`）。
   總表適合當歷史紀錄，**不適合當唯一的更正管道**。
2. 看到「推測／未解」時，**再 grep 那個關鍵字**（函式位址、欄位名）
   確認沒有別處標「已驗證」。信心等級低的敘述特別值得交叉查 ——
   因為它會誘發「去解一遍」。

判準：**「標推測」不等於「還沒解」，只等於「寫那一行的時候還沒解」。**

### ⚠ 找數字：掃常數，不要掃結果

**當年的數字幾乎都是從少數幾個基數用公式算出來的，不是查表存起來的。**
所以「在執行檔裡搜不到遊戲／攻略顯示的那個數字」是**正常的**，
不代表機制不在檔案裡 —— 代表你搜錯東西了。

| 搜什麼 | 結果 |
|---|---|
| 掉落上限 `2088`、附魔費 `10548`、售價 `30619` | 搜不到（全是算出來的）|
| 把懷疑的**底數**編成 IEEE double 去掃 | `1.7` → `0x115f0`、`2.6` → `0x115e0`，**相鄰 16 bytes，同一個常數池** |
| 順著 `mov bx,0x40f0`（唯一一處）| 直接落在戰鬥勝利結算（`docs/re/56`）|

**做法**：把懷疑的常數 `struct.pack('<d', x)` 成 8 bytes 掃全檔
（16-bit 遊戲也要試 `<f`、Q16.16、BCD）；命中後改掃「誰載入它」——
`mov bx,imm16` 的那三個 byte 通常只有一兩處，等於直接指到使用點。

**沒命中也是情報。** `1.4`／`1.2`／`0.05`／`19.0`／`35.0` 全部掃不到，
因為它們是**用四個立即數 push 上堆疊**的（`docs/re/49` §1）。
所以「常數在不在資料段」本身就在告訴你它怎麼被用：
在 → `mov bx,offset` + 載入（會被多處共用，值得追）；
不在 → 內嵌在單一處，跟著那段碼走就好。

**找到常數只是第一關，讀出來還有第二關。** 同一輪差點把整條經驗值公式接錯：
`Exp per chr: %ld  Gold: %ld` 前面 push 的第一個值被我當成 EXP，
但 **C 的參數是右到左推的** —— 最後 push 的才是第一個 `%ld`。
看到 `printf` 就停下來倒著數。

（另一面：手上有很多觀察值時可以直接反推，但要**先分維、看累積不看逐級** ——
逐級值是差分，差分會把冪次結構打散。見 `docs/re/55` §3。）

### 每一輪都要做

0. **先查（見上一節）** —— 這一步跳過的代價，這份文件用三個實例記著
1. 更新受影響的 markdown
2. **重新檢視既有斷言，清掉被推翻的**（不是加註解了事，要讓讀者不被舊結論誤導）
3. commit + push
4. 更新這份 CONTEXT.md 的「現況一覽」

### 環境硬規則

- **編譯一律走 docker**（`golang:1.22-bookworm`），不污染系統
- Python 不得在系統 pip install，要用就 docker
- `workplace/orig/` 唯讀，不得修改
- 原版資料、截圖、Ghidra 專案全部 gitignore

### 多 agent 協調

- **agent 還在跑時量到的中間產物不能當結論**（踩過：把迭代暫態誤判為失敗）
- **`git add` 要指名檔案，不要加目錄**（踩過：掃進別的 agent 仍在編輯的檔案）
- 每個 agent 的結論**協調者要獨立複核一條證據鏈**才收

---

## 7. Worklist（狀態的單一真相來源）

> **2026-07-27 重新盤點。** 這一節整個重寫過：舊版是從**反組譯的缺口**往回列的，
> 而反組譯那一側已經沒剩多少了 —— 現在絕大多數缺口是「**解完沒接**」。
> 每一輪的敘事留在上面的「這一輪」段落，本節只留**狀態**。
> 斷言「某項做完了／沒做」之前先 `grep` 程式碼（`rulebook/63`）。
> 玩家可見功能的另一張視角表：[`docs/manual-coverage.md`](docs/manual-coverage.md)。

### 7.0 一句話現況

SDD 的規格階段結束（九份 spec 全 READY），引擎 M1–M5 完成，
**主線從開局到結局的每一段都實機跑過**（`docs/re/64`）。

> **2026-07-29 最後收尾基準（取代本節底部過期的「建議下一步」）。**
> A6 已完成「正常前期垂直切片＋後期高風險抽樣」，不是尚未開始；F2–F6
> 也已完成。最後一輪依固定順序進行：① 清掉 C/E 清單中已被後來證據解開、
> 但未回填的舊項目；② 補場景跳轉 debug 指令；③ 完成第三套 Modern EGA
> 與 F8 三向切換；④ 再跑一次 A6 抽樣；⑤ **抽樣通過後才打包**。
> 發行閘門是正常玩家流程、存檔相容、繁中 16×15 顯示與 EGA/CGA 歷史素材
> 都不能因偵錯或美化功能退步。
>
> **這不代表整個專案完成。** 本輪交付的是可發行基線；仍開著的工作包括
> Modern EGA P1 使用者審稿及 P2–P4 逐格素材。
> 在這些項目結案前，狀態只能寫「本輪完成／專案進行中」，不可寫 goal achieved。
>
> **2026-07-29 程式碼收尾稽核新增一項並已修正：**怪物 AI 的 5×5 法術會
> 正確挑中心及否決誤傷，效果端卻讀玩家游標留下的 `aoeX/aoeY`。現在由 AI
> 明確傳入目標座標，玩家仍走自己的游標；Xvfb 全套測試與回歸測試通過
>（`docs/playtest/14`）。這再次證明「規則層已解」不等於呼叫端一定接對。
>
> 同輪把 `battleterrain.go` 明列的 tile 4 缺口用 IDA 9.4 收掉：`DS:0A20`
>／`0A32` 是 3×3 相鄰索引表，控制 5×5 中央橫／直窄道。兩方向逐格測試，
> 並在真實 `MAP3 (17,46)` 強制開戰，部署與第 1 回合均正常（`docs/re/108`）。
>
> **C13 也由消費端結案。** `sub_11CBF` 直接證明裝備槽 `11h`–`14h`
> 分別是 MaxSP／速度／力量／技巧，`sub_18248` 的戰鬥建單位路徑證明
> `15h` 才是武器特效。換裝、脫裝與升級共用重建邏輯，載入／存檔保留
> 已持久化的有效欄位，戰鬥讀取該有效值；
> 兵器庫釘頭鎚 `12h/0Ch` 現在確實提供速度 +2（`docs/re/109`）。
>
> **C7 第二數值欄結案方式相反：證明它沒有 consumer。** `ITEMS.DAT`
> 數值表基址 `ds:5300` 的全檔六個讀取點只碰價格、charge kind 與四個
> 效果類別，沒有 `record+2`。舊猜名 `WeaponSlot` 已撤回為 `UnusedFlag`，
> 原值保留但不驅動 DOS remake 規則（`docs/re/110`）。
>
> PC-98 日文排版參考也從 Krynn 兩作擴成六款 Gold Box 樣本：另納入
> *Pool of Radiance*、*Curse of the Azure Bonds*、*Secret of the Silver
> Blades* 與 *Pools of Darkness* 的探索、對話、商店、地圖及戰鬥畫面。
> 系列共同規律與《冬之魔》的裁決記在 `docs/ui/03`；第三方截圖只連結，
> 不複製進 repo。
> 研究另整理成 `research-pc98-golden-box-ui` 的漸進式 knowledge skill：
> 唯一來源在 `~/my_skill/knowledge-base/retro-cht/`，`~/.codex/skills` 只放
> 符號連結；只有 metadata 關鍵字常駐，詳細 corpus 與量測規則按需讀取。
> 已推送 my_skill commit `399b496`。
>
> **同日 gameplay closure 再抓到兩個消費端缺口並補上。** 營地法術尾段、
> 營地／戰鬥魔法物品與四種充能規則已依 `1000:11e5`、`1000:2262`、
> `138d:1e72`、`17c5:18ab` 串回 UI（`docs/re/114`）；普通攻擊也不再從
> 玩家與 AI 固定傳 0，而會套背後 +12、附魔 ×3、劍擊 +10 與狀態計數 ×−4。
> **幻術消失機率隨後由 IDA 9.4 結案（`docs/re/115`）**：戰鬥主迴圈
> 對 side 3／13 在輪到行動前執行 `Roll(10) < 3`；Roll 值域為 1–10，
> 所以是 20%，不是舊 `docs/re/06` 誤讀的吐息 30%。remake 已接入一次性
> RNG 判定、死亡清理與「消散」戰鬥訊息，並測試 2／3 邊界及同幀不重擲。
> 相鄰的「召喚物建立當回合不能行動」也已結案：`138d:0033–011A`
> 在施法前拍好行動佇列，`138d:3161` 只寫 12–14 召喚槽、不追加
> `DS:5152/5154`，因此下回合才首次入列；remake 已有同構測試，另驗證
> 新幻象可在第一次正式動作前先消失。
> runtime 畫面也已用 `-battle-illusion-fixture` 固定重播：第 2 回合開始時
> 幻象從戰場移除、紀錄顯示「測試幻象　消散了」，接著正常輪到 Stumpy，
> 見 `docs/playtest/19`。
>
> **2026-07-29 Modern Icon 新基線（取代下方舊 Modern EGA 生產路線）。**
> 使用者已把第三主題正式改名為 Modern Icon，核准
> `modern-ega-concept.png` 作主要延伸方向、`modern-ega-m0-terrain-study-b.png`
> 作輔助；明確否決 M1-B runtime trial、32×28 runtime proof 與 direct-downscale
> failed 三圖。remake 不再受像素美術或 32×28 生產尺寸限制，也不得把概念圖
> downscale。新架構須保留邏輯索引、格位、anchor、碰撞及劇情狀態，改在最終
> 1280×800 畫布的高解析呈現層重畫。首張非像素延伸稿 v1 因浮島圓盤不利拼接
> 自行退回；正投影方格 v2 收在 `docs/design/img/modern-icon-direction-v2.png`。
> **高解析呈現層第一步已實跑**：新增 `-modern-icon-dir`、逐索引 manifest 與
> `64×56` 不透明 PNG loader，明確拒絕舊 32×28 trial；世界地圖會在最終
> 1280×800 畫布覆寫已列索引，規則仍讀原版資料。獨立重繪的森林母稿已跑進
> runtime（`docs/playtest/20`），但為了管線驗證暫掛大量出現的 `0x23`，重複
> 圖樣有接縫，故只算「架構通過、美術不通過」，不能量產或稱完成。
> 隨後新增 `tools/mapwindow` 取得同場景真實 counts，確認 `23=61` 平原、
> `14=9` 深海，並以兩張逐格重繪母稿取代錯掛森林。第二次 runtime 畫面
> `modern-icon-m1-terrain-runtime.png` 的平原低頻與深海相容性通過本場景；
> `0x17` 左海右陸岸線也已逐格重畫並實跑；單格內過渡通過，但其他方向岸線、
> 樹木、城鎮、隊伍及特殊格仍是底稿，尚非完整主題。
>
> **以下為被新基線取代的舊 Modern EGA 研究紀錄。** 新增低紋理、大形優先的 B 方向與
> 強制 32×28 原生資訊量證明，建議 B 作 M1 runtime 基準；同時完成 PNG atlas
> loader／manifest 契約，會嚴格擋錯尺寸、錯格數、半透明與逃出 theme 目錄的
> 路徑。`-modern-theme-dir` 可裝候選 atlas，留空仍走安全的完整調色預覽。
> 這些是 P2 前置完成，不等於使用者已核准或逐格美術已量產。
> PNG 路徑已再做端到端實機驗收：`tools/modernpreview` 匯出五張預覽 atlas，
> `-modern-theme-dir` 載回後與原本記憶體調色路徑的 640×400 截圖 SHA-256
> 完全相同且 `cmp=0`（`docs/playtest/15`）。衍生原版的暫存 atlas 驗後刪除，
> 只保留工具與證據。
> 接著把 B 審稿圖七格真的縮成 32×28、覆蓋少數已證索引並經 loader 進遊戲；
> 實機立即抓到海岸接縫、平原高頻重複與角色被壓小三個失敗。畫面保留在
> `docs/design/img/modern-ega-b-direct-downscale-failed.png` 作反證；
> 裁決是保留 B 的大形／色彩方向，但 M1 必須依原版每個 index、邊界 continuity
> 與 1-bit silhouette 手工像素化，禁止直接縮圖。
> 第一批真正 32×28 的 M1-B bounded trial 已完成（`artwork/modern-ega/m1/`、
> `docs/playtest/17`）：正式 loader 只覆蓋有語意證據的 `01/14/62/23/2e/1e/1f`，
> 平原／深水平鋪與北向隊伍兩步 anchor 通過實機檢查。`17` 海岸與 `63` 山峰仍
> 明列候選而未裝入。這證明製作方法可行，不等於完整 atlas 或美術核准。

> **2026-07-27 收週更新。** 上一版寫「兩塊玩法整個缺席：地城道具與陷阱互動」
> —— **兩塊都做完了**。陷阱在 A1，地城道具**六個指令全接**（`I` 於本輪收尾，
> `docs/re/96`），靈視兩支、治療水池、戰鬥檢視也都接上了。
> **A10 也收掉了**：地城道具的字串抽出來翻完，`dwstrings check` 500/500。
>
> **同日再一輪：A7 的 16 格全部接上，C2 附魔服務也結案**
> （`docs/re/98`–`102`）。這一輪連帶關掉三個掛了兩輪的問題 ——
> 附魔服務的入口（不在城鎮設施裡）、`+0xb9 = 1` 的寫入端（恆世寶珠）、
> 以及估價那幾個浮點常數是係數還是指數（**是指數**，執行檔裡有手寫的 pow）。
>
> **A9 也清完了**（`docs/re/103`）。三項技能一次補齊：附身走的是
> 「施法選單只列學過的系」那道閘門 —— 那一道同時讓 12/13/14 與
> 五系符文 17–21 共**八項**技能第一次有效果。追硬化皮膚那個 `+2` 時
> 順手把 `docs/re/06` 掛了很久的**護甲防護表**核完，發現原本的
> 「防護 = 型別 − 7」是錯的（鎖子甲起每件少 1 點），
> 旁邊還有一張沒人看過的**職業護甲上限表**（巫師只能穿布甲）。
>
> **A8 也清完了**（`docs/re/104`）—— 而且它的題目本身是錯的：分類寫在
> `nSS.DAT` 的類別編號裡，不需要逐筆判斷。順帶補上原版的 `R) Read descr.`。
>
> **玩法缺口只剩 A6（全程試玩）。**
> ~~換句話說：已知的玩法缺口清完了，剩下的是「連起來玩得完嗎」。~~
> —— **2026-07-28 修正**：去排 A6 的路線就撞到一個沒接的閘門
> （世界地圖邊界換圖，`docs/playtest/10`）。「已知的缺口清完了」這句話
> 成立的前提是**清單本身完整**，而「解完但沒有呼叫端」這一類不會出現在
> 任何待辦清單上 —— 它在文件裡是綠的、在測試裡是綠的、在畫面上看起來像撞牆。

---

### A. 玩法缺口（最高優先）

排序原則：**擋不擋破關 → 玩家花了錢／花了智力點卻拿不到東西 → 其餘**。
每一項都標「反組譯狀態／引擎狀態」兩欄，因為這兩件事現在幾乎總是不同步。

| # | 項目 | 反組譯 | 引擎 | 下一步 |
|---|---|---|---|---|
| ~~A1~~ | ~~陷阱互動~~ | **全解**（`docs/re/68`、`91`）| **已接並實跑閉合**（`docs/playtest/09`、`18`）：毒針扣血、水池六輪 `2,2,2,1,0,1`、類別 6 的「即使再小心」、`L` 白框、技能 11「已解除」與無技能固定 seed 命中 25% 都有畫面證據 | — |
| ~~A2~~ | ~~地城道具系統~~ | **六個指令全解**（`docs/re/95`、`96`）。`I` 的答案是「**它不在動作表裡**」：走 switch case 5，就地設 `ds:0x5c66 = 0x80`，由繪圖常式把有道具的格子改畫成 tile `0x38`。對不上的原因是選單元件回傳 **index + 4**，轉換表是 **15 格**不是 11 格 —— 七格已驗證動作碼 7/7 全中 | **六個指令全接**：`T`／`D`／`E`／`M`／`U`／`I`，含 `MAPn.MAP` 寫回。`I` 實跑驗過（地圖 1 的 (18,29)，視野內 7 個標記，對照組無標記，軌跡記 17 格）| — |
| ~~A3~~ | ~~戰鬥 `?` 檢視~~ | **已解**（`docs/re/92`）：跳表 case 10 → `0xc8a6`，逐行版面、兩個技能閘門（`-0x63b` 怪物學識／`-0x64d` 戰術，差 18 ＝ id 25 與 7 的間距）| **已接並實跑閉合**（`docs/playtest/18`）：怪物／隊員屬性、C／B 走訪、白框、戰術目標與召喚物 HP／SP 全有畫面證據；實跑另修掉紅色命令面板遮住檢視卡右半的繪製 bug | — |
| ~~A4~~ | ~~治療水池~~（tile `0x35`）| **全解**（`docs/re/90`）| **已接**：`internal/game/pool.go` ＋ `cmd/demonwinter/poolui.go`，`TriggerHardBlock` 改名 `TriggerPool`。實跑地圖 3 的 `(14,48)`（全遊戲僅此一格）：Menhir `15/27` 連喝三口 `+1/+1/+4` → `21/27`，額度 `7→4`，座標從頭到尾沒動 | — |
| ~~A5a~~ | ~~觀室（技能 27）~~ | **全解**（`docs/re/93` §1）：往前三格、一天**三**次、peek 模式（陷阱只 announce 不觸發） | **已接並實跑閉合**（`docs/playtest/18`）：`trap-pool` 固定朝西，第一次 V 顯示前方陷阱且不觸發；前三次可用、第四次顯示靈視耗盡 | — |
| ~~A5b~~ | ~~鑑物（技能 28）~~ | **全解**（`docs/re/93` §2）：一天三次、`Roll(3)` 有 1/3 失敗、選一件**地城道具**後印它的 `+4` 欄 | **已接**：`internal/game/viewitem.go` ＋ `dungeonui.go`，`X` 鍵（原版主選單就叫 `X)View item`）| A2 做完就解封了。實跑驗過：站在 `Cage` 上鑑物 → `/Iron key`；失敗兩次各扣一次額度；第四次耗盡 |
| ~~A6~~ | ~~全程試玩（破關驗收）~~ | — | **前期完整垂直切片＋後期高風險抽樣完成**（使用者 2026-07-28 同意此驗收範圍）。工具為 `-trace`／`cmd/dwroute -world`／`tools/playthrough.sh`／`-autofight`。| 正常流程已跑通：新遊戲建五人、購物／分裝／換裝、隨機戰鬥與戰利、正常死亡／復活／治療、狗頭人營地首領、四人升 2 級、跨三張圖並依疲勞紮營後抵達加穆爾神殿。實跑期間修掉四個只有串起來才看得到的缺口：世界邊界換圖、建角陣型、地城 DATA1..5 動態切換、戰鬥重建漏帶死亡狀態；`dwroute` 也修掉 EXITS 傳送時漏印觸發鍵。後期依攻略抽驗既有機制：新格里昂成功購船（1000→390、船體 75/75）、英文密語輸入、結局長文、三符印／光之環／禁錮既有分段實機證據（`docs/re/59`–`64`）。重複大型地城不逐格重跑。 |
| ~~A7~~ | ~~地點劇情表 16 格~~ | **全解**（`docs/re/83` 座標 ＋ `docs/re/65`／`99`／`100`／`101`／`102` 內容）| **16 格全部接上了**（`docs/re/65`／`83`／`98`–`102`）| — |
| ~~**A8**~~ | `nSS.DAT` 的類別 1／2 | **已完成**（`docs/re/104`）| ~~判定哪些事件是一次性的~~ —— **分類寫在類別編號裡**，105 筆一筆都不用判。缺的是 `FUN_222f_0a90` 那道觸發閘門 | 已接：`internal/game/eventgate.go` ＋ `R) Read descr.`。類別 1 一次性、類別 2 可重讀，兩種都實跑驗過 |
| ~~A10~~ | ~~地城道具字串沒中文化~~ | — | **翻完了**：`dwstrings dungeonitems` 抽出 **112 條**（300 條裡只有這些能翻 —— `+3` 座標、`+4` 查表鍵、`*` 佔位、`T`/`P`/`S` 參數都不能動，判準在 `cmd/dwstrings/dungeonitems.go`），三個 subagent 分批譯、主迴圈逐條把關，`DUNGEONITEM` 已納入 `check` 的完整五道檢查。譯名對齊 `docs/walkthrough/` 既有用字（布袋／玉製火炬／蛇形石柱／寒冰神殿…），新增 `glossary.md` 第 23 節 | — |

> **主線解謎提示鏈已經是中文了**（庫瑞克、阿薩特、惡魔水晶、冥河擺渡人、寒冰神殿、光之環）。
> 實跑驗過：地圖 1 的 (13,26) 按 `T` 列出 `/藍色血清`／`/紅色血清`，
> 拿起來按 `E` 跳出「一個玻璃瓶，裝著透明的藍色液體。」
>
> 現在最划算的是 **A6 全程試玩** —— A1／A2 接上之後地城段第一次跑得完整。

> ✅ **A7 case 15（光之環的門）已修（`docs/re/98`）。** 原本不是「沒接」，
> 是**接得太寬**：閘門只看 `mapID == 5` 不比對座標，等於符印沒解完就在
> 整張地圖 5 一步都走不了。A6 試玩沒撞到是因為那幾段都帶 `-glyphs`。
> 現在掛回 `locationPlot` 的 case 15，照原版「走上去 → 印訊息 → 推回來」，
> 推回公式代進四格全部落回剛才那一格（`internal/game/glyph.go`
> 的 `CircleOfLightPushBack`，附反例測試證明它不是通用的「退一步」）。
> 實機四組驗過，含**「地圖 5 其他地方走得動」這一組反面證據**（`docs/re/98` §3）。

### A9. 技能：學得到但沒有效果

31 項技能裡有 ~~8~~ ~~6~~ ~~4~~ ~~3~~ ~~2~~ **0** 項在規則層零呼叫端，兩項種族天生能力也接完了。
**這一格清空了**（`docs/re/103`）。
**學院會收玩家的錢教這些。** 明細與 id 見 [`docs/manual-coverage.md`](docs/manual-coverage.md) §7.1。

| id | 技能 | 卡在哪 |
|---|---|---|
| ~~7~~ | 戰術 | **已接並實跑**（`docs/playtest/18`：檢視面板顯示「牠打算攻擊 Wopple」） |
| ~~10~~ | 察覺陷阱 | **已接**（`L` 的 100% 分支）|
| ~~11~~ | 解除陷阱 | **已接並實跑**（`docs/playtest/18`：`-give-skill=11` 顯示「已解除」並走 attr ← 0）|
| ~~13~~ | 附身 | **已接**：入口就是**施法選單的技能閘門**（`docs/re/103` §1）。引擎原本把 43 筆法術列給每個人，所以 12/13/14 與五系符文 17–21 一共**八項**技能都沒有效果；補上閘門後全部生效。效果端（`EffectPossession`）本來就在。⚠ 別跟法術搞混：`docs/re/23` 的附身術公式是**怪物對玩家**那一份 |
| ~~25~~ | 怪物學識 | **已接**（沒有它就看不到怪物屬性，實機驗過）|
| ~~27~~ | 觀室 | **已接並實跑**（`docs/playtest/18`：peek 陷阱與每日三次額度） |
| ~~28~~ | 鑑物 | **已接**（`X` 鍵）。A2 做完就解封了，實跑驗過 |
| ~~30~~ | 硬化皮膚 | **已接**：`+2`，正好等於防護表裡的皮甲，對上手冊「額外提供皮甲等級的防護」。⚠ 連帶推翻兩份筆記把 `+0xe6` 讀成盾牌／裝備旗標——那是技能旗標第 30 格（`docs/re/103` §2）|
| ~~—~~ | 黑暗視覺（矮人）| **已接**（`docs/re/97` §5.2）：它的效果就是**地城視野半徑 +1**（`光源 + 1`，上限 4），不是另一套照明機制。⚠ 這一列原本寫「出貨隊伍光照 0 ＋ 三個矮人」當佐證 —— **兩個數字都錯**，那份存檔是光源 1、沒有矮人（手寫 parse 漂掉）。3×3 的結論不變但理由不同，見 `docs/re/97` §5.3 |
| ~~—~~ | 察覺靈光（精靈）| **已接**：戰利品結算時 `Roll(9) <= 精靈人數 + 4`，過了再看道具四個欄位（`docs/re/103` §3）。看**種族**不看技能旗標，死人也算 |

---

### B. 已完成（不要重查）

> 這一組列出來只為了擋住「再查一次」。**每一項都對過程式碼。**

| 區塊 | 狀態 |
|---|---|
| 移動回傳碼分派表 21 格 | 16 格有語意、3 格是子常式，**沒有空格**（`docs/re/58`／`65`／`69`）|
| `nSS.DAT` 全鏈 | 解碼／查表／改寫／存回，**逐位元組對原版驗過**（`docs/re/77`／`78`）|
| `EXITS.DAT` 出口表 | 6-byte 記錄，換地圖已接並實跑走出地城（`docs/re/85`）|
| 主線 | 三符印 → 解咒 → 光之環 → 禁錮 → 結局，**五段全部實機驗過**（`docs/re/59`–`64`）|
| 三場夢／艾瑞戈爾／兩道密語 | 全接（`docs/re/80`／`83`／`84`）|
| 全隊死亡 + 戰鬥傷害寫回 | 接上並驗過（`docs/playtest/08`）|
| 建角 + 新遊戲 | 接上（`docs/re/87`／`88`／`89`）；起點是世界地圖 34 的 (28,50)|
| 紮營 14 項 | 全接（`docs/re/33`）|
| 城鎮八設施 + 市集六指令 | 全接（`docs/re/19`；碼頭買船修船也已接，舊註解說「只有報價」是過期斷言）|
| 戰鬥九指令 | 全接，**只缺 `?`**（見 A3）|
| 劇情文字中譯 | 368/368（100%）|

---

### C. 規則完整性（有 oracle 可對，不擋 A）

| # | 項目 | 現況 |
|---|---|---|
| ~~C1~~ | ~~拿原版對價格量級與戰鬥金幣~~ | **完成（2026-07-29，`docs/re/113`）**。原版 Seaside 市集 dagger 顯示 2 Gold，remake 同為 2；固定遭遇 4×level 1＋1×level 2 的原版正常結算顯示 Gold 31，落在 remake 公式預測 26–33，且同畫面 EXP 24 與怪物資料相符。 |
| ~~C2~~ | ~~**附魔服務**~~（＝ A7 case 6）| **結案並實裝**（2026-07-27，`docs/re/102`）。入口是**地圖 2 (28,5) 的一格地點劇情**（不是城鎮設施 —— 之前查遍八設施與市集 12 選項都沒有，方向錯了）。<br>費用 ＝ `(ItemValue(新附魔) − ItemValue(舊附魔)) × (20 − 材質) ÷ 10`，`docs/re/55` 的 `F(n,c)` 是它的展開式。三道閘門（只有已鑑定的武器護甲、上限 +10、錢不夠不動）都照抄，實機驗過 |
| ~~C3~~ | ~~海戰~~ | **完成（2026-07-28）**。IDA 9.4 定位 `sub_1C427` 與六支子函式，16-byte 船戰單位、命中／傷害、AI、船體寫回與 150–199 步重設均有位址證據（`docs/re/105`）。已接航海遭遇、1/2/3 點航行、四向砲擊、誤擊、海盜／海怪 AI、邊界逃離、沉船全滅、純 EXP 結算及 `SHIP.SHE/SHP` |
| ~~C4~~ | ~~移動操作與原版不同~~ | **完成（2026-07-29）**。探索定案採現代絕對方向：方向鍵轉向並前進；背後攻擊仍讀實際面向。海戰保留相對轉舵／直行與原版移動點成本。決策與差異已寫入 `docs/spec/04-movement.md`、`docs/manual-coverage.md` 與 README |
| ~~C5~~ | ~~怪物屬性的搬移指令未定位~~ | **完成（2026-07-29，`docs/re/111`）**。`sub_17C5D` 不是固定目的 MOV，而是用 DS:`0A44` 的 11-word 表間接搬移；原始表 `2,6,9,3,4,11,5,18,13,7,17` 直接證明 field 7 → `unit+24h` EXP、field 8 → `unit+1Ah` level。原先同構／值域結論現已有寫入端直接證據。 |
| ~~C6~~ | ~~`MONSTER.DAT` 第 11 欄 `special`~~ | **完成**。`docs/re/23` 的 AI／吐息路徑已證明是種族／元素類型：8–11 為四種龍吐息，7 為冰系；載入單位 `+0x22`，並由免疫規則交叉驗證。舊 worklist 未隨文件更新 |
| ~~C7~~ | ~~`ITEMS.DAT` 第二數值欄~~ | **完成（2026-07-29，`docs/re/110`）**。IDA 9.4 對 `ds:5300` 全部六個 consumer 的稽核只讀價格、charge kind 與四個效果類別，沒有任何 `record+2` 讀取。舊名 `WeaponSlot` 撤回為 `UnusedFlag`；parser 保留原值，但 DOS remake 不據此新增行為。 |
| ~~C8~~ | ~~時間 wrap 值 `38/35/23`~~ | **結案（`docs/re/107` §2）**：IDA `sub_228B6`／`sub_22A53` 明確比較 `26h/23h/17h`，即 38/35/23；鐘要求 `hour >= 24`、床設 25 形成獨立交叉證據。手冊的 26 小時是 Apple II 描述，不能覆蓋 DOS 執行檔 |
| ~~C9~~ | ~~零星 trailer 欄位~~ | **完成（2026-07-29，`docs/re/112`）**。最後的 `+09h` 經 IDA 全檔 consumer 稽核確認為此 DOS binary 未使用的保留 byte：literal `[bx+9]` 三處皆屬其他記錄，陣型索引四條路徑均嚴格 `<9`，下一欄直接是 `+0Ah/+0Ch` 金幣。remake 改名 `Reserved09` 並保留任意值 round-trip，不發明玩法。舊稱「戶外商隊 size base `[0x4c90]`」已更正：`4C90` 是 `EXITS.DAT` arena 指標，商隊基準是 `+0xaf`／出口記錄參數（`docs/re/107` §3）。`+0xaa`＝水池次數；`+0xc1`＝成神抉擇閂鎖 |
| ~~C12~~ | ~~靈視技能一天三次還是一次~~ | **DOSBox 實跑已複核，3 才對**（2026-07-27，`docs/re/93` §4.1）。把存檔的 `+0xad` 預設成 **2** 按一次 `V` 照樣跑（`You see nothing`），預設成 **3** 就印 `Your psychic powers are weak`。兩個相鄰值配成一對 → 上限 ＝ 3。手冊的「一天一次」是 Apple II 版的規則。引擎的 `PsychicUsesPerDay = 3` 不必改。<br>**方法值得記**：原本要「連按四次 `V`」，但原版按一次顯示訊息、再按一次只是關訊息，靠送鍵時序數次數會飄（實測出現非交替序列）—— 改成**把計數器直接寫成邊界值再按一次**就一次過（`docs/re/93` §4.2）|
| ~~C11~~ | ~~陷阱打死人時「全隊各掉 1 點」~~ | **靜態讀完就結案，不需要 DOSBox**（2026-07-27）。`FUN_222f_0619` 是「每走一步的 HP 結算」：模式 0 走**巨魔再生**（`+0xfd` 加到上限 `+0xfc`），模式 `0x80` 走**全隊各 −1**（`JG` 在檢查種族之前就跳走，所以**巨魔在這條路上照樣受傷**），扣到 0 就設 `+0x102 = 5` 並印死亡訊息。<br>陷阱那一側（`25be:1491`–`14f7`）的順序是：印死亡訊息 → `+0x102 = 5` → `+0xfd = 0` → 才呼叫 `0619`。所以**已死者在迴圈開頭就被跳過**（不會 `DEC 0 → 0xff`），而**其餘人各扣 1 點是真的** —— 原版是借用那支結算常式來跑死亡判定，`−1` 是共用的副作用而不是設計意圖。<br>`internal/game/steptick.go` 早就逐條照著實作（含跳過死者、巨魔在高模式照樣流血），**不必改** |
| ~~C10~~ | ~~密門密道~~ | **實跑結案（`docs/playtest/13` §2）**：由地圖 1 鐵籠房間 (13,26) 依一般移動 `Down, Left×4, Up` 穿過外觀為牆的西側密門，到達 (9,26)；可重播腳本為 `a6-secret-door-smoke.txt` |
| ~~C13~~ | ~~裝備槽常駐效果類型未解~~ | **完成（2026-07-29，`docs/re/109`）**。IDA 9.4 `sub_11CBF` 直接分派 `11h` MaxSP、`12h` 速度、`13h` 力量、`14h` 技巧；`sub_18248` 證明 `15h` 是武器戰鬥特效。remake 會在換裝、脫裝與升級重建有效值，戰鬥讀有效欄位，載入／存檔保留其持久狀態；兵器庫釘頭鎚 `12h/0Ch` 實為速度 +2。 |

---

### D. 中文化收尾

| # | 項目 | 現況 |
|---|---|---|
| D1 | ~~介面文案硬編在 Go~~ **✅ 完成（2026-07-28）** | `cmd/demonwinter/*.go` 的正式介面文字全部走 `i18n.Translator.UI`：**2026-07-29 實數 749 條**（`assets/lang/zh-Hant/ui.txt`，語意化 key）；檢查器另列出 19 條只供開發者 debug／`-scene` 使用的描述與格式。這個數字會隨 debug 書籤一起長，不能沿用舊快照。<br>**守住它的是 `dwstrings uicheck`**（`cmd/dwstrings/uicheck.go`）：查 key 都在目錄裡、目錄沒孤兒、目錄譯文＝程式碼 fallback、還剩幾條硬編，另擋 Big5 打不出來的字與 fallback 裡的 `\n`。第一次跑就抓到 **51 條打錯 key 的靜默退回** —— 那種錯畫面一模一樣，只是永遠不走目錄，所以這個檢查不是可選的。<br>41 條 key 是**算出來的**（`trapNameKey(c)`、`"proving.colour."+c`…），靜態掃不到配對，程式碼那一側用 `ui:dynamic` 標記宣告（規約見 `docs/i18n/ui-key-conventions.md`）|
| D2 | 密語提示 **定案不翻** | 符文是要玩家自己建對照表的解謎機制，答案要用英文輸入。施力點放在手冊與提示（`docs/re/72` §6）|
| D3 | 標題花體 logo **定案不重繪** | 1988 年的美術與署名是歷史紀錄（`rulebook/83`／`93`）|
| D4 | ~~`Bungei` 待譯~~ **✅ 完成** | 官方英文手冊的 `Punji pit` 明列 50% 跌落、1–6 傷害，與 executable case 2 逐項相同；`A Bungei pit!` 是誤拼，不是專名。原文 key 保留，繁中依 glossary 譯「竹籤陷阱」（`docs/re/68` §5）|

---

### F. 畫面改版（2026-07-28 新增）

量測、問題清單與 ASCII／SVG 線框在 [`docs/ui/01-ui-assessment.md`](docs/ui/01-ui-assessment.md)，
約束與順序在 [`docs/ui/02-ui-plan.md`](docs/ui/02-ui-plan.md)。
定案：**保留原版骨架、重做觀感**；**素材（sprite／tileset）維持原版不動**；
畫布先維持 640×400；中文一律點陣字。

| # | 項目 | 現況 |
|---|---|---|
| ~~F0~~ | 截圖流程固定 | **已做**：`tools/readme_shots.sh`，六張各自寫明要展示什麼，支援單張重拍 |
| ~~F-字型~~ | 中英混排一粗一細 | **已做**：`-ascii eten` 走倚天全形英數（只換字母與數字，標點維持原版字模 —— 全形 `．` 是置中的點）。格寬不變，`PadCells` 系列一行都沒動 |
| ~~**F1**~~ | 偵錯欄下架 ＋ 金幣／糧食 ＋ 常駐隊伍表 | **已做**。偵錯六欄（步數／內縮／座標／地形／圖塊／偵錯鍵）收進 `F12` 疊層，空出來的位置給金幣、糧食與五人的生命法力。隊伍表欄位照原版 `# NAME HP SP`（**只印目前值不印上限** —— 21 格放不下 `24/24 29/29`，原版也只印目前值）。<br>順帶：鍵位提示改成**釘在下緣**，訊息長度不再把它擠出畫面；營地那行寫給自己看的進度註記刪掉。<br>⚠ 抬頭列**沒有做成全寬**：全寬要把地圖往下移 16 px，那是 F3 的版面常數。金幣糧食先放在右欄第一行 |
| ~~F2~~ | ~~地圖／戰場雙線白框、移除常駐格線、EGA 提示色~~ | **完成**，EGA／CGA 實機截圖驗收 |
| ~~F3~~ | ~~版面常數重排、訊息移到地圖框下方~~ | **完成**，CGA 依實際框高動態避讓 |
| ~~F4~~ | ~~共用紅底選單~~ | **完成**：世界、戰鬥、營地、城鎮共用元件與 disabled 網點 |
| ~~F5~~ | ~~城鎮重用地圖視窗、段落行距、標點壓縮~~ | **完成**，斷行與繪製共用前進量 |
| ~~F6~~ | ~~原版戰鬥 sprite 與 `GOT.FNE`~~ | **完成**：隊員 `COMBAT.SHE`、敵人 `MONSTER.SHE`；哥德字型用於建角英文題名／姓名 |

---

### E. 素材與打包

| # | 項目 | 現況 |
|---|---|---|
| ~~E1~~ | ~~`FILES.DAT` arena 索引 2/3/6/8/14/15~~ | **結案（`docs/re/107` §1）**：2＝`ITEMLOCB.DAT`（256 B）、3＝`FILES.DTT`（5,829 B）、6＝視野遮罩、8＝遭遇表；14＝256 B 戰場／視野格網 scratch、15＝200 B 名稱字串 scratch。IDA/Ghidra consumer 與 alias 關係已集中記錄 |
| ~~E2~~ | ~~噴吐動畫的圖塊來源~~ | **完成（2026-07-29）**。IDA 9.4 `sub_15088:15255–15329` 證明 race 8/9/10 分別選 tile 6/7/8，race 11 隨機選 6–8，再交給共同圖塊繪製 `sub_1F50E`。原版就是沿吐息錐逐格畫 `DEMON/WINTER` 地形圖塊，不存在另一套遺失特效；remake 已照接 |
| ~~E3~~ | ~~CGA 缺 `GOT.FNT`~~ | **已收束**：原始配套確實沒有 `GOT.FNT`，CGA theme 沿用 `GOT.FNE`；一般中文／英數維持倚天 16×15。F8 預載並原子切換 DEMON/WINTER/COMBAT/MONSTER/SHIP，避免半幀 atlas |
| ~~E4~~ | ~~headless 確定性回歸測試 + 打包~~ | **本輪 gameplay closure 後已重跑完成（2026-07-29）**：全套 Go/Ebiten、500/500 翻譯、748/748 UI、固定遭遇、密門、Modern EGA 海戰與 F8 均通過；`2026.07.29-closure` 包不含原版資料／倚天字型，SHA-256 與解壓後 `-list-scenes` smoke 通過（`docs/playtest/13` §5） |

---

### 過期的歷史建議（僅保留決策脈絡）

> 下文是 2026-07-28 排程快照，已由 §7.0 的 2026-07-29 收尾基準取代。

**A6 全程試玩破關驗收**（開場三段已跑通，見 §7 A6 那一列）。
它同時是最有效的缺口偵測器：這一輪光是排路線就挖出三個
「解完＋測完＋文件寫好、但一個呼叫端都沒有」的洞
（`world.CrossEdge`、`Formation.AddMember`，加上「未測就給 0」的 `initialSP`）。
那一類缺口**不會出現在下面任何一張清單上**，因為清單是照「解了沒」列的。

A6 之外，唯一擋玩家體驗（而不是擋破關）的是 **F2–F5 的畫面改版**；
C 與 E 兩組都不擋破關。

~~「排在 A6 前面的是 F1」~~ —— F1 已於 2026-07-28 完成（金幣／糧食／隊伍表
上了常駐畫面，偵錯欄收進 F12），那個前置條件不再成立。
