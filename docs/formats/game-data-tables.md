# Demon's Winter 遊戲規則資料表格式

> 分析對象：`workplace/orig/demwin/DEM_DATA/` 底下的 `MONSTER.DAT`、`ITEMS.DAT`、`PARTY.DAT`／`PARTY.BAK`、
> `TEMPLAT1/2/4/5.DAT`。方法：靜態逐 byte 分析（十六進位比對、跨記錄自相似性交叉驗證、
> `PARTY.DAT` vs `PARTY.BAK` diff）、DOSBox 交易實測、以及反組譯定址交叉驗證。
> 對應解析器：`tools/parse_party.py`、`tools/parse_monster.py`、`tools/parse_items.py`。
>
> **最後大幅更新：2026-07-25。** 角色記錄的欄位表在該日大改 —— 道具槽起點由 `0x1a`
> 更正為 `0x0c`、種族欄位定位到 `0xf5`、`0x102` 由「位元旗標」更正為列舉值、
> 新增技能旗標陣列等六個欄位。若你手上有更早的版本，以本版為準。

## 0. 總覽與最重要的修正

任務指派時的假設是「`MONSTER.DAT`／`ITEMS.DAT` 是固定長度二進位記錄，記錄長度 = 檔案大小 ÷ 怪物數，
怪物名字在 `DEMON.INT` 裡」。**這兩點都不成立，實際格式比假設簡單、也更容易解析**：

- `MONSTER.DAT` 和 `ITEMS.DAT` 其實是**純 ASCII 文字檔**，用 `\x00`（NUL）取代空白/逗號當分隔符，
  不是二進位固定記錄。名字本身就內嵌在檔案裡（例如 `Orc\x006\x009\x007\x0015\x00...`），
  不需要去 `DEMON.INT` 找。
- `DEMON.INT`（173,380 bytes）整份跑過 `strings` 抽取後只有 3,131 行，**完全沒有怪物名或道具名**，
  只有介面文字、戰鬥訊息、城鎮/地城的旁白文字。原本「怪物名在 DEMON.INT」的假設是錯的——已用
  `grep -iE 'goblin|orc|troll|...'` 對 `DEMON.INT` 抽出的字串和 `DEMON.EXE` 的 `strings` 輸出
  雙重確認零命中，怪物名只存在於 `MONSTER.DAT` 本身。
- `TEMPLAT%d.DAT` **不是「預建角色範本」**。`DEMON.INT` 字串裡 `TEMPLAT%d.DAT` 這個檔名字串，
  緊接在「你進入一間堆滿舊武器與護甲的軍械庫」「浮動的光球」「發光的光球」「取走道具？」
  這段特殊藏寶室劇情文字之後（`workplace/ref/demon_int_strings.txt` 對應原始 `strings` 掃描結果第
  2742–2749 行，`DATA%d.TXT` / `Combat!` / `TEMPLAT%d.DAT` / `The spheres glow brighter.` /
  `Character to take?`）。這代表 `TEMPLAT%d.DAT` 是**特殊藏寶室裡那件獨一無二道具的資料檔**，
  跟 `DATA%d.TXT`（該地點的旁白文字）成對載入，不是角色存檔範本。`TEMPLAT3.DAT` 缺席，
  最可能的解釋是遊戲裡只有 4 個這種藏寶室場景（或第 3 個場景重用了其他檔案），而不是「範本 3 遺失」。
  詳見第 4 節。

## 1. `PARTY.DAT` / `PARTY.BAK`（隊伍存檔）

### 1.1 整體結構

檔案大小 1,494 bytes = 5 個角色記錄（每個 `0x104` = 260 bytes）＋ 194 bytes 隊伍共用資料（trailer）。

```
0x000 - 0x103   1 號角色記錄（260 bytes）
0x104 - 0x207   2 號角色記錄
0x208 - 0x30b   3 號角色記錄
0x30c - 0x40f   4 號角色記錄
0x410 - 0x513   5 號角色記錄
0x514 - 0x5d5   隊伍共用資料（194 bytes，trailer）
```

角色記錄間距 `0x104` 是攻略原文給的絕對位址互減得出，**已用本次分析重新驗證**：直接在
`PARTY.DAT` 十六進位內容裡找到 5 個角色姓名字串（`Wopple` / `Stumpy` / `Podgom` / `Norman` /
`Menhir`），起始位址分別是 `0x000`、`0x104`、`0x208`、`0x30c`、`0x410`，彼此差值全部是 `0x104`，
與攻略給的經驗值欄位間距完全吻合（等於是找到了角色記錄的**真正起點**，攻略原文只給了記錄中段
經驗值欄位的位址，沒提過記錄其實從姓名欄開始）。

### 1.2 單一角色記錄欄位表（相對位移＝相對於該記錄起始 `0x000`）

| 欄位 | 相對位移 | 長度 | 型別 | 值域（本存檔實際出現） | 語意 | 驗證狀態 |
|---|---|---|---|---|---|---|
| 姓名 | `0x00` | 保留 12 bytes，NUL 結尾 | ASCII 字串 | `Wopple`／`Stumpy`／`Podgom`／`Norman`／`Menhir`（DAT），`Fyndus`／`Norman`／`Kirian`／`Stumpy`／`Wopple`（BAK） | 角色姓名 | **已驗證**：直接讀出可辨識的英文名字，且與 `PARTY.BAK` 對照同一位址是另一組名字 |
| **道具槽陣列** | `0x0c - 0xb5` | 170 bytes（10 槽 × 17 bytes） | — | 見「1.3 道具槽」 | 角色隨身道具欄。**起點是 `0x0c` 不是 `0x1a`** —— 見 1.3 的修正說明 | **已驗證 2026-07-25**（戰鬥碼定址 + 存檔內容 + DOSBox 實測三方一致）|
| 未知區 | `0xb6 - 0xc3` | 14 bytes | — | 本存檔 5 名角色全部是 `0x00` | 未知 | 未知 |
| 經驗值（EXP） | `0xc4` | **4 bytes**，little-endian，數值封頂 `0x00FFFFFF` | 整數 | DAT 第1號角色 `0x000432` = 1074 | 經驗值 | **已驗證 2026-07-25**：神殿捐獻用 `ADD word[+0xc4]` / `ADC word[+0xc6]` 做 32-bit 加法，封頂檢查 `CMP word[+0xc6],0xFF` / `CMP word[+0xc4],0xFFFF`（`docs/re/19` §3.2.1）。攻略記「3 bytes」講的是**數值上限**，本文原記亦然；**儲存寬度是 4 bytes**，兩者各對一半 |
| **已學技能旗標陣列** | `0xc8 - 0xe6` | 31 bytes | 1 byte/技能，非零＝已學會 | 見下 | 索引＝技能 id（0–30，對照表見 `docs/re/21` §1）| **已驗證**：學院教技能與「剩餘可用點數」兩個獨立呼叫點交叉驗證；本存檔 5 名角色解出的技能全部與其職業吻合（見 1.6）|
| 力量（天生值） | `0xc4+0x24`＝`0xe8` | 1 byte | 整數 | 12–15 | 力量，不含裝備加成 | 攻略原文已明講位址；長度／型別由 5 名角色位址間距反推，本次驗證：跟「力量（含加成）」欄位在無裝備時數值相等（見下） |
| 技巧（天生值） | `0xc4+0x25`＝`0xe9` | 1 byte | 整數 | 12–15 | 技巧，不含裝備加成 | 同上 |
| 最大法力值（天生值） | `0xc4+0x26`＝`0xea` | 1 byte | 整數 | 5–29 | 最大法力值，不含裝備加成 | 同上；數值分布合理（各角色差異大，類似法師/非法師的差別） |
| **神祇呼喚成功率** | `0xeb` | 1 byte | 整數 0–20（直接存百分比）| 本存檔全部 `0` | 戰鬥中呼喚神祇的成功率。每次成功後 `−5`，到神殿祈禱重設回 20 | **已驗證**（`docs/re/19` §3.3／§3.4）|
| **束縛法術等級** | `0xec` | 1 byte | 整數 | 本存檔全部 `0` | 治療所「解除束縛」的費用＝此值 × 費率；復活時一併清零 | **假設，中信心**（`docs/re/19` §5.2）|
| **信奉的神祇 id** | `0xf0` | 1 byte | 整數 0–9 | 本存檔全部 `0` | 5 位司祭神 + 5 位薩滿神 | **已驗證**（祈禱／改宗程式碼直接讀寫）|
| **等級** | `0xf4` | 1 byte | 整數 | 本存檔全部 `3` | 角色等級。祈禱費用 `= 等級 × 50`、復活費用 `= 等級 × 費率` 都讀它 | **已驗證** |
| **種族** | `0xf5` | 1 byte | 整數 0–4 | DAT: `1,0,0,0,0` | `0`人類 `1`精靈 `2`矮人 `3`黑暗精靈 `4`巨魔 | **已驗證 2026-07-25**：`2aed:0aab` 讀它後 `×6` 索引種族上限表（`docs/re/21` §3.2）。**這解決了「種族存在哪裡」的長期未解項** |
| **職業** | `0xf6` | 1 byte | 整數 0–9 | DAT: `6,7,5,1,0` | `0`遊俠 `1`聖騎士 `2`蠻族 `3`武僧 `4`牧師 `5`盜賊 `6`巫師 `7`術士 `8`靈視者 `9`學者 | **已驗證**：學院與改宗兩處獨立呼叫點都拿它當技能學費表的欄索引（`docs/re/19` §8）|
| 速度（天生值） | `0xc4+0x2f`＝`0xf3` | 1 byte | 整數 | 12–15 | 速度，不含裝備加成 | **已驗證且修正攻略筆誤**，見 1.4 節 |
| 速度（含加成） | `0xc4+0x33`＝`0xf7` | 1 byte | 整數 | 12–15 | 速度，含裝備加成 | 已驗證：本存檔 5 名角色均無裝備影響速度，天生值＝含加成值，數值完全相等，符合預期 |
| 力量（含加成） | `0xc4+0x34`＝`0xf8` | 1 byte | 整數 | 12–15 | 力量，含裝備加成 | 同上驗證方式 |
| 智力 | `0xc4+0x35`＝`0xf9` | 1 byte | 整數 | 12–16 | 智力 | 攻略原文已明講位址；是否另有「含加成」版本未知 |
| 耐力 | `0xc4+0x36`＝`0xfa` | 1 byte | 整數 | 11–16 | 耐力 | 同上 |
| 技巧（含加成） | `0xc4+0x37`＝`0xfb` | 1 byte | 整數 | 12–15 | 技巧，含裝備加成 | 已驗證（天生值＝含加成值） |
| 最大生命值 | `0xc4+0x38`＝`0xfc` | 1 byte | 整數 | 21–27 | 生命值上限 | 攻略原文已明講位址；是否另有「不含加成」版本未知 |
| 目前生命值 | `0xc4+0x39`＝`0xfd` | 1 byte | 整數 | 15–26 | 目前生命值 | 已驗證：5 號角色（Menhir）目前生命值 15 < 上限 27，是唯一「掉血中」的角色，符合「這是目前值」的預期（其餘 4 名角色目前值＝上限值，代表滿血） |
| 最大法力值（含加成） | `0xc4+0x3a`＝`0xfe` | 1 byte | 整數 | 5–29 | 最大法力值，含裝備加成 | 已驗證（天生值＝含加成值） |
| 目前法力值 | `0xc4+0x3b`＝`0xff` | 1 byte | 整數 | 5–29 | 目前法力值 | 已驗證：本存檔全部角色目前值＝上限值（滿魔），與生命值欄位的驗證邏輯一致 |
| 裝備武器槽索引 | `0x100` | 1 byte | 整數，`0xFF`＝未裝備 | DAT: `1,0,0,0,0` | 目前裝備的武器在道具槽的第幾格 | **已驗證**：戰鬥建立段以它 `×0x11` 定位道具槽，取出的型別與各角色的武器技能吻合（見 1.6）|
| 裝備護甲槽索引 | `0x101` | 1 byte | 整數，`0xFF`＝未裝備 | DAT: `3,255,1,1,1` | 目前裝備的護甲在道具槽的第幾格 | **已驗證**：同上；取出的護甲型別與手冊附錄 E 的職業穿著限制吻合（見 1.6）|
| **角色狀態** | `0x102` | 1 byte | **單一列舉值**（不是位元旗標）| 本存檔全部 `0` | `0`正常 `1`中毒 `2`/`3`/`4`束縛（值＝施加束縛的符文系 id）`5`死亡 | **已驗證 2026-07-25**：治療所的完整分支（`docs/re/19` §5.2）+ 戰鬥死亡結算寫 `5`（`docs/re/20` §9.3）。**原記「位元旗標」是錯的** |
| 未知 | `0x103` | 1 byte | — | 值不固定 | 未知 | 未知 |

角色記錄總長度核算：`0x00`(姓名12) + `0x0c`–`0xb5`(道具槽 170) + `0xb6`–`0xc3`(未知 14)
+ `0xc4`(EXP 4) + `0xc8`–`0xe6`(技能旗標 31) + `0xe7`–`0xff`(屬性區 25)
+ `0x100`–`0x103`(裝備索引與狀態 4) = `0x104`（260）。
**整個 260 bytes 的每一段都已歸類**，剩下的未知只在 `0xb6`–`0xc3` 這 14 bytes
與屬性區內的少數空位。

### 1.3 道具槽陣列（`0x0c - 0xb5`，10 槽 × 17 bytes）

> ### ⚠ 起點修正（2026-07-25）：是 `0x0c`，不是 `0x1a`
>
> 本節原本記載起點為 `0x1a`，依據是一次 DOSBox 交易實測。
> **那次實測的資料是對的，但解讀錯了。** 四個變動位元組在 `0x0c` 起點下全部有解，
> 而且解釋得更精確：
>
> | 變動 | 原解讀（`0x1a` 起點）| 正確解讀（`0x0c` 起點）|
> |---|---|---|
> | 角色0 `+0x0c`：`1 → 255` | 「裝備武器槽索引」變未裝備 | **slot 0 的型別欄 → 空** |
> | 角色1 `+0x1d`：`255 → 1` | *原文未解釋*（會落在 slot 0 的第 3 byte）| **slot 1 的型別欄 → `1`，正是 `ITEMS.DAT` 索引 1 = small axe，也正是實驗中交易的那件道具** |
> | 角色1 `+0x2b`：`0 → 10` | slot 1 的起點 | **slot 1 的附魔欄 = 10**（附魔值以 `+10` 偏移儲存）|
> | 角色1 `+0x2c`：`0 → 1` | slot 1 的第 2 byte | **slot 1 的 `+0x0f`** |
>
> 決定性證據是戰鬥單位建立段（`docs/re/20` §6）：
> `iVar6 = 角色位移 + 槽位 × 0x11` 之後，同一段程式碼讀 `[iVar6 + 0x0c]` 當**武器型別**、
> 讀 `[iVar6 + 0x1a]` 當**附魔值**。兩者相距 `0x0e`，
> 只有「記錄起點 = `0x0c + 槽位 × 0x11`」能同時成立。
>
> **連帶推翻**：`0x0c` 也不是「裝備武器槽索引」（那個在 `0x100`）。
> 本節原本觀察到的所有規律，在新起點下反而全部有了解釋（見下）。

### 槽記錄的內部結構（17 bytes）

| 位移 | 語意 | 證據 |
|---|---|---|
| `+0x00` | **道具型別**＝`ITEMS.DAT` 的記錄索引，`0xFF`＝空槽 | 戰鬥碼拿它 `+1` 當武器骰表索引；本存檔解出的型別與職業/技能全部相稱（見 1.6）|
| `+0x01` `+0x03` | **附帶法術**的索引（最高位元 `0x80` 是旗標）| 掉寶生成器 `1990:1ac0`–`1b2f`，只有武器會長（`docs/re/48` §6.2）|
| `+0x02` `+0x04` | 附帶法術的強度 | 同上。估價的已鑑定加價 `(兩者相加) × 270`（`docs/re/44` §3）|
| `+0x05` `+0x06` | 使用次數上限／已用次數（相等＝用完，`0xFF`＝不計次）| `docs/re/26` §3.3 |
| `+0x07` `+0x08` | 效果索引（與法術同一個命名空間）／效果強度 | `17c5:19dd` |
| `+0x09` `+0x0b` | 裝備常駐效果類型：`11h` MaxSP、`12h` 速度、`13h` 力量、`14h` 技巧、`15h` 武器戰鬥特效 | `sub_11CBF` 與 `sub_18248`（`docs/re/109`） |
| `+0x0a` `+0x0c` | 效果值（以 `+10` 偏移儲存）| 同上。寫入端是掉寶生成器 `1990:19c0`–`1a2c`，武器與護甲才有 |
| `+0x0d` | **驅邪成功率**（`rnd(100) > 它`＝失敗，越大越好驅）| `1000:19c8`；只有掉落生的詛咒品才寫（`docs/re/48` §7）|
| `+0x0e` | **附魔加成**（以 `+10` 偏移儲存，`10`＝無附魔）| 戰鬥碼 `unit.附魔 = slot[+0x0e] − 10` |
| `+0x0f` | **材質／品質類別**（1–8）：決定名稱前綴（Silver／Gold…）與價格倍率 | 名稱表 `ds:0x1852`、倍率表 `ds:0x18b3`，同一個索引的兩張平行表（`docs/re/48` §1）|
| `+0x10` | **已鑑定旗標** | 市集鑑定：`if (slot[+0x10] != 0) 印 "It's already identified!"`，付費後設 `1`（`docs/re/19` §7.5）|

17 個 byte 全部有名字。這 5 名角色的起始裝備是平凡武具，
所以本存檔裡除了型別、`+0x0e`（10）與 `+0x0f`（1）之外全是 0 ——
**光看這份存檔永遠解不出其餘欄位**，要去找唯一的寫入端（掉寶生成器）。

### 舊觀察在新起點下的解釋

本節原先列了四條「語意未知」的觀察。改用 `0x0c` 起點後全部有解 ——
這本身就是起點正確的旁證：

| 原觀察（以 `0x1a` 為起點）| 新解釋 |
|---|---|
| 「byte0：有裝備的 slot 幾乎全是 `0x0a`，空 slot 是 `0x00`」| 那是**前一格的附魔欄**（`+0x0e`）。`0x0a` = 10 = 附魔 0，所以每件普通裝備都是 `0x0a`。原文推論「不可能是道具索引，比較像 slot 已使用的常數標記」——方向對，但它其實是附魔欄 |
| 「byte1：`0x01` 或 `0x02`」| 前一格的 `+0x0f` |
| 「byte2：`0x00` 或 `0x01`」| 前一格的**已鑑定旗標**（`+0x10`）|
| 「byte3：空 slot 是 `0xFF`，有裝備的是 `0x00`/`0x08`/`0x09`」| **這一格的道具型別**。`0xFF`＝空、`0x00`＝匕首、`0x08`＝布甲、`0x09`＝皮甲 —— 全部對得上 `ITEMS.DAT` 索引 |

17-byte 週期本身是對的（原文用 `0xFF`/`0x0a` 的出現位置分析推出來，
並用 `PARTY.DAT` vs `PARTY.BAK` diff 確認變動不跨邊界）。**錯的只有相位。**

清空一格道具的程式碼（`206a:0e20`–`0e39`）也印證了 stride 與槽數：

```asm
MOV CL,0x4 / SHL AX,CL / ADD AX,DX   ; ×16 + ×1 = ×0x11
ADD AX,CX                             ; 角色位移 + 槽位×0x11
MOV byte ptr ES:[BX + SI + 0xc],0xff  ; 型別欄設為空
INC [BP-2] / CMP AX,0xa / JL          ; 共 10 槽
```

### 1.4 `0x2fb` 疑似筆誤的結論

攻略原文在 3 號角色標記 `0x2fb` 為「Max SP（最大法力值）」，但依照其他 4 名角色的欄位間距，
`0x2fb` 對應的相對位移是 `0xc4+0x2f`，跟其他角色的「速度（天生值）」欄位一致，跟已經有專屬位址的
「最大法力值（天生值）」（`0xc4+0x26`）是兩個不同位置。

**結論：`0x2fb` 確實是筆誤，正確欄位是「速度（天生值）」，不是「最大法力值」。**

驗證方式不只是位移比對（這是攻略原文自己也提到的推理方式），本次分析額外做了**數值內容交叉驗證**：
用 `tools/parse_party.py` 實際讀出 3 號角色在 `0x2fb`（記錄內相對位移 `0xf3`）的值是 `12`，
跟同一角色「速度（含加成）」欄位（`0xc4+0x33`＝`0x2ff`）的值同樣是 `12` 完全相等。本存檔 5 名角色
都沒有裝備會影響速度的道具，所以「天生值＝含加成值」對速度成立；如果 `0x2fb` 真的是最大法力值，
它應該要等於「最大法力值（含加成）」欄位（`0xc4+0x3a`＝`0x306`）的值 `22`，但實際上 `0x2fb` 是 `12`，
兩者不相等，直接排除「`0x2fb` 是法力值」的可能。**這比純粹的位移推理更強一層：不只是位置對得上，
數值內容本身也符合「這是速度」的預期，符合「這是最大法力值」的預期會矛盾。**

### 1.5 隊伍共用資料（trailer，194 bytes，`0x514 - 0x5d5`）

| 欄位 | trailer 內相對位移 | 絕對位址 | 長度 | 驗證狀態 |
|---|---|---|---|---|
| 隊伍金幣 | `0x0a` | `0x51e` | **未知，假設 3 或 4 bytes little-endian** | 攻略原文只說「和經驗值一樣是反序儲存」，沒講長度。本次分析嘗試用 `PARTY.DAT` vs `PARTY.BAK` 交叉驗證長度，但兩份存檔在這個欄位後面兩個 byte（`0x51e+3`、`0x51e+4`）剛好都是 `0x00`，**無法從現有資料判斷究竟是 3 byte（類比經驗值）還是標準的 4 byte long（x86 常見寫法）**。取 3 bytes 讀出 DAT＝65、BAK＝530，取 4 bytes 結果相同（因為第 4 byte 都是 0）——這個巧合正好表示無法消歧，誠實列為未知，不採信任一種猜測 |
| **3×3 陣型格** | `0x00 - 0x08` | `0x514 - 0x51c` | 9 bytes | **已驗證**（`docs/re/34`）。九個格子排成 `A B C / D E F / G H I`，各存一名成員的隊伍索引，`0xFF`＝空格。四個呼叫端：Reorder（`1000:0379`）、離隊（`0x14af2`）、入隊（`0x15089`）、佈陣（`0xc615`）。`PARTY.DAT` 的 `00 ff 01 ff 02 ff 03 ff 04` 是「隔一格站一個」，`PARTY.BAK` 的 `00 01 02 03 04 ff ff ff ff` 是「擠在前五格」 |
| 未解 | `0x09` | `0x51d` | 1 byte | 陣型格與金幣之間。兩份存檔都是 0。**原本被「10 bytes 的順序表」吃掉了** |
| 「L/D」旗標陣列 | `0x0e - 0x14` | `0x522 - 0x528` | 7 bytes | 未知：兩份存檔在這 7 個 byte 只出現 `0x4c`（ASCII `L`）或 `0x44`（ASCII `D`）兩種值，DAT 是 `L L L L D L D`，BAK 是 `L D D D D L D`。是否真的是 ASCII 字母，還是恰好落在這個值域的旗標，未能判斷 |
| 其餘 trailer 區域 | 其餘位移 | — | — | 未知。已用 `tools/parse_party.py --diff` 列出 `PARTY.DAT` 與 `PARTY.BAK` 之間全部 214 個不同的 byte（含上面已分析的部分），供之後比對用，但**沒有嘗試把每一個都指認出語意**——本存檔只有一組對照樣本，無法排除巧合 |

「遊戲內時間（小時/日/月）」「隊伍朝向」：**未解**。`trailer` 內 `0x1a-0x1c`（`13, 1, 2`）數值範圍
剛好落在「26 小時制的小時／日／月索引」的合理範圍內，一度懷疑是時間欄位，但同一位移在
`PARTY.BAK` 讀出 `8, 71, 88`——`71`、`88` 已經超出任何合理的「日」或「月索引」範�族（遊戲月份只有
以寶石命名的十幾種），**這個假設被自己的交叉驗證推翻，因此不採信，時間欄位維持未知**。

#### DOSBox 動態 diff 補充（2026-07-24，見 [`docs/re/01-dosbox-reference.md`](../re/01-dosbox-reference.md)）

上面靠 DAT/BAK 靜態比對解不出的「朝向／時間」，在 DOSBox 實跑移動實驗後有進展。
方法：在遊戲中往不同方向各走一步、存檔，用 `tools/parse_party.py --diff` 比對。
位移以 trailer 起始 `0x514` 為基準。**這是對 reference 實跑的結果，信心等級照原始實驗如實標註**：

| 相對 `0x514` | 絕對 | 移動時的變化 | 語意 | 信心 |
|---|---|---|---|---|
| `+0xa1` | `0x5b5` | 僅左右移動時變動 | **X 座標** | 高 |
| `+0xa2` | `0x5b6` | Down +1／Up −1 | **Y 座標**（螢幕座標，向下為正） | 高 |
| `+0xa4` | `0x5b8` | 對應順時針四方位 | **隊伍朝向**，`0=北 1=東 2=南 3=西` | **已驗證**（2026-07-25 雙向確認，見下） |
| `+0xa0` | `0x5b4` | 每步 +1 | **遊戲內時間／回合計數**候選 | 中（尚未對到遊戲內時鐘 UI） |
| `+0x9c` | `0x5b0` | 每步 −1 | 與 `+0xa0` 反向連動，口糧／體力候選 | 低 |

**朝向的反向確認已完成（2026-07-25，DOSBox 實測）**：

```
基準            Y=34  facing=2（南）
 ↓ 按 Up
after-up        Y=33  facing=0（北）   ← Y 減 1，朝向轉北
 ↓ 按 Down
reverse-confirm Y=34  facing=2（南）   ← Y 加 1，朝向轉回南
```

雙向對稱，`0=北`／`2=南` 確認。座標軸方向也一併確認（Y 向南為正）。
存檔證據：`workplace/dosbox/saves/PARTY.exp3-*.dat`。

**仍待複核**：時間候選（`+0xa0`／`+0x9c`）是否對得上遊戲內顯示的時／日／月。
2026-07-25 的實測**未能完成** —— 隊伍初始位置被困在極小的中庭，
250 次方向鍵幾乎都撞牆，時間欄位始終沒動；Camp→Sleep 回應 "You are restless" 被拒絕。
下一輪要先把隊伍移到開闊地形，並測試尚未嘗試的時間觸發途徑（`Cook`、戰鬥回合）。

### 1.6 五名角色的完整解碼（交叉驗證）

把上述欄位一次全部套用到 `PARTY.DAT` 的 5 名角色，結果與手冊規則**逐項吻合**，
這是整份欄位表最強的一次交叉驗證：

| 角色 | 種族(`0xf5`) | 職業(`0xf6`) | 已學技能(`0xc8+`) | 裝備武器 | 裝備護甲 |
|---|---|---|---|---|---|
| Wopple | 1 精靈 | 6 巫師 | 17 火焰符文、21 靈魂符文 | 型別 0 匕首 | 型別 8 **布甲** |
| Stumpy | 0 人類 | 7 術士 | 12 幻術、14 召喚 | 型別 0 匕首 | **無**（`0xFF`）|
| Podgom | 0 人類 | 5 盜賊 | 1 劍術、10 察覺陷阱 | 型別 2 短劍 | 型別 9 皮甲 |
| Norman | 0 人類 | 1 聖騎士 | 1 劍術、29 讀心 | 型別 2 短劍 | 型別 9 皮甲 |
| Menhir | 0 人類 | 0 遊俠 | 1 劍術、8 狩獵 | 型別 2 短劍 | 型別 9 皮甲 |

對得上的規則：

- **每人恰好 2 項技能** —— 手冊「角色剛創建時只能從該職業常見的簡短技能列表中選擇兩項」
- **技能與職業相稱** —— 巫師拿兩個符文系、術士拿幻術＋召喚（手冊：吟唱多半由術士使用）、
  遊俠拿狩獵、盜賊拿察覺陷阱
- **武器與技能相稱** —— 有「劍術」的三人拿短劍（手冊附錄 E：短劍需劍術）；
  兩個施法者沒有武器技能，拿的是匕首（附錄 E：匕首「所需技能：無」）
- **護甲與職業限制相稱** —— 手冊附錄 E 的護甲表列明**巫師與術士不能穿皮甲**：
  巫師 Wopple 穿布甲（型別 8）、術士 Stumpy 乾脆沒穿；其餘三人穿皮甲（型別 9）
- 型別值 `0`/`2`/`8`/`9` 直接就是 `ITEMS.DAT` 的記錄索引
  （0 dagger、2 short sword、8 cloth、9 leather）

若技能 id 表、職業 id、道具槽起點、型別欄位任何一項判錯，這張表就會出現不合理的組合。

## 2. `MONSTER.DAT`（怪物屬性表）

### 2.1 檔案格式

**不是固定長度二進位記錄。** 整個 3,853 bytes 是純 ASCII 文字，用 `\x00`（NUL byte）取代空白/逗號
分隔每一個欄位。用 `data.split(b'\x00')` 直接可以拆出 1,188 個 token（扣掉檔尾的空 token），
`1188 ÷ 12 = 99`，整除無餘數。**每隻怪物 = 1 個名字字串 + 11 個數字欄位（ASCII 十進位文字，
含負號），共 12 個 token。共 99 隻怪物，已全部解出（列表見 2.3 節）。**

> **原始資料瑕疵（2026-07-25，Go 解碼器實作時發現）**：索引 49「Swarm」的最後一個數值欄位
> （token 599）原始內容是 `"0 "`，**帶一個尾端空白**，不是純數字。全檔僅此一筆，`ITEMS.DAT` 乾淨。
>
> 這筆瑕疵先前一直沒被發現，因為 Python 的 `int()` 對前後空白寬容（`int("0 ") == 0`），
> `tools/parse_monster.py` 跑起來完全正常；Go 的 `strconv.Atoi` 嚴格得多，直接報錯才暴露出來。
> **解析器一律要對 token 做 trim** —— `internal/assets/gamedata` 已比照 Python 行為處理，
> 兩個語言的解析器對同一份輸入必須給出一致結果。

### 2.2 11 個數字欄位語意

| 順序 | 欄位 | 語意 | 驗證狀態 | 驗證依據 |
|---|---|---|---|---|
| 1 | speed | 速度 | 已驗證 | 數值分布（4–26）與 `PARTY.DAT` 角色的速度屬性同量級；風元素（Wind elemental）速度全表最高（26），符合「風系怪物速度最快」的直覺 |
| 2 | strength | 力量 | 已驗證 | 巨人/龍/惡魔類力量遠高於一般人形怪物，量級與 `docs/walkthrough/part-2.md` 種族力量上限（14–30）相容 |
| 3 | skill | 技巧 | 已驗證 | 同上，量級與 PC 屬性一致 |
| 4 | hp | 生命值 | 假設 | 「戰士」序列（Lvl 1 fighter → Lvl 15 fighter）此欄位是 `8,14,18,25,32,36,42,48,55`，單調遞增，量級合理，但沒有直接證據排除這其實是「耐力」屬性而非直接儲存的 HP（全表範圍 4–200，不是按強度排序的檔案，所以只能看單一序列的遞增趨勢，不能說整份檔案單調遞增） |
| 5 | attack_type | 攻擊/武器類型索引，負值＝帶毒 | **已驗證** | 對照 `DEMON.INT` 字串裡的武器類型清單（`Hands, Dagger, Small ax, Shortsword, Mace, Morningstar, Broadsword, Battle ax, 2-hand sword, Mace, 2-hand sword, Battle ax, 2-hand sword, Bite`，索引 0–13），數值 `13` 精準對應到 `Bite`（蜘蛛、老鼠、蝙蝠等咬擊系怪物），數值 `0` 對應 `Hands`（殭屍、幽靈等徒手/不死系）。**所有蛇類怪物（Cobra、Death adder、Cobra mage、Pit viper、Rattlesnake、Silver snake、Salamander）這個欄位全部是負值 `-13`**，剛好是 `Bite` 的負版本，精準對應「毒咬」的遊戲機制（蛇類攻擊帶毒），這個模式在 99 隻怪物裡完全沒有例外 |
| 6 | sprite_index | 圖像索引（`MONSTER.SHP`/`MONSTER.SHE`） | **已驗證** | 同種系怪物數值完全一致：4 種熊（Brown/Grizzly/Arctic/Cave bear）全部是 `7`；3 種狼系（Timber/Winter wolf、Devil dog）全部是 `8`；7 種龍（Baby/Small/Large/Great/Fire/Wind/Ice dragon）全部是 `27`；6 種蛇類全部是 `12`。這種「外觀相同的怪物共用同一個值」的規律，只有「圖像索引」能合理解釋，其餘欄位都不會有這種按「外觀種系」分組的規律 |
| 7 | **armour** | **護甲點數**（原判讀「每回合攻擊次數」已推翻，見 `docs/re/57` §4.1）| **已驗證** | 這一欄搬進戰鬥單位的 `+0x0a`，而那個欄位被 `Armor: %3d pts.`（`ds:0x0b15`）印出來，命中判定時直接從命中值減掉（`0x9f4d`）。原本的依據「戰士序列 1,2,3,4,4,4,5,6,6 遞增」對護甲同樣成立，分不出兩者 |
| 8 | experience | 擊殺經驗值 | **已驗證** | 「戰士」序列（Lvl 1 fighter → Lvl 15 fighter）此欄位是 `23,34,41,70,84,165,215,372,650`，單調遞增；全表範圍 23 → 10000（Guardian），終盤劇情怪物（Xeres 3500、Jesric/Eregore 各 5000、Guardian 10000）明顯高出一般怪物一個量級，且 `DEMON.INT` 字串裡有 `Exp per chr: %ld Gold: %ld` / `Exp per char: %d`（戰鬥結算畫面），證實遊戲確實有「每隻怪物對應經驗值」這個機制 |
| 9 | level | 怪物等級／難度分級（1–10） | **已驗證** | **用途已解**（原標「用途未知」）：戰鬥勝利金幣的指數，每隻怪出 `1.7^level + Roll(2.1^level) + 3`（`docs/re/56`、`docs/re/57`）。這一欄搬進戰鬥單位的 `+0x1a`，與召喚生物表的 w8（成本基數）是同一個欄位，12 隻同名生物逐筆相同。仍然**不等於**怪物名稱裡的數字（`Lvl 6 fighter` 這欄是 `5`、`Lvl 15 fighter` 是 `9`）—— 是與名稱脫鉤的難度分級，最高 10 被多隻終盤怪物共用 |
| 10 | sp | 法力值上限 | **已驗證** | 「法師」序列（Lvl 1 mage → Lvl 16 wizard）此欄位是 `8,12,18,24,35,40,45,50,55`，單調遞增且只有法師系怪物這個值明顯偏高，非法師系怪物大多是 `0`，跟 `PARTY.DAT` 的法力值屬性語意一致 |
| 11 | special | 特殊能力/元素索引，`-1`＝無 | 假設 | 觀察到龍系怪物（Fire/Wind/Ice dragon）此欄位分別是 `8,9,10`（連續遞增，疑似「吐息屬性索引」），元素系怪物（Fire/Metal/Wind/Ice/Spirit elemental）分別是 `1,2,3,7,5`（不連續，跟猜測的「五大符文系順序」對不上），整體規律不夠乾淨，只能算假設 |

### 2.3 怪物清單

完整 99 隻怪物清單（含全部 11 個欄位原始數值）由 `tools/parse_monster.py` 直接列印，
不在文件裡重複貼一次（避免手動謄寫出錯，實際數值以程式輸出為準）：

```
python3 tools/parse_monster.py
```

摘要：等級 1 的基礎怪物（Lvl 1 fighter/mage、Orc、Spider、Rat、Bat 等）到終盤 Boss
（Jesric、Eregore、Guardian，經驗值分別 5000／5000／10000）共 99 筆，名稱涵蓋一般野獸、
不死系（Skeleton／Zombie／Ghost／Ghoul／Spectre／Wraith）、元素系、龍系、盜賊系（Thug／Rogue／
Brigand／Highwayman／Master thief／Assassin）、以及劇情專屬怪物（Xeres、Jesric、Eregore、Guardian，
都能對應到 `translations/glossary.md` 或攻略裡出現過的劇情名詞）。

## 3. `ITEMS.DAT`（道具表）

### 3.1 檔案格式

跟 `MONSTER.DAT` 同款：純 ASCII 文字，NUL 分隔，不是固定長度二進位記錄。
724 bytes 拆出 240 個 token，`240 ÷ 8 = 30` 整除無餘數。**每個道具 = 1 個名字字串 +
7 個數字欄位，共 8 個 token。共 30 個道具，已全部解出。**

### 3.2 道具清單與驗證錨點

前 13 筆道具的名稱與順序，跟 `translations/glossary.md` 第 8 節「武器與護甲」表的原版遊戲內順序
**逐一比對完全一致**（這是本檔最強的驗證錨點）：

```
0 dagger（匕首）       1 small axe（小斧）     2 short sword（短劍）
3 mace（釘頭鎚）       4 morning star（晨星鎚） 5 broad sword（闊劍）
6 battle axe（戰斧）   7 2-hand sword（雙手劍）
8 cloth（布甲）        9 leather（皮甲）        10 chain（鎖子甲）
11 scale（鱗甲）       12 plate（板甲）
```

其餘 17 筆是特殊/任務道具：`crown`（冠冕）、`vial`（藥水瓶，出現兩次，可能是不同子類）、
`ring`（戒指）、`wand`（魔杖）、`staff`（法杖）、`rod`（權杖）、`gem`（寶石）、`amulet`（護身符）、
`medallion`（勳章）、`figurine`（雕像）、`talisman`（護符）、`salve`（藥膏）、`torch`（火把）、
`lantern`（提燈）、`Demon Crystal`（惡魔水晶）、`Orb/Evertime`（恆世寶珠，對應
`translations/glossary.md` 第 1 節劇情物品 `Orb of Evertime`／恆世寶珠）。

### 3.3 7 個數字欄位語意

| 順序 | 欄位 | 語意 | 驗證狀態 | 依據 |
|---|---|---|---|---|
| 1 | price | 售價（金幣） | **已驗證** | 8 把武器售價 `2,6,15,13,20,30,65,100` 大致隨武器強度遞增（匕首最便宜、雙手劍最貴；唯一例外是 mace 的 13 比 short sword 的 15 便宜，跟 `docs/walkthrough/part-2.md` 武器力量需求表的順序 mace(7)>short sword(6) 不完全對齊，但整體趨勢仍是弱武器便宜、強武器貴），5 件護甲售價 `5,15,40,200,400` 嚴格隨防護力遞增，量級與攻略 `docs/walkthrough/part-6.md` 第 5 節列出的裝備售價數量級（幾百到幾萬，那些是「附魔後」的價格，基礎價格本來就該低很多）方向一致 |
| 2 | weapon_slot | 是否佔用「武器手」欄位（1/0） | 假設 | 8 把武器與飾品類（戒指/魔杖/法杖/權杖/護身符/勳章/雕像/護符/藥膏）都是 `1`，5 件護甲跟純道具（寶石/火把/提燈/惡魔水晶/恆世寶珠）都是 `0`。跟 `DEMON.INT` 字串裡 `Weapon: ` / `Armor: ` 兩種不同的裝備提示（分別對應「武器欄位」「護甲欄位」兩個獨立槽位）的介面設計吻合，但邊界不完全乾淨（例如 `vial` 藥水瓶是 `0`，理論上該算消耗品而非武器/護甲，跟這個假設不衝突；但 `salve` 藥膏也是消耗品卻是 `1`，兩者行為不一致，無法完全排除這是別的意思） |
| 3 | category | 道具分類索引 | 假設 | 武器、護甲、`crown`、`medallion` 都是 `3`；`wand`／`staff`／`rod`／`talisman`／`salve` 都是 `1`；`vial`（兩筆）都是 `2`；`ring`／`gem`／`amulet`／`figurine`／`torch`／`lantern`／`Demon Crystal`／`Orb/Evertime` 都是 `0`。四種取值、分組看起來有規律但無法直接對應到某個已知的字串表，只能算假設 |
| 4–7 | f3–f6（4 個未定欄位） | 未知 | 未知 | **同一大類（8 把武器 / 5 件護甲）內部這 4 個欄位數值完全相同**（8 把武器全部是 `0,10,8,9`；5 件護甲全部是 `12,7,7,7`），代表這不是「每種武器獨立的傷害骰/最低力量需求」（那些數值查 `docs/walkthrough/part-2.md` 是各武器不同的，1-3 到 1-12 傷害、6 到 18 力量需求，跟這裡「全部武器共用同一組數字」矛盾）。推測這組數字是與「武器」「護甲」這個大類本身綁定的共用參數（可能是圖示座標、或某種基礎判定表索引），實際傷害骰/力量需求很可能寫死在 `DEMON.EXE`，不在 `ITEMS.DAT` 裡 |

## 4. `TEMPLAT1/2/4/5.DAT`（特殊藏寶室道具檔，非角色範本）

### 4.1 用途修正

見第 0 節說明：這組檔案透過 `DEMON.INT` 字串的 sink 位置（`TEMPLAT%d.DAT` 緊接在藏寶室劇情文字
之後）確認是**特殊藏寶室裡唯一道具的資料**，不是預建角色範本。`TEMPLAT3.DAT` 缺席最可能是遊戲裡
只有 4 個這種場景，不是「檔案遺失」。

### 4.2 檔案內容觀察

256 bytes，**不是** `PARTY.DAT` 那種「姓名字串 + 二進位屬性」格式（檔案裡完全找不到可辨識的
ASCII 姓名或道具名字串），也**不是** `MONSTER.DAT`/`ITEMS.DAT` 那種 NUL 分隔 ASCII 文字格式
（`\x00` 在檔案裡的分布沒有規律的 token 邊界）。

實際內容分兩段：

- `0x00 - 0xdf`（224 bytes）：低數值（`0x00`、`0x02`、`0x03`、`0x06`、`0x0d` 這幾種值反覆出現），
  排列方式呈現對稱、漸縮的區塊圖案（例如 `TEMPLAT1.DAT` 從 `0x20` 開始，每一橫排的非零欄位數量
  隨列數遞增再遞減），視覺上非常像小型點陣圖（icon/sprite）的像素索引資料，而不是數值表格。
  四個檔案（TEMPLAT1/2/4/5）彼此的數值分布不同（1/2 用 `0,3,13`；4 用 `0,2,4,6`；5 用 `0,2,6`），
  符合「每個場景放的是不同道具、需要不同小圖」的假設。
- `0xe0 - 0xff`（32 bytes）：結構明顯不同，是一串小整數配對，並在檔尾出現固定的
  `fe fe ff ff` 四 byte 重複樣式（四個檔案都有），懷疑是道具本身的遊戲機制欄位
  （對照 `DEMON.INT` 字串 `Constant: / Dormant: / Invoked: %d pt. / with %d charges /
  Breakable (%d%%)` 這組附魔相關欄位，可能是這件道具的附魔/特殊能力定義），但沒有進一步證據，
  未能拆出實際欄位邊界。

**沒有嘗試做像素解碼**（需要知道確切的色彩深度、調色盤、尺寸才能還原成圖片，屬於圖像格式範疇，
不在本次「規則資料表」任務範圍內；具體方法建議見第 6 節）。

## 5. 未解欄位清單

> **2026-07-25 大幅收斂。** 原本 9 項，其中 4 項已由反組譯解決（下表「已結案」），
> 剩餘 5 項仍需動態測試或進一步反組譯。

### 已結案

| 原項目 | 結論 |
|---|---|
| `PARTY.DAT` 職業（Class）「完全未找到」| **在 `0xf6`**（0–9）。學院與改宗兩處獨立呼叫點都拿它當技能學費表的欄索引 |
| `PARTY.DAT` 道具槽內部語意 | **已解**（1.3 節）：`+0x00` 型別＝`ITEMS.DAT` 索引、`+0x0e` 附魔（存 `+10`）、`+0x10` 已鑑定旗標。**連帶更正陣列起點為 `0x0c`** |
| `PARTY.DAT` 已學技能清單「完全未找到候選位置」| **在 `0xc8`–`0xe6`**，31 bytes、1 byte/技能。原文推測「可能藏在裝備欄那 170 bytes 裡」——方向錯了，是因為當時把道具槽的範圍算成 `0x1a`–`0xc3`，把技能陣列的位置整個吃掉了 |
| `PARTY.DAT` 隊伍金幣長度（3 或 4 bytes）| **4 bytes，數值封頂 `0x00FFFFFF`**。與 EXP 同一套處理：市集出售的加錢程式碼用 32-bit `ADD/ADC` 後做 `0x00FFFFFF` 封頂檢查（`docs/re/19` §7.4）。原本「無法消歧」是因為只能看存檔；看程式碼就沒有歧義 |

### 仍未解

| 檔案 | 欄位 | 現狀 | 建議下一步 |
|---|---|---|---|
| `PARTY.DAT` | `0xb6`–`0xc3`（14 bytes）| 本存檔全零 | 動態 diff |
| `PARTY.DAT` | 隊伍位置與朝向 | 陣型格（trailer `0x00-0x08`）已驗證，見 `docs/re/34`；朝向在 trailer `+0xa4` | — |
| `PARTY.DAT` | 遊戲內時間（時/日/月）| 一度懷疑是 trailer `0x1a-0x1c`，已被 `PARTY.BAK` 交叉驗證推翻 | DOSBox 內讓遊戲時間流逝後 diff，鎖定規律遞增的 byte。**這同時是「一天幾小時（26 vs 38）」的驗證路徑** |
| `MONSTER.DAT` | **11 欄全部定案** | 與召喚生物表同構，12 隻同名生物有 96 個數值零誤差；第 7 欄是護甲、第 9 欄是 level。第 11 欄 `special` 已由 `docs/re/23` 的 AI／吐息與免疫路徑證明是種族／元素類型（8–11 龍、7 冰系） | 已完成；噴吐的實際繪圖 tile 6／7／8 另見 `docs/re/106` |
| `ITEMS.DAT` | `f1`–`f6` 六個欄位 | 30 件商品的欄位已全部 dump（3. 節）。武器全是 `1 3 0 10 8 9`、護甲全是 `0 3 12 7 7 7`，看得出是「類別 + 可帶的能力類別」，逐欄語意未定。**`f0`（價格）已確認**，是市集定價的基礎值 | 比對手冊附錄 C（魔法道具）列出的「各類道具可帶哪些能力」與 `f4`–`f6` 的值 |
| `TEMPLAT*.DAT` | 整體格式（疑似圖像）| 只完成用途定位，byte 級格式未解 | 比照已知 shape 檔格式，或 DOSBox 進到對應藏寶室截圖比對 |

## 6. 建議下一步（依優先度）

1. **DOSBox 動態 diff 測試（最高優先，能一次解決最多未解欄位）**：
   在 DOSBox 裡對同一個角色分別做「裝備一件已知道具」「學一個已知技能」「移動遊戲內時間」
   「改變朝向」「金幣拉到超過 1600 萬」這幾個單一變數的操作，每次操作後存檔並跟操作前的存檔
   `python3 tools/parse_party.py --diff <before> <after>` 比對，只有做這一步才能把 1.3 節的裝備欄、
   技能清單、時間欄位、金幣長度這幾個「數值規律看起來合理但無法排除巧合」的假設，
   確定為「已驗證」。
2. **反組譯 `DEMON.EXE` 讀取 `PARTY.DAT`/`MONSTER.DAT`/`ITEMS.DAT` 的程式碼**：直接找到讀取這些檔案
   欄位的組合語言片段，可以不靠猜測直接讀出欄位順序與長度，尤其能解決 `MONSTER.DAT` 的
   `special` 與 `ITEMS.DAT` 的 category/weapon_slot 這幾個「規律合理但語意不確定」
   的欄位（level 與 armour 已於 `docs/re/57` 解決，special 已於 `docs/re/23` 解決）。`tools/ghidra_headless.sh` 已經在專案裡，可以直接用。
3. **`TEMPLAT*.DAT` 的圖像格式解碼**：如果確認是點陣圖，跟 `MONSTER.SHP`/`MONSTER.SHE` 這類已知
   「shape 檔」格式做比對，可能可以共用同一套解碼邏輯。
