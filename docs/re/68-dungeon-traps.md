# 68 — 地城陷阱的數值

`docs/re/66` §3 把 `0x19ed7` 那張 9 格表定位成地城陷阱，
但只有字串沒有數字。這一篇把擲點讀出來。

---

## 1. 毒針（確定）

```
19ad5  印 "Poison needle hits "
19ae5  call 15be:153a          ; 選一名角色，回傳索引
19af1  cx = Roll(4)            ; ← 傷害 1–4
19afd  si = 索引 × 0x104
19b09  al = 角色[+0xfd]        ; 目前 HP
19b10  ax = HP − cx
19b19  call 15be:145c(索引, 新HP)
19b21  回傳 0 → call 15be:000c ; 全隊死亡（＝動作 0x18）
```

**傷害是 `Roll(4)`，打隨機一名角色。** 這條完整讀過：
從擲點、減 HP、寫回、到全滅判定都在上面。

`15be:000c` ＝ `25be:000c` ＝ `docs/re/50` 的動作 `0x18`
（`A cold breeze` / `chills the air...` / `...all characters` / `have died.`）——
**兩張表在這裡接起來了**。

### 攻略為什麼特別點名毒針

攻略 `part-3.md` §19 說「陷阱頂多是個麻煩，**除非運氣夠差被毒針扎中**」。
傷害只有 1–4，對 HP 20 以上的角色不痛 —— 那為什麼點名它？

~~合理的解釋是毒針會附帶中毒狀態~~ —— **讀完 `15be:145c` 後這條不成立**（見 §2）。

真正的原因是**毒針沒有命中判定**：

| 陷阱 | 逃過的機會 |
|---|---|
| 坑 | `: safe` |
| 長矛／飛鏢 | `%smisses` |
| 池 | `swims out.` |
| **毒針** | **沒有** —— 選人、擲傷害、直接扣血 |

其他三種都有閃過的機會，只有毒針必中。技能「察覺陷阱／解除陷阱」
之所以「大多沒什麼意義，除非被毒針扎中」，是因為那是**唯一躲不掉的那個**。

---

## 2. 兩支共用常式（B11，已解）

### `25be:145c(新HP, 索引)` — 套用 HP 並處理死亡

```
1ac43  if (新HP > 0) {
1ac5a      角色[索引][+0xfd] = 新HP;  重畫;  return 1
       }
1ac6c  死亡處理：HP 設 0 → ds:0x15ca = 0x80 → 呼叫 222f:0619（docs/re/63）
1ace8  掃全隊：只要有人 +0x102 != 5，回傳 1
1ad14  return（0 ＝ 全隊死亡）
```

**回傳值是「還有人活著嗎」**，不是「這一擊有沒有生效」——
毒針那邊 `test ax,ax / jne` 的語意因此確定。

**這支不碰中毒。** 它只寫 `+0xfd`（HP），沒有動 `+0x102` 以外的狀態，
而 `+0x102` 也只被設成 `5`（死亡）。

### `25be:153a` — 隨機選一名隊員

```
1ad25  al = party[+0x9a]        ; 隊伍人數
1ad2d  ax = Roll(人數)          ; 1..n
1ad33  dec ax                   ; → 0..n−1
```

陷阱打誰是**純隨機**，與站位、職業、狀態都無關。

---

## 3. 其餘四種（運算元已追完）

### 坑

```
19bb3  Roll(2)
19bbd  == 1 → 印 ": safe"，結束
       != 1 → 印 ": falls"
19c06  Roll(6) → HP − 擲點 → 25be:145c
```

**50% 安全，掉下去 1–6 傷害。**

### 長矛／飛鏢

```
19c8f  Roll(5) + 1 → [bp-0xa]     ; 飛鏢數量 2–6，[bp-8]=1 起算的迴圈
19cc5  Roll(2)                     ; 每一支的命中判定（不中印 "%smisses"）
19d32  Roll(3) → HP − 擲點
19d5d  Roll(4) → [bp-4] -= 擲點    ; 第二條傷害路徑
       → 25be:145c
```

**射 2–6 支**，每支各自判定命中。這是唯一會連打多次的陷阱。

### 池

```
19df7  Roll(3)                     ; 游出來／溺水
19e16  Roll(3) − 1 → [bp-4]        ; 傷害 0–2
19e20  if (ds:0x52f6 == 7) [bp-4] += 2   ; ← 酸池多 2 點
       印 "Drowns. %d damage"
```

**`ds:0x52f6 == 7` 就是酸池** —— 而那正是 `docs/re/65` 的事件選擇子。
這直接證實了 §4：case 6／7 的差別是**普通版與酸版**，
由選擇子決定印哪一句、傷害加不加 2。**不是「偵測／踩到」。**

### 警報

```
19eac  Roll(5)
19eb6  party[+0x9c] = 擲點
```

**它不造成傷害 —— 它把隨機遭遇倒數設成 1–5 步。**

`+0x9c` 是 `docs/re/51` 解出的遭遇倒數（走一步減一，歸零就開打）。
所以 `You set off an alarm!` 的意思是**幾步之內必定遇敵**，
而不是「扣血」。這是整組陷阱裡唯一不碰 HP 的一個。

> `CONTEXT.md` §7 B8 列著「`+0x9c` 兩處不明重置」——
> 這是**第三處**，而且語意明確。那兩處仍未解，但至少現在知道
> 這個欄位不只在戰鬥後被重設。

---

## 4. 兩種進入路徑

`docs/re/66` §3 注意到三種陷阱各佔兩格（case 2-3、4-5、6-7），
兩格指向**同一段程式碼**。現在有了字串可以對照：

- `A Bungei pit!` 與 `Poison pit!` 是同一格裡的兩條字串
- `A pool!` 與 `An acid pool!` 同理

所以那兩格的差別不是「偵測／觸發」，而更可能是
**普通版與有害版**（一般坑／毒坑、一般池／酸池），
由進入時的參數決定印哪一句。`docs/re/66` §3 當時猜的「偵測到／踩到」**不成立**。

---

## 5. 待譯

`Bungei pit` 的 `Bungei` 在 `translations/glossary.md` 與攻略裡都查不到 ——
可能是專有名詞（地名或族名）。**新增譯名前需要先確認它的來源**，
不要憑音譯造一個。

---

## 6. worklist

- **B10 解決**：五種陷阱的運算元全部追完
- **B11 解決**：`25be:145c`（套用 HP／死亡／回傳全滅與否）、`25be:153a`（隨機選人）
- **一條推測被推翻**：毒針不附帶中毒，它特別的地方是**沒有命中判定**
- 新增待譯：`Bungei`
